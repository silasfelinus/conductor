# Human-managed project overrides. Written by the workspace UI.
# Agents: check this before claiming tasks — skip projects where status != active.
# Priority controls how often agents pick tasks (high = more attention).
# status:   active | continuous | paused | retired | finished
# priority: low | normal | high | urgent
# kind:     software | content | proposal | brainstorm (overrides roadmap kind for UI)
#
# liveUrl/channelKey/tabKey below (where present) are an informational mirror
# only, kept in sync by hand. Kind Robots owns these presentation fields
# directly via utils/projectPlacements.ts's PROJECT_PLACEMENTS map + PATCH
# /api/projects/{id}. scripts/sync_projects.py (now a compatibility shim over
# sync_kind_robots_projection.py) is a one-way, commit-stamped projection
# sender for coordination state and does NOT push these three fields back to
# kind_robots, despite older per-entry comments below saying otherwise (kind-
# robots/t-066, 2026-08-24; see the coat-dance entry's own note for the same
# correction made in-place there).

overrides:
  - slug: kapowarr
    status: finished # flipped 2026-09-11 — 71/71 tasks, 9/9 milestones, and Silas confirmed the
    # one gate no sandbox could ever close: "Kapowarr has had successful Sab downloads, pretty
    # sure torrents as well. Confirmed." (t-024). NOT a task-count flip: t-024 needed his real
    # indexer and his real SABnzbd, and the project's goal — a polished comic manager with
    # reliable discovery, metadata, imports, downloads and automation — is met and live-verified.
    # Flipped rather than left active because tests/test_current_project_lifecycle.py correctly
    # refuses an active project with zero open tasks: an active project with nothing to do is a
    # lie about the queue. This is a personal fork Silas keeps evolving, so REOPENING IS EXPECTED
    # and cheap when he wants more — davinci and ruler-hooked both came back the same way. Added
    # 2026-08-15 as a personal fork evolution project.
    priority: high
    kind: software

  - slug: mona-salai
    status: paused # tabled 2026-07-26 by Silas — preserve roadmap and research for possible revival
    priority: low
    kind: software
    liveUrl: /mona-salai
    channelKey: lab
    tabKey: mona-salai

  - slug: superkate-services-calculator
    status: finished # flipped 2026-07-26 -- all 37 tasks done, nothing open (t-030 decided: no dedicated backend until usage grows beyond Superkate)
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-services-calculator' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /projects/build/stylist
    channelKey: plan
    tabKey: stylist

  - slug: superkate-hairstyle-ai
    status: paused # tabled 2026-07-26 (Silas) -- pausing alongside pinball-hero/career-transition pattern
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-hairstyle-ai' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /projects/build/stylist
    channelKey: plan
    tabKey: stylist

  - slug: kindrobots-unraid
    status: active
    priority: low # lowered 2026-07-25 per Silas: "we'll return to it later, low priority
    # task for future development" (re: t-012's ProxySQL pooling observation)
    kind: software

  - slug: conductor
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor
    channelKey: plan
    tabKey: projects

  - slug: kind-robots
    status: active
    priority: normal

  - slug: text-generation
    status: active # added 2026-08-16 by Silas — first-class cloud and private/self-hosted text generation
    priority: normal
    kind: software

  - slug: newsfeed
    status: finished
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'newsfeed' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/newsfeed
    channelKey: home
    tabKey: newsfeed

  - slug: global-ui
    status: finished # flipped 2026-07-26 (Silas) -- all 25 tasks done, nothing open;
    # last task closed was t-016 (site-audit weekly review folded into select_role.py's
    # role system instead of needing its own dedicated trigger)
    priority: normal
    kind: software

  - slug: interface-vision
    status: active # opened 2026-08-01 (Silas) -- the release blocker for beta
    # Successor to global-ui, which was flipped `finished` at 25/25 tasks while the
    # design system it shipped sat at ~7% adoption (29 of ~421 component files use any
    # kr-* class; the hand-rolled equivalent appears 366 times across 145 files). This
    # project re-opens that work with numeric exit criteria enforced by a CI script
    # whose allow-list only ever shrinks. Do not mark it finished on task count alone.
    priority: normal
    kind: software

  - slug: ecosystem-map
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    kind: software

  - slug: humboldt-scoop
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle): the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2, "dissolve the Lab
    # channel into forever nav") and 'humboldt-scoop' moved to plan/projects
    # alongside coat-dance/sketchy/ruler-hooked; this doc-only field was never
    # updated to match. projectPlacements.ts is CI-enforced (verifyChannelContent.ts)
    # and was already correct — only this comment/value was stale.
    liveUrl: /plan/projects/humboldt-scoop
    channelKey: plan
    tabKey: projects

  - slug: humboldt-scoop-cms
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop-cms' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /scoop-cms
    channelKey: admin
    tabKey: scoop-cms

  - slug: model-builder
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'model-builder' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /model-builder
    channelKey: build
    tabKey: model-builder

  - slug: animation-manager
    status: continuous
    priority: low
    kind: software

  - slug: animation-studio
    status: retired # retired 2026-07-19 (conductor/t-039) — superseded by animation-manager,
    # which mirrors its research (t-001) and pitch-queue (t-002) tasks (both done there) and
    # is the actively-developed autonomous continuation (4/10 tasks done vs 1/8). Only
    # animation-studio's genuinely distinct work (t-003 Gravity Garden) already shipped and
    # is marked done. Remaining animation-studio ready tasks (t-001, t-002, t-004) duplicate
    # animation-manager scope already done or in flight there (t-004 "Track animation
    # attempts as Component records with Reactions" vs animation-manager's own t-004 of the
    # same description) -- keeping animation-studio claimable risked a Worker re-doing
    # already-shipped work, the same collision class as animation-manager/t-008 (see
    # TALKBACK.md 2026-07-14). roadmap.yaml kept as historical record, not deleted.
    priority: low
    kind: software

  - slug: approval-portal
    status: retired
    priority: low

  - slug: digital-storefront
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'digital-storefront' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /sanctuary
    channelKey: sanctuary
    tabKey: giftshop

  - slug: ai-art-academy
    status: finished # flipped 2026-09-11 by Silas, in session: "Ai art academy is actually an
    # outdated project to test things. It has gone as far as it needs, I don't see it becoming
    # an app, it should be considered finished and closed as is." A deliberate scope end, not a
    # task-count flip and not a failure: the Academy did its job as a testbed for the Kontext
    # remix path and the art-history teaching surface, and the web surface stays live at
    # /academy. The five open tasks were closed as out of scope rather than deferred — t-062/
    # t-063 (Android/iOS builds), t-064 (cross-platform QA), t-065 (his acceptance of all three
    # builds) all presupposed a mobile app he has now ruled out; t-061's PWA-first choice had
    # left native "unless Silas asks for a store listing", and this answers that in the negative.
    # t-079 (the Kontext corrupted-noise investigation) was closed BY REFERENCE to
    # coloring-book/t-039, which owns the same root cause and stays live — the finding is not
    # lost with this project.
    priority: low
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ai-art-academy' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /academy
    channelKey: play
    tabKey: academy

  - slug: coloring-book
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coloring-book' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /coloring
    channelKey: play
    tabKey: coloring

  - slug: humboldt-impropriety-calendar
    status: retired
    priority: low
    kind: brainstorm

  - slug: brainstorm
    status: finished # flipped 2026-09-11 by Silas, in session: "I'm going to accept brainstorm
    # as acceptable and cleared. Project can be marked completed." 32/32 tasks and 7/7 milestones
    # done. NOT a task-count flip: t-023 is a real playtest gate that FAILED once (2026-09-07,
    # paragraph drift and recycled example content), was fixed by kind_robots#2507, retested by
    # Silas on 2026-09-11, failed again on three new defects (lens selection treated as a floor
    # rather than an allow-list; no new-pitch control; candidate cards not sharing a baseline),
    # and only accepted after kind_robots#2615 fixed all three. See t-023's note for the full
    # record, including one documented residual accepted as-is: the FOCUSED batch shape still
    # badges a per-candidate lens identically to an ASSORTMENT quota, so a focused batch can look
    # varied at a glance. If that becomes annoying in use, file a fresh task rather than reopening
    # this project -- the interface-vision precedent is that reopening is for unmet goals, not for
    # polish someone thought of later.
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'brainstorm' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /brainstorm
    channelKey: build
    tabKey: brainstorm

  - slug: sketchy
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'sketchy' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — see humboldt-scoop
    # entry above for the 'lab' channel dissolution this doc field never followed.
    liveUrl: /plan/projects/sketchy
    channelKey: plan
    tabKey: projects

  - slug: pinball-hero
    status: retired
    priority: low
    kind: content

  - slug: art-generator-connect
    status: finished # flipped 2026-07-26 -- all 22 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. The ArtJob admin dashboard lives at /artjob under the
    # "art" dashboard channel, tab "artjob".
    liveUrl: /artjob
    channelKey: art
    tabKey: artjob

  - slug: mural-design
    status: active
    priority: low
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'mural-design' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /build/mural
    channelKey: admin
    tabKey: mural

  - slug: storybook
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry for
    # 'storybook' in kind_robots utils/projectPlacements.ts (channelKey: 'play',
    # tabKey: 'storybook', route: '/storybook') — conductor/storybook t-010;
    # channelKey corrected play -> plan 2026-09-12 (the play channel was dissolved 2026-07-28).
    liveUrl: /storybook
    channelKey: plan
    tabKey: storybook

  - slug: davinci
    status: retired # retired 2026-09-12 (Silas-directed session) — merged INTO storybook.
    # Silas, 2026-09-09: "merge the projects, and kill any reference that says we should
    # separate them. we care about having a solid *single* interface that is a stylish and
    # effective storymaker with many endings. Whatever has been done should be merged."
    # The code merge shipped as kind_robots PR #2549 (Da Vinci is Storybook's `life` shape;
    # /play/davinci is a 301 to /storybook; davinci-page.vue became
    # components/storybook/storybook-life-run.vue), but this registry was never touched, so
    # the Kind Robots Projects page kept showing two Active projects for one product
    # (Silas, 2026-09-12). `retired` rather than `finished`: the product is not done (the
    # 1,024-ending art coverage is still open), it was superseded — the same reading
    # animation-studio and serendipity used. The three tasks still open here moved to
    # storybook: t-018 -> storybook/t-027, t-019 -> storybook/t-028, and the visual gate
    # t-022 folded into storybook/t-016 (one gate for one screen). roadmap.yaml kept as
    # historical record, not deleted. Placement mirror lines dropped: /play/davinci is a
    # redirect and the `play` channel was dissolved 2026-07-28; kind_robots
    # utils/projectPlacements.ts (CI-enforced) already sends the slug to /storybook.
    priority: normal
    kind: software

  - slug: media-watchlist
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'media-watchlist' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/watchlist
    channelKey: plan
    tabKey: watchlist

  - slug: conductor-app
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor-app' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor-app
    channelKey: plan
    tabKey: conductor-app

  - slug: alexa-integration
    status: paused # paused 2026-09-11 by Silas, in session: "alexa integration should be paused.
    # it's not an active project." Was sitting at 21/22 with t-022 (his cross-width visual
    # acceptance) as the only open item, which made it look one glance away from finished --
    # it was actually work he no longer wants. Roadmap and the completed voice/relay research
    # are kept as a historical record, not deleted, same convention as mona-salai and
    # superkate-hairstyle-ai. Removed from projects/priority.yaml, and t-022 is no longer a
    # live gate: audit_human_gates.py excludes non-active projects by default, so it will stop
    # being reported as something only Silas can unblock.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'alexa-integration' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/voice-lab
    channelKey: plan
    tabKey: voice-lab

  - slug: mermaids-of-venice
    status: paused # paused 2026-09-11 by Silas, in session: "Mermaids of Venice, pause the
    # project". Its three open gates were all work only he can do — t-002 (the personal note
    # for the landing page), t-007 (working through the ~140-item typo list in his own hand),
    # t-009 (the parked mermaidsofvenice.com plan) — so the project could not move without him
    # and was generating gate noise every sweep while he was not working on it. Pausing stops
    # that: audit_human_gates.py excludes non-active projects by default. Nothing is abandoned
    # — editorial/VERY-IMPORTANT.md, the site plan, and the edition-3 support all stay as they
    # are. Previously: reopened 2026-08-03 for the edition 3 revision.
    priority: normal
    kind: content

  - slug: packmaker
    status: finished # flipped 2026-07-26 -- all 10 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'packmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /packs
    channelKey: build
    tabKey: packs

  - slug: coat-dance
    status: active
    priority: normal
    kind: content
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coat-dance' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (coat-dance/t-010 cycle): the 'lab' channel was
    # dissolved 2026-07-28 (kind_robots commit 6cd42c2, "dissolve the Lab channel
    # into forever nav") and coat-dance moved to plan/projects alongside the other
    # unfinished pitches (sketchy, ruler-hooked, humboldt-scoop, also corrected this
    # cycle); this doc-only field was never updated to match.
    # projectPlacements.ts is CI-enforced (verifyChannelContent.ts) and was
    # already correct — only this comment/value was stale. t-010 step (3)'s
    # "admin Placements click" does not apply to this field: sync_projects.py
    # never wrote channelKey/tabKey downstream (test_sync_projects.py asserts as
    # much) — this file's values are documentation only.
    liveUrl: /plan/projects/coat-dance
    channelKey: plan
    tabKey: projects

  - slug: career-transition
    status: retired # archived (reaffirmed 2026-07-16 by Silas) — conductor itself may become the career
    priority: low
    kind: content

  - slug: engagement
    status: finished
    priority: normal
    kind: software

  - slug: wishmaster
    status: retired # retired 2026-08-09 by Silas — duplicated project creation without distinct supporting infrastructure; preserve roadmap/research as history only
    priority: low
    kind: software

  - slug: challenge-center
    status: finished # 20/20 tasks done — flipped 2026-07-17 with Silas's in-session approval
    priority: high
    kind: software

  - slug: serendipity
    status: retired # retired 2026-07-29 (conductor burst-mode cycle) -- this project's
    # actual kind_robots product (the story-weaving task engine described by this
    # project's own roadmap.yaml m1-m5) was permanently renamed to Taskmaster
    # (kind_robots docs/products/storybook-taskmaster-boundary.md: "The former
    # task-story product is renamed completely to Taskmaster, leaving the Serendipity
    # name and route free for the voice-led experience"; commit 5509ac4 "Complete
    # permanent Taskmaster cutover" did the file-level rename, serendipity-page.vue ->
    # taskmaster-page.vue, serendipityStore.ts -> taskmasterStore.ts). The freed
    # /serendipity route and name were then reclaimed this same morning (kind_robots
    # commit 36a86bf, "refactor: make Serendipity the canonical voice route") for the
    # Alexa voice-led surface previously at /serendipity-voice -- which is a different
    # product, already correctly tracked end-to-end under conductor's own
    # alexa-integration project (see its notes_from_silas: "Build the Alexa work as the
    # Serendipity voice surface..."). See the new `taskmaster` entry below for the
    # renamed narrative product's tracking. roadmap.yaml kept as historical record, not
    # deleted, same convention as animation-studio's retirement.
    priority: low
    kind: software

  - slug: taskmaster
    status: active # onboarded 2026-07-29 (conductor burst-mode cycle) -- retroactive
    # tracking for an already-shipped kind_robots product: the story-weaving task
    # engine formerly tracked here as `serendipity` (see that entry's retirement note
    # above), permanently renamed per kind_robots docs/products/storybook-taskmaster-boundary.md.
    # Verified via code inspection: components/pages/taskmaster-page.vue (724 lines)
    # and stores/taskmasterStore.ts (999 lines) are a mature, complete implementation,
    # not a scaffold.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by sync_projects.py.
    # Values match the canonical PROJECT_PLACEMENTS['taskmaster'] entry in kind_robots
    # utils/projectPlacements.ts (channelKey: 'play', tabKey: 'taskmaster', route: '/taskmaster').
    liveUrl: /taskmaster
    channelKey: play
    tabKey: taskmaster

  - slug: appmaker
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'appmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /appmaker
    channelKey: plan
    tabKey: appmaker

  - slug: recipe-box
    status: retired
    priority: low
    kind: software

  - slug: ruler-hooked
    status: active # REOPENED 2026-08-26 (Silas, in session: "We should start generating
    # art for The Ruler is Hooked... We have hundreds of art pieces we'll need for this
    # project"). The `finished` value below was accurate for the PoC milestone and
    # misleading for the product: FULL-GAME-GAP-AUDIT.md (2026-08-26) verified that
    # fishing is still a placeholder 50%-chance counter, no Fishopedia exists, no
    # gameplay art has landed at all, and the region manifest alone implies 37 layer
    # assets before any card/character/fish art. Full-game work (m5 art production is
    # open now; the m4 vertical slice is specified in the audit and not yet filed) is
    # tracked on the roadmap again rather than hidden behind a completed task count.
    # PRIOR RATIONALE, kept for the record -- flipped 2026-08-18 (conductor scheduled
    # Agent run) -- all 15 tasks
    # done, nothing open. Goal ("playable slideshow fishing/kingdom-management PoC,
    # offline-playable") is met: m1/m2 shipped the PoC (t-001..t-013), t-014 expanded
    # content depth (6->25 cards, 1->3 arcs, 2->4 endings), t-009 covered the Steam/Unreal
    # research half of m3, and t-015 shipped an installable/offline PWA wrapper (app
    # packaging), closing out m3's three named components. Deliberately-deferred polish
    # (site-wide PWA conversion, offline SPA-nav caching via Nuxt Content, install-prompt
    # UI on other routes; an actual Steam/Unreal port) remains documented in t-015's and
    # t-009's own notes as future bucket-list scope, not required by the stated PoC goal.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ruler-hooked' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — see humboldt-scoop
    # entry above for the 'lab' channel dissolution this doc field never followed.
    liveUrl: /plan/projects/ruler-hooked
    channelKey: plan
    tabKey: projects

  - slug: music-mentor
    status: finished
    priority: low
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry for
    # 'music-mentor' in kind_robots utils/projectPlacements.ts (channelKey: play,
    # tabKey: music-mentor, route: /music-mentor) — conductor music-mentor/t-006.
    liveUrl: /music-mentor
    channelKey: play
    tabKey: music-mentor

  - slug: dream-cycle
    status: continuous
    priority: low# idle fallback by design — must never outrank real ready work
    kind: software

  - slug: lora-ingestion
    status: active # onboarded 2026-07-31 -- this project had a roadmap.yaml and
    # appeared in STATUS.md (32.5%, m1 done) but was never added here, and the
    # registry is what defines projects for the kind_robots front end
    # (server/api/conductor/projects.get.ts iterates the overrides list, not the
    # projects/ directory). It was therefore invisible on the site and skipped by
    # sync_projects.py entirely. `kind` deliberately omitted so the endpoint falls
    # back to the roadmap's own `infrastructure`, which the override vocabulary
    # (software|content|proposal|brainstorm) does not cover.
    priority: normal

  - slug: kind-economy
    status: active # created 2026-08-19 by Silas — the money layer for Kind Robots:
    # Stripe going live, the three-way paid-usage split (platform / anti-malaria
    # mission / creator), creator payouts, mission remittance, and the Save One
    # Human restart-or-retire decision. Declared a "vitally important tentpole"
    # in-session, hence priority: urgent. See projects/kind-economy/DESIGN-BRIEF.md.
    priority: urgent
    kind: software

  - slug: cthulhuquarium
    status: active # added 2026-08-24 by Silas — long-planned silly project, finally started
    # priority raised to high 2026-08-24 by Silas, in session: "i want it to be the main
    # task worked on by agents over the next few days."
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row (id 2112) by
    # sync_kind_robots_projection.py. Values match the canonical PROJECT_PLACEMENTS
    # entry for 'cthulhuquarium' in kind_robots utils/projectPlacements.ts.
    liveUrl: /play/aquarium
    channelKey: play
    tabKey: aquarium

  # NOTE 2026-08-24: a concurrent scheduled session scaffolded this project under the
  # misspelled slug `cthuluquarium` (conductor#2765/#2766) while this session was
  # working. Silas confirmed the correct spelling in-session, the Kind Robots Project
  # 2112 row was repointed (slug + conductorSlug -> cthulhuquarium), and the
  # misspelled project/override were removed. Do not reintroduce `cthuluquarium`.
  #
  # UPDATE 2026-08-25: the leftover duplicate KR Project row from that mixup (id
  # 2113, title/slug "cthuluquarium", conductorSlug "cthuluquarium") was still live
  # and kept tripping check_project_scaffold_drift.py's forward check, since no
  # `projects/cthuluquarium/roadmap.yaml` was ever supposed to exist (conductor/t-125
  # flagged this exact row and deferred the fix). Archived it (isActive: false,
  # status: ARCHIVED via DELETE /api/projects/2113) and cleared its conductorSlug
  # (PATCH) so it no longer claims a scaffold. check_project_scaffold_drift.py now
  # passes clean. Project 2112 (the real one) is untouched.

  - slug: scene-animator
    status: active # added 2026-08-24 by Silas — admin folder-to-video automation
    priority: normal
    kind: software

  - slug: mandarin-tutor
    status: finished # flipped 2026-09-11 by Silas, in session: "mandarin is acceptable. layout
    # confirmed good." That is the cross-width human visual acceptance t-021 was holding for --
    # AGENTS.md does not let a user-facing software project flip on task count alone, and the
    # sandbox could only confirm the route returned real SSR markup, never how it looked.
    # 21/21 tasks, 4/4 milestones. Added 2026-08-24 by Silas as a visual linguistics-forward
    # Mandarin learning tool; was order[0] in projects/priority.yaml per his 2026-08-25 lead-project
    # decision, and is removed from that queue here — see tests/test_project_priority_contract.py,
    # whose pinned lead moves to Cthulhuquarium (the project he named immediately behind it in the
    # same call).
    priority: normal
    kind: software

  - slug: rainbow-butterflies
    status: active # added 2026-08-28 by Silas — AMI-led ethical outreach, agent collaboration, and anti-malaria fundraising
    priority: normal
    kind: software
