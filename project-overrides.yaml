# Human-managed project overrides. Written by the workspace UI.
# Agents: check this before claiming tasks — skip projects where status != active.
# Priority controls how often agents pick tasks (high = more attention).
# status:   active | continuous | paused | retired | finished
# priority: low | normal | high | urgent
# kind:     software | content | proposal | brainstorm (overrides roadmap kind for UI)

overrides:
  - slug: kapowarr
    status: active # added 2026-08-15 by Silas — personal fork evolution project
    priority: high
    kind: software

  - slug: mona-salai
    status: paused # tabled 2026-07-26 by Silas — preserve roadmap and research for possible revival
    priority: low
    kind: software
    liveUrl: /mona-salai
    channelKey: lab
    tabKey: mona-salai

  - slug: superkate-services-calculator
    status: finished # flipped 2026-07-26 -- all 37 tasks done, nothing open (t-030 decided: no dedicated backend until usage grows beyond Superkate)
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-services-calculator' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /projects/build/stylist
    channelKey: plan
    tabKey: stylist

  - slug: superkate-hairstyle-ai
    status: paused # tabled 2026-07-26 (Silas) -- pausing alongside pinball-hero/career-transition pattern
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-hairstyle-ai' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /projects/build/stylist
    channelKey: plan
    tabKey: stylist

  - slug: kindrobots-unraid
    status: active
    priority: low # lowered 2026-07-25 per Silas: "we'll return to it later, low priority
    # task for future development" (re: t-012's ProxySQL pooling observation)
    kind: software

  - slug: conductor
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor
    channelKey: plan
    tabKey: projects

  - slug: kind-robots
    status: active
    priority: normal

  - slug: text-generation
    status: active # added 2026-08-16 by Silas — first-class cloud and private/self-hosted text generation
    priority: normal
    kind: software

  - slug: newsfeed
    status: finished
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'newsfeed' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/newsfeed
    channelKey: home
    tabKey: newsfeed

  - slug: global-ui
    status: finished # flipped 2026-07-26 (Silas) -- all 25 tasks done, nothing open;
    # last task closed was t-016 (site-audit weekly review folded into select_role.py's
    # role system instead of needing its own dedicated trigger)
    priority: normal
    kind: software

  - slug: interface-vision
    status: active # opened 2026-08-01 (Silas) -- the release blocker for beta
    # Successor to global-ui, which was flipped `finished` at 25/25 tasks while the
    # design system it shipped sat at ~7% adoption (29 of ~421 component files use any
    # kr-* class; the hand-rolled equivalent appears 366 times across 145 files). This
    # project re-opens that work with numeric exit criteria enforced by a CI script
    # whose allow-list only ever shrinks. Do not mark it finished on task count alone.
    priority: normal
    kind: software

  - slug: ecosystem-map
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    kind: software

  - slug: humboldt-scoop
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle): the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2, "dissolve the Lab
    # channel into forever nav") and 'humboldt-scoop' moved to plan/projects
    # alongside coat-dance/sketchy/ruler-hooked; this doc-only field was never
    # updated to match. projectPlacements.ts is CI-enforced (verifyChannelContent.ts)
    # and was already correct — only this comment/value was stale.
    liveUrl: /plan/projects/humboldt-scoop
    channelKey: plan
    tabKey: projects

  - slug: humboldt-scoop-cms
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop-cms' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /scoop-cms
    channelKey: admin
    tabKey: scoop-cms

  - slug: model-builder
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'model-builder' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /model-builder
    channelKey: build
    tabKey: model-builder

  - slug: animation-manager
    status: continuous
    priority: low
    kind: software

  - slug: animation-studio
    status: retired # retired 2026-07-19 (conductor/t-039) — superseded by animation-manager,
    # which mirrors its research (t-001) and pitch-queue (t-002) tasks (both done there) and
    # is the actively-developed autonomous continuation (4/10 tasks done vs 1/8). Only
    # animation-studio's genuinely distinct work (t-003 Gravity Garden) already shipped and
    # is marked done. Remaining animation-studio ready tasks (t-001, t-002, t-004) duplicate
    # animation-manager scope already done or in flight there (t-004 "Track animation
    # attempts as Component records with Reactions" vs animation-manager's own t-004 of the
    # same description) -- keeping animation-studio claimable risked a Worker re-doing
    # already-shipped work, the same collision class as animation-manager/t-008 (see
    # TALKBACK.md 2026-07-14). roadmap.yaml kept as historical record, not deleted.
    priority: low
    kind: software

  - slug: approval-portal
    status: retired
    priority: low

  - slug: digital-storefront
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'digital-storefront' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /sanctuary
    channelKey: sanctuary
    tabKey: giftshop

  - slug: ai-art-academy
    status: active
    priority: low
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ai-art-academy' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /academy
    channelKey: play
    tabKey: academy

  - slug: coloring-book
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coloring-book' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /coloring
    channelKey: play
    tabKey: coloring

  - slug: humboldt-impropriety-calendar
    status: retired
    priority: low
    kind: brainstorm

  - slug: brainstorm
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'brainstorm' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /brainstorm
    channelKey: build
    tabKey: brainstorm

  - slug: sketchy
    status: finished # flipped 2026-07-26 -- all 8 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'sketchy' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — see humboldt-scoop
    # entry above for the 'lab' channel dissolution this doc field never followed.
    liveUrl: /plan/projects/sketchy
    channelKey: plan
    tabKey: projects

  - slug: pinball-hero
    status: retired
    priority: low
    kind: content

  - slug: art-generator-connect
    status: finished # flipped 2026-07-26 -- all 22 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. The ArtJob admin dashboard lives at /artjob under the
    # "art" dashboard channel, tab "artjob".
    liveUrl: /artjob
    channelKey: art
    tabKey: artjob

  - slug: mural-design
    status: active
    priority: low
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'mural-design' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /build/mural
    channelKey: admin
    tabKey: mural

  - slug: storybook
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry for
    # 'storybook' in kind_robots utils/projectPlacements.ts (channelKey: 'play',
    # tabKey: 'storybook', route: '/storybook') — conductor/storybook t-010.
    liveUrl: /storybook
    channelKey: play
    tabKey: storybook

  - slug: davinci
    status: active # reopened 2026-07-31 (Silas-directed session: "making da Vinci
    # work start to finish"). Was flipped `finished` 2026-07-26 on the reading that
    # all 15 tasks were done with nothing open -- true of the task list, but t-015
    # closed by writing the AI-narration spec and explicitly declining to file the
    # build task ("left for Silas/next cycle to scope as its own task rather than
    # bundling a large implementation into this spec-only pass"). The project was
    # therefore marked finished with its central gap unfiled: a play-loop API and
    # 1024 seeded endings with nothing generating the prose a player reads. t-016
    # files and closes that build.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'davinci' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /play/davinci
    channelKey: play
    tabKey: davinci

  - slug: media-watchlist
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'media-watchlist' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/watchlist
    channelKey: plan
    tabKey: watchlist

  - slug: conductor-app
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor-app' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor-app
    channelKey: plan
    tabKey: conductor-app

  - slug: alexa-integration
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'alexa-integration' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — the 'lab' channel
    # was dissolved 2026-07-28 (kind_robots commit 6cd42c2); this doc-only field
    # never followed. projectPlacements.ts is CI-enforced and was already correct.
    liveUrl: /plan/voice-lab
    channelKey: plan
    tabKey: voice-lab

  - slug: mermaids-of-venice
    status: active # reopened 2026-08-03 by Silas — edition 3 revision is in progress; keep editorial support and progress-gated character reviews moving
    priority: normal
    kind: content

  - slug: packmaker
    status: finished # flipped 2026-07-26 -- all 10 tasks done, nothing open
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'packmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /packs
    channelKey: build
    tabKey: packs

  - slug: coat-dance
    status: active
    priority: normal
    kind: content
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coat-dance' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (coat-dance/t-010 cycle): the 'lab' channel was
    # dissolved 2026-07-28 (kind_robots commit 6cd42c2, "dissolve the Lab channel
    # into forever nav") and coat-dance moved to plan/projects alongside the other
    # unfinished pitches (sketchy, ruler-hooked, humboldt-scoop, also corrected this
    # cycle); this doc-only field was never updated to match.
    # projectPlacements.ts is CI-enforced (verifyChannelContent.ts) and was
    # already correct — only this comment/value was stale. t-010 step (3)'s
    # "admin Placements click" does not apply to this field: sync_projects.py
    # never wrote channelKey/tabKey downstream (test_sync_projects.py asserts as
    # much) — this file's values are documentation only.
    liveUrl: /plan/projects/coat-dance
    channelKey: plan
    tabKey: projects

  - slug: career-transition
    status: retired # archived (reaffirmed 2026-07-16 by Silas) — conductor itself may become the career
    priority: low
    kind: content

  - slug: engagement
    status: finished
    priority: normal
    kind: software

  - slug: wishmaster
    status: retired # retired 2026-08-09 by Silas — duplicated project creation without distinct supporting infrastructure; preserve roadmap/research as history only
    priority: low
    kind: software

  - slug: challenge-center
    status: finished # 20/20 tasks done — flipped 2026-07-17 with Silas's in-session approval
    priority: high
    kind: software

  - slug: serendipity
    status: retired # retired 2026-07-29 (conductor burst-mode cycle) -- this project's
    # actual kind_robots product (the story-weaving task engine described by this
    # project's own roadmap.yaml m1-m5) was permanently renamed to Taskmaster
    # (kind_robots docs/products/storybook-taskmaster-boundary.md: "The former
    # task-story product is renamed completely to Taskmaster, leaving the Serendipity
    # name and route free for the voice-led experience"; commit 5509ac4 "Complete
    # permanent Taskmaster cutover" did the file-level rename, serendipity-page.vue ->
    # taskmaster-page.vue, serendipityStore.ts -> taskmasterStore.ts). The freed
    # /serendipity route and name were then reclaimed this same morning (kind_robots
    # commit 36a86bf, "refactor: make Serendipity the canonical voice route") for the
    # Alexa voice-led surface previously at /serendipity-voice -- which is a different
    # product, already correctly tracked end-to-end under conductor's own
    # alexa-integration project (see its notes_from_silas: "Build the Alexa work as the
    # Serendipity voice surface..."). See the new `taskmaster` entry below for the
    # renamed narrative product's tracking. roadmap.yaml kept as historical record, not
    # deleted, same convention as animation-studio's retirement.
    priority: low
    kind: software

  - slug: taskmaster
    status: active # onboarded 2026-07-29 (conductor burst-mode cycle) -- retroactive
    # tracking for an already-shipped kind_robots product: the story-weaving task
    # engine formerly tracked here as `serendipity` (see that entry's retirement note
    # above), permanently renamed per kind_robots docs/products/storybook-taskmaster-boundary.md.
    # Verified via code inspection: components/pages/taskmaster-page.vue (724 lines)
    # and stores/taskmasterStore.ts (999 lines) are a mature, complete implementation,
    # not a scaffold.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by sync_projects.py.
    # Values match the canonical PROJECT_PLACEMENTS['taskmaster'] entry in kind_robots
    # utils/projectPlacements.ts (channelKey: 'play', tabKey: 'taskmaster', route: '/taskmaster').
    liveUrl: /taskmaster
    channelKey: play
    tabKey: taskmaster

  - slug: appmaker
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'appmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /appmaker
    channelKey: plan
    tabKey: appmaker

  - slug: recipe-box
    status: retired
    priority: low
    kind: software

  - slug: ruler-hooked
    status: finished # flipped 2026-08-18 (conductor scheduled Agent run) -- all 15 tasks
    # done, nothing open. Goal ("playable slideshow fishing/kingdom-management PoC,
    # offline-playable") is met: m1/m2 shipped the PoC (t-001..t-013), t-014 expanded
    # content depth (6->25 cards, 1->3 arcs, 2->4 endings), t-009 covered the Steam/Unreal
    # research half of m3, and t-015 shipped an installable/offline PWA wrapper (app
    # packaging), closing out m3's three named components. Deliberately-deferred polish
    # (site-wide PWA conversion, offline SPA-nav caching via Nuxt Content, install-prompt
    # UI on other routes; an actual Steam/Unreal port) remains documented in t-015's and
    # t-009's own notes as future bucket-list scope, not required by the stated PoC goal.
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ruler-hooked' in kind_robots utils/projectPlacements.ts.
    # Corrected 2026-08-14 (conductor/coat-dance t-010 cycle) — see humboldt-scoop
    # entry above for the 'lab' channel dissolution this doc field never followed.
    liveUrl: /plan/projects/ruler-hooked
    channelKey: plan
    tabKey: projects

  - slug: music-mentor
    status: active
    priority: low
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry for
    # 'music-mentor' in kind_robots utils/projectPlacements.ts (channelKey: play,
    # tabKey: music-mentor, route: /music-mentor) — conductor music-mentor/t-006.
    liveUrl: /music-mentor
    channelKey: play
    tabKey: music-mentor

  - slug: dream-cycle
    status: continuous
    priority: low# idle fallback by design — must never outrank real ready work
    kind: software

  - slug: lora-ingestion
    status: active # onboarded 2026-07-31 -- this project had a roadmap.yaml and
    # appeared in STATUS.md (32.5%, m1 done) but was never added here, and the
    # registry is what defines projects for the kind_robots front end
    # (server/api/conductor/projects.get.ts iterates the overrides list, not the
    # projects/ directory). It was therefore invisible on the site and skipped by
    # sync_projects.py entirely. `kind` deliberately omitted so the endpoint falls
    # back to the roadmap's own `infrastructure`, which the override vocabulary
    # (software|content|proposal|brainstorm) does not cover.
    priority: normal

  - slug: kind-economy
    status: active # created 2026-08-19 by Silas — the money layer for Kind Robots:
    # Stripe going live, the three-way paid-usage split (platform / anti-malaria
    # mission / creator), creator payouts, mission remittance, and the Save One
    # Human restart-or-retire decision. Declared a "vitally important tentpole"
    # in-session, hence priority: urgent. See projects/kind-economy/DESIGN-BRIEF.md.
    priority: urgent
    kind: software
