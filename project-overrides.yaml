# Human-managed project overrides. Written by the workspace UI.
# Agents: check this before claiming tasks — skip projects where status != active.
# Priority controls how often agents pick tasks (high = more attention).
# status:   active | paused | retired | finished
# priority: low | normal | high | urgent
# kind:     software | content | proposal | brainstorm (overrides roadmap kind for UI)

overrides:
  - slug: superkate-services-calculator
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-services-calculator' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /stylist
    channelKey: lab
    tabKey: stylist

  - slug: superkate-hairstyle-ai
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'superkate-hairstyle-ai' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /stylist
    channelKey: lab
    tabKey: stylist

  - slug: kindrobots-unraid
    status: active
    priority: normal
    kind: software

  - slug: conductor
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor
    channelKey: plan
    tabKey: projects

  - slug: kind-robots
    status: active
    priority: normal

  - slug: newsfeed
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'newsfeed' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /newsfeed
    channelKey: lab
    tabKey: newsfeed

  - slug: global-ui
    status: active
    priority: normal
    kind: software

  - slug: ecosystem-map
    status: active
    priority: normal
    kind: software

  - slug: humboldt-scoop
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /humboldt-scoop
    channelKey: lab
    tabKey: humboldt-scoop

  - slug: humboldt-scoop-cms
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'humboldt-scoop-cms' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /scoop-cms
    channelKey: admin
    tabKey: scoop-cms

  - slug: model-builder
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'model-builder' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /model-builder
    channelKey: build
    tabKey: model-builder

  - slug: animation-manager
    status: active
    priority: high
    kind: software

  - slug: animation-studio
    status: retired # retired 2026-07-19 (conductor/t-039) — superseded by animation-manager,
    # which mirrors its research (t-001) and pitch-queue (t-002) tasks (both done there) and
    # is the actively-developed autonomous continuation (4/10 tasks done vs 1/8). Only
    # animation-studio's genuinely distinct work (t-003 Gravity Garden) already shipped and
    # is marked done. Remaining animation-studio ready tasks (t-001, t-002, t-004) duplicate
    # animation-manager scope already done or in flight there (t-004 "Track animation
    # attempts as Component records with Reactions" vs animation-manager's own t-004 of the
    # same description) -- keeping animation-studio claimable risked a Worker re-doing
    # already-shipped work, the same collision class as animation-manager/t-008 (see
    # TALKBACK.md 2026-07-14). roadmap.yaml kept as historical record, not deleted.
    priority: low
    kind: software

  - slug: approval-portal
    status: retired
    priority: low

  - slug: digital-storefront
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'digital-storefront' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /sanctuary
    channelKey: sanctuary
    tabKey: giftshop

  - slug: ai-art-academy
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ai-art-academy' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /academy
    channelKey: play
    tabKey: academy

  - slug: coloring-book
    status: active
    priority: high
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coloring-book' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /coloring
    channelKey: play
    tabKey: coloring

  - slug: humboldt-impropriety-calendar
    status: retired
    priority: low
    kind: brainstorm

  - slug: brainstorm
    status: active
    priority: normal
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'brainstorm' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /brainstorm
    channelKey: build
    tabKey: brainstorm

  - slug: sketchy
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'sketchy' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /sketchy
    channelKey: lab
    tabKey: sketchy

  - slug: pinball-hero
    status: retired
    priority: low
    kind: content

  - slug: art-generator-connect
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. The ArtJob admin dashboard lives at /artjob under the
    # "art" dashboard channel, tab "artjob".
    liveUrl: /artjob
    channelKey: art
    tabKey: artjob

  - slug: mural-design
    status: active
    priority: high
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'mural-design' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /mural
    channelKey: lab
    tabKey: mural

  - slug: storymaker
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry for
    # 'storymaker' in kind_robots utils/projectPlacements.ts (channelKey: 'play',
    # tabKey: 'storymaker', route: '/storymaker') — conductor/storymaker t-010.
    liveUrl: /storymaker
    channelKey: play
    tabKey: storymaker

  - slug: davinci
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'davinci' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /davinci
    channelKey: lab
    tabKey: davinci

  - slug: media-watchlist
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'media-watchlist' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /watchlist
    channelKey: lab
    tabKey: watchlist

  - slug: conductor-app
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'conductor-app' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /conductor-app
    channelKey: plan
    tabKey: conductor-app

  - slug: alexa-integration
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'alexa-integration' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /voice-lab
    channelKey: lab
    tabKey: voice-lab

  - slug: mermaids-of-venice
    status: retired # archived 2026-07-16 by Silas — not fixed on current tasks; may revisit
    priority: low
    kind: content

  - slug: packmaker
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'packmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /packs
    channelKey: build
    tabKey: packs

  - slug: coat-dance
    status: active
    priority: normal
    kind: content
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'coat-dance' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /coat-dance
    channelKey: lab
    tabKey: coat-dance

  - slug: career-transition
    status: retired # archived (reaffirmed 2026-07-16 by Silas) — conductor itself may become the career
    priority: low
    kind: content

  - slug: engagement
    status: finished
    priority: normal
    kind: software

  - slug: wishmaster
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'wishmaster' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /wishmaster
    channelKey: plan
    tabKey: wishmaster

  - slug: challenge-center
    status: finished # 20/20 tasks done — flipped 2026-07-17 with Silas's in-session approval
    priority: high
    kind: software

  - slug: serendipity
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py, same mechanism as art-generator-connect. Values match the
    # canonical PROJECT_PLACEMENTS entry for 'serendipity' in kind_robots
    # utils/projectPlacements.ts (channelKey: 'play', tabKey: 'serendipity',
    # route: '/serendipity') — NOT tutorialChannels.scenario, which is a
    # different, unrelated grouping used only by the tutorial-card system
    # (conductor/serendipity t-012; corrected same session after initially
    # writing channelKey: scenario by mistake).
    liveUrl: /serendipity
    channelKey: play
    tabKey: serendipity

  - slug: appmaker
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'appmaker' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /appmaker
    channelKey: plan
    tabKey: appmaker

  - slug: recipe-box
    status: retired
    priority: low
    kind: software

  - slug: ruler-hooked
    status: active
    priority: normal
    kind: software
    # Frontend placement — synced to the kind_robots Project row by
    # sync_projects.py. Values match the canonical PROJECT_PLACEMENTS entry
    # for 'ruler-hooked' in kind_robots utils/projectPlacements.ts (conductor/t-per-project polish tasks).
    liveUrl: /ruler-hooked
    channelKey: lab
    tabKey: ruler-hooked

  - slug: dream-cycle
    status: active
    priority: low # idle fallback by design — must never outrank real ready work
    kind: software
