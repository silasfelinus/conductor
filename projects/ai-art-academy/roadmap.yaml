project: ai-art-academy
kind: software
autonomous: true
goal: >
  Teach people the history of art — movements, styles, and public-domain creators —
  and let them remix a starter image or their own upload in any style they learn
  about, powered by the Kind Robots Kontext network.
notes_from_silas: >
  New project 2026-07-10. Adjacent to sketchy (which teaches drawing): AI Art Academy
  teaches the history of art, art styles, and creators. ONLY publicly available art and
  dead artists — we don't rip off real creators. Users choose a starter image or upload
  their own, then remix in a learned style via our kontext network. The groundwork is
  kind_robots components/art/art-styler.vue — that becomes the new front end for this
  project with a much bigger scope (plain art-styling stays as an option inside the
  Academy). One task must track down publicly available LoRAs that communicate styles;
  if kontext isn't best due to LoRA availability versus model knowledge, agents may
  recommend another engine (see DESIGN-BRIEF.md: recommendation is Kontext-first,
  prompt-mode for famous dead artists, LoRAs where evaluation shows they win).
  This is the TEST RUN of the autonomous project initiative: Claude has full reign over
  this project and art-styler.vue. Art generation via our backend is fully supported and
  pre-approved. Escalate only actual human gates. Keep the project running even without
  my input — when there's nothing to do, do a style pass, upgrade the roadmap, or
  generate more art inspirations (never-idle rule, AGENTS.md).
milestones:
  - id: m1
    title: Design brief, policy, and model strategy
    weight: 15
    status: done
  - id: m2
    title: Style registry and LoRA/knowledge evaluation
    weight: 20
    status: in-progress
  - id: m3
    title: Curriculum content (movements, styles, artists)
    weight: 20
    status: in-progress
  - id: m4
    title: Academy front end in kind_robots (art-styler evolution)
    weight: 30
    status: done
  - id: m5
    title: Project art and inspiration assets
    weight: 5
    status: in-progress
  - id: m6
    title: Continuous improvement loop
    weight: 10
    status: in-progress
tasks:
  - id: t-001
    milestone: m1
    title: Write the AI Art Academy design brief and model strategy
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-10T00:00:00Z'
    note: >
      DESIGN-BRIEF.md written 2026-07-10 from Silas's session direction: scope,
      public-domain-only ethical boundary, Kontext-first model strategy (prompt-mode
      for canonical dead-artist styles, curated LoRAs where evaluation wins,
      SDXL+IP-Adapter documented as unbuilt fallback), product shape, autonomy contract.
  - id: t-002
    milestone: m1
    title: Confirm the Academy scope and brief match Silas's intent
    status: done
    approved_by_human: true
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-10T20:00:00Z'
    note: >
      APPROVED by Silas in-session 2026-07-10 (recorded by Reviewer per the
      human-gate clearance rule). Brief and scope confirmed as written:
      public-domain/dead-artists boundary, Kontext-first strategy, art-styler.vue
      growth path with Style Lab kept inside the Academy.
  - id: t-003
    milestone: m2
    title: Hunt publicly available style LoRAs and build the style-LoRA registry
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-10T08:30:00Z'
    note: >
      DONE 2026-07-10: docs/style-lora-registry.md written. 8 of 16 target styles
      LoRA-backed + 2 bonus (Van Gogh, Pop Art); 8 prompt-mode (fine per brief). Best
      find: Owen777/Kontext-Style-Loras, a 22-style pack trained natively on Kontext
      dev for restyling. Ethics catches applied (living-artist LoRAs excluded;
      cubism ships movement-only). LICENSING FLAG spun out to t-011 (needs-human):
      most LoRAs are FLUX.1-dev NON-COMMERCIAL license. Civitai candidates parked in
      a needs-Silas table (login-gated downloads). Environment note: proxy blocked
      direct Civitai/HF fetches, so unverified licenses are marked; re-verify at
      download time. Original spec follows:
      Survey openly licensed, publicly downloadable style LoRAs compatible with
      FLUX/Kontext (Civitai open-license filters, Hugging Face) that communicate
      historical movements or dead-artist styles: impressionism, ukiyo-e, art nouveau,
      baroque chiaroscuro, cubism, woodblock, stained glass, illuminated manuscript,
      etc. Record license, download source, trigger phrase, and suggested weight.
      Cross-reference the LoRAs already on the art server (resource store + art-styler
      BUILTIN_STYLES). Output: projects/ai-art-academy/docs/style-lora-registry.md with
      a machine-readable YAML block per style. Respect the ethical boundary: skip LoRAs
      named for living artists or active brands. No downloads/spend — registry only;
      flag any LoRA that would require paid access as a line item for Silas.
  - id: t-004
    milestone: m2
    title: Evaluate prompt-only Kontext vs LoRA per style and record remix configs
    status: needs-human
    soft_gate: true
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-18T11:17:59Z'
    depends_on: t-003
    note: >
      FOR SILAS: UPDATE 2026-07-18 — the sandbox-egress clause below is stale:
      EGRESS-BLOCKERS.md now shows kind-robots.vercel.app reachable (HTTP 200,
      2026-07-18T11:17:11Z, grep "ai-art-academy/t-004"), not blocked as the
      2026-07-16 text claimed. Reachability still varies by session (blocked as
      recently as 2026-07-17T10:55Z per the same ledger) — re-run
      `scripts/recheck_egress_blocks.py kind-robots.vercel.app --task ai-art-academy/t-004`
      before relying on it. This does NOT change the actual gate: item (2) below
      (home relay token + DB up) is still unverified from this sandbox and is the
      real blocker, and dispatching live production generation still needs Silas's
      explicit go-ahead regardless of egress state.
      FOR SILAS: UPDATE 2026-07-16 — KR_API_TOKEN is NO LONGER the blocker (it is now
      provisioned in the Claude Code environment env-var field + the GitHub Actions
      secret; verified present in a session env this date). But two things keep this
      task from running, and BOTH matter: (1) The conductor sandbox CANNOT reach the
      kind_robots API — the egress gateway returns 403 CONNECT to kind-robots.vercel.app
      (same allowlist block as the museum hosts), confirmed this date [SUPERSEDED, see
      2026-07-18 update above]. So a burst/worker
      SANDBOX session can never run or verify generation directly; the real art pipeline
      is the `auto-art-generate` GitHub Action (runs on a GitHub runner with open egress
      + secrets.KR_API_TOKEN, has workflow_dispatch). (2) Rendering needs the home relay
      up with the correct KR_RELAY_TOKEN (a SEPARATE admin apiKey; can be the same value
      as KR_API_TOKEN as long as KR_RELAY_USER_ID == the server's BETA_ADMIN_USER_ID,
      default 1) AND the kind_robots DB up — the 2026-07-16 10:59Z auto-art-generate run
      showed both consume steps warning "home render box may be offline / jobs timed out"
      and "No image files in projects/process/", i.e. nothing rendered. NEXT STEP: this
      is human-gated (dispatching live production generation needs Silas's explicit go-
      ahead, per the safety gate). Once the relay's stale token is fixed + DB confirmed
      up, dispatch `auto-art-generate` (limit 1) and confirm an image lands, THEN flip
      this to `ready` and run the A/B. Kept soft needs-human. Registry input (t-003) is
      complete.
      Original spec follows:
      Using the generation backend (pre-approved), A/B a fixed test image through each
      candidate Academy style: prompt-only instruction vs LoRA (where available in the
      registry from t-003). Judge composition preservation and style fidelity. Record a
      winning remix config per style (mode: prompt|lora|hybrid, prompt template,
      loraPath/trigger/weight) in docs/style-remix-configs.yaml, keeping the generated
      comparison images as traceable evidence (prompt/model/seed metadata per the
      generated-art rule). If any must-have style fails on Kontext entirely, document
      the SDXL+IP-Adapter fallback case in the same file rather than building it.
  - id: t-005
    milestone: m3
    title: Write the art-history curriculum outline (movements, styles, artists)
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-10T08:30:00Z'
    note: >
      DONE 2026-07-10: docs/curriculum-outline.md written — 14 movements (Greek vase
      painting through De Stijl), YAML skeleton + full prose, all artists died 1953
      or earlier, 14/14 movements have one verified open-access source URL (Met x6,
      AIC x5, NGA, Rijksmuseum, Wikimedia); remaining examples marked unverified for
      batch re-check at seed time. Cubism uses Gris/Gleizes (Picasso/Braque are
      inside the 70-year window — prose context only). Front-end seed
      (kind_robots stores/seeds/academyStyles.ts, PR #143) uses matching slugs.
      Remix-quality flags: cubism and de-stijl may remix poorly — t-004 will judge.
      Original spec follows:
      Draft the v1 curriculum: 10-16 movements spanning the timeline (e.g. Renaissance,
      Baroque, Ukiyo-e, Romanticism, Impressionism, Post-Impressionism, Art Nouveau,
      Expressionism, Cubism, Bauhaus, Surrealism-era public domain works...), each with
      era, key ideas, recognition cues, 2-4 dead public-domain artists with short bios,
      and 3-6 example works referenced from open-access collections (Met CC0, AIC CC0,
      NGA, Rijksmuseum, Smithsonian, Wikimedia PD) with source+license per image.
      Output: docs/curriculum-outline.md structured so it can convert directly into
      front-end seed data. Every style entry must map to a remix config (t-004) or
      explicitly note "lesson-only, no remix yet."
  - id: t-006
    milestone: m3
    title: Write the public-domain policy and curate the starter image library plan
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-10T08:30:00Z'
    note: >
      DONE 2026-07-10: PUBLIC-DOMAIN-POLICY.md (verified 2026 US cutoff: published
      1930 or earlier, rolling year-96 rule; conservative combined rule: artist died
      before 1956 AND work within US cutoff, default-deny; provenance JSON schema;
      anti-style-laundering clause; Style Lab exemption) and
      docs/starter-image-library.md (21 starters across 7 subject groups, 11 verified
      against live official open-access pages, local-copies-plus-manifest hosting
      plan targeting kind_robots public/images/academy/starters/). Original spec follows:
      PUBLIC-DOMAIN-POLICY.md: the concrete rule set for what art and artists the
      Academy may include (death-year thresholds, US public-domain cutoffs, open-access
      license types we accept, how to record provenance). Plus the starter-image plan:
      a curated set of public-domain works users can remix without uploading, with
      per-image source/license metadata and download list (hotlink vs local copy
      decision). Flag any genuinely ambiguous licensing question as a needs-human line
      item; do not park the task on it.
  - id: t-007
    milestone: m4
    title: 'kind_robots: scaffold the Art Academy surface wrapping art-styler'
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-10T08:30:00Z'
    note: >
      DONE 2026-07-10: kind_robots PR #143 merged (squash 78bb545). /academy page
      with academy dashboard channel (Timeline / Styles / Remix Studio / Style Lab),
      14-movement seed registry (stores/seeds/academyStyles.ts), academyStore
      progress persistence, and art-styler extended for external registries +
      prompt-mode styles with builtin behavior unchanged. Verified: eslint,
      prettier, vue-tsc (zero new errors). Academy tab images queued in
      art-prompts.yaml requests. Original spec follows:
      Cross-repo task (kind_robots). Create the Academy front end: an academy dashboard
      channel or /academy page with tabs for Timeline, Styles, Remix Studio, and Style
      Lab (the existing art-styler.vue mounted as-is for free play). Follow the
      dashboardConfigs pattern in stores/helpers/dashboardHelper.ts (canonical tab
      registry — no temporary bridges). Remix Studio reuses art-styler's source-image
      machinery but drives its style list from the Academy style registry (curriculum
      seed from t-005 + remix configs from t-004) with a historical-context blurb per
      style. Claude has standing authority over art-styler.vue refactors for this
      project (Silas 2026-07-10); keep the component reusable for the plain styler use.
  - id: t-008
    milestone: m4
    title: 'kind_robots: download Academy starter image library + provenance manifest'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-17T11:51:30Z'
    depends_on: t-007
    note: >
      SPLIT 2026-07-15 (conductor/t-010 roadmap-upgrade pass) from the original
      3-part t-008, which bundled a real-download task with two front-end wiring
      tasks — oversized for one landable PR per the Failure-triage "scope"
      category. This task is part 1 only: download the 21 public-domain starter
      images per docs/starter-image-library.md into
      public/images/academy/starters/ with the provenance manifest
      (starters.manifest.json), re-verifying each source URL at download time per
      PUBLIC-DOMAIN-POLICY.md. Parts 2 and 3 are now their own tasks: t-013
      (example-works strip) and t-014 (Remix Studio source-picker tab, depends on
      this task's manifest existing).

      EGRESS: metmuseum.org / upload.wikimedia.org reachability is tracked in
      EGRESS-BLOCKERS.md (grep "ai-art-academy/t-008") — blocked with a fresh
      CONNECT-tunnel 403 across four consecutive sessions 2026-07-15 → early
      2026-07-17, then REACHABLE at the 2026-07-17T07:51Z recheck (this varies by
      session/environment, so re-run scripts/recheck_egress_blocks.py before
      claiming). An environment limitation, not a task defect — transient per
      Failure-triage, so stays `ready` without burning passes.
    claimed_by: claude-conductor-autonomous-20260717T1150Z
    claimed_at: '2026-07-17T11:51:30Z'
  - id: t-013
    milestone: m4
    title: 'kind_robots: add example-works strip to Academy lesson detail'
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-17T21:00:00Z'
    depends_on: t-007
    note: |-
      DONE 2026-07-17 (~20:56 UTC, conductor autonomous hourly cycle, claude-conductor-hourly-20260717T2048Z): applied the prepared handoff (docs/t-013-remaining-example-works.md) directly in a local kind_robots checkout -- the connector-blob-truncation limitation that produced the handoff doesn't apply to a session with real git access. Downloaded and verified the three remaining images (Franz Marc's The Yellow Cow, Guggenheim 49.1210; Juan Gris's Fantômas, NGA 1976.59.1, resized to the documented 1600px-long-edge/quality-88 spec; Kandinsky's Composition 8, Guggenheim 37.262), added exampleWorks entries to stores/seeds/academyStyles.ts and matching records to examples.manifest.json. This completes the Academy example-works strip at 21/21 movements. Verified: npm run test:academy-examples-manifest (21/21), prettier clean, eslint clean, full-project vue-tsc --noEmit exit 0. kind_robots PR #372 merged (squash 805bb44). Full source-verification detail and prior partial-landing history (18/21 via PR #363) preserved below.

      REVIEWED 2026-07-17 (burst-mode conductor cycle, reviewer session claude-conductor-hourly-20260717b): merged kind_robots PR #363 (squash 9f12bd8) and conductor PR #705 -- ships 6 more movements (gothic, northern-renaissance, rococo, neoclassicism, symbolism, pointillism), taking the total to 18 of 21. All kind_robots CI checks green (TypeScript, Contract verifiers incl. verifyAcademyExamplesManifest.ts, GitGuardian) and all 21 conductor CI checks green. Sourced entirely from curriculum-outline.md's own pre-identified expected candidates (previously flagged unverified due to museum-egress blocks in an earlier session) -- verified live against the Met Collection API (isPublicDomain: true) plus PUBLIC-DOMAIN-POLICY.md section 1.3 (artist died 70+ years ago AND work created/published on or before the US cutoff year): Duccio's Madonna and Child (Met 2004.442), Memling's Tommaso and Maria Portinari (Met 14.40.626-27), Chardin's Soap Bubbles (Met 49.24), David's The Death of Socrates (Met 31.45), Moreau's Oedipus and the Sphinx (Met 21.134.1), Seurat's Circus Sideshow (Met 61.101.17). Images downloaded full-res and resized to a 1600px long edge to match the manifest's existing file-size convention. Remaining scope, kept ready once this PR lands (partial-scope landing, not a rejection): (1) cubism and bauhaus -- still need a different public-domain source; the AIC-sourced Juan Gris/Kandinsky/Klee works curriculum-outline.md once marked VERIFIED are now confirmed non-public-domain via api.artic.edu's is_public_domain:false with live ARS/VG Bild-Kunst copyright notices. (2) NEW FINDING this cycle: expressionism also has no exampleWorks entry in academyStyles.ts and was not mentioned in the prior cycle's scope note as remaining work -- unclear whether it was silently missed in an earlier pass or intentionally deferred; needs a source hunt (Munch/Kirchner/Marc, all pass section 1.3) same as the other six landed this cycle. Original split-history: split 2026-07-15 from t-008 (part 2 of 3); scope expanded 2026-07-16 (t-010 cycle) from 16 to 21 movements.

      Manual recovery run after restoring the hourly Conductor Worker; remaining scope is the Expressionism, Cubism, and Bauhaus public-domain example works.

      Source audit and exact cross-repo implementation handoff completed in projects/ai-art-academy/docs/t-013-remaining-example-works.md; connector-safe fallback PR is ready for merge.

      Merged conductor PR
    claimed_by: claude-conductor-hourly-20260717T2048Z
    claimed_at: '2026-07-17T20:56:00Z'
  - id: t-014
    milestone: m4
    title: 'kind_robots: wire starter library into Remix Studio source picker'
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-17T19:20:21Z'
    depends_on: t-008
    note: >
      Split 2026-07-15 from the original t-008 (part 3 of 3) — genuinely depends
      on t-008's local starters/ directory and starters.manifest.json existing.
      Wire the starter library into the Remix Studio source picker as a third
      tab next to Upload/Gallery, reusing art-styler's existing source-image
      machinery.
    claimed_by: claude-conductor-hourly-20260717d
    claimed_at: '2026-07-17T18:51:10Z'
  - id: t-009
    milestone: m5
    title: Generate project art and Academy inspiration images
    status: needs-human
    soft_gate: true
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-18T11:17:59Z'
    note: >
      FOR SILAS: UPDATE 2026-07-18 — see t-004's 2026-07-18 update for the same
      correction: EGRESS-BLOCKERS.md now shows kind-robots.vercel.app reachable
      (HTTP 200, grep "ai-art-academy/t-004"), not blocked as the 2026-07-16 text
      below claims. Reachability varies by session; re-run
      `scripts/recheck_egress_blocks.py kind-robots.vercel.app --task ai-art-academy/t-009`
      before relying on it. Gate (2) below (home relay + DB) is unchanged and is the
      real remaining blocker either way.
      FOR SILAS: UPDATE 2026-07-16 — KR_API_TOKEN is NO LONGER the blocker (same
      correction as t-004; token now in the env-var field + GitHub secret, verified
      present this date). Remaining gates, both real: (1) the conductor sandbox can't
      reach the kind_robots API (egress 403 CONNECT to kind-robots.vercel.app), so
      [SUPERSEDED, see 2026-07-18 update above]
      generation runs via the `auto-art-generate` GitHub Action (open-egress runner +
      secrets.KR_API_TOKEN), not from a sandbox session; (2) the home render relay must
      be up with the correct KR_RELAY_TOKEN (+ KR_RELAY_USER_ID == BETA_ADMIN_USER_ID)
      and the kind_robots DB up — the 10:59Z auto-art-generate run this date rendered
      nothing (both consume steps warned box-offline/timeout). Dispatching live
      generation is human-gated (needs Silas's explicit go-ahead per the safety gate).
      NEXT STEP: once the relay's stale token is fixed + DB up, dispatch auto-art-generate
      (limit 1), confirm an image lands, then flip this to `ready`. Kept soft needs-human.
      Original spec follows:
      Icon/card/hero prompts are queued in projects/art-prompts.yaml (added 2026-07-10).
      Generate and commit them via the auto art pipeline (pre-approved), then generate
      3+ Academy inspiration images (e.g. one landmark public-domain work re-imagined
      across several movements as a teaching strip). Keep prompt/model/source metadata.
      Remove ART-PROMPTS.md entries once files land in projects/images/.
  - id: t-011
    milestone: m2
    title: Decide the commercial-licensing posture for FLUX-dev/Kontext LoRAs
    status: done
    approved_by_human: true
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-10T20:00:00Z'
    note: >
      DECIDED by Silas in-session 2026-07-10 (recorded by Reviewer per the
      human-gate clearance rule). Ruling: no paid generation on
      non-commercially-licensed weights, ever. When monetization arrives, use
      only license-clean or officially supported engines. Concretely:
      (1) FLUX.1 schnell (Apache 2.0) is approved for paid TEXT-TO-IMAGE on our
      own servers — Silas asked "can't I use schnell instead?" and the answer is
      yes for t2i. (2) Kontext-style REMIX/EDIT has no schnell equivalent
      (Kontext dev is non-commercial), so paid remixing must route through an
      officially supported endpoint: BFL's Kontext pro/max API, fal/Replicate
      licensed hosting, or OpenAI images (whose terms allow commercial use of
      outputs). (3) Free-tier/educational/internal generation continues
      unchanged on the current stack, including dev-based LoRAs. Economy work
      (coloring-book t-008) should design token pricing around per-image
      licensed-API costs. Dev-based weights and dev-trained LoRAs stay excluded
      from every paid path.

  - id: t-012
    milestone: m2
    title: Confirm resolve_deps.py treats decision-style approved_by_human (t-011) the same as brief-confirmation gates
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-14T19:13:59Z'
    note: 'DONE (verification only, 2026-07-14, burst-mode cycle): confirmed scripts/resolve_deps.py''s satisfied() has no task-type/kind branching at all -- it only inspects status, gate_human, and approved_by_human, so a licensing DECISION (t-011''s shape: status done + approved_by_human true, no gate_human) and a brief-confirmation checkpoint (status done + gate_human true + approved_by_human true) are structurally identical to it. No code change needed. Added tests/test_resolve_deps.py (12 tests) covering satisfied() directly plus end-to-end main() promotion of a waiting task through both gate shapes, an unapproved-gate negative case, multi-dependency AND-gating, --dry-run no-write, and _template skip -- this repo had zero prior test coverage for resolve_deps.py. Also confirmed next_ready_task.py and audit_roadmaps.py implement the identical type-agnostic satisfied()/dependency_satisfied() logic independently (noted as a follow-up dedup opportunity, not fixed here -- out of this task''s scope). Full suite (200 tests) and audit_roadmaps.py (0 errors) both green. Original task note: Kaizen from PR #362 (Reviewer, 2026-07-10): t-002 and t-011 both now carry approved_by_human: true + status: done, but t-011 is a licensing DECISION rather than a scope-confirmation checkpoint. Verify resolve_deps.py''s dependency check treats both shapes identically (it should only care about approved_by_human + status: done, not the task''s semantic type) so any task depends_on: t-011 correctly flips from waiting to ready on the next Worker cycle. If the resolver has any type-specific branching, fix it.'
    claimed_by: conductor-burst-1784056316
    claimed_at: '2026-07-14T19:11:56Z'

  - id: t-016
    milestone: m6
    title: Document academy-style-detail.vue's three usage contexts atop the component
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-15T22:20:00Z'
    note: >
      Kaizen from t-010's 2026-07-15 21:15 UTC cycle (kind_robots PR #301, merged).
      academy-style-detail.vue is reused in three call sites
      (academy-styles-browser.vue, academy-timeline.vue, academy-remix.vue) each
      passing a different subset of showClose/showRemixButton/@remix — the
      showRemixButton no-op-handler bug that PR fixed happened precisely because a
      new prop's meaning wasn't obvious from the component alone. Add a short
      comment atop the component (or in its script block) listing the three usage
      contexts and what each expects, so a future prop addition doesn't silently
      miss one. Small, docs-only, no design judgment needed.
      DONE 2026-07-15T22:20Z: kind_robots PR #302 merged (squash, sha 176eb60),
      adding a comment block atop the <script setup> in academy-style-detail.vue
      listing all three call sites and what each expects. Diff verified docs-only
      (comment addition, no logic touched); all 3 CI checks (TypeScript, Contract
      verifiers, GitGuardian) green before merge. conductor PR #579 (this roadmap
      update) merged separately, sha 105bd4c.
    claimed_by: claude-burst-20260715-2300
    claimed_at: '2026-07-15T22:06:16Z'

  - id: t-017
    milestone: m6
    title: Add a regression check that fails if academy-style-detail.vue gains a new caller undocumented in its usage-context comment
    status: done
    owner: worker
    passes: 1
    stakes: reversible
    retry_context: >
      pass 1 failed (quality): kind_robots PR #303 (utils/scripts/verifyAcademyStyleDetailCallers.ts)
      fails the TypeScript check for real, not the known pre-existing baseline — verified by
      reproducing locally (`npm run test` on the PR branch) and confirming main at 176eb60 is
      currently green on the same typecheck.yml workflow. Error: line 55,
      `documentedCallers.add(match[1])` — the project's tsconfig sets
      `noUncheckedIndexedAccess: true`, so `match[1]` from `lineRegex.exec(contents)` types as
      `string | undefined`, not `string`. Do differently: guard the assignment (e.g.
      `if (match[1]) documentedCallers.add(match[1])`, or an `assert.ok(match[1])` check
      consistent with the file's existing `node:assert/strict` usage) before merging. Everything
      else about the PR (approach, test coverage, facet-alias-smoke being the separate known
      pre-existing failure) was correct — this is a one-line fix, not a rewrite. See review
      comment: https://github.com/silasfelinus/kind_robots/pull/303#issuecomment-4986167791
    note: >
      Kaizen from t-016 (Reviewer, 2026-07-15): t-016 added a hand-written comment
      documenting academy-style-detail.vue's three call sites
      (academy-styles-browser.vue, academy-timeline.vue, academy-remix.vue) and what
      props each passes — but a hand-written comment can silently go stale the same
      way the undocumented prop did before t-010/t-016 (PR #301 fixed a
      showRemixButton no-op-handler bug caused exactly by this). Add a small test
      (e.g. a grep-based check in the existing test suite, or a lint rule) that
      fails when a new file imports/uses academy-style-detail.vue without a
      matching entry in the doc comment, so the documentation can't drift from
      reality the next time a caller is added. kind_robots PR #302's own body and
      the conductor PR #579 body both omitted the handoff template's
      Stakes/Flags-for-Reviewer/Kaizen-suggestion sections — substituting this
      kaizen since neither PR offered one.
      REJECTED 2026-07-15T23:20Z (pass 1, quality): see retry_context above. kind_robots
      PR #303 left open for the fix rather than opening a new PR.
      DONE 2026-07-15T23:26Z: pushed the exact one-line guard the retry_context asked
      for (`const filename = match[1]; if (filename) documented.add(filename)`) on the
      same PR, re-ran `npm run test` locally to confirm the TypeScript check passes,
      and confirmed the failure timing was a race — the fix commit's CI (queued
      23:12:10Z) hadn't finished when the pass-1 rejection posted (23:13:03Z). kind_robots
      PR #303 merged clean (TypeScript + Contract verifiers green; facet-alias-smoke's
      failure is the separate known pre-existing baseline, unrelated to this change).
    claimed_by: claude-burst-20260715-2300
    claimed_at: '2026-07-15T23:25:33Z'
    updated: '2026-07-15T23:26:30Z'

  - id: t-010
    milestone: m6
    title: Academy continuous improvement pass
    status: claimed
    recurring: true
    passes: 0
    stakes: reversible
    updated: '2026-07-21T06:13:58Z'
    note: |-
      Standing never-idle task (re-arms to ready after every completion, per the recurring-task rule). Each cycle pick ONE: (a) front-end style/polish pass on Academy or art-styler surfaces, (b) roadmap upgrade — refine tasks, detail the next milestone, prune stale notes, (c) generate more art inspirations or style preview thumbnails, (d) expand the curriculum with a new movement/artist entry (respecting PUBLIC-DOMAIN-POLICY.md). Note which option ran in the PR. This task never reaches done and does not count toward milestone progress.
      RAN 2026-07-21 (~05:05-05:57 UTC, claude-conductor-scheduled-burst-20260721T0505Z): option (a) front-end polish (lane 1), per the checklist's next-preferred-lane note (previous cycle ran lane 4, curriculum depth). Dispatched an Explore subagent over the full in-scope surface (art-styler.vue, image-upload.vue, all components/academy/*.vue, academyStore.ts, styleHelper.ts, academyStyles.ts) with the checklist's exclusion list of every bug class already fixed in prior cycles. Found a real, verifiable rendering bug in `art-styler.vue`'s `sourceImageSrc`/`resultImageSrc` computeds: both treated `img.path` as a URL fallback whenever `img.imagePath` was empty. Traced end-to-end that this is wrong for the Remix Studio's primary flow — `ArtImage.path` is a real URL only for folder-synced collections (`server/utils/syncFolderCollection.ts`, which also sets `imagePath` to the same value); every upload flow instead writes a bracketed metadata tag (`'[UploadedImage]'`, `'[UserAvatar]'`, `'[BotImage]'`, etc. — confirmed all 7 write sites across the app) into `path`, and `imagePath` is only populated outside production (`server/utils/UploadArtImage.ts` line 106). So in production, selecting an uploaded gallery image as a remix source hit `path = '[UploadedImage]'` (truthy) and rendered a broken-image icon in both the "Ready to style" banner and the Source/Result comparison panel, even though a usable `thumbnailData` payload had just been fetched by `selectGalleryImage()` and was sitting unused one branch below. (Noted but did not adopt for this fix: two competing shared resolvers already exist in kind_robots, `utils/artImageSource.ts`'s `isProbablyPath()` and `utils/artImageSrc.ts`'s `resolveArtImageSrc` — the former already handles this exact placeholder-tag case correctly, the latter has the same bug as the pre-fix art-styler.vue code — but neither is used by any Academy/art-styler component today, and the two files also collision-warn on a duplicated `ArtImageLike` export at build time; consolidating art-styler.vue onto one of them is a bigger, separate refactor, filed as a candidate for a future lane-1 or lane-2 cycle rather than bundled into this fix.) Added a small `isPlaceholderImagePath()` guard (bracket-wrapped strings only) so the fallback chain skips a placeholder tag and falls through to `thumbnailData`/`imageData`, leaving folder-synced images (genuine URL in `path`) unaffected. Verified: `npx eslint` and `npx prettier --check` both clean (after `--write` reformatted the two multi-line ternaries), full-project `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #745 (branch `claude/keen-fermat-vev7uz`): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian) — merged (merge commit, not squash; 1 commit, 15 additions/2 deletions/1 file, matching this session's diff exactly with no drift). Updated continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap accuracy (lane 2) — this cycle ran lane 1, so lane 2 is next in the 1→2→3→4 rotation. Rearmed to `ready`.
      RAN 2026-07-21 (~04:05-04:35 UTC, claude-conductor-scheduled-burst-20260721T0405Z): option (d) curriculum depth (lane 4), per the checklist's next-preferred-lane note (previous cycle ran lane 2, roadmap accuracy). Tried lane 3 (inspiration/preview assets) first with a genuinely fresh queued job (job 1184, greek-vase-painting.webp — not 816/855/957/1014/1175): `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 1 --timeout 90` queued it (200 OK) but timed out after 90s still queued; a direct `GET /api/art/queue/1184` confirmed `status: PENDING`, `claimedAt`/`claimedBy` both null, `updatedAt` identical to `createdAt` — same never-claimed signature as every prior probe, home relay still down. Fell back to lane 4 per the checklist's own instruction. Added a 26th movement, Song Dynasty Landscape Painting (`song-dynasty-landscape`, curriculum-outline.md §26) — the second non-Western entry (after persian-miniature) and the first from East Asia beyond Ukiyo-e, filling a real gap in the curriculum's geographic coverage. All 3 example works fetched and confirmed **VERIFIED** directly via `WebFetch` of their Wikimedia Commons file pages: Fan Kuan's *Travelers Among Mountains and Streams* (c. 1000, artist c. 960 – c. 1030, National Palace Museum Taipei), Guo Xi's *Early Spring* (1072, artist d. c. 1090, National Palace Museum Taipei), and Xu Daoning's *Fishermen on a Mountain Stream* (c. 1049, artist d. 1052, The Nelson-Atkins Museum of Art) — all three carry Creative Commons Public Domain Mark 1.0 tags on their Commons file pages and pass PUBLIC-DOMAIN-POLICY.md §1.3 with a wide margin (all three artists died roughly 900-1000 years ago; all three works date to c. 1000-1072, centuries before the 1930/1931 US-publication prong even matters). Confirmed no existing movement covers Chinese art before adding this. Queued its style-preview prompt in art-prompts.yaml (`kind-robots-academy-style-preview-song-dynasty-landscape`), still blocked on the same relay issue as the other 25. Front-end sync to `academyStyles.ts` and a `style-lora-registry.md`/`teaching-notes.md` entry deliberately deferred to future cycles, matching how persian-miniature landed across separate cycles (curriculum addition, then LoRA-registry entry, then teaching-notes row, then academyStyles.ts sync via kind_robots PR #616) rather than one. `python scripts/audit_roadmaps.py` — 0 errors, 11 warnings, 46 info findings, same baseline as before this change, no new findings. Conductor-docs-only change; no kind_robots PR needed this cycle. Updated continuous-improvement-checklist.md's rotation state and its curriculum-coverage table (26 movements, 26 style-preview prompts queued); next preferred lane is front-end polish (lane 1) — this cycle ran lane 4, so lane 1 is next in the 1→2→3→4 rotation. Rearmed to `ready`.
      RAN 2026-07-21 (~03:00-03:10 UTC, claude-conductor-scheduled-burst-20260721): option (b) roadmap accuracy (lane 2), per the checklist's next-preferred-lane note (previous cycle ran lane 1, front-end polish, PR #737). Ran `scripts/audit_roadmaps.py` (same 0-errors/11-warnings/46-info baseline, only this project's 2 pre-existing `APPROVAL_WITHOUT_GATE` info findings) and `scripts/check_pr_merged_drift.py` (all 20 candidates unverifiable via this sandbox's direct GitHub API access, as usual — cross-checked manually via GitHub MCP `pull_request_read`). Found and fixed a real, verifiable staleness bug, though outside this project: `coat-dance/t-010` sat at `status: review` referencing kind_robots PR #650, but that PR merged 2026-07-20T16:12:43Z — over a day earlier (the 2026-07-20 ~20:00 UTC lane-2 cycle below had already confirmed #650 merged and logged "no drift," but never checked whether coat-dance's own roadmap status field reflected that — the same status-field-lags-its-own-note bug class as this task's own 2026-07-21 ~01:00 UTC self-correction). `coat-dance/t-010`'s note already said the remaining scope (art-relay-blocked asset gen, admin Placements click) was "kept ready," confirming the intended post-merge state — fixed by setting `status: ready`, `owner: null`, clearing `claimed_by`/`claimed_at`, and recording the drift-fix rationale in its note. Also spot-checked `kind_robots#834` referenced in this task's own history (already correctly documented as a prior no-op-diff merge, not live drift). Did not re-probe t-019/t-033 (still-404 starter-styles path and home-relay-down blockers) — both fresh-checked only ~2 hours earlier this same day with no signal anything changed, per this checklist's own blocker-discipline rule. Conductor-docs-only change; no kind_robots PR needed this cycle. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is inspiration/preview assets (lane 3, recheck with a fresh queued job, not 1175) falling back to lane 4 if still blocked. Rearmed to `ready`.
      RAN 2026-07-21 (~02:35-02:45 UTC, claude-conductor-agentrun-20260721T0235Z): option (a) front-end polish (lane 1), per the checklist's next-preferred-lane note (previous cycle ran lane 4, curriculum depth). Dispatched an Explore subagent over the full in-scope surface (art-styler.vue, image-upload.vue, all components/academy/*.vue, academyStore.ts, styleHelper.ts) with the checklist's exclusion list of every bug class already fixed in prior cycles. Found a real, verifiable gap: `academy-remix.vue`'s `<academy-style-detail>` usage (Remix Studio sidebar) had no `:key`, so switching styles in the embedded art-styler grid patched the same component instance in place instead of remounting it — `academy-style-detail.vue` only calls `markLessonViewed()` in `onMounted`, so every style opened this way after the first in a session silently never got credited as viewed (no checkmark, no `lessonsViewedCount` increment, no error, miss persisted to localStorage). Confirmed by reading both files directly: same bug class PR #646 fixed in `academy-styles-browser.vue`, but that fix never touched this component's independent instance. Fixed with `:key="academyStore.selectedStyle.slug"`, mirroring the existing pattern exactly. Verified: `npx eslint` and `npx prettier --check` both clean, full-project `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #737 (branch `claude/vigilant-edison-3owhvw`): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian) — merged squash `5ad8b57`. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap accuracy (lane 2) — this cycle ran lane 1, so lane 2 is next in the 1→2→3→4 rotation. Rearmed to `ready`.
      RAN 2026-07-21 (~02:11-02:35 UTC, claude-conductor-agentrun-20260721T0211Z): option (d)/lane 4 (curriculum depth, LoRA-registry follow-up), per the checklist's next-preferred-lane note (previous cycle ran lane 2, roadmap accuracy). Tried lane 3 (inspiration/preview assets) first with a genuinely fresh queued job: `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 1` queued job 1175 (greek-vase-painting.webp, not 1173/1014/957/855/816) — direct API check confirmed `status: PENDING`, unclaimed, `updatedAt` unchanged since creation, same never-claimed signature as every prior check back to 2026-07-18. Fell back to lane 4 per the checklist's own instruction. `neoclassicism` (curriculum §6) was one of the original 16 target styles with no registry entry at all, deferred across many prior lane-4 cycles in favor of newer movements. With Hugging Face and Civitai both freshly confirmed reachable (`recheck_egress_blocks.py`, both HTTP 200), searched for the movement's central figure and found `NobodyButMeow/french-neoclassic-portrait-style-jacques-louis-david` (Civitai model 1113267, FLUX.1 D) — model card discloses its sole training corpus as 19 portrait paintings by Jacques-Louis David (d. 1825, the movement's central figure per curriculum-outline.md §6), comfortably past the dead-70-years boundary. Verified the download is genuinely no-login by following the actual redirect chain (`civitai.com/api/download/...` -> `307` -> `b2.civitai.com` -> `200`), not just reading the page. Promoted to LoRA mode in `style-lora-registry.md` (v1.5 update: coverage-summary paragraph, curriculum-slug-mapping table row, machine-readable YAML entry, full per-style notes section) and re-validated the YAML block still parses (24 entries, `neoclassicism` present once). Also rechecked `artic.edu` (American Regionalism §24 example works, still unverified from the 2026-07-19/20 cycles): still blocked, now bot-challenged (`cf-mitigated: challenge`) rather than the earlier `402` — same practical outcome, logged to `EGRESS-BLOCKERS.md` per the blocker-discipline rule instead of repeating recheck prose. `scripts/audit_roadmaps.py` — 0 errors, same 11-warning/46-info baseline, no new findings. Conductor-docs-only change; no kind_robots PR needed (the registry is a conductor-repo doc, not synced into the kind_robots front end). Updated continuous-improvement-checklist.md's rotation state; next preferred lane is front-end polish (lane 1) — this cycle ran lane 4, so lane 1 is next in the 1→2→3→4 rotation. Rearmed to `ready`.
      RAN 2026-07-21 (~01:00-01:20 UTC, claude-conductor-burst-20260721T0100Z): option (b) roadmap accuracy (lane 2), per the checklist's next-preferred-lane note (previous cycle ran lane 1, front-end polish, PR #733/#942). Found and fixed a real process-accuracy bug in this task itself: the prior cycle's note said "Rearming to `ready` per the recurring-task convention once PR #733 merges," and PR #733 (kind_robots) merged cleanly (squash `1113274d`, confirmed via `list_commits`) — but the conductor-side PR (#942) that recorded the note only appended the note text; it never actually flipped this task's `status:` field, so t-010 sat at `status: claimed` (holding its claim from `claude-conductor-scheduled-20260721T0011Z`) with no session actively working it and no way for a future picker to reclaim it short of the 90-minute stale-claim TTL. Verified via `claim_task.py --dry-run` (ALREADY_CLAIMED against a fresh `claimed_at`, i.e. not yet stale) and by inspecting the PR #942 diff directly (`git show 3777f86 -- projects/ai-art-academy/roadmap.yaml`: only the note line was added, no `status:` change). Root cause: the recurring-task rearm is a manual step at the end of a cycle's implementation, and this cycle's own note text asserted it would happen "once PR #733 merges" — a promise for a later step that nothing then executed, since the PR that recorded the promise was the same PR that should have fulfilled it after the kind_robots merge landed (which happened after the conductor PR's own diff was already written). Fixed by flipping `status: ready` directly in this cycle. Also reconfirmed t-019 still genuinely blocked (`get_file_contents` on kind_robots' `public/images/academy/styles/` still 404s) and re-checked lane 3: queued one fresh live job via `consume_art_requests.py --id-prefix kind-robots-academy-style-preview --live --limit 1` (job 1173, greek-vase-painting.webp) — still `status: PENDING`, unclaimed, same home-relay-down signature as every prior check back to 2026-07-18; not pursuing lane 3 further this cycle. `scripts/audit_roadmaps.py` — 0 errors, 11 warnings, 46 info findings, same 2 pre-existing findings, no new ones (this status-drift bug isn't a shape the script checks for). Milestones m1-m6 re-verified against current task statuses: no drift (m6 correctly `in-progress` per the prior cycle's fix). Conductor-docs-only change; no kind_robots PR needed this cycle. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is inspiration/preview assets (lane 3, recheck with a fresh queued job, not 1173) falling back to lane 4 if still blocked. Rearmed to `ready`.
      RAN 2026-07-21 (~00:11-00:30 UTC, claude-conductor-scheduled-20260721T0011Z): option (a) front-end polish (lane 1), per the checklist's next-preferred-lane note (previous cycle ran lane 4, curriculum depth). Dispatched an Explore subagent over the full in-scope Academy/art-styler surface with an exclusion list of every bug class already fixed in prior cycles. Found a real, verifiable gap: `art-styler.vue`'s `handleFileSelect` (browse-click file-input path) passed any OS-picked file straight to `processUploadedFile()` with zero type validation, unlike its sibling `handleDrop` (drag-and-drop path), which validated via `file.type.startsWith('image/')` — most OS file dialogs let a user switch to "All Files" even with `accept` set, so a non-image selected via browse could silently produce a broken preview or an opaque backend error on style transfer. Extracted a shared `isAcceptedImageFile()` predicate (exact match against `image/png`/`image/jpeg`/`image/webp`) and applied it to both handlers, also tightening `handleDrop`'s looser check (previously let GIF/SVG/BMP/TIFF through silently, contradicting the "PNG · JPEG · WebP" UI copy). Verified: `npx eslint` clean, `npx prettier --check` clean, full-project `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #733 (branch `claude/vigilant-edison-w3m45u`). Updated continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap accuracy (lane 2). Rearming to `ready` per the recurring-task convention once PR #733 merges.
      RAN 2026-07-20 (~20:00-20:20 UTC, claude-conductor-burst-20260720T2000Z): option (b) roadmap accuracy (lane 2), per the checklist's next-preferred-lane note (previous cycle ran lane 1, front-end polish, PR #672). Spot-checked PR-merge drift via GitHub MCP `pull_request_read` (direct `check_pr_merged_drift.py` API calls still 403 in this sandbox, same known limitation): confirmed kind_robots#672 (this project's most recent), kind_robots#650 (coat-dance/t-010, also flagged by the drift script), and conductor#868 all genuinely `merged: true` — no drift. `scripts/audit_roadmaps.py` — 0 errors, 11 warnings, 46 info findings; same 2 pre-existing info-level `APPROVAL_WITHOUT_GATE` findings for this project (t-002/t-011), not defects. Reconfirmed t-019 still genuinely blocked (`get_file_contents` on kind_robots' `public/images/academy/styles/` still 404s). Found and fixed a real milestone-accuracy bug: m6 ("Continuous improvement loop", weight 10) was `status: done`, but t-035 (`kind_robots: batch-generate the 25 queued Academy style-preview thumbnails once the home relay recovers`, non-recurring, milestone m6) is `status: ready` — t-035 was tagged onto m6 (alongside the t-029/t-030 kaizen follow-ons) at some point after m6 was last verified done, and the milestone was never revisited. Flipped m6 to `in-progress` to match (t-016/t-017/t-029/t-030 done, t-035 ready, t-010 itself excluded as recurring per AGENTS.md). Verified against `scripts/build_status.py`'s weighted-milestone formula: this moves ai-art-academy's computed progress from 77.5% to 72.5% on the next STATUS.md regeneration (m6 done→in-progress: -5) — a real, verifiable correction, not cosmetic. Checked the other five milestones (m1-m5): all already accurate against their tasks' current statuses. Conductor-docs-only change; no kind_robots PR needed this cycle. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is inspiration/preview assets (lane 3, recheck with a fresh queued job, not 957) falling back to lane 4 if still blocked. Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~18:12-18:35 UTC, claude-conductor-agent-20260720T1830Z): option (a) front-end polish (lane 1), per the checklist's next-preferred-lane note (previous cycle ran lane 4, curriculum depth). Dispatched an Explore subagent over all 5 Academy components plus art-styler.vue/image-upload.vue with an exclusion list of every pattern already fixed in prior cycles, so it had to find something genuinely new. Found a real, verifiable bug in `image-upload.vue`'s `handleBatchUpload()`: on a fully-successful batch upload, the function set the success confirmation banner text and then immediately called `clearQueue()`, which unconditionally resets `message.value`/`error.value` back to `''` on the very next line, in the same synchronous tick — so the "✓ N images uploaded" confirmation never actually rendered on the happy path (the most common outcome). The failure/partial-failure path was unaffected (clearQueue() isn't called there), which is why the asymmetry survived this many polish cycles. Fixed by reordering so clearQueue() runs before the message/error assignment. Verified: `npx eslint` and `npx prettier --check` both clean, full-project `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #672 (branch claude/vigilant-edison-gkpke6): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian) — merged squash `070a7b8`. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap accuracy (lane 2). Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~17:12-17:26 UTC, claude-conductor-agentrun-20260720T1712Z): tried lane 3 (inspiration/preview assets) first per the checklist's next-preferred-lane note, with a fresh queued job rather than re-polling job 816. `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 1` queued job 957 (greek-vase-painting.webp) against `https://kind-robots.vercel.app/api/art/queue`, then the script's own poll loop hit a one-off `Connection reset by peer` on a GET to `/api/art/queue/957` and aborted early (before its 600s timeout) rather than retrying — a genuinely new failure *shape* worth recording, but a follow-up direct `curl` to the same endpoint moments later returned clean (`success: true`) with job 957 still `status: PENDING`, `updatedAt` identical to `createdAt` (17:12:14.245Z) — i.e. still never claimed. Confirms the connection reset was a transient network blip in this session's egress path, not new evidence of relay recovery or a new relay-side failure mode; the underlying blocker signature (home relay not claiming queued jobs) is unchanged from every prior check back to 2026-07-18. Fell back to lane 4 per the checklist's own instruction, picking the two already-identified small gaps from the prior cycle's note: added the missing v1.5-addition (2026-07-20) PUBLIC-DOMAIN-POLICY.md re-check paragraph for §25 (Persian Miniature Painting) to `curriculum-outline.md`, and added `persian-miniature` to the "Flagged as likely-poor remixers" tier list (mirrors the spatial-logic-vs-composition-preservation tension already flagged for `cubism` in that same list and already called out for this movement in `teaching-notes.md` row 25). `python scripts/audit_roadmaps.py` — 0 errors, 11 warnings, 46 info findings, no new findings. Conductor-docs-only change; kind_robots PR: none needed (curriculum-outline.md and teaching-notes.md are conductor-repo docs, not synced into the kind_robots front end). Updated continuous-improvement-checklist.md's rotation state; next preferred lane is front-end polish (lane 1) — this cycle ran lane 4 (curriculum-depth fallback after lane 3 stayed blocked), so lane 1 is next in the 1→2→3→4 rotation. Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~14:11-14:35 UTC, claude-burst-rotation-20260720T1420Z): option (b) roadmap accuracy (lane 2), per the checklist's next-preferred-lane note (previous cycle ran lane 1, front-end polish, PR #646). Ran `scripts/check_pr_merged_drift.py` (clean, no drift) and `scripts/audit_roadmaps.py` (same 2 pre-existing info-level `APPROVAL_WITHOUT_GATE` findings for this project, t-002/t-011 — not defects). Checked t-019's blocker directly against kind_robots (`get_file_contents` on `public/images/academy/styles/` still 404s) — still accurately blocked, matching the checklist's coverage table. Found one real staleness bug: t-033's note claimed "kind-robots/t-038 (stale workflow file)" as a live co-blocker of the "Contract verifiers" CI check, but `projects/kind-robots/roadmap.yaml` shows t-038 has been `status: done` since 2026-07-18T14:30:32Z — about 6 minutes after t-033's note was written, so it was accurate at the time and has since gone stale. Confirmed "Contract verifiers" is healthy on recent kind_robots PRs (#646, #648) with no recurrence of that failure. Corrected t-033's note in place: its remaining blocker is now described as solely the Academy examples-manifest home-relay write-access issue, no longer compounded by the unrelated repo-wide CI break. Conductor-docs-only change; no kind_robots PR needed this cycle. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is inspiration/preview assets (lane 3, recheck with a fresh queued job per the prior cycle's own note — do not re-poll job 816) falling back to lane 4 if still blocked. Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-19 (~11:04-11:15 UTC, claude-conductor-burst-20260719T1104Z): option (a) front-end polish, per the checklist's rotation rule (previous cycle ran lane 2, roadmap accuracy, so lane 1 was next preferred). Dispatched an Explore subagent over every Academy component (academy-manager.vue, academy-timeline.vue, academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue) plus the two shared components/art/ surfaces (art-styler.vue, image-upload.vue), with an explicit exclusion list of everything already fixed in prior cycles (PRs #275, #301, #332, #371, #380, #383, #385, #387, #397, #515). Found a real, verifiable focus-management bug: `academy-styles-browser.vue`'s `closeStyle()` restores keyboard focus to the grid button that opened the style detail panel, but that button unmounts (deleting its `gridRefs` entry) when an active search query no longer matches its style while the panel is still open — closing the panel at that point silently dropped focus to `<body>`, exactly the regression this code was originally written to prevent. Added a `searchInputRef` (always mounted) as a fallback focus target when the original grid button is gone. Verified: `npx eslint` and `npx prettier --check` both clean, `npm run test` (`vue-tsc --noEmit`) 0 errors. kind_robots PR #520 (branch claude/keen-fermat-academy-focus-fallback): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian), no review comments — merged squash `a174d16`. Rearmed to `ready` per the recurring-task convention — continuous-improvement-checklist.md's rotation state updated to record front-end polish as the completed lane, next preferred roadmap accuracy.
      RAN 2026-07-19 (~09:13-09:19 UTC, claude-conductor-agentrun-20260719T0919Z): option (b) roadmap accuracy, per the checklist's rotation rule (previous cycle ran lane 1, front-end polish, so lane 2 was next preferred). Audited milestone status against actual task states rather than re-running a front-end sweep. Found two real, material drifts: (1) m4 ("Academy front end in kind_robots", weight 30) was still `status: in-progress` even though all 15 of its tasks (t-007/008/013/014/015/018/020/023/025/026/027/028/031/032/034) are `done` — t-029/t-030 were already moved out to m6 in an earlier cycle, so nothing non-done remains in m4 — flipped to `done`. (2) m3 ("Curriculum content", weight 20) was `status: done` despite carrying t-033 (`kind_robots: fix examples.manifest.json contract failure`, needs-human/soft_gate since 2026-07-18T14:22Z, still open) — this is genuine drift, not a stale finding: the 2026-07-18 ~11:18 UTC roadmap-accuracy cycle explicitly verified "m3 (done) ... matches its tasks' actual statuses" and that audit ran *before* t-033 was claimed at 14:22Z the same day, so m3 was correct then and has silently drifted since. Flipped m3 to `in-progress` to match. Net effect on `scripts/build_status.py`'s weighted formula: ai-art-academy's computed progress moves from 72.5% to 77.5% on the next STATUS.md regeneration (m3 done→in-progress: -10; m4 in-progress→done: +15; net +5) — a real, verifiable correction, not cosmetic. Checked the other four milestones (m1, m2, m5, m6) against their tasks' actual statuses: all already accurate (m2/m5 in-progress correctly reflect t-004/t-009 both still needs-human; m1/m6 done correctly have zero non-recurring non-done tasks). Ran `python3 scripts/audit_roadmaps.py` — 0 errors, same pre-existing warning baseline, none touching this project. Did not touch t-019 (checked: `public/images/academy/styles/` still doesn't exist in the local kind_robots checkout, so still genuinely blocked). Conductor-docs-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-19 (~07:05-07:16 UTC, two concurrent sessions converged on the same cycle — see TALKBACK.md for both accounts): option (d) curriculum depth. Synced american-regionalism (movement #24, added to curriculum-outline.md in a prior 2026-07-19 cycle) into stores/seeds/academyStyles.ts, mirroring the t-020/t-031/t-034 pattern (keyIdeas/recognitionCues/artists condensed from curriculum-outline.md §24; failureMode verbatim from teaching-notes.md row 24; remix.template verbatim remix_hint; inserted at sortYear 1928, chronologically after bauhaus). previewImageSrc and exampleWorks intentionally omitted — no preview image has generated yet, and 3 of 4 example works lack an accessionId (required field), matching the existing ashcan-school/suprematism convention of deferring exampleWorks backfill. Verified via vue-tsc + eslint (clean) and the three academy contract checks (test:academy-style-detail-callers, test:academy-starter-manifest both pass; test:academy-examples-manifest has 3 pre-existing failures — expressionism/cubism/bauhaus missing examples.manifest.json entries, confirmed identical on the pre-change commit via git stash, already tracked as t-033, not touched by this change). kind_robots PR #506 (branch worker/ai-art-academy-t-010-american-regionalism-sync) merged squash `ccd6022`. The PR's diff also listed `components/art/stylist-mask-brush.vue`/`stylist-restyle.vue` as changed — a second reviewing session confirmed both byte-identical to what's already on `main` (merged separately as superkate-hairstyle-ai/t-018, kind_robots PR #834, under a different squash SHA) via direct `git show` diff against `origin/main`, so the merge was a genuine no-op for those two files — the known "PR shows a larger diff than expected because main already has equivalent content under a different commit SHA" pattern from conductor/CLAUDE.md. All 3 CI checks green (TypeScript, Contract verifiers, GitGuardian). Rearmed to `ready` per the recurring-task convention — coverage table in continuous-improvement-checklist.md updated to reflect american-regionalism now synced (24/24).
      RAN 2026-07-18 (~13:20 UTC, conductor agent run, claude-conductor-agentrun-20260718T1311Z): option (d) curriculum depth, per the checklist's rotation rule (previous cycle ran lane 2, roadmap accuracy — twice in a row, see the entry below — so lane 3 was checked first and confirmed still blocked: `public/images/academy/styles/` still doesn't exist in kind_robots, verified via local checkout `ls`, not just GitHub API; moved to lane 4). Found a real, verifiable gap left over from the ~00:08 UTC cycle that added Suprematism as the 22nd movement: `docs/style-lora-registry.md` never got a Suprematism entry at all — missing from the curriculum-slug-mapping table, the machine-readable `styles:` block, and the per-style notes — and `docs/teaching-notes.md`'s per-style teaching-notes table (and its "companion files"/section-3 headers) still said "21 movements", never updated after Suprematism landed. Fixed both: added `suprematism` (prompt-mode, `prompt_hint` copied verbatim from `docs/suprematism-lesson.md`'s own "Try It" instruction so the two docs can't drift apart) to the registry's mapping table, machine-readable block, and a new per-style-notes entry (v1.3 update, matching the existing v1.1/v1.2 update-note convention); added the Suprematism row (#22, Hard, same "discards-the-subject" framing as `de-stijl`) to teaching-notes.md's per-style table, fixed its stale 21-movement references to 22, and added a pointer from §4 (worked example lessons) to the existing standalone `docs/suprematism-lesson.md` rather than duplicating that content inline. Also updated `docs/continuous-improvement-checklist.md`'s "Teaching scaffold" coverage row to reflect the now-complete table instead of "add a Suprematism row when that lands". Verified: the registry's `styles:` YAML block still parses clean (`yaml.safe_load` via regex extraction, 22 entries, `suprematism` present) and `scripts/audit_roadmaps.py` reports the same 0 errors / 6 pre-existing warnings baseline as every prior cycle, none touching this project. Conductor-docs-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-18 (~12:14 UTC, conductor scheduled session, claude-conductor-scheduled-20260718T121450Z-pid4339): option (b) roadmap accuracy — direct follow-up on the two lower-confidence observations the immediately-prior cycle flagged but declined to act on unilaterally. (1) m6 ("Continuous improvement loop", weight 10): confirmed its only non-recurring tasks (t-016, t-017) are both `done`, and per AGENTS.md "recurring tasks don't count toward milestone progress" t-010 (this task) is excluded from the bucket entirely — flipped m6 from `in-progress` to `done`. (2) t-029/t-030: confirmed via a full listing of every other m4 task (16 tasks, all titled `kind_robots: ...` and genuinely about the Academy front end / art-styler evolution) that t-029/t-030 (titled `conductor: ...`, about `recheck_egress_blocks.py`'s bot-challenge detection — a conductor-repo tooling script with zero relation to kind_robots or the Academy front end) were a clear mis-tag, not a debatable judgment call. Re-tagged both from `milestone: m4` to `milestone: m6`, where they actually belong (both were themselves kaizen follow-ons from this project's own continuous-improvement loop). Verified this doesn't change m4's `done` status (already correct with or without them) and confirmed m6 stays correctly `done` after the move (t-016/t-017/t-029/t-030 all done, t-010 excluded as recurring). Ran `python3 scripts/audit_roadmaps.py` — 0 errors, same 6 pre-existing warnings baseline as before this change, none touching this project. Did not touch t-019 (still blocked — no preview thumbnails landed in kind_robots yet, unchanged since the immediately-prior cycle's recheck) or the m2/m5 in-progress milestones (both still accurate). Conductor-docs-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-18 (~11:18 UTC, conductor scheduled session, claude-conductor-scheduled-20260718T1730Z-review): option (b) roadmap accuracy, per the checklist's rotation rule (previous cycle ran lane 1, front-end polish, so lane 2 was next). Dispatched a research subagent to re-verify every checkable claim in this roadmap rather than re-running a front-end sweep. Found one real, material item: t-004 and t-009's notes both claimed (as of 2026-07-16) that the conductor sandbox cannot reach kind-robots.vercel.app (403 CONNECT). A live recheck this session (`curl` direct, then `scripts/recheck_egress_blocks.py kind-robots.vercel.app --task ai-art-academy/t-004`) got a real HTTP 200 (140KB Nuxt page), matching EGRESS-BLOCKERS.md's newly-appended 2026-07-18T11:17:11Z entry — but the same ledger shows this host WAS blocked as recently as 2026-07-17T10:55Z, confirming reachability is session-dependent, not a permanent fix. Updated both tasks' notes with a dated correction pointing at the ledger entry instead of hand-written recheck prose (per conductor/t-052's convention), and made clear this does NOT flip either task to ready: gate (2) (home relay token + DB reachability) is still unverified from this sandbox, and dispatching live generation stays human-gated regardless of egress state. Everything else audited came back accurate: t-019's blocker (kind_robots' public/images/academy/styles/ still doesn't exist — confirmed via direct `ls` in the local kind_robots checkout, not just GitHub API) is still real; docs/curriculum-outline.md's 22-movement count and docs/starter-image-library.md's Suprematism-exclusion rationale are both internally consistent with what this roadmap claims; milestones m1/m3/m4 (done) and m2/m5 (in-progress) all still match their tasks' actual statuses. Two lower-confidence, non-blocking observations left unfixed for a future pass/Silas judgment call rather than acted on unilaterally: m6 arguably qualifies as done now (its only non-recurring tasks, t-016/t-017, are both done, and t-010 is recurring so per AGENTS.md doesn't count toward milestone progress) but "continuous improvement loop" reads as intentionally-ongoing, not a literal checklist; and t-029/t-030 are tagged milestone m4 despite being conductor-tooling tasks unrelated to m4's actual subject (doesn't change m4's done status either way). Conductor-docs-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-18 (~10:22 UTC, conductor-burst rotation, claude-conductor-burst-20260718T-aiartacademy): option (a) front-end polish pass, per the checklist's rotation rule (previous cycle ran lane 4, curriculum depth, so lane 1 was next). Dispatched an Explore subagent over every Academy component (academy-manager.vue, academy-timeline.vue, academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue) plus the two shared components/art/ surfaces (art-styler.vue, image-upload.vue), explicitly excluding everything already fixed in prior cycles (PRs #275, #301, #332, #371, #380, #383, #385, #387), so it had to find something genuinely new. Found a real, verifiable gap: image-upload.vue's connected-model "unlink" button (icon-only, `mdi:close`) had zero accessible name — no `title`, no `aria-label` — an outlier against every other icon-only button in the same file, which all have at least a `title` fallback (e.g. the file-remove button uses a dynamic `:title`). Added a dynamic `:aria-label="`Unlink ${connectedModelType} #${connectedModelId}`"` mirroring the file's own existing dynamic-label pattern — one attribute, no logic change. Verified: eslint clean, prettier clean, full-project `npm run test` (`vue-tsc --noEmit`, provisioned via conductor's `provision_kind_robots_deps.sh`) exit 0. kind_robots PR #397 (branch claude/keen-fermat-zow9l1): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian), no review comments — merged squash 05ce7032.
      RAN 2026-07-18 (~07:30 UTC, conductor-scheduled session, claude-conductor-scheduled-20260718T0705Z): option (d) curriculum-coverage depth, per the checklist's rotation rule (previous cycle ran lane 2, roadmap accuracy, so any lane but 2 was eligible; lane 3 confirmed still blocked — `public/images/academy/styles/` still doesn't exist in kind_robots via GitHub `get_file_contents`). The checklist's own coverage table said the "before adding a 23rd movement" gap-closing list had exactly one open item: "Starter library — Add a Suprematism starter image/provenance entry." Investigated rather than executing blindly: cross-checked whether any of the 8 movements added after the starter library's original v1 pass (Gothic Panel Painting, Northern Renaissance, Rococo, Symbolism, Neo-Impressionism/Pointillism, Bauhaus, Neoclassicism, Suprematism) has a dedicated starter-library entry — none do, confirming the library was never meant to be movement-indexed; it's a fixed set of 21 general-subject remix bases (portraits/landscapes/animals/still-life/botanical) that any of the 22 taught movements remixes into, not works sampled from each movement. Also checked whether a Suprematist work itself would even qualify under the library's own stated selection criteria (clear focal subject, structure that survives restyling into wildly different movements) — Malevich's own example works (Black Square, White on White, etc.) are near-featureless geometric abstractions, the opposite of what the criteria ask for, so adding one would both violate the library's design intent and produce an unusable remix base. Documented this finding in `docs/starter-image-library.md` (new paragraph after the verification-status note) and corrected the checklist's coverage-table row to remove the stale action item and record why coverage is complete at 21. Conductor-docs-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-18 (~06:11 UTC, conductor scheduled session, claude-conductor-scheduled-20260718T0611Z): option (b) roadmap accuracy, per the checklist's rotation rule (the previous two cycles both ran lane 1, front-end polish — PRs #383 and #385 — so lane 2 was next in sequence and overdue). Audited `docs/continuous-improvement-checklist.md`'s coverage table against current roadmap/file state rather than re-running a front-end sweep a third time in a row. Found two stale lines: (1) the lesson-seed-entries row still said "21 synced to academyStyles.ts" with next action "Land t-031 (Suprematism sync)", but t-031 has been `status: done` since earlier today — confirmed via kind_robots PR history (squash-merged, referenced in t-031's own note) — so all 22 movements are now synced; (2) the style-previews row said "Suprematism not yet queued", but grepping `projects/art-prompts.yaml` found `kind-robots-academy-style-preview-suprematism` already present (queued during the ~00:08 UTC cycle today, before t-031 even landed) — the checklist just never got updated after either event. Fixed both lines to reflect current reality and removed the now-completed action items. Cross-checked the rest of the table (example works, starter library, remix configs, teaching scaffold) against the roadmap and confirmed no further drift: the starter-library row's "add a Suprematism entry" action is still genuinely open (grepped `docs/starter-image-library.md` — no Suprematism entry exists there yet), so left it unchanged as a real gap, not stale. Also ran `scripts/audit_roadmaps.py` (0 errors, 6 warnings) — none of the 6 warnings touch ai-art-academy (they're animation-studio/humboldt-scoop/kind-robots/packmaker override or gate hygiene), so nothing else to correct here this cycle. Verified milestones m1-m6 all still match their tasks' actual statuses (t-031 landing under m4, which was already `done`, doesn't change any milestone bucket). Conductor-docs-only change; no kind_robots PR this cycle.
      RAN 2026-07-18 (~05:20 UTC, conductor-burst rotation, claude-conductor-burst-20260718T050419Z): option (a) front-end polish pass, per the checklist's rotation rule (previous cycle ran lane 4, curriculum depth, so lane 1 was next). Dispatched an Explore subagent over every Academy component plus the two shared components/art/ surfaces (art-styler.vue, image-upload.vue) with an explicit exclusion list of everything already fixed in prior cycles (PRs #275, #301, #332, #371, #380, #383), so it had to find something genuinely new. Found a real, verifiable gap: art-styler.vue's four selectable-button grids (gallery-image thumbnails, starter-image thumbnails, category filter toggles, and the style picker — the single most important control in every Academy Remix Studio flow) signaled selection state only visually (border/checkmark swap), with zero `aria-pressed`/`aria-selected`/`aria-current` anywhere in the file — an outlier against this codebase's own established convention for selectable-card grids (`card-picker.vue` line 24, `avatar-picker.vue` already set `aria-pressed`). Added `:aria-pressed` to all four buttons, bound to each grid's existing selected-state boolean (`selectedSourceImage?.id === image.id`, `selectedStarterFile === entry.file`, `activeCategory === cat`, `isSelectedStyle(style)`) — no new logic, one line each. Verified: eslint clean, prettier clean, full-project `npm run test` (`nuxi prepare` + `vue-tsc --noEmit`) exit 0 via conductor's `provision_kind_robots_deps.sh`. kind_robots PR #385 (branch claude/keen-fermat-w41wly): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian), no review comments, merged squash 33af6070. Also found and stashed (not committed — out of scope, unrelated to this task) a set of stale untracked files in the local kind_robots worktree (components/code/, components/composition/, matching cypress/prisma-generated files) left over from an interrupted checkout predating this session; they diverge from the abandonware/ copies a recent migration already moved that content to, so committing them would have silently resurrected a removed feature under this PR — parked via `git stash -u` instead.
      CORRECTION 2026-07-16 (later same session): the "KR_API_TOKEN still absent" line in the RAN entry immediately below is WRONG — it was carried forward from prior cycles without re-checking the live env. The token IS present (env-var field + GitHub secret). t-004/t-009 notes now carry the accurate blockers: the sandbox can't reach the kind_robots API (egress 403), so generation runs via the auto-art-generate GitHub Action, not sandbox sessions; and rendering needs the home relay (correct KR_RELAY_TOKEN + KR_RELAY_USER_ID) and the DB up. Dispatching live generation is human-gated. (Re-applied fresh after PR #617 was closed dirty.)
      RAN 2026-07-17 (conductor-burst-hourly rotation, ~05:07 UTC, claude-conductor-burst-hourly-20260717T0507Z): option (b) roadmap upgrade. Rechecked blockers first: EGRESS-BLOCKERS.md already carried a fresh (~1h-old, 04:07:49Z) metmuseum.org/upload.wikimedia.org recheck for t-008, so skipped re-probing the same hosts again this cycle rather than burn a redundant entry — t-008/t-013 stay ready-but-blocked on that existing evidence. Ran `scripts/audit_roadmaps.py` for the whole repo (0 errors, 5 warnings) — none of the 5 warnings touch this project (they're animation-studio/ challenge-center/humboldt-scoop override hygiene, out of scope here). Found a real milestone-accuracy bug instead: m5 ("Project art and inspiration assets", weight 5) was still `status: not-started` even though one of its three tasks (t-022, the 21-movement preview-thumbnail prompt queue) has been `done` since 2026-07-16, and a second (t-009) is soft needs-human, not simply unstarted — only t-019 is genuinely not-yet-begun. Flipped m5 to `status: in-progress` to match; verified against `scripts/build_status.py`'s weighted-milestone formula that this is the correct bucket (`done`=1.0, `in-progress`=0.5, `not-started`=0.0 of the milestone's weight) and that the fix moves this project's computed progress from 65.0% to 67.5% on the next STATUS.md regeneration — a real, verifiable correction, not cosmetic. Checked the other five milestones (m1-m4, m6) against their tasks' actual statuses: all already accurate, no further changes needed. Did not prune any RAN history in this task's own note — it's a legitimate audit trail (each entry adds new signal: what ran, what was found, what changed) rather than repetitive filler, so pruning it would destroy real information for no roadmap-accuracy gain; revisit only if a future pass finds entries that are pure duplicates. Conductor-only change; no kind_robots PR needed this cycle.
      RAN 2026-07-17 (conductor-burst-hourly rotation, claude-burst-2026-07-17-hourly): option (a) front-end polish pass. Rechecked blockers first: fresh `curl` CONNECT to metmuseum.org, upload.wikimedia.org, huggingface.co, and civitai.com all still return "CONNECT tunnel failed, response 403" — same signature as every prior cycle, so t-008/t-013/t-021 stay `ready`-but-blocked (recheck notes added to t-008 directly; t-013/t-021 unchanged since their own last recheck already covers the same signature). t-019 stays blocked too: confirmed via GitHub `get_file_contents` that kind_robots' public/images/academy/styles/ still doesn't exist, so still zero preview thumbnails landed. Found a real copy inconsistency instead: academy-manager.vue's loading spinner said "waking five centuries of dead masters" and content/academy.md's description said "from Greek vases to De Stijl grids" — both stale from before the curriculum grew from its original ~5-centuries scope to 21 movements spanning Greek vase painting (c. 600 BCE) through Bauhaus (1919-1933, the true chronologically-last entry in stores/seeds/academyStyles.ts, added after De Stijl by t-018). Both already conflicted with the correct "twenty-five centuries" copy already live in academy-timeline.vue's header. Fixed both to match. kind_robots PR #332 (branch claude/keen-fermat-ozs6fe): prettier clean, copy-only change, no logic touched.
      RAN 2026-07-16 (claude session claude-ai-art-academy-curriculum-xnm2wl, ~10:22 UTC): options (d) + (b) combined — a Silas-directed content-depth pass ("full reign, pure curriculum/content generation this session"). Rechecked blockers first, same signature as every prior cycle: KR_API_TOKEN still absent (t-004/ t-009 stay needs-human) and museum egress still 403 (t-008/t-013 stay blocked), so no art generation and no image-download work this session — pure markdown/YAML content, which needs neither. (d) Expanded the curriculum from 16 to 21 movements in docs/curriculum-outline.md: added Gothic Panel Painting (§17), Northern Renaissance (§18), Rococo (§19), Symbolism (§20), and Neo-Impressionism/ Pointillism (§21) — full prose sections + YAML skeleton entries + remix_hints, all cleared against PUBLIC-DOMAIN-POLICY.md §1.3 (newest death Signac 1935; all example works pre-1930; Fauvism/Matisse deliberately excluded as post-1930). New example-work URLs marked (unverified) — egress-blocked — with real accession numbers for later spot-check. Updated the outline's intro count, "Lesson-only vs remixable" tiers, and public-domain safety-check paragraph. Also enriched docs/style-lora-registry.md: added prompt-mode entries for the 3 genuinely-new slugs (northern-renaissance/rococo/symbolism), introduced an optional `prompt_hint` field carrying the Kontext prompt-mode instruction (documented in the block header; also backfilled onto the existing gothic/pointillism entries), and added per-style teaching notes for all five. Wrote a NEW file docs/teaching-notes.md: an 8-beat reusable lesson scaffold, a per-style teaching-notes table for all 21 movements (teaching angle / remix difficulty / failure mode), and three fully-worked example lessons (Ukiyo-e, Impressionism, Pointillism). (b) Roadmap upgrade: filed four concrete follow-ons — t-020 (seed the 5 new movements into kind_robots academyStyles.ts), t-021 (LoRA hunt + prompt_hint backfill for the new/older prompt-mode styles), t-022 (queue preview thumbnails for the 5 new movements, extending t-019's batch), t-023 (wire the teaching-notes lesson scaffold into the Academy lesson-detail component) — and extended t-013's note so the example-works strip covers all 21 movements. Conductor docs only; no kind_robots code touched this pass.
      RAN 2026-07-16 (conductor-burst-hourly, ~08:06 UTC): option (c) style preview thumbnails. Rechecked blockers first: KR_API_TOKEN still absent (t-004/t-009 stay needs-human) and a fresh `curl` CONNECT to metmuseum.org and upload.wikimedia.org still returns "CONNECT tunnel failed, response 403" — same signature as every prior cycle, t-008/t-013 stay blocked. Found a real gap: none of the 16 curriculum movements in kind_robots' `stores/seeds/academyStyles.ts` had a `previewImageSrc` set, and `public/images/academy/styles/` doesn't exist yet — the style browser has never shown a single preview thumbnail. Queued all 16 as new `requests:` entries in `projects/art-prompts.yaml` (ids `kind-robots-academy-style- preview-<slug>`), each pointing at `public/images/academy/styles/{slug}.webp` (256x256) with a prompt built from that style's actual `recognitionCues` in the seed file — the same simple subject (a small robot beside a windowsill potted plant) rendered once per movement so the eventual thumbnails are visually comparable side by side, matching the "same simple landscape painted differently" idea already used for the Academy's dashboard-tab thumbnail. This is a prompt-catalog-only change (conductor repo); no image was generated in this session — generation happens via the existing ChatGPT-driven art-prompts.yaml pipeline, consistent with how every other pending batch in that file works. Follow-up not done here: once images land in kind_robots, `academyStyles.ts` needs each style's `previewImageSrc` set and the style browser/timeline components wired to render it — filed as a new task below (t-019) rather than attempted blind in this pass.
      RAN 2026-07-16 (conductor-burst-hourly, ~05:05 UTC): option (b) roadmap upgrade, per TALKBACK.md's own endorsement (2026-07-16, t-018 entry) to rotate away from two consecutive option-(d) cycles. Rechecked blockers first: KR_API_TOKEN still absent (`env | grep KR_API_TOKEN` empty) and a fresh CONNECT to metmuseum.org/upload.wikimedia.org via the agent-proxy status endpoint still shows connect_rejected/403 — same signature as every prior cycle, t-008/t-013 stay blocked. Found and fixed a real roadmap-hygiene gap: t-009 (art generation) shares t-004's exact blocker (missing KR_API_TOKEN) but, unlike t-004, had never been converted to soft needs-human — so next_ready_task.py was silently re-picking and re-confirming it blocked every single cycle in ai-art- academy's priority slot, right after t-008/t-013. Applied t-004's established fix: t-009 is now `status: needs-human`, `soft_gate: true`, with a FOR SILAS note explaining the mirrored precedent and preserving the full original task spec unchanged below it. This unblocks the rotation: ai-art-academy's next `ready` pick is now this task (t-010) itself rather than looping on an unworkable art-generation task. Checked milestone m5 ("Project art and inspiration assets") and m2 — both remain accurately `not-started`/`in-progress` with no further detail needed now that their sole blocking task is properly gated. No stale notes found elsewhere in this roadmap worth pruning this pass (existing RECHECKED/RAN entries are still each adding new signal, not repeating). Conductor-only change; no kind_robots PR this cycle.
      RAN 2026-07-16 (conductor-burst-hourly): option (d) curriculum expansion. Rechecked blockers first: KR_API_TOKEN still absent (t-004/t-009 stay blocked) and the museum-egress 403 is still live — confirmed via `curl $HTTPS_PROXY/__agentproxy/status` plus a direct CONNECT attempt to metmuseum.org (403), so t-008/t-013 stay blocked too, same signature as every prior cycle. Added a 16th movement, Bauhaus (`bauhaus`, 1919-1933, Kandinsky/Klee/Moholy-Nagy — all died 1944-1946, safely public domain) to docs/curriculum-outline.md after De Stijl: full prose section, recognition cues, 4 example works (2 VERIFIED via WebSearch against artic.edu CC0 pages — Kandinsky's *Orange* 1923 and Klee's *Architecture* 1921 — plus Kandinsky's *Circles in a Circle* and Moholy-Nagy's *Fotogramm* marked expected/unverified), remix_hint, and a YAML skeleton entry. Added `bauhaus` to the "good but watch the output" remix-quality list (three distinct artist styles risk the model averaging into generic geometric abstraction) and updated the public-domain safety-check paragraph's "most recent example work" from 1924 to 1926 (Moholy-Nagy's Fotogramm) and added Kandinsky (d. 1944) to the death-date spot-check list. Filed t-018 to sync the new slug into kind_robots' stores/seeds/academyStyles.ts, mirroring t-015's Neoclassicism sync — not done here (conductor docs only, no kind_robots code touched this pass).
      RAN 2026-07-15 (conductor-burst-hourly, ~21:15 UTC): option (a) front-end polish pass. Rechecked blockers first: KR_API_TOKEN still absent (`env | grep KR_API` empty, t-004/t-009 stay blocked) and metmuseum.org still a fresh 403 via WebFetch (t-008/t-013 stay blocked) — same signature as every prior cycle, nothing new to log on those tasks this pass. Reviewed all five components/academy/*.vue files in kind_robots for real (not manufactured) polish opportunities and found one: `academy-style-detail.vue`'s footer "Remix an image in {style}" button is reused in three contexts (academy-styles-browser.vue, academy-timeline.vue — real navigation CTA — and academy-remix.vue, which renders the same component *inside* the Remix Studio itself as the side panel, where the button was wired to a no-op `@remix="() => {}"` and did nothing on click). Added a `showRemixButton` prop (default true) and hid it in the Remix Studio context only, removing the dead click without touching the two contexts where it's a real CTA. kind_robots PR #301 (branch claude/keen-fermat-0dvz1n): eslint clean, prettier clean, full project `vue-tsc --noEmit` reported 0 errors. Also reconfirmed this cycle (unrelated, filed separately): kind-robots/t-022's production DB outage is still active 12+ hours in — 719 503s vs 54 200s in the last hour via Vercel MCP `get_runtime_logs`, pool still active=0/idle=0 despite the limit=10 fix from PR #299/#300 landing, so the root cause is DB/infra reachability, not app-side pool config. Pushed a notification; no agent action possible per BOUNDARY.md.
      RAN 2026-07-15 (conductor-burst-hourly, ~11:30 UTC): option (d) curriculum expansion. Rechecked t-004/t-008/t-009/t-013 blockers first with a different tool than prior cycles (WebFetch to metmuseum.org, not curl) — still a fresh 403; confirms the museum-egress block isn't proxy-tool-specific, so no need to re-test it again next cycle. WebSearch (not WebFetch) does work in this sandbox and was usable to source real Met/Wikimedia object references without needing direct fetch access. Added a 15th movement, Neoclassicism (`neoclassicism`, c. 1750-1830, David/Ingres/Canova/Kauffman — all died 1807-1867, safely public domain) to docs/curriculum-outline.md between Baroque and Ukiyo-e: full prose section, recognition cues, 4 example works, remix_hint, YAML skeleton entry, and added to the "strong remix candidates" list with a texture-flattening caveat. Renumbered sections 6-14 to 7-15 and bumped the intro's movement count 14→15. Kaizen candidate for next cycle: the new movement's slug needs to land in kind_robots' stores/seeds/academyStyles.ts (mirroring t-005's seed, PR #143) before it's visible in the live curriculum — that's a small, independently-landable cross-repo follow-up, not done here (conductor docs only, no kind_robots code touched this pass).
      RAN 2026-07-15 (conductor-hourly-20260715-0048): option (b) roadmap upgrade. Flipped milestone m1 to done (both its tasks, t-001/t-002, were already done — stale status). Split the oversized 3-part t-008 into t-008 (starter-image download only), t-013 (example-works strip, independently landable), and t-014 (source-picker wiring, depends_on: t-008) so future cycles can land each part as its own scoped, verifiable PR instead of risking a scope-failure on the monolith. No code touched; t-004/t-009 remain blocked on a token-bearing session for the generation backend (KR_API_TOKEN unset here).
      RAN 2026-07-15 (conductor-burst-hourly, ~06:15 UTC): option (a) front-end style/polish pass. t-004/t-009 still blocked (no KR_API_TOKEN this session); t-008/t-013 also confirmed blocked this cycle (sandbox 403s on metmuseum.org/upload.wikimedia.org — see their updated notes), so picked the front-end path instead. kind_robots PR #275: removed a duplicated-state ref in academy-remix.vue (local activeStyleSlug that hand-mirrored academyStore.selectedStyleSlug — now reads the store directly), widened academy-styles-browser.vue's search to match recognitionCues (previously unsearchable despite being shown on every card), and added aria-expanded/aria-label to the timeline and style-gallery toggle controls. Verified eslint/prettier clean and vue-tsc introduces zero new errors against the 82 pre-existing (t-020 baseline). PR #275 merged: the PR's only failing check (TypeScript) was confirmed pre-existing on main at the base commit (45c1b047, unrelated to this diff — same t-020 backlog), Contract verifiers and GitGuardian were green, so merged after noting that in a PR comment.
      RAN 2026-07-19 (~08:11-08:30 UTC, claude-conductor-agentrun-20260719T0811Z): option (a) front-end polish, per the checklist's rotation rule (previous cycle ran lane 4, curriculum depth — American Regionalism sync — so lane 1 was next). Confirmed t-019's blocker is still real before picking a different lane (`ls public/images/academy/styles/` in the local kind_robots checkout: directory doesn't exist, zero preview images landed). Reviewed all five components/academy/*.vue files for a genuinely new gap: academy-timeline.vue's lesson-toggle button had no `aria-controls`, and the expanded academy-style-detail panel it reveals had no `id` — the sibling academy-styles-browser.vue already has this exact wiring (wrapping div with `:id`, button `aria-controls` pointing at it) but the timeline view was missing both. Added the wrapping `div` with `:id="academy-style-detail-${style.slug}"` and `:aria-controls` on the toggle button. Verified `npm run test` (vue-tsc --noEmit) and `npx eslint components/academy/academy-timeline.vue` both clean. kind_robots PR #515 (branch claude/vigilant-edison-mu8lk4): GitGuardian, TypeScript, and Contract verifiers all green, no review comments — merged squash `567fc7e`. Note: this task's roadmap record was briefly corrupted between ~08:18-08:30 UTC by a stuck `task-events/` entry (a different session's ~07:20 roadmap-accuracy-lane event, queued behind an unrelated YAML syntax bug in the events file) that finally processed and stomped this session's live `claimed` state with its own stale `status: review` + a truncated note fragment — cleaned up as part of this same edit; see TALKBACK.md for the full account. Rearmed to `ready` per the recurring-task convention — continuous-improvement-checklist.md's rotation state updated to record front-end polish as the completed lane.
      RECHECKED 2026-07-19 (~12:20-12:35 UTC, conductor-burst-hourly rotation, after this session's higher-priority kind_robots CI-janitor Todo #446 landed): option (b) roadmap accuracy, per the checklist's next-preferred pointer. Cross-checked every milestone (m1-m6) against its tasks' actual current statuses one field at a time — for the first time in this task's audit history, found zero drift: m1/m4/m6 done with all non-recurring tasks done; m2/m3/m5 in-progress each correctly gated by exactly their one open needs-human/ready task (t-004, t-033, t-009/t-019 respectively). `scripts/audit_roadmaps.py` still reports the same 0-errors/6-warnings baseline, none touching this project. Rechecked t-019's blocker directly (not from a stale note): `public/images/academy/styles/` still doesn't exist in the local kind_robots checkout, so it stays genuinely un-claimable. Also spot-checked lane (d)'s one open item — the American Regionalism *American Gothic* citation's "unverified this cycle by direct fetch" caveat — with a fresh `WebFetch` to `artic.edu/artworks/6565/american-gothic`: still HTTP 402 through the session egress proxy, identical signature to the cycle that wrote that caveat, so a `WebSearch` cross-check (accession 1930.934, Friends of American Art Collection, public domain via pre-1931 US publication) reconfirms facts already recorded rather than adding anything new — no doc change warranted. Lane (a) front-end polish was not re-run: the immediately-prior cycle (`claimed_at` below, ~11:04-11:15 UTC) already ran it earlier this same hour. Net result: three of the four lanes checked this cycle, all either already-accurate or still blocked on unchanged, previously-documented gates; no scoped reversible change to make, so no kind_robots PR opened and `t-010` is left as-is (still `ready`) rather than forcing cosmetic churn. Per AGENTS.md's "note the blocker and move to the next project" allowance for a burst-mode rotation slot. Conductor-docs-only change (this note).
      RAN 2026-07-19 (~17:04-17:25 UTC, claude-conductor-agentrun-20260719T-t010): option (a) front-end polish, per the checklist's rotation rule (previous cycle completed lane 1 at 11:04-11:15 and a 12:20-12:35 recheck of lanes 2/4 found no drift, so this cycle re-verified lane 3's blocker first — `ls public/images/academy/styles/` in the local kind_robots checkout: directory still doesn't exist, zero preview images landed — then dispatched a fresh Explore subagent over all 7 in-scope files with an explicit exclusion list covering every previously-fixed item (PRs #275, #301, #332, #371, #380, #383, #385, #387, #397, #515, #520), since lanes 2/4 had just been exhaustively rechecked with zero new findings an hour earlier. Found a real, verifiable bug: `image-upload.vue`'s batch-upload success/failure overlays were keyed by `item.file.name` (a plain string) via `succeededNames`/`failedNames` Sets, but `queuedFiles` items are keyed by a synthetic id and nothing dedupes by filename (`addFiles`/`uploadStore.validateFile` only check MIME type) — so two queued files sharing a name (e.g. two camera photos both `IMG_0001.jpg`) collide in those Sets. If one succeeds and the other fails, **both** thumbnails render both the success checkmark and failure X, misreporting the actual per-file outcome. Fixed by keying on the actual `File` object reference instead of name: added `file?: File` to `ImageUploadResult` in `stores/uploadStore.ts` (populated in both `succeeded.push`/`failed.push` inside `uploadBatchForActiveTarget`), changed the component's two Sets from `Set<string>` to `Set<File>`, updated the two template checks to `.has(item.file)`. Verified: `npx eslint`/`npx prettier --check` on both changed files reproduce only the same pre-existing `getSelectedCollectionId` unused-var error and formatting warning confirmed present on `main` via `git stash` (not introduced by this change); `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #544 (branch claude/vigilant-edison-e3ygys) — merging once CI is green. Rearming to `ready` per the recurring-task convention — continuous-improvement-checklist.md's rotation state updated.
      RAN 2026-07-19 (~18:26-18:35 UTC, claude-conductor-agentrun-20260719T1826Z): option (b) roadmap accuracy. Corrected this task's own continuous-improvement-checklist.md, which still said kind_robots PR #544 was "merging pending CI" -- confirmed via the GitHub MCP pull_request_read that it merged at 2026-07-19T17:25:23Z. Also found (independently, alongside a concurrent conductor/t-071 session building a drift-detector for exactly this class of bug) that this task itself had drifted: status stayed `claimed` for ~50 minutes after PR #544 merged despite this task's own prior note already saying "rearming to ready." Rearmed via conductor PR #868 before this cycle's claim. Rechecked the three `needs-human` tasks (t-004, t-009, t-033) against their stated blockers (relay/DB down, home-server upload access) -- all three still accurately blocked, no drift, no roadmap edit needed. Updated the checklist's rotation state; next preferred lane is front-end polish (lane 1). Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-19 (~19:04-19:15 UTC, claude-conductor-burst-fhv6z4): option (a) front-end polish, per the checklist's rotation rule (previous cycle ran lane 2, roadmap accuracy, so lane 1 was next preferred). Dispatched an Explore subagent over all 7 in-scope files (academy-manager.vue, academy-timeline.vue, academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue, art-styler.vue, image-upload.vue) with an explicit exclusion list of everything already fixed in prior cycles (PRs #275, #301, #332, #371, #380, #383, #385, #387, #397, #515, #520, #544). Found a real, verifiable bug: `image-upload.vue`'s `addFiles()` silently filtered out any file that failed `uploadStore.validateFile()` (non-PNG/JPEG/WebP) and unconditionally cleared `error.value`/`message.value` right after, with zero user-visible feedback. Drag-and-drop bypasses the hidden `<input accept="...">` constraint (that attribute only affects the native picker dialog), so a mixed-type drop silently loses the invalid file with no indication why the preview grid shows fewer tiles than files dropped -- and `uploadStore.uploadBatchForActiveTarget`'s own working "N skipped, invalid type" messaging was unreachable from this component since invalid files never survive `addFiles` long enough to reach it. Fixed by counting skipped files and setting `error.value` to a skip-count message (`"${n} file(s) skipped -- PNG, JPEG, or WebP only"`) when any are rejected, reusing the existing error banner already wired in the template. Verified: `npx eslint`/`npx prettier --check` on `image-upload.vue` clean (confirmed the one `uploadStore.ts` eslint error, `getSelectedCollectionId` unused-var, is pre-existing and unrelated); `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #547 (branch claude/keen-fermat-fhv6z4): all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian), no review comments -- merged squash 2026-07-19T19:14Z. Rearming to `ready` per the recurring-task convention -- continuous-improvement-checklist.md's rotation state updated; next preferred lane is roadmap accuracy (lane 2).
      RAN 2026-07-19 (~22:00-22:20 UTC, claude-conductor-burst-20260719T2154Z): option (b) roadmap accuracy. Audited all 6 milestones against their tasks' actual statuses -- no drift (m1/m4/m6 done correctly have zero non-done non-recurring tasks; m2/m3/m5 in-progress correctly reflect their open needs-human/ready tasks). Per blocker discipline did not re-probe t-004/t-009/t-033 (checked fresh 18:26 UTC, same cycle window). Ran `python3 scripts/check_pr_merged_drift.py` (built conductor/t-071, 2026-07-19) to look for stuck claimed/review tasks -- it printed "No drift found" but every one of its 12 GitHub API lookups was failing silently with HTTP 403 (confirmed via stderr and a direct `curl` with `$GITHUB_TOKEN` against `api.github.com`, which also 403'd -- this session type only has GitHub MCP tool access, not direct REST/token access, so `urllib.request` calls to the GitHub API can never succeed here). That is a real, verifiable tooling bug, not just this session's bad luck: `check()` treated a failed lookup identically to a confirmed-open PR, so a 100%-failed run reported the same "No drift found" text and exit code 0 as a genuine clean audit -- indistinguishable to a caller checking only the exit code. Fixed in `scripts/check_pr_merged_drift.py`: `check()` now returns `(findings, unresolved)`, `render()` reports unresolved lookups explicitly (with a pointer to re-check via the GitHub MCP `pull_request_read` tool) and never claims "No drift found" when anything is unresolved, and `main()` exits 2 (distinct from 0=clean / 1=drift) when any candidate couldn't be verified. Updated the 3 existing `check()`/`render()` tests in `tests/test_check_pr_merged_drift.py` for the new tuple signature and added 2 new tests covering the unresolved path. Verified: `python3 -m pytest tests/` -- 427 passed, 1 skipped (pre-existing), 0 failed; full suite unaffected. Re-ran the fixed script -- confirmed it now correctly reports 12/12 unresolved with exit 2 instead of a false-clean exit 0. The 12 flagged candidates were all self-referential mentions inside this same task's own note (already-documented merged PRs from t-010's prior cycles), not a live stuck task, so no roadmap edit was needed beyond this fix. Conductor-only change (scripts/ + tests/ + this roadmap); no kind_robots PR needed this cycle. Updated the checklist's rotation state; next preferred lane is inspiration/preview assets (lane 3, still gated on `public/images/academy/styles/` existing -- unchecked this cycle per blocker discipline, last confirmed absent 2026-07-19 08:11 UTC) or curriculum depth (lane 4) if lane 3 is still blocked. Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~00:12-01:10 UTC, claude-conductor-agent-run-20260720T0011Z): checklist's preferred lane was inspiration/preview assets (lane 3), so tried it first. Added a scoped --id-prefix filter to scripts/consume_art_requests.py (new filter_by_id_prefix() helper, tested in tests/test_consume_art_requests.py) so a live run only drains the 24 (now 25) kind-robots-academy-style-preview-* entries in art-prompts.yaml requests:, not the ~130 other unrelated pending requests from other projects sharing that same queue. Ran python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 2: the API accepted job 816 (queued, 200 OK), but polling GET /api/art/queue/816 directly for 10+ minutes showed status staying PENDING with claimedAt/claimedBy both null the whole time -- the home relay never picked it up. This is a fresh, direct, live-attempt confirmation of the same relay-down blocker t-004/t-009/t-033 already document (prior cycles only confirmed the symptom indirectly, e.g. public/images/academy/styles/ still absent -- this is the first cycle to actually queue a job and watch it go unclaimed). No files were left in a bad state: consume_art_requests.py only marks a request done on a successful render, so art-prompts.yaml's pending entries are untouched by the attempt itself. Fell back to lane 4 (curriculum depth) per the checklist's own fallback instruction. Added movement #25, Persian Miniature Painting (curriculum-outline.md slug persian-miniature) -- the first non-Western/non-Japanese entry beyond Ukiyo-e, covering the Timurid Herat and early Safavid schools (c. 1400-1600). Researched and directly WebFetch-verified all 3 example works against their Wikimedia Commons file pages (commons.wikimedia.org was reachable this cycle, unlike the artic.edu 402s noted in earlier cycles): Yusuf Fleeing the Advances of Zulaikha (Kamal ud-Din Bihzad, 1488, Egyptian National Library and Archives, PD-Mark) and two British Library folios from Nizami's Khamsa/Iskandarnama (c. 1494-95, PD-Art/PD-old-100-expired). Artist Bihzad died 1535 -- both PUBLIC-DOMAIN-POLICY.md prongs pass with centuries of margin, no ambiguity to escalate. Queued its style-preview prompt in art-prompts.yaml (kind-robots-academy-style-preview-persian-miniature, 256x256, same robot-companion-on-windowsill template as the other 24) -- also blocked on the same relay issue, status stays pending. Did NOT sync persian-miniature into kind_robots stores/seeds/academyStyles.ts this cycle -- deliberately deferred to a future kind_robots PR, matching how t-020/t-031/t-034 landed prior movements as separate syncs after the curriculum-outline.md addition. Updated continuous-improvement-checklist.md's rotation state (next preferred lane: front-end polish, lane 1) and coverage table (25 movements, 25 style-preview prompts queued, persian-miniature marked not-yet-synced). Verified: python3 -m pytest tests/test_consume_art_requests.py -q -- 9 passed (was 8; added test_filter_by_id_prefix). projects/art-prompts.yaml re-parses clean via yaml.safe_load (154 requests, 25 academy previews). Conductor + conductor-docs-only change (curriculum-outline.md, continuous-improvement-checklist.md, art-prompts.yaml, scripts/consume_art_requests.py, tests/test_consume_art_requests.py); no kind_robots PR this cycle. Rearming to ready per the recurring-task convention.
      RAN 2026-07-20 (~02:16-02:35 UTC, claude-worker-t010-frontend-20260720T1500Z): option
      (a) front-end polish, per the checklist's rotation rule (previous cycle ran lane 4,
      curriculum depth, so lane 1 was next preferred). Dispatched an Explore-style search
      over all 7 in-scope files (academy-manager.vue, academy-timeline.vue,
      academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue,
      art-styler.vue, image-upload.vue) plus their academyStore.ts/styleHelper.ts
      dependencies, cross-checked against `git log` per file to confirm which prior PRs
      already covered it (excluding #275/#301/#332/#371/#380/#383/#385/#387/#397/#515/
      #520/#544/#547). Found a real, verifiable bug: `art-styler.vue`'s `handleDrop`
      silently discarded any dragged file whose `type` didn't start with `image/` (or
      had no MIME type, which some OS file managers report) -- `isDragging` reset, but
      no error, no state change, total silence. This is the exact drag-and-drop
      silent-rejection bug class already fixed in `image-upload.vue` (PR #547), but that
      PR never touched `art-styler.vue`, which has its own independent drop handler.
      Fixed by reusing the component's existing `errorMessage` feedback pattern (already
      used in `selectStarterEntry`'s and `runStyleTransfer`'s catch blocks, already
      rendered in the template) rather than adding new UI. Everything else in the 7-file
      set was checked and ruled out: focus-restoration, aria-* wiring, the
      showRemixButton contract, Set-based file-identity keys, and the four aria-pressed
      grids in art-styler.vue are all already correct per prior cycles. This session's
      local kind_robots checkout had unrelated pre-existing uncommitted changes from an
      earlier interrupted session, so the change was authored and committed directly via
      the GitHub Contents API instead (diff hand-verified against the fetched pre-change
      file: exactly 8 additions / 3 deletions / 1 file, scoped to the `handleDrop`
      function body) rather than running eslint/vue-tsc locally -- flagged this
      explicitly in the PR body and asked the Reviewer to gate on CI rather than trust
      local verification. kind_robots PR #603 (branch
      claude/ai-art-academy-t010-art-styler-drop-feedback): all 3 CI checks came back
      green (TypeScript, Contract verifiers, GitGuardian) -- merged squash `2bf1bf7`.
      Updated docs/continuous-improvement-checklist.md's rotation state; next preferred
      lane is roadmap accuracy (lane 2). Rearming to `ready` per the recurring-task
      convention.
      RAN 2026-07-20 (~04:10-04:40 UTC, claude-conductor-scheduled-20260720T0410Z): lane 3
      (inspiration/preview assets) was next per the checklist's rotation state, so tried
      it first. Rechecked the home relay directly rather than trusting the ~01:10Z note:
      `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-
      preview-" --live --limit 2` again got job 855 accepted (200 OK) but it sat
      PENDING/unclaimed after 10+ minutes -- same signature, no change, relay still down.
      Fell back to lane 4 (curriculum depth), but the ~01:10Z cycle had already fully
      researched and verified movement #25 (Persian Miniature) -- nothing left to add
      there -- so instead finished that cycle's own explicitly-deferred follow-up: synced
      `persian-miniature` into kind_robots' `stores/seeds/academyStyles.ts` (was the one
      thing the 01:10Z note said it deliberately did NOT do this cycle). Split this out of
      t-035 (which had bundled the sync with the still-blocked thumbnail batch-generation)
      into its own completed unit -- see t-035's note for full detail. kind_robots PR #616
      merged squash `99ba5ff3`: added the entry (era c. 1400-1600, sortYear 1400, artists
      Kamal ud-Din Bihzad and Sultan Muhammad), inserted between `renaissance` and
      `northern-renaissance`. exampleWorks/previewImageSrc deferred to a future task
      (images need sourcing/verification, not just data entry), matching the
      ashcan-school/t-031 precedent. Verified: eslint clean, full-project `npm run test`
      (vue-tsc --noEmit) exit 0 before and after rebasing onto latest kind_robots main,
      prettier unchanged. All 3 kind_robots PR checks green (TypeScript, Contract
      verifiers, GitGuardian) before merge. Curriculum content is now 25/25 synced to the
      seed file. Updated continuous-improvement-checklist.md's rotation state; next
      preferred lane is front-end polish (lane 1). Rearming to `ready` per the
      recurring-task convention.
      RAN 2026-07-20 (~06:06-06:20 UTC, claude-conductor-burst-20260720T060611Z): option (a)
      front-end polish, per the checklist's rotation rule (previous cycle ran lane 4,
      curriculum depth, so lane 1 was next preferred). Dispatched an Explore subagent over
      all 7 in-scope files (academy-manager.vue, academy-timeline.vue,
      academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue,
      art-styler.vue, image-upload.vue) with an explicit exclusion list of every
      previously-fixed bug class (focus restoration, aria-* wiring, showRemixButton no-op,
      Set-based file-identity keying, silent drag-and-drop rejection). Found a real,
      verifiable bug: `image-upload.vue`'s `handleBatchUpload()` left `queuedFiles`
      completely untouched after a partial failure, so clicking "Upload N images" again to
      retry the failed files re-submitted the *entire* original batch, including files
      that already succeeded — `uploadStore.uploadSingleFile` always creates a new
      `ArtImage` row (not an idempotent upsert), so a retry after a partial failure created
      duplicate `ArtImage` rows and duplicate collection/model attachments for the
      already-succeeded files. Fixed by splicing succeeded files out of `queuedFiles`
      (revoking their object URLs, matching the pattern already used in
      `clearQueue`/`removeFile`) as soon as the batch result comes back, so only the
      genuinely-failed files remain queued for retry. Verified: `npx eslint`/`npx prettier
      --check` on the changed file both clean; `npm run test` (`vue-tsc --noEmit`) exit 0.
      kind_robots PR #622 (branch claude/keen-fermat-be7jf0): all 3 CI checks green
      (TypeScript, Contract verifiers, GitGuardian), no review comments — merged squash
      `20ea16fd`.
      Filed a kaizen follow-up in the PR body (unrelated pre-existing bug: the
      clean-batch-success path sets a success `message.value` then immediately calls
      `clearQueue()`, which resets it back to `''`). Updated
      continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap
      accuracy (lane 2). Rearmed to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~08:06-08:35 UTC, claude-conductor-burst-20260720T080634Z): option (b)
      roadmap accuracy, per the checklist's rotation rule (previous cycle ran lane 1,
      front-end polish, so lane 2 was next preferred). Ran
      `python scripts/check_pr_merged_drift.py` (clean, 0 claimed/review PR references
      found) and `python scripts/audit_roadmaps.py` (0 errors/warnings for this project;
      2 pre-existing info-level `APPROVAL_WITHOUT_GATE` findings on t-002/t-011, not
      defects). Found a real staleness bug while re-reading t-033's blocker chain: the
      cross-repo handoff doc `docs/t-013-remaining-example-works.md` (written 2026-07-17)
      still listed `examples.manifest.json` and its 3 example-work images as files to
      commit directly into the `kind_robots` git repo, but the 2026-07-19 discovery already
      on record in this checklist's "Example works" row established that the manifest
      actually lives on `media.acrocatranch.com`, not in git -- the handoff doc predated
      that finding and was never corrected, so a future session with home-relay access
      could easily follow its stale file list and commit to paths the contract verifier
      (`verifyAcademyExamplesManifest.ts`) never reads. Confirmed via `kind_robots` source
      (`utils/scripts/mediaContractSource.ts`: the manifest is fetched through
      `readMediaText()`/`mediaSourceDescription()` against `MEDIA_ROOT` or
      `https://media.acrocatranch.com`, never the git tree) and a live check
      (`get_file_contents` on `public/images/academy/examples/` 404s -- the directory does
      not exist in the repo). Corrected the handoff doc in place: split "Files to change"
      into an in-repo half (`academyStyles.ts` only) and a media-server half routed
      through the home relay, and rewrote "Remaining action" into the two genuinely
      independent steps. Added a short pointer addendum to t-033's note above so the
      correction isn't only discoverable by opening the doc. Conductor-docs-only change,
      no kind_robots PR (t-033/t-035 remain genuinely blocked on home-relay write access,
      unchanged by this fix). Updated continuous-improvement-checklist.md's rotation
      state; next preferred lane is inspiration/preview assets (lane 3), noting it was
      last confirmed blocked 2026-07-20 ~04:10Z and this cycle's media-URL spot-check
      (still 404) is not a fresh live-queue probe. Rearming to `ready` per the
      recurring-task convention.
      RAN 2026-07-20 (~10:04-10:35 UTC, worker/burst-1784541890): checklist's preferred lane was inspiration/preview assets (lane 3), so tried it first. Reconfirmed the blocker with a fresh, authenticated poll of the exact job queued by the prior cycle: `GET /api/art/queue/816` still returned `status: PENDING` with `updatedAt` unchanged since the job was created (~00:14 UTC) -- roughly 10 hours with zero relay activity, same signature as every prior check. Fell back to lane 4 (curriculum depth) per the checklist's own instruction, and per its "finish known coverage gaps before a 26th movement" rule picked the one non-blocked gap the coverage table already named: added a Persian Miniature Painting row (#25) to `docs/teaching-notes.md`'s per-style table (mode `prompt`, difficulty **Hard** -- the movement's inverted spatial logic, distant figures placed higher rather than smaller, fights the same "preserve composition" tension already documented for cubism/de-stijl/suprematism) and bumped the section header from 24 to 25 movements. Also added the missing `persian-miniature` placeholder row to `style-lora-registry.md`'s curriculum-slug-mapping table, mirroring the existing `ashcan-school`/`american-regionalism` "not yet in the registry" convention (no LoRA search performed this cycle). Verified: `python3 -c "import yaml; yaml.safe_load(...)"` on both edited docs is N/A (plain Markdown, not YAML); confirmed no other RAN entry already covered this gap by grepping `persian` (case-insensitive) across both files before editing. Conductor-docs-only change (teaching-notes.md, style-lora-registry.md, this checklist); no kind_robots PR needed -- neither file is synced into the kind_robots front end. Left two related gaps unfixed, out of this cycle's scope and noted in the checklist for a future pass: `curriculum-outline.md` never got a "v1.5 addition re-check" public-domain-policy paragraph for §25 (every addition since v1.1 has one; the underlying VERIFIED research already exists in §25 itself, so this would be a low-risk writeup, not new research), and persian-miniature is not yet categorized in curriculum-outline.md's "Lesson-only vs remixable" tier lists. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is front-end polish (lane 1). Rearming to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~12:07-12:30 UTC, claude-burst-rotation-20260720T120752Z): checklist's preferred lane was front-end polish (lane 1), per the prior cycle's note. Dispatched a general-purpose subagent over all 7 in-scope files (academy-manager.vue, academy-timeline.vue, academy-styles-browser.vue, academy-style-detail.vue, academy-remix.vue, art-styler.vue, image-upload.vue) plus their store/composable dependencies, with an explicit exclusion list of every previously-fixed bug class (PRs #275, #301, #332, #371, #380, #383, #385, #387, #397, #515, #520, #544, #547, #603, #622). Found and independently verified a real, verifiable data-correctness bug: `academy-styles-browser.vue` renders the expanded style's detail panel through a single unkeyed `academy-style-detail` instance; its click handler (`expandedSlug = expandedSlug === style.slug ? null : style.slug`) moves directly between two non-null slugs when the user picks a new grid tile without closing the current panel, so `v-if="expandedStyle"` stays true the whole time and Vue patches the same component instance in place instead of remounting it. `academy-style-detail.vue` records a lesson as viewed only in `onMounted` (`markLessonViewed(props.lesson.slug)`), so switching styles this way silently stopped tracking every style after the first one opened in a session -- no checkmark, no `lessonsViewedCount` credit, no error. Confirmed `academy-timeline.vue` does not share the bug: each style lives in its own `v-for` `<li :key="style.slug">`, so switching styles there always mounts a genuinely distinct instance. Fixed by adding `:key="expandedStyle.slug"` to the `academy-style-detail` usage in `academy-styles-browser.vue`, forcing a remount (and therefore a fresh `onMounted`) on every slug change. Verified: `npx prettier --check` clean; this sandbox has no `node_modules` installed (no local `npm install` run) so eslint/vue-tsc could not run locally -- relying on kind_robots CI (TypeScript, Contract verifiers, GitGuardian) same as the exception path prior cycles have used when local tooling was unavailable. kind_robots PR #646 (branch `claude/keen-fermat-y6jjjw`), all 3 CI checks green (TypeScript, Contract verifiers, GitGuardian), merged squash `98cfdc8`. Updated continuous-improvement-checklist.md's rotation state; next preferred lane is roadmap accuracy (lane 2). Rearmed to `ready` per the recurring-task convention.
      RAN 2026-07-20 (~22:10-22:35 UTC, claude-conductor-agentrun-20260720T2210Z): checklist's preferred lane was inspiration/preview assets (lane 3), so tried it first with a genuinely fresh queued job (job 1014, greek-vase-painting.webp, not 816/855/957). `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 1 --timeout 90` queued job 1014 (200 OK) but timed out after 90s still queued; a direct `curl` to `/api/art/queue/1014` confirmed `status: PENDING` with `updatedAt` identical to `createdAt` -- same never-claimed signature as jobs 816/855/957, home relay still down. Fell back to lane 4 per the checklist's own instruction. `docs/style-lora-registry.md` had no entry at all for `persian-miniature` (added to the curriculum two cycles earlier, at Section 25) -- the curriculum-slug-mapping table still said "not yet in the registry." With Hugging Face and Civitai both freshly confirmed reachable this cycle (`scripts/recheck_egress_blocks.py huggingface.co`/`civitai.com`, both HTTP 200), ran an actual LoRA search via WebSearch/WebFetch instead of deferring it again. Found `batchku/storai-persian-miniature` (Hugging Face, FLUX.1-dev base, no login, trigger `ali_persian-miniature`, trained via fal.ai's generic flux-lora-fast-training service) but did not promote it: the model card discloses no training artists/artwork, so the dead-70-years ethical boundary (DESIGN-BRIEF.md) can't be confirmed from the source data -- same standard already applied to the rejected `gothic` Civitai candidate (S-8). Also checked and ruled out Civitai's "Mughal Miniature Painting Style" (a related but visually distinct Indo-Persian tradition, off-register for this Timurid/Safavid lesson) and Shakker-Labs/FLUX.1-dev-LoRA-Miniature-World (a false-positive name match -- tilt-shift diorama photography, not painting). Added `persian-miniature` as an 11th prompt-mode registry entry: machine-readable block entry, curriculum-slug-mapping table row flipped from "not yet in the registry" to "matches," and a full per-style notes section documenting the search and rejection rationale -- `prompt_hint` copied verbatim from `curriculum-outline.md` Section 25's remix_hint so the two docs can't drift apart. Verified the registry's `styles:` YAML block still parses clean (23 entries, 11 prompt / 12 lora, `persian-miniature` present) via `yaml.safe_load`, and `scripts/audit_roadmaps.py` reports the same 0-errors/11-warnings/46-info baseline as before this change, none touching this project. Conductor-docs-only change; no kind_robots PR needed this cycle -- the registry is a conductor-repo doc, not synced into the kind_robots front end. Updated `continuous-improvement-checklist.md`'s rotation state; next preferred lane is front-end polish (lane 1) -- this cycle ran lane 4, so lane 1 is next in the 1->2->3->4 rotation. Rearming to `ready` per the recurring-task convention.
    owner: worker
    claimed_by: claude-conductor-scheduled-burst-20260721T0611Z
    claimed_at: '2026-07-21T06:13:58Z'

  - id: t-015
    milestone: m4
    title: 'kind_robots: add Neoclassicism to the academyStyles.ts front-end seed'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-15T13:17:22Z'
    note: 'Merged: kind_robots PR #291 (squash, sha 9ec971e). Neoclassicism entry mirrored into stores/seeds/academyStyles.ts (slug, era, artists, remix template), inserted between baroque and ukiyo-e to match curriculum ordering. PR''s TypeScript check was red from 19 pre-existing errors unrelated to this one-file additive change (tracked at kind-robots/t-020, none in the changed file) — Contract verifiers and GitGuardian green, mergeable_state: unstable (not blocked), base matched current origin/main exactly. Merged past the confirmed-pre-existing failure per established precedent (PRs #256, #271, #290).'
    claimed_by: burst-20260715-1300
    claimed_at: '2026-07-15T13:08:16Z'

  - id: t-018
    milestone: m4
    title: 'kind_robots: add Bauhaus to the academyStyles.ts front-end seed'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-16T02:20:00Z'
    note: >
      DONE 2026-07-16: kind_robots PR #305 merged (squash, sha a3fa4c3).
      Added a `bauhaus` entry to stores/seeds/academyStyles.ts (era
      1919-1933, Germany; Kandinsky/Klee/Moholy-Nagy, all died 1944-1946),
      inserted after `de-stijl` to match curriculum ordering, content
      sourced verbatim from docs/curriculum-outline.md §16. Verified:
      prettier clean, full project `npm run test` (vue-tsc --noEmit) exit 0
      with zero errors before push; all 4 PR checks (TypeScript, Contract
      verifiers, GitGuardian, Vercel deployment) green before merge. Branch
      worker/ai-art-academy-t-018 auto-deleted on merge.
    claimed_by: claude-burst-hourly-20260716-0200
    claimed_at: '2026-07-16T02:06:17Z'

  - id: t-019
    milestone: m5
    title: 'kind_robots: wire academyStyles.ts previewImageSrc + render thumbnails in the style browser'
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-16T08:15:00Z'
    note: >
      Filed 2026-07-16 (conductor-burst-hourly, t-010 option (c) follow-up).
      projects/art-prompts.yaml now has 16 pending `requests:` entries
      (ids `kind-robots-academy-style-preview-<slug>`) queuing a 256x256
      preview thumbnail for every curriculum movement at
      public/images/academy/styles/{slug}.webp. Once those images are
      generated and land in kind_robots via the normal art-prompts.yaml →
      distribute_images.py pipeline, this task sets each style's
      `previewImageSrc` field in stores/seeds/academyStyles.ts (the field
      already exists in the AcademyStyle interface — see the doc comment
      on line 44 — it's just never been populated) and confirms the style
      browser/timeline components actually render it (fall back cleanly to
      the current no-image state for any style whose thumbnail hasn't
      landed yet, since the 16 images won't all generate in the same
      batch). Blocked until at least one of the queued images exists in
      kind_robots' public/images/academy/styles/ — do not claim this task
      before verifying at least one file is present, or it will just
      re-discover the same "nothing to wire yet" state.

  - id: t-020
    milestone: m4
    title: 'kind_robots: add the 5 v1.1 curriculum movements to the academyStyles.ts front-end seed'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-16T11:22:00Z'
    note: >
      Filed 2026-07-16 (t-010 curriculum-expansion cycle). docs/curriculum-outline.md
      grew from 16 to 21 movements (§17 gothic, §18 northern-renaissance, §19 rococo,
      §20 symbolism, §21 pointillism — all cleared against PUBLIC-DOMAIN-POLICY.md
      §1.3). Mirror each new movement into kind_robots stores/seeds/academyStyles.ts
      exactly as t-015 (Neoclassicism) and t-018 (Bauhaus) did: one entry per slug
      (slug, name, era, artists, recognitionCues, remix template from the outline's
      remix_hint), inserted in chronological position per each entry's `era` (they
      were appended to the outline's prose but the machine-readable skeleton and the
      `era` field give the true order). Content sourced verbatim from the outline —
      no new facts invented. Small, additive, one-file change; verify prettier +
      vue-tsc clean before opening the kind_robots PR. Independently landable now
      (does not depend on the preview-image work in t-019/t-022).

      DONE 2026-07-16T11:22Z (burst-hourly cycle): kind_robots PR #313 opened on
      claude/keen-fermat-qrthru with all 5 entries (21 total in the raw array,
      confirmed via slug count). Verified `prettier --check` clean and `npm test`
      (nuxi prepare + vue-tsc --noEmit) 0 TypeScript errors. CI green (TypeScript,
      Contract verifiers, GitGuardian all passed) — self-merged (squash) as
      c807bde. Milestone m4 unaffected in status (still in-progress; other m4
      tasks remain open).
    claimed_by: burst-20260716110858
    claimed_at: '2026-07-16T11:08:59Z'
    pr: https://github.com/silasfelinus/kind_robots/pull/313

  - id: t-021
    milestone: m2
    title: 'Hunt LoRAs for the 5 new prompt-mode movements and backfill prompt_hint on older entries'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-17T19:32:00Z'
    note: 'Filed 2026-07-16 (t-010 curriculum-expansion cycle). The five new movements all ship prompt-mode in docs/style-lora-registry.md; none has a login-free FLUX LoRA yet. Extend the registry the way t-003 did: hunt openly downloadable, PD/dead- artist-safe LoRAs for gothic, northern-renaissance, rococo, symbolism, and pointillism (Civitai starting points already parked in the needs-Silas table — S-7 pointillism, S-8 gothic — but check for login-free Hugging Face options first, and record license/trigger/weight per the existing schema). Same environment caveat as t-003: HF/Civitai may be egress-blocked this session, so web-search-sourced entries stay `license: unverified` until read off the model card. BACKFILL DONE 2026-07-17 (conductor-burst-hourly): claimed, then completed the registry-only half of this task (no egress needed) and released the rest back to `ready`. Added `prompt_hint` to the 6 older prompt-mode entries still missing it — ukiyo-e, baroque-chiaroscuro, cubism, stained-glass, byzantine-mosaic, art-deco (the note''s original "8" count was stale: gothic and pointillism, 2 of the original 8, already got their hints in the same v1.1 batch that introduced the field). Each hint is derived verbatim from that style''s existing per-style prose recipe in the same file — no new facts invented. Updated the field''s own doc-comment to drop the "can be backfilled" TODO language. Confirmed fresh via EGRESS-BLOCKERS.md (2026-07-17T03:05Z entries, this session) that huggingface.co/civitai.com remain 403-blocked from this sandbox, so the LoRA-hunting half stays `ready`-but-blocked (transient, no pass burned) for a session with open egress to those hosts, or Silas''s own environment. DONE 2026-07-17 (conductor-burst cycle, claude-conductor-burst-20260717T1900Z): first session with genuinely open egress to both huggingface.co and civitai.com (recheck_egress_blocks.py logged both `reachable` this time, see EGRESS-BLOCKERS.md — every prior entry for these two hosts was `blocked`). Resolved all five movements: (1) northern-renaissance upgraded to mode:lora — Muapi/jan-van-eyck-style (HF, no login, FLUX.1-dev, trigger "Jan van Eyck Style"), a direct hit named for the movement''s founding master (d. 1441). (2) symbolism upgraded to mode:lora — Kaamalauppias/finnish-symbolism-art-lora-v2 (HF, no login, FLUX.1-dev, trained on Finnish Symbolism/Gallen-Kallela d.1931); flagged the generic `TOK` trigger and missing sample images as a t-004 quality-verification item, and noted a CC-BY-SA-4.0 SDXL alternate (idiootti-ilves/Akseli_Gallen-Kallela) as a future-retrain reference only (wrong base architecture to wire directly). (3) gothic (S-8) — now readable via Civitai''s public model API; its description discloses zero training provenance (page content is literally an unrelated recipe), so it is NOT promoted even though reachable — can''t confirm the dead-70-years boundary. (4) pointillism (S-7) — fully verified via the same API: trained on Signac (d.1935) and van Rysselberghe (d.1926), both PD-safe, FLUX.1 D base, documented trigger words; actual weight download still returns HTTP 401 without a Civitai login (confirmed directly), so it stays in the needs-Silas table, now with verified detail instead of a blind link. (5) rococo — no viable candidate: the one FLUX rococo-labeled hit (Muapi/1890s-rococo-opera-sd1-sdxl-pony-flux) trains on a modern "1890s fantasy opera movie still" look, rejected as off-register same as the Picasso-cubism precedent; stays prompt-mode, genuinely nothing better found. Registry LoRA-backed count: 8 → 10 (of 16 original target styles). Full detail + updated needs-Silas table rows in docs/style-lora-registry.md. Conductor-only change (documentation registry, no kind_robots code); no kind_robots PR needed this cycle. MERGED conductor PR #716 (squash e1604f8) -- all 20 CI checks green.'
    claimed_by: claude-conductor-burst-20260717T1900Z
    claimed_at: '2026-07-17T19:08:00Z'

  - id: t-022
    milestone: m5
    title: 'Queue 256x256 style-preview thumbnail requests for the 5 new movements in art-prompts.yaml'
    status: done
    owner: worker
    passes: 1
    stakes: reversible
    updated: '2026-07-16T20:09:33Z'
    note: >
      Filed 2026-07-16 (t-010 curriculum-expansion cycle). t-010's ~08:06 cycle queued
      16 `requests:` entries in projects/art-prompts.yaml (ids
      `kind-robots-academy-style-preview-<slug>`) for a 256x256 preview thumbnail per
      v1 movement at public/images/academy/styles/{slug}.webp. Extend that batch to
      the 5 v1.1 movements (gothic, northern-renaissance, rococo, symbolism,
      pointillism) using the exact same pattern: same shared subject (small robot beside
      a windowsill potted plant) for side-by-side comparability, prompt built from each
      movement's recognitionCues / remix_hint in the curriculum outline. Prompt-catalog-
      only change (conductor repo); actual generation happens later via the existing
      art-prompts.yaml → distribute_images.py pipeline. Feeds t-019 (which wires
      previewImageSrc once images land).
      RAN 2026-07-16 (claude-conductor-burst-20260716-t022-thumbs): added the 5 missing
      requests: entries (gothic, northern-renaissance, rococo, symbolism, pointillism)
      to projects/art-prompts.yaml, each built from that movement's own Recognition
      cues in docs/curriculum-outline.md §17-21, mirroring the exact prompt shape and
      shared subject/composition boilerplate the other 16 entries already use so the
      full 21-movement set stays visually comparable. Prompt-catalog-only change;
      verified with `yaml.safe_load` (21 kind-robots-academy-style-preview-* entries
      present, file parses clean) — no image generation triggered, no kind_robots code
      touched. t-019 remains blocked until at least one of the 21 queued images lands.
    claimed_by: claude-conductor-burst-20260716-t022-thumbs
    claimed_at: '2026-07-16T20:09:33Z'

  - id: t-023
    milestone: m4
    title: 'kind_robots: wire the teaching-notes lesson scaffold (Try-It / Reflect) into the Academy lesson-detail component'
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-16T15:47:00Z'
    note: >
      DONE 2026-07-16 (autonomous hourly conductor cycle): merged kind_robots PR #315
      (squash 7806771a9b6492af5a0e290a74dd05cea9ee42a6) — added the "Try It" (remix
      instruction from `remix.template`, a `recognitionCues`-driven what-to-expect line,
      a `remix.mode`-aware failure-mode note) and "Reflect" (three reusable
      comprehension/critique prompts) beats to `academy-style-detail.vue`. All 3
      kind_robots checks green (GitGuardian, TypeScript, Contract verifiers). Per-style
      failure-mode text intentionally stayed mode-level generic rather than threading 21
      one-off strings from teaching-notes.md into academyStyles.ts — additive, scoped,
      reversible.
    claimed_by: claude-conductor-burst-20260716-t023
    claimed_at: '2026-07-16T14:06:59Z'

  - id: t-024
    milestone: m3
    title: Reconcile divergent slugs between curriculum-outline.md and style-lora-registry.md
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    note: |-
      DONE 2026-07-16 (autonomous hourly conductor cycle): added a "Curriculum slug
      mapping" section to docs/style-lora-registry.md (between "Coverage summary" and
      "Machine-readable registry") — a full 21-row table mapping every
      curriculum-outline.md `slug` to its style-lora-registry.md `style_slug`
      counterpart (or `(none)` where no registry entry exists yet), explicitly
      flagging the 3 divergent pairs (`baroque`/`baroque-chiaroscuro`,
      `renaissance`/`renaissance-fresco`, `post-impressionism`/
      `post-impressionism-van-gogh`) with a one-line reason each (the registry slug is
      an artist/technique-specific take narrower than the general movement, not a
      naming inconsistency), and listing the 6 registry-only bonus entries with no
      curriculum counterpart at all (`stained-glass`, `watercolor`, `oil-painting`,
      `sumi-e`, `art-deco`, `pop-art`) so a future seed-sync task knows to skip them.
      Verified the mapping programmatically (parsed both files' slug lists and diffed
      the sets) rather than by eye — table matches the actual file contents exactly:
      21/21 curriculum slugs and 21/21 registry slugs accounted for. Docs-only change
      in the conductor repo, no kind_robots PR needed.

      Original spec follows: Kaizen from the curriculum-expansion cycle merge (Reviewer, autonomous hourly
      cycle, 2026-07-16). docs/curriculum-outline.md and docs/style-lora-registry.md use
      divergent slugs for the same movement in several places — e.g. `baroque` vs
      `baroque-chiaroscuro`, `renaissance` vs `renaissance-fresco`,
      `post-impressionism` vs `post-impressionism-van-gogh`. Add either a
      small cross-file consistency check (script or CI step comparing the two
      YAML skeleton blocks' slug sets) or a documented slug-mapping table, so
      the seed-sync task (t-020, wiring the 5 newest movements into
      academyStyles.ts) can't accidentally wire the wrong style to the wrong
      lesson. Do this before or alongside t-020 lands.
    claimed_by: conductor-hourly-20260716-1349
    claimed_at: '2026-07-16T13:53:00Z'
    updated: '2026-07-16T13:53:00Z'

  - id: t-025
    milestone: m4
    title: 'kind_robots: thread per-style failure-mode text from docs/teaching-notes.md into academyStyles.ts'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-023's merge (Reviewer, autonomous hourly conductor cycle,
      2026-07-16, kind_robots PR #315). The new "Try It" beat's failure-mode note in
      `academy-style-detail.vue` is currently mode-level generic (prompt vs lora, two
      strings total) because the actual per-style failure-mode guidance lives only in
      conductor's `docs/teaching-notes.md`, not in `academyStyles.ts`'s data model.
      Add a `failureMode` (or similar) field to the style registry / seed, backfill it
      from teaching-notes.md's per-style notes for all 21 movements, and swap the
      component's mode-level computed for a per-style lookup with the current
      mode-level text as fallback when a style hasn't been backfilled yet. Scoped,
      additive, reversible — no dependency on kind-robots/t-022's DB outage.
      DONE 2026-07-16T19:06:00Z (autonomous hourly conductor cycle, acting as both
      Worker and Reviewer since no separate Worker session was active): implemented,
      opened, and merged kind_robots PR #319 (squash 38beaea7). Added `failureMode?:
      string` to the AcademyStyle interface and backfilled all 21 movements from
      teaching-notes.md §3's "Key failure mode / watch-for" column; wired
      academy-style-detail.vue's Try It beat to use it with the prior mode-level text
      preserved as a fallback for any future unbackfilled style. Verified via
      verifyAcademyStyleDetailCallers.ts, eslint, prettier, and a full project
      vue-tsc --noEmit (0 errors). All 4 CI checks green before merge.
    claimed_by: conductor-hourly-20260716
    claimed_at: '2026-07-16T18:51:14Z'
    updated: '2026-07-16T19:06:00Z'

  - id: t-026
    milestone: m4
    title: Diff-check t-025's backfilled failureMode strings against teaching-notes.md's source table
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-16T21:15:00Z'
    note: >
      Kaizen from t-025's merge (kind_robots PR #319, 2026-07-16). The 21
      `failureMode` values in `stores/seeds/academyStyles.ts` were adapted from
      conductor's `docs/teaching-notes.md` §3 table prose (cleaned up capitalization
      and quoting for TS string literals) rather than copied byte-for-byte. Read the
      table and the seed side by side and confirm none of the 21 drifted in meaning
      during that cleanup — flag and fix any that did.

      DONE 2026-07-16T21:15Z (burst-hourly cycle): read all 21 `failureMode` values in
      kind_robots `stores/seeds/academyStyles.ts` (main @ 748c645) side by side against
      `docs/teaching-notes.md` §3's per-style table, slug by slug (greek-vase-painting
      through bauhaus). No meaning drift in any entry — differences are limited to
      punctuation/article normalization for TS string literals (e.g. "May add vessel
      curvature/border framing" -> "May add vessel curvature or border framing"; "Keeps
      the photo's saturated/contrasty color" -> "Keeps the photo's saturated, contrasty
      color") and light TS-prose smoothing that preserves the original claim (e.g.
      expressionism's seed adds "for a broader result" as a natural completion of the
      table's "(registry note)" pointer, de-stijl's seed spells out "rather than
      expecting recognizable content"). Count also confirmed: exactly 21 `slug:` and 21
      `failureMode:` entries in the seed file, matching the table's 21 rows. No
      kind_robots PR needed — nothing to fix. Closing clean.
    claimed_by: claude-conductor-burst-20260716-t026-diffcheck
    claimed_at: '2026-07-16T21:07:15Z'
  - id: t-027
    milestone: m4
    title: 'kind_robots: validate starters.manifest.json against PUBLIC-DOMAIN-POLICY.md schema in CI'
    status: done
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-17T12:52:07Z'
    note: >
      Kaizen from t-008's merge (kind_robots PR #358, 2026-07-17). The manifest
      schema (workTitle, artist, artistDied, year, collection, accessionId,
      sourceUrl, license, licenseTermsUrl, retrievedDate per
      PUBLIC-DOMAIN-POLICY.md §3) is currently enforced only by hand at write
      time. Add a small script (Zod or plain JS) that validates
      public/images/academy/starters/starters.manifest.json against the
      required-field schema and the license enum (CC0 | PD-Mark |
      Open-Access-Terms, with licenseTermsUrl required for the latter), and wire
      it into an existing lightweight CI check (e.g. contract-tests.yml) so a
      future edit to this file can't silently drop a provenance field.
    claimed_by: claude-conductor-autonomous-20260717T1252Z
    claimed_at: '2026-07-17T12:52:07Z'
  - id: t-028
    milestone: m4
    title: 'kind_robots: generalize the provenance schema validator to cover the future academy-styles example-work registry'
    status: done
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-17T14:05:00Z'
    depends_on: t-013
    note: >
      Kaizen from t-027's merge (kind_robots PR #359, 2026-07-17). Fulfilled as a
      side effect of t-013's kind_robots PR #360 (2026-07-17): the PR extracted
      the shared field-list/license-enum check into
      utils/scripts/academyProvenanceSchema.ts and rewired both
      verifyAcademyStarterManifest.ts and the new
      verifyAcademyExamplesManifest.ts to validate against it — exactly the
      generalization this task asked for. Closed done rather than waiting on
      t-013's remaining 12 movements, since the schema-sharing work itself is
      complete regardless of how many example-work entries exist yet.

  - id: t-029
    milestone: m6
    title: 'conductor: teach recheck_egress_blocks.py to distinguish a Cloudflare bot-challenge from a genuine reachable/blocked response'
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: '2026-07-17T17:00:00Z'
    note: >
      DONE (2026-07-17, autonomous hourly cycle, claude-conductor-hourly-20260717):
      scripts/recheck_egress_blocks.py's probe_host() and append_entry() now
      return/accept a three-way status string ("blocked" | "bot-challenged" |
      "reachable") instead of a bool. A response (success or HTTPError) carrying
      a `cf-mitigated` header is stamped "bot-challenged" with the header value
      recorded in the ledger detail line, instead of being silently folded into
      "reachable" as before. main()'s marker map and EGRESS-BLOCKERS.md's `##`
      line both carry the new status verbatim. No other script calls probe_host
      or append_entry, so this was a clean in-place signature change; all 9
      existing tests were updated for the new string status (was bool) and 4 new
      tests cover the bot-challenged path (HTTPError-with-header, 200-with-header,
      and the ledger-append/marker-print paths) — 13/13 pass, full suite
      332/332 pass. Manually verified end-to-end with a monkeypatched urlopen
      raising an HTTPError carrying `cf-mitigated: challenge`. Historical
      EGRESS-BLOCKERS.md entries (e.g. www.artic.edu, recorded before this fix)
      are left as-is per the ledger's append-only convention; the next real
      recheck of that host will now stamp bot-challenged automatically instead
      of needing a hand-written manual note like t-013 had to add.
    claimed_by: null
    claimed_at: null

  - id: t-030
    milestone: m6
    title: 'conductor: extend recheck_egress_blocks.py bot-challenge detection beyond the cf-mitigated header'
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-17T17:49:08Z'
    note: >
      Kaizen from t-029 (2026-07-17). t-029 detects a Cloudflare bot-challenge
      only via the `cf-mitigated` response header. Some Cloudflare configurations
      serve a JS/managed challenge without that header — e.g. a 503 "Just a
      moment..." interstitial identified only by `server: cloudflare` plus a
      `cf-chl`-prefixed Set-Cookie or a `__cf_chl_rt_tk` marker in the HTML body.
      HEAD requests (what probe_host uses) won't get a body, so the header/cookie
      signals are the practical path. Add a fallback check for these secondary
      signals so the header-only heuristic doesn't miss non-cf-mitigated
      challenge variants, and add tests for each new signal.
    claimed_by: claude-conductor-hourly-20260717c
    claimed_at: '2026-07-17T17:49:08Z'

  - id: t-031
    milestone: m4
    title: 'kind_robots: add Suprematism to the academyStyles.ts front-end seed'
    status: done
    passes: 0
    stakes: reversible
    updated: '2026-07-18T00:57:02Z'
    note: 'Filed 2026-07-18 (t-010 curriculum-expansion cycle). docs/curriculum-outline.md grew from 21 to 22 movements: §22 Suprematism (`suprematism`, era 1913-1919, sole artist Kazimir Malevich 1879-1935), cleared against PUBLIC-DOMAIN-POLICY.md §1.3 with wide margin (artist died 1935, all four example works 1915-1918). Mirror the new movement into kind_robots stores/seeds/academyStyles.ts exactly as t-015 (Neoclassicism) and t-018 (Bauhaus) did: one entry (slug, name, era, artists, recognitionCues, remix template from the outline''s remix_hint), inserted in chronological position per the `era` field — Suprematism (1913-1919) sits before De Stijl (1917-1931) and Bauhaus (1919-1933) in true chronological order even though it is appended last in the outline''s prose. Content sourced verbatim from the outline — no new facts invented. Small, additive, one-file change; verify prettier + vue-tsc clean before opening the kind_robots PR. Independently landable now (does not depend on the preview-image work in t-019/t-022; a Suprematism preview-thumbnail prompt was queued separately in art-prompts.yaml this same cycle).  DONE 2026-07-18T00:57Z (conductor-hourly-20260718T0048Z): kind_robots PR #379 merged (squash, sha c7033961). Added the `suprematism` entry to stores/seeds/academyStyles.ts (slug, name, era, sortYear, region, keyIdeas, recognitionCues, artists, failureMode, remix template), inserted before de-stijl/bauhaus per its 1913 sortYear -- content sourced verbatim from docs/curriculum-outline.md §22, mirroring t-015/t-018''s pattern. exampleWorks intentionally omitted this pass (separate follow-up, matching how t-018/t-020 landed). Verified: prettier --write then --check clean, eslint 0 errors, full-project `npm run test` (nuxi prepare + vue-tsc --noEmit) exit 0. All 3 kind_robots PR checks green (TypeScript, Contract verifiers, GitGuardian) before merge.'
    owner: worker
    claimed_by: claude-conductor-hourly-20260718T0048Z
    claimed_at: '2026-07-18T00:49:06Z'

  - id: t-032
    milestone: m4
    title: 'kind_robots: fix style-remix race condition mis-attributing which style gets marked remixed'
    status: done
    passes: 0
    stakes: reversible
    updated: '2026-07-18T11:20:48Z'
    note: >
      Filed 2026-07-18 (t-010 lane-1 front-end-polish investigation, conductor-scheduled
      session claude-conductor-scheduled-20260718T0810Z). Explore-subagent sweep of
      components/art/art-styler.vue and components/academy/academy-remix.vue (dispatched
      after this session's own attempted lane-3 art generation failed for an unrelated
      reason -- see this task's note and TALKBACK.md) found a genuine data-integrity race
      condition, not a manufactured issue: art-styler.vue's style swatches have no
      `:disabled` guard tied to `isGenerating` (only the textarea and Clear button do), so
      a user can pick a different style while a style-transfer request is still in flight.
      `runStyleTransfer()` captured the style used for the request in a local `const style`
      (correct), but its completion `emit('generated', result.data)` only sent the
      resulting ArtImage, never which style produced it. academy-remix.vue's `onGenerated`
      then read `academyStore.selectedStyleSlug` -- the CURRENT store value at completion
      time, not the style actually used for that request -- to call
      `academyStore.markStyleRemixed(slug)`. Failure scenario: pick Baroque, click Apply
      (generation starts), switch to Cubism while it's still running (allowed, no guard),
      Baroque's result lands and gets attributed to Cubism instead -- corrupting the
      Academy's persisted (localStorage) "styles remixed" progress count/checkmarks shown
      in academy-timeline.vue and academy-styles-browser.vue.

      DONE 2026-07-18T11:32Z: kind_robots PR #408 merged (squash, sha 6dfb89e). Changed
      art-styler.vue's `generated` emit to carry the `StyleEntry` actually used for the
      request (`emit('generated', result.data, style)`, `style` being the local const
      captured at the start of `runStyleTransfer()`), and academy-remix.vue's `onGenerated`
      to read that emitted style (`style?.slug`) instead of re-reading
      `academyStore.selectedStyleSlug` after the async request settles. Verified: eslint 0
      errors, prettier clean, full-project `npm run test` (nuxi prepare + vue-tsc --noEmit)
      exit 0, all 3 kind_robots PR checks green (TypeScript, Contract verifiers,
      GitGuardian). Checked all 3 existing `@generated` callers (avatar-picker.vue,
      coloring-book-page.vue x2) -- none read a second argument, so the emit-signature
      change is backward compatible. Kaizen suggestion (not filed as a task): the style
      swatches still have no `:disabled`/pointer-events guard during `isGenerating`, so a
      user can still visually reselect mid-request even though attribution is now correct
      -- a UX follow-up could disable them to match the existing textarea/Clear-button
      pattern.
    owner: worker
    claimed_by: claude-conductor-scheduled-20260718T0810Z-followup
    claimed_at: '2026-07-18T11:20:48Z'

  - id: t-033
    milestone: m3
    title: 'kind_robots: fix examples.manifest.json contract failure (expressionism, cubism, bauhaus)'
    status: needs-human
    owner: worker
    passes: 0
    stakes: reversible
    note: >-
      FOR SILAS: Blocked on media-server write access, not on missing research. `npm run
      test:academy-examples-manifest` fails on kind_robots main because the `expressionism`,
      `cubism`, and `bauhaus` entries in stores/seeds/academyStyles.ts each reference an
      exampleWorks image (the-yellow-cow-49-1210.jpg, fantomas-1976-59-1.jpg,
      composition-8-37-262.jpg) that returns 404 from https://media.acrocatranch.com --
      confirmed by curl from this session. academyStyles.ts is the canonical metadata copy
      and examples.manifest.json is its media mirror (see the header comment in
      verifyAcademyExamplesManifest.ts), so the fix is uploading the 3 missing entries and
      images, not deleting the seed data. The full patch -- exact academyStyles.ts diff,
      exact examples.manifest.json entries (with real width/height/bytes), and the verified
      public-domain source URLs for all 3 works (Wikimedia Commons + NGA, CC0/PD-Mark) -- was
      already assembled by an earlier session and is sitting unapplied at
      projects/ai-art-academy/docs/t-013-remaining-example-works.md. That session hit the
      same wall: uploading to media.acrocatranch.com requires the home Windows relay
      (ops/home-server/SELF-HOSTED-MEDIA.md), which no sandboxed agent session can reach.
      TO APPROVE: run the acquisition + upload for the 3 images named in the handoff doc
      through the home relay (or drop them in manually at the paths listed), then have any
      session apply the academyStyles.ts + manifest patch from that doc and confirm `npm run
      test:academy-examples-manifest` passes. That unblocks the "Contract verifiers" CI
      check for every kind_robots PR, not just Academy ones -- also referenced by
      kind-robots/t-038 (stale workflow file) as a co-blocker of the same check.
      UPDATE 2026-07-20 (t-010 roadmap-accuracy cycle): corrected a stale assumption inside
      the handoff doc itself -- it previously listed examples.manifest.json and the 3 images
      as files to commit directly in the kind_robots git repo. Confirmed via kind_robots
      source (utils/scripts/mediaContractSource.ts, verifyAcademyExamplesManifest.ts) and a
      live check (get_file_contents on public/images/academy/examples/ 404s -- the directory
      does not exist in git) that only stores/seeds/academyStyles.ts is an in-repo commit;
      examples.manifest.json and the images are fetched from
      https://media.acrocatranch.com/images/academy/examples/... at build/test time and must
      go through the home relay, never a GitHub PR. Doc corrected in place (see its
      2026-07-20 correction note); this task's actual blocker (home-relay write access) is
      unchanged.
      UPDATE 2026-07-20 (t-010 roadmap-accuracy cycle, later same day): the
      "also referenced by kind-robots/t-038 ... as a co-blocker of the same check" line
      above is now stale. Checked projects/kind-robots/roadmap.yaml: t-038 (delete the
      stale thin-social-store-codemod.yml workflow breaking `test:workflow-paths`/
      "Contract verifiers" CI) shows `status: done`, resolved 2026-07-18T14:30:32Z --
      about 6 minutes after this task's note above was originally written at 14:24:39Z,
      so it was accurate at the time but has been fixed since. Confirmed the
      "Contract verifiers" check is healthy on recent kind_robots PRs (e.g. #646, #648)
      with no sign of that workflow-file failure recurring. This task's remaining
      blocker is solely the Academy examples-manifest home-relay write access described
      above; it is no longer compounded by an unrelated repo-wide CI break.
    claimed_by: claude-scheduled-20260718-1420-26944
    claimed_at: '2026-07-18T14:22:06Z'
    updated: '2026-07-18T14:24:39Z'
    soft_gate: true

  - id: t-034
    milestone: m4
    title: 'kind_robots: add Ashcan School to the academyStyles.ts front-end seed'
    status: done
    passes: 0
    stakes: reversible
    updated: '2026-07-18T22:19:21Z'
    note: >-
      Filed 2026-07-18 (t-010 curriculum-expansion cycle, lane 4). docs/curriculum-outline.md
      grew from 22 to 23 movements: §23 Ashcan School (`ashcan-school`, era c. 1900-1913, four
      artists: Robert Henri d.1929, George Bellows d.1925, John Sloan d.1951, William Glackens
      d.1938), cleared against PUBLIC-DOMAIN-POLICY.md §1.3 with wide margin (newest death 1951,
      all four example works 1902-1922). Mirror the new movement into kind_robots
      stores/seeds/academyStyles.ts exactly as t-015 (Neoclassicism), t-018 (Bauhaus), and t-031
      (Suprematism) did: one entry (slug, name, era, artists, recognitionCues, remix template
      from the outline's remix_hint), inserted in chronological position per the `era` field --
      Ashcan School (c. 1900-1913) sits before De Stijl/Bauhaus/Suprematism in true
      chronological order even though it is appended last in the outline's prose. Content
      sourced verbatim from the outline -- no new facts invented. Small, additive, one-file
      change; verify prettier + vue-tsc clean before opening the kind_robots PR. Independently
      landable now (does not depend on the preview-image work in t-019; an Ashcan School
      preview-thumbnail prompt was queued separately in art-prompts.yaml this same cycle).
      Once the seed entry lands, a follow-up pass can add the four VERIFIED example-works
      entries (docs/curriculum-outline.md §23) to examples.manifest.json, mirroring t-013.


      DONE 2026-07-18T22:19Z (conductor agent run, claude-conductor-agentrun-20260718T2300Z):
      kind_robots PR #464 merged (squash 9d9ed8ef). Added the ashcan-school entry to
      stores/seeds/academyStyles.ts (23rd curriculum movement), inserted chronologically
      (sortYear 1900, between art-nouveau and expressionism) per the array's runtime sort.
      Content sourced verbatim from curriculum-outline.md §23 and teaching-notes.md's
      failure-mode column (no new facts invented). Verified: prettier clean, eslint clean,
      full-project vue-tsc --noEmit exit 0, all 3 kind_robots CI checks green (TypeScript,
      Contract verifiers, GitGuardian). Example works left for a follow-up task per t-013
      precedent.
    owner: null
    claimed_by: claude-conductor-agentrun-20260718T2300Z
    claimed_at: '2026-07-18T22:11:27Z'

  - id: t-035
    milestone: m6
    title: 'kind_robots: batch-generate the 25 queued Academy style-preview thumbnails once the home relay recovers'
    status: ready
    passes: 0
    stakes: reversible
    updated: '2026-07-21T04:25:04Z'
    note: |-
      Kaizen from the 2026-07-20 t-010 cycle. All 25 `kind-robots-academy-style-preview-*` requests in `projects/art-prompts.yaml` are queued and `status: pending`, but the home relay is not claiming jobs -- confirmed live this cycle, not just inferred from the target directory's absence: `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live --limit 2` got job 816 accepted by `POST /api/art/queue` (200 OK) but it sat `status: PENDING`, `claimedAt`/`claimedBy` both null, for 10+ minutes of direct polling. Cheapest recheck: `GET /api/art/queue/816` first (no new job needed) -- once it shows a non-PENDING status, the relay is back. When it is: run `python scripts/consume_art_requests.py --id-prefix "kind-robots-academy-style-preview-" --live` (no --limit) to drain all 25 in one pass -- the `--id-prefix` filter added this cycle keeps it from also draining the ~130 other unrelated pending requests sharing that same queue.
      SPLIT 2026-07-20T04:35Z (claude-conductor-scheduled-20260720T0410Z, t-010 cycle): this task originally bundled the thumbnail batch-generation above with a separate, unblocked half -- syncing the 25th movement, Persian Miniature Painting, into stores/seeds/academyStyles.ts. Split them out since the sync half didn't need the relay at all. DONE (sync half): kind_robots PR #616 merged (squash 99ba5ff3). Added the `persian-miniature` entry (era c. 1400-1600, sortYear 1400, artists Kamal ud-Din Bihzad and Sultan Muhammad), inserted between `renaissance` and `northern-renaissance` per its era. Content sourced verbatim from curriculum-outline.md §25 -- no new facts invented. exampleWorks/previewImageSrc deliberately left for a follow-up (same pattern as ashcan-school/t-031), since real image files still need sourcing/verification, not just data entry. Verified: eslint clean, full-project `npm run test` (vue-tsc --noEmit) exit 0 both before and after rebasing onto latest main, prettier unchanged. All 3 kind_robots PR checks green (TypeScript, Contract verifiers, GitGuardian) before merge. This task (t-035) now covers only the still-blocked thumbnail-generation half; re-ran the relay recheck this same cycle (queued job 855 for greek-vase-painting via consume_art_requests.py --live) and it again sat PENDING/unclaimed after 10+ minutes -- home relay still down, no change from the 2026-07-20T01:10Z reading. Stays `ready`-but-blocked (transient, no pass burned) for a session with the home relay reachable, or Silas's own environment. RECHECKED 2026-07-21 (~04:20 UTC, conductor scheduled agent run): correction to this task's own cheap-recheck heuristic -- 'GET /api/art/queue/816 first, once it shows a non-PENDING status the relay is back' is NOT reliable evidence of current availability. Checked it this cycle and it now shows status: DONE, but updatedAt is 2026-07-20T12:52:34Z -- a one-off completion many hours in the past, not proof the relay is online right now. Verified the relay is in fact still down for new work: queued a fresh serendipity dashboard-tab art request (kind_robots-bound, conductor/serendipity t-012) as job 1187 and it FAILED (connection reset by peer, never claimed); spot-checked five more recent job ids (1180-1186, created within the prior ~40 minutes) and all sat PENDING/unclaimed. Did NOT attempt the 25-thumbnail batch this cycle given this evidence -- would have wasted 25 live generation slots against a relay that isn't accepting work. Next session: recheck relay availability by queuing a FRESH job (or checking the newest few job ids), not by re-checking job 816's now-stale terminal status.
