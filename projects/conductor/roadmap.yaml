project: conductor
kind: software
notes_from_silas: "Improvements to the conductor / AI_Networker system itself — CI, scripts,\nops\
  \ tooling, recurring security audits, and agent access boundaries. Keep\nchanges small and reversible.\
  \ Nothing that touches billing, secrets, deploys,\nauth policy, production data, or write permissions\
  \ without needs-human.\n"
milestones:
- id: m1
  title: CI + branch protection gates
  weight: 20
  status: not-started
- id: m2
  title: Ops tooling (triage, cost guards, monitoring)
  weight: 30
  status: not-started
- id: m3
  title: Per-project kaizen prompts + honey-do lists for Silas
  weight: 50
  status: not-started
- id: m4
  title: Security auditor + privacy boundary manager
  weight: 80
  status: not-started
tasks:
- id: t-001
  milestone: m1
  title: Add GitHub Actions CI lint/build gate for worker/* PRs
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Created .github/workflows/ci.yml with three jobs: validate-yaml (all roadmap.yaml + project-overrides.yaml),
    lint-python (py_compile check for scripts/*.py), build-ts-projects (npm ci + build + lint for any
    TypeScript subproject touched in the PR). Skips STATUS.md and workspace.html paths. Branch protection
    update is a human action (requires GitHub admin settings).

    '
- id: t-002
  milestone: m2
  title: Build PR triage board script
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Created scripts/build_pr_triage.py. Reads project-overrides.yaml for active slugs, loads all
    roadmaps, calls GitHub PR API (GITHUB_TOKEN env var) for open worker/* PRs, and writes PR-TRIAGE.md
    with three sections: open PRs table (mergeability, gate status, next action), in-flight tasks roadmap
    snapshot, and pending human approval gates. Degrades gracefully when GITHUB_TOKEN is absent. Run:
    python scripts/build_pr_triage.py

    '
- id: t-003
  milestone: m3
  title: "Build scripts/build_kaizen.py — per-project kaizen prompt + honey-do list"
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  depends_on: t-002
  note: "Build scripts/build_kaizen.py. For each active project it emits:\n1. KAIZEN PROMPT — the\
    \ single most impactful action Silas can take right now, one sentence framed as a direct instruction\
    \ (e.g. \"Approve the stack spec in humboldt-scoop-cms/t-001\"). Priority order: needs-human tasks\
    \ first, then gate_human gates with approved_by_human: false, then passes==3 blocked tasks, then stale\
    \ claimed tasks with no recent commit.\n2. HONEY-DO LIST — every other action only Silas can\
    \ unblock, as a markdown checkbox list ordered by downstream impact. Each item names the task id and\
    \ the exact decision or file to touch. Agent-workable items are excluded entirely.\nOutput: KAIZEN.md\
    \ at the repo root, regenerated each cycle alongside PR-TRIAGE.md. The approval-portal (when built)\
    \ reads the same data to render interactive checkboxes on each project card.\n"
- id: t-004
  milestone: m4
  title: Write SECURITY-MANAGER.md threat model and audit runbook
  status: done
  owner: worker
  updated: '2026-07-01T00:00:00Z'
  passes: 0
  stakes: needs-human
  gate_human: true
  approved_by_human: true
  note: 'FOR SILAS: SECURITY-MANAGER.md is at the repo root. It defines the security manager role, threat
    model covering Dreams/projects/private files/family-only access/agent write boundaries, a severity
    ladder, and safe automation rules for agents (Vercel, GitHub Actions, secrets, production data, deployment
    gates). No live auth, secrets, or deploys were touched. TO APPROVE: Read SECURITY-MANAGER.md. Set
    approved_by_human: true and status: done in this task. Unblocks: conductor/t-005 (authz regression
    tests).

    '
- id: t-005
  milestone: m4
  title: Add automated authz regression tests for Dreams and projects
  status: done
  owner: worker
  updated: '2026-07-02T15:12:48-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-004
  note: 'Added fixture-only authz regression checks in scripts/authz_regression.py and tests/test_authz_regression.py.
    Coverage includes anonymous, non-owner, non-family, normal-user, family-member, agent, and admin access
    for private Dreams, project files, project metadata, generated assets, family-only resources, prompt
    instruction influence, write/delete boundaries, and claimed-user-id API bypass attempts. Merged in PR #89.

    '
- id: t-006
  milestone: m4
  title: Add GitHub Actions security audit workflow
  status: done
  owner: worker
  updated: '2026-07-02T17:22:00-07:00'
  passes: 0
  stakes: needs-human
  gate_human: true
  depends_on:
  - t-004
  - t-005
  note: 'Added .github/workflows/security-audit.yml and merged in PR #95. It runs on PRs
    to main, manual dispatch, and a weekly cron. Checks include dependency audits where
    lockfiles exist, roadmap YAML validation, Python syntax checks, fixture authz regression
    tests, a simple secret-pattern smoke check, and safe no-token smoke runs for conductor
    scripts. It does not use secrets, call live authenticated endpoints, probe external
    targets, change Vercel, deploy, publish, or touch production data.

    '
- id: t-007
  milestone: m4
  title: Build security report artifact and maintenance queue
  status: done
  owner: worker
  updated: '2026-07-03T03:11:45-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-006
  note: >
    Completed in PR #109. Added scripts/build_security_report.py and SECURITY-REPORT.md.
    The report builder writes a safe summary with audit status, authz fixture findings,
    configuration-marker warnings, open security PRs, and a prioritized maintenance queue.
    It avoids including credential values, private file contents, personal data, or live endpoint probes.

- id: t-008
  milestone: m2
  title: Cross-repo image distribution pipeline (distribute_images.py)
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Added scripts/distribute_images.py (three-tier routing from projects/process/), scripts/setup_hooks.py
    + scripts/hooks/pre-commit (git hook installer), and build_workspace.py updates (inspirations: section
    preservation, KIND_ROBOTS_ROOT awareness). Distributed 10 conductor project assets (engagement, global-ui,
    wishmaster, media-watchlist icon/card/hero). Merged via PR #52 (branch claude/cross-repo-image-organization-flm391).
    Protocol violations (no task claim, wrong branch prefix, Claude acting as Worker) documented in TALKBACK.md
    and projects/conductor/TALKBACK.md.

    '
- id: t-009
  milestone: m2
  title: "Build scripts/sync_projects_to_dreams.py — slug-parity sync bridge"
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: "Build scripts/sync_projects_to_dreams.py. For each active conductor project (from project-overrides.yaml\
    \ where status == active), call the kind_robots API to upsert a Dream with dreamType PROJECT and matching\
    \ slug. Sync fields: title (from roadmap project:), description (from notes_from_silas first paragraph),\
    \ projectStatus (map conductor active→\
    ARCHIVED). Use PATCH /api/dreams/<id> if a Dream with that slug already exists, POST /api/dreams otherwise.\
    \ Requires KR_API_TOKEN env var. Log each upsert (created/updated/skipped) to stdout. Never delete\
    \ a Dream; only create or update. This is the canonical conductor → kind_robots bridge; run it\
    \ at the end of each Worker cycle after task work is complete.\n"
- id: t-011
  milestone: m2
  title: Add Claude session startup sweep hook
  status: done
  owner: worker
  updated: '2026-07-01T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Added .claude/hooks/session-start.sh and .claude/settings.json. Hook fires only in remote Claude
    Code sessions (CLAUDE_CODE_REMOTE guard), collects git state, roadmap ready/needs-human/claimed tasks,
    and TALKBACK tail. Merged via PR #73 (branch claude/startup-sweep-test-bh0w3s). Automates the session
    startup steps defined in CLAUDE.md. Gap: no open-PR check (requires GitHub API credentials from within
    the hook) — tracked as t-012.

    '
- id: t-012
  milestone: m2
  title: Add open-PR check to session startup sweep hook
  status: done
  owner: worker
  updated: '2026-07-01T00:00:00Z'
  passes: 0
  stakes: reversible
  depends_on: t-011
  note: 'The session-start.sh sweep (t-011) collects git state, roadmap tasks, and TALKBACK tail but skips
    step 3 from CLAUDE.md (check for open PRs). Add a curl call to the GitHub API using GITHUB_TOKEN env
    var to list open worker/* and claude/* PRs and include them in the sweep output. Degrade gracefully
    if GITHUB_TOKEN is absent. Output format should match the existing sections in the hook.

    '
- id: t-013
  milestone: m2
  title: "Add GitHub Actions workflow to auto-run sync_projects_to_dreams.py on push to main"
  status: done
  owner: worker
  updated: '2026-07-02T16:30:00-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-009
  note: >
    Added .github/workflows/project-dream-sync.yml and merged in PR #94. It runs on pushes
    to main and manual dispatch; installs PyYAML; runs scripts/sync_projects_to_dreams.py
    with KR_API_TOKEN from repository secrets; and writes the script output to the GitHub
    Actions step summary. The script already skips cleanly when KR_API_TOKEN is absent.

- id: t-010
  milestone: m2
  title: "Write scripts/generate_changelog.py — per-cycle change summary"
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Currently the only change record is STATUS.md (auto-generated, broad) and individual task notes.
    Add scripts/generate_changelog.py: reads git log since the last CHANGELOG.md entry, groups commits
    by project slug, and appends a dated section to CHANGELOG.md at the repo root. Format: date header,
    then one bullet per project that changed listing task id + commit title. Runs at the end of each Worker
    cycle alongside sync_projects_to_dreams.py.

    '
- id: t-011
  milestone: m2
  title: Add .claude/hooks/session-start.sh SessionStart hook to automate the conductor sweep
  status: done
  owner: silasfelinus
  updated: '2026-07-01T07:26:15Z'
  passes: 0
  stakes: reversible
  note: 'Retroactive task. PR #73 (branch claude/startup-sweep-test-bh0w3s, Claude-authored commit)
    added .claude/hooks/session-start.sh + .claude/settings.json: a SessionStart hook that runs
    only when CLAUDE_CODE_REMOTE=true, gathers git branch/status/log, scans all roadmap.yaml
    files for ready/needs-human/claimed tasks, and tails TALKBACK.md, then injects it as a JSON
    message. Silas opened and merged this himself (not via worker/* flow) before the Reviewer
    session reached it, so no Reviewer merge action was taken or needed. Same role-boundary
    pattern as PR #52 (t-008): a claude/* branch bypassing the Worker/Reviewer roadmap loop —
    but this time merged by the human owner directly, not by the Reviewer, so it is not a
    Reviewer-side violation. Noted in projects/conductor/TALKBACK.md. This hook duplicates the
    manual "Session startup" steps already added to CLAUDE.md directly on main (commit b0f664e)
    a few minutes earlier in a separate session — worth Silas deciding whether to trim the
    CLAUDE.md manual instructions now that the hook automates the same sweep.

    '

- id: t-014
  milestone: m2
  title: "Run authz regression tests in Worker PR CI (ci.yml), not only Security Audit"
  status: done
  owner: worker
  updated: '2026-07-02T23:17:30-07:00'
  passes: 0
  stakes: reversible
  note: >
    Completed in PR #108. Worker PR CI now includes an authz-regression job that installs pytest
    and runs tests/test_authz_regression.py, matching the fixture command from security-audit.yml.

- id: t-015
  milestone: m2
  title: "Document a cross-repo task mode in AGENTS.md for connector branch-creation blocks"
  status: done
  owner: worker
  updated: '2026-07-03T17:11:19-07:00'
  passes: 0
  stakes: reversible
  note: >
    Completed in PR #141. Added a Cross-repo tasks section to AGENTS.md documenting
    target-repo branch/PR handling and the conductor-doc fallback when connector/tooling
    blocks target-repo branch creation or writes.

- id: t-016
  milestone: m2
  title: Add a targeted single-field roadmap.yaml updater so cross-repo fallback tasks can flip task status without a full-file rewrite
  status: done
  owner: worker
  updated: '2026-07-04T00:11:14-07:00'
  passes: 1
  stakes: reversible
  note: 'Completed in PR #148 (script) and PR #149 (tests + multiline-safety fixes). scripts/set_task_field.py updates one scalar field on one task with a line-oriented edit, preserving surrounding YAML; when PyYAML is available it re-parses the result and refuses invalid writes. Verified by tests/test_set_task_field.py, run in Worker PR CI. This note was itself written by the script as a live multiline-note replacement test.'

- id: t-017
  milestone: m2
  title: Fix failing test_queue_missing_project_art ordering/default-size assertion
  status: done
  owner: worker
  updated: '2026-07-04T01:18:00-07:00'
  passes: 0
  stakes: reversible
  note: >
    Completed in PR #150. The queue script behavior was kept project-first/variant-order;
    the brittle test now looks entries up by (project, variant) before asserting default
    sizes, and Worker PR CI now includes tests/test_queue_missing_project_art.py.

- id: t-018
  milestone: m2
  title: Wire scripts/set_task_field.py into the Worker cycle for claim and status roadmap updates
  status: done
  owner: null
  updated: '2026-07-04T10:26:14Z'
  passes: 1
  stakes: reversible
  note: 'Reviewer audit (pass 2, PR #153): confirmed. write_roadmap() removed from run_worker.py; claim_task() and set_task_status() now shell into scripts/worker_task_status.py, exactly the remaining work called out in the prior audit note. Verified: 21 tests pass (test_run_worker_status_integration.py + test_worker_task_status.py + test_set_task_field.py), py_compile clean, and a live --dry-run of set_task_field.py against this roadmap confirms only this task''s status line changes. Merged to main as PR #153.'

- id: t-019
  milestone: m2
  title: Add a --dry-run mode to worker_task_status.py that prints the fields it would change without writing
  status: done
  owner: worker
  updated: '2026-07-04T11:45:00Z'
  passes: 0
  stakes: reversible
  note: 'Completed in PR #155 (self-merged by Worker). Reviewer audit: ran the full suite locally (pytest tests/test_worker_task_status.py tests/test_set_task_field.py tests/test_run_worker_status_integration.py -- 23 passed, including the two new dry-run tests), py_compile clean, and a live --dry-run against a real task confirmed no roadmap write occurs. Diff is scoped to worker_task_status.py + its test file only. Confirmed done.'

- id: t-020
  milestone: m2
  title: Add a CI smoke job that runs worker_task_status.py --dry-run against a scratch roadmap fixture so a regression in the dry-run/no-write guarantee fails CI, not just local pytest
  status: done
  owner: worker
  updated: '2026-07-04T12:30:00Z'
  passes: 0
  stakes: reversible
  note: 'Completed in PR #157 (self-merged by Worker; no open worker/* PR remained at Reviewer session start, so this is a post-merge audit). Reviewer audit: read the diff (adds a worker-status-dry-run-smoke job to .github/workflows/ci.yml), confirmed it is additive-only alongside the existing jobs, and reproduced the job body locally -- wrote the same scratch projects/ci-dry-run-smoke/roadmap.yaml fixture, ran worker_task_status.py --dry-run claim then --dry-run done --note against it, hashed before/after, confirmed byte-identical. Also confirmed --dry-run is a top-level flag on the argparse parser so the job''s command-line ordering (--dry-run before the subcommand) is valid, python -m py_compile on worker_task_status.py is clean, and .github/workflows/ci.yml parses as valid YAML. Task itself was left at status: ready with owner: null after the merge -- Worker did not flip its own roadmap status in this cycle. Confirmed done.'

- id: t-021
  milestone: m2
  title: Add a scripts/worker_merge_pr.py helper that merges the Worker's own PR and immediately calls worker_task_status.py done in one atomic step, so a self-merge can never land without its roadmap status flip
  status: done
  owner: worker
  updated: '2026-07-04T06:19:00-07:00'
  passes: 0
  stakes: reversible
  note: 'Completed in PR #159. Added scripts/worker_merge_pr.py plus tests/test_worker_merge_pr.py. The helper squash-merges a Worker PR, pulls main, calls scripts/worker_task_status.py done, commits the roadmap status update, and pushes main as one CLI path so the merge/status flip are paired. Reviewer audit: py_compile clean, full pytest suite (44 tests) green including the 2 new tests. Confirmed done.'

- id: t-022
  milestone: m2
  title: Make scripts/worker_merge_pr.py recover from an already-merged PR by treating GitHub's "already merged" response as success and still calling worker_task_status.py done, instead of erroring before the status flip
  status: done
  owner: worker
  updated: '2026-07-04T17:30:00Z'
  passes: 0
  stakes: reversible
  note: 'Completed in Silas-directed session (worker pass). merge_pr now carries the HTTP status on WorkerMergeError; a 405 from the merge endpoint triggers a GET verification — if the PR is already merged, the script continues to mark_task_done instead of erroring, closing the merged-but-not-marked-done gap. Distinguishes 405 already-merged from 405 not-mergeable (the latter still errors). Covered by two new tests in tests/test_worker_merge_pr.py; full suite 46 passed.'
- id: t-023
  milestone: m2
  title: worker_merge_pr.py falls back to the session branch when pushing the done-status flip to main is rejected
  status: ready
  owner: null
  updated: '2026-07-04T19:05:00Z'
  passes: 0
  stakes: reversible
  note: 'Kaizen from t-022. commit_done_status() pushes the roadmap flip straight to origin/main;
    permission-restricted sessions (git proxy allows only the designated claude/* branch) get a 403
    after a successful merge, leaving the merged-but-not-marked-done gap that t-022 recovery then has
    to close on rerun. Fix: on a rejected main push, commit the flip on the current branch and print
    a clear instruction that it lands via the session PR instead.'
- id: t-024
  milestone: m1
  title: Fix pre-existing failures in tests/test_run_worker_status_integration.py
  status: done
  owner: claude
  updated: '2026-07-08T00:00:00Z'
  passes: 1
  stakes: reversible
  note: >
    DONE (Claude, 2026-07-08): Diagnosis — stale tests, not a regression.
    conductor #254 rewrote run_worker.py from a task-claiming worker into a
    read-only healthcheck; claim_task/set_task_status/_run_worker_task_status
    were removed but the tests still scraped for them, so they raised
    ValueError('substring not found') on clean main. Rewrote the file to pin
    the CURRENT contract: no task-status mutation surface, never serializes
    YAML back out, only write_text is the transient digest, plus a
    py_compile smoke test. Full suite now green (72 passed). Bonus: found and
    fixed a second stale test of the same kind — test_distribute_images.py
    test_write_gallery_manifest_nested still asserted stem-only manifest
    entries after conductor #260 deliberately switched to full filenames.
- id: t-025
  milestone: m2
  title: Build a new-project scaffold helper script
  status: ready
  owner: null
  updated: '2026-07-06T00:00:00Z'
  passes: 0
  stakes: reversible
  note: >
    Kaizen from Worker's mural-design/t-001 PR (#249, reviewed 2026-07-06). Worker
    noted that scaffolding a new Dream project currently means separately touching
    projects/priority.yaml, project-overrides.yaml, projects/<slug>/roadmap.yaml,
    projects/<slug>/DESIGN-BRIEF.md, and ART-PROMPTS.md — easy to miss a surface (this
    PR skipped a CONTROL.md block and the art-prompt entries). Build a script that
    takes a slug/title/goal and generates all of the above in one pass so future
    project scaffolds don't drop a surface.
- id: t-026
  milestone: m2
  title: Reviewer trigger fires with no open worker/* PR to review, repeatedly
  status: ready
  owner: null
  updated: '2026-07-07T05:00:00Z'
  passes: 0
  stakes: reversible
  note: >
    Pattern across at least 4 Reviewer sessions now (root TALKBACK.md 2026-07-05
    evening through 2026-07-07): the Reviewer is triggered expecting a worker/*
    PR to review, but list_pull_requests(state=open) returns zero results in both
    conductor and kind_robots, and no roadmap task anywhere is at status: review
    or status: claimed. Each session burns a full sweep (git log, PR list, roadmap
    grep, TALKBACK tail) to reach the same "nothing to do" conclusion, already
    flagged twice as a likely scheduler/trigger cadence mismatch with no fix filed.
    Investigate what fires the Reviewer session (cron/webhook cadence vs. actual
    Worker PR volume) and either slow the trigger interval, or make it conditional
    on an open worker/* PR existing before spinning up a full session.
