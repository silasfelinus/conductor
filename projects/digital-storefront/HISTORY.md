# digital-storefront — task history archive

Full `note:` prose for completed digital-storefront tasks, moved out of `roadmap.yaml` so the
Kind Robots projection payload (`scripts/sync_kind_robots_projection.py`, hard limit
4,000,000 bytes) stays well clear of its ceiling. See conductor/t-158.

Each note below is the **verbatim** original — nothing is summarized or trimmed. The
`roadmap.yaml` task keeps its own first sentence plus a pointer here. The HTML comment
markers delimit each note exactly; `archive_done_task_notes.py --verify-only` re-extracts
every note and checks it byte-for-byte against what the roadmap recorded.

## t-003 — Seed the KR-logo/DLC catalog and resolve the storefront's two pricing/POD-entry-point splits

<!-- note:begin t-003 -->
Split from t-038 (slice 1, conductor sweep 2026-08-08) -- the items too large/judgment-heavy for a single-session slice. See STOREFRONT-REALITY-AUDIT-2026-08-08.md's "Real gaps remaining" section for the originating detail. Scope:
1. Seed a real KR-logo Product/POD SKU (currently MISSING -- no artImageId, no catalog row). Needs either a real ArtImage row for the KR logo image (check whether one already exists before generating a new one; project art generation is pre-approved per AGENTS.md's "Generated image approval rule") or a non-art-dependent SKU shape if the POD/checkPrintEligibility path can't reasonably apply to a fixed logo asset -- decide which and document why in the PR.
2. Give DLC a live catalog: an admin route/script that turns a Packmaker Pack into a Product with metadata.packId (webhook fulfillment already handles this correctly -- server/api/stripe/webhook.post.ts's handleProductPurchase creates the Grant; it just has nothing to sell yet), plus wire server/utils/contentAccess.ts's packId check into the actual Dream/Facet/Character/Reward content routes (its own header comment explicitly says this isn't done yet).
3. Decide whether to restore a live entry point for the orphaned server/api/store/pod-checkout.post.ts flow (e.g. an art-detail/gallery "Print this" action pointing at a live, non-abandonware component) or formally retire it in favor of the general-cart featured-art POD path (giftshop-interact.vue's addFeaturedPrint -> shopping-cart.vue -> checkout.post.ts) that already works end-to-end today. If retiring, say so in a code comment near pod-checkout.post.ts so a future reader doesn't assume it's live.
4. Decide whether stores/seeds/cartItems.ts should be retired in favor of the real Product table now that two parallel pricing sources coexist (checkout.post.ts prices off the hardcoded array; product-checkout.post.ts/pod-checkout.post.ts price off Product rows) -- or whether the split is intentional (e.g. cartItems.ts covers generic non-Product-backed items like the flat-rate donation/ tokens/POD swag that don't need a DB row) and should stay, just documented as such.
Do not duplicate products whose production is explicitly owned by coloring-book/packmaker/Mermaids; integrate their finished outputs instead. Items 1-2 touch the database (a Product/Pack seed + an admin write path) -- reversible (Product.active can flip false, Pack->Product linkage can be removed), not destructive, but real state, so verify carefully and describe exactly what was seeded/wired in the PR body.
Slice 1 (conductor sweep, 2026-08-08): closed items 1, 3, and 4. Item 1: no Product/POD SKU shape was invented -- PrintJob.artImageId is a required non-nullable FK and checkPrintEligibility is fundamentally ArtImage-shaped, so a schema-free non-art SKU isn't possible without a migration (out of scope). Instead seeded a real ArtImage row (scripts/seed_kr_logo_art_image.ts, idempotent, upsert-by-fileName) pointing at the existing public/icon-512x512.png logo asset, storefrontFeatured: true, checkpointResourceId: null (checkPrintEligibility's base-model branch, unconditionally eligible) -- this makes the logo genuinely purchasable today via the already-working general-cart path (storefront-featured -> giftshop-interact's Featured prints -> cart -> checkout.post.ts -> webhook -> PrintJob), no new route/component needed. Wired into scripts/vercel-build.mjs's production-deploy maintenance step (alongside seed_achievements.ts/seed_contenders.ts) so it reconciles on every deploy rather than needing a manual --write run -- could not run --write against a real DB from this sandbox (no prod access here, same limitation t-011's seed_products.ts had). Item 3: confirmed pod-checkout.post.ts is genuinely orphaned (its only caller, print-swag.vue, lives under nuxt.config.ts's abandonware/**/*.vue auto-import ignore, so it can never resolve as a component) -- documented it as formally retired in a header comment rather than deleting it, in favor of the general-cart path item 1 now uses too. Item 4: documented cartItems.ts's header explaining its intentional coexistence with per-instance Product rows (generic per-type catalog vs. per-(printType, artImageId) instances the webhook's GIFTSHOP_TYPE_TO_PRODUCT_TYPE map/ handleGiftshopCartPurchase creates lazily at fulfillment) -- not a duplicate catalog, a real design split, now explained instead of silently left ambiguous. kind_robots PR: silasfelinus/kind_robots#1614. Verified: eslint (0 new problems, ratchet unchanged at 392/27), vue-tsc --noEmit clean, prettier clean, seed script dry-run validates.
Item 2 (DLC live catalog + wiring contentAccess.ts's packId check into the Dream/Facet/Character/ Reward routes) is genuinely larger/judgment-heavy -- confirmed via investigation that Pack/packId are already schema-complete (kind-robots/t-037) and the webhook's Grant-creation side already works, but the actual route wiring touches FOUR different, currently-inconsistent ad hoc access-check implementations (dreams/facets/characters/rewards each do it differently), one of which (characters/[id].get.ts) has a pre-existing, unrelated access-control gap (no check at all) that touching this code would surface and need to scope separately. Split into t-004 as this task's remaining scope per AGENTS.md's umbrella-sweep convention.
<!-- note:end t-003 -->

## t-004 — Give DLC a live catalog and wire contentAccess.ts's packId check into the actual content routes

<!-- note:begin t-004 -->
Split from t-003 (slice 1, conductor sweep 2026-08-08, item 2) -- too large/judgment-heavy for that slice.
Dispatched a read-only investigation subagent before writing any code. It confirmed the four ad-hoc access-check implementations (dreams, facets, rewards, characters) and, critically, that characters/[id].get.ts AND rewards/[id].get.ts / rewards/index.get.ts / rewards/random.get.ts all have NO access check whatsoever today -- not a fourth variant pattern, an outright absence. It also found characters/index.get.ts's list branch runs prisma.character.findMany() with no `where` at all, leaking every user's private/inactive characters into the app-wide character-picker store.
Shipped as kind_robots PR #1630 (all verification green: eslint 0 problems, prettier clean, vue-tsc --noEmit clean, lint ratchet 392/27 unchanged; no live DB in this sandbox so not integration-tested):
1. New admin route server/api/admin/packs/[id]/product.post.ts -- turns a Packmaker Pack into a purchasable DLC Product (requireAdminApiUser + readBody, matches the wonderlab convention). Idempotent upsert by Product.slug, sets metadata: JSON.stringify({ packId }) and type: 'DLC' -- exactly what the webhook's existing handleProductPurchase already parses to create a PACK Grant.
2. Wired contentAccess.ts's existsActiveGrant (canView's packId branch) into the three routes where it was a genuinely additive, behavior-preserving extension of an existing check: dreams/[id].get.ts, dreams/[id]/facets.get.ts (both via assertDreamAccess), and facets/[id].get.ts (its local isOwner||isAdmin||(isPublic&&!isMature) boolean, isMature gate preserved unconditionally). Every existing caller's outcome is unchanged; only a new Grant-based fallback is added.
Deliberately NOT bundled into this task, split into two follow-ons instead of silently expanding scope or leaving a security gap unflagged:
- t-005 (new, hard needs-human/security): the pre-existing characters/[id].get.ts, characters/index.get.ts, rewards/[id].get.ts, rewards/index.get.ts, rewards/random.get.ts "no access check at all" gap. Fixing it is this app's FIRST access check on those routes -- a real behavior change for any current caller relying on the open read (e.g. characters/index.get.ts's bulk loader feeding the app-wide character-picker store), not an additive extension like the Dream/Facet routes above. Whether any admin/moderation/packmaker-generator flow currently depends on that open read is a product call only Silas can make before this is safe to fix.
- t-006 (new, ready/software): Dream/Facet LIST routes (dreams/index.get.ts, facets/index.get.ts, facets/catalog.get.ts's loadFacetCatalogEntries) filter via a set-based Prisma `where`-clause with no per-row hook to extend -- needs a Grant-subjectId prefetch step, real design work, not a mechanical canView call. Reversible, no security gap (these routes already correctly exclude non-public/non-owned rows; they just aren't Pack-Grant-aware yet), safe for normal Worker pickup.
Seeding an actual DLC Product row against a real Pack (verifying the full purchase-to-unlock loop end-to-end) still needs a live Packmaker Pack to test against and production DB access neither this nor prior sandboxes had -- unchanged from the original task note.
kind_robots PR #1630 merged (squash 0a69a96).
<!-- note:end t-004 -->

## t-005 — SECURITY: Character and Reward read routes have no access check at all -- decide whether to close before wiring packId Grants

<!-- note:begin t-005 -->
FOR SILAS: split off digital-storefront/t-004 (conductor sweep 2026-08-08) after a read-only investigation of kind_robots' server/api/ confirmed a real, pre-existing security gap unrelated to the DLC/packId work t-004 was scoped for.
What was found: server/api/characters/[id].get.ts and server/api/characters/index.get.ts (when called with no id, it runs prisma.character.findMany() with NO where clause at all) return every Character row -- including other users' private, unpublished ones -- to any caller, authenticated or not. server/api/rewards/[id].get.ts, rewards/index.get.ts, and rewards/random.get.ts have the same gap for Reward rows. None of these routes call any auth/visibility check today; it is not a fourth variant access pattern, it is an outright absence.
Why this needs your call rather than an agent just fixing it: characters/index.get.ts's unfiltered list feeds stores/characterStore.ts's app-wide character-picker (used by the packmaker generator and any character-select UI), and startEditingCharacter/selectCharacter fall back to that in-memory list before ever hitting [id].get.ts. Fixing [id].get.ts alone without also fixing the list route would be inconsistent (list still leaks the row; direct fetch would then 403/404 on reload). Fixing both closes the gap but is a genuine behavior change for any current caller that happens to rely on the open read -- no admin/moderation tool doing so was found in-repo, but that is an absence of evidence, not a guarantee, and it is exactly the kind of live-data-exposure call this repo's own hard-gate rule ("Security flags requiring acknowledgement") reserves for you.
TO APPROVE: read this note, then either (a) confirm no live flow depends on the open read and set approved_by_human: true / status: ready so a Worker can add the isOwner||isAdmin||isPublic check (and packId Grant fallback, completing the packId wiring t-004 started) to all five routes together, or (b) flag a flow that does depend on it so the fix can special-case it, or (c) say this is lower-priority than it looks and downgrade stakes/gate as you see fit. What unblocks: closing this unblocks completing the Dream/Facet/Character/Reward packId-Grant wiring t-004 began, and removes a live private-data-exposure gap independent of DLC/Pack work entirely.
UPDATE (same conductor sweep, after merging t-004): found a stranded kind_robots branch, worker/digital-storefront-t-004-20260808-a83f (10 commits, last pushed 2026-08-08T08:38:19-07:00, tip 5c20bcb), from the same session whose stale roadmap claim on this task was reclaimed to do the t-004 work above. It went silent -- no PR, no TALKBACK entry -- but its commits already implement almost exactly what this task asks Silas to decide on: canView() wired into characters/[id].get.ts, characters/index.get.ts, rewards/[id].get.ts, and rewards/index.get.ts (same isOwner/isAdmin/isPublic-then-Grant shape as this session's own Dream/Facet fix, correct auth?403:404 semantics), PLUS a new contentAccess.ts export `viewablePackIds(userId)` that prefetches a user's active PACK Grant subjectIds for exactly the list-route filtering t-043 is scoped for -- it also rewrote dreams/index.get.ts and facets/index.get.ts's list `where` clauses using it. Spot-read the diffs: well-structured, consistent with this repo's existing patterns, no obvious correctness issues. NOT rescued/merged in this session for two reasons: (1) it's ~9 hours stale against current main (its merge-base predates several since-merged PRs, including this session's own t-004 changes to the same Dream/Facet files -- would need real conflict resolution, not a clean apply) and (2) most importantly, merging it would make exactly the access-policy decision this task exists to get from Silas, unilaterally, on his behalf, which defeats the point of the hard gate regardless of code quality. Recorded here so approving this task doesn't mean starting from scratch: once Silas answers (a)/(b)/(c) above, whoever picks this up should treat that branch as a reference implementation to rebase, not a blind merge, and should also fold t-043's list-route scope into the same pass since this branch already solved both together. Branch left in place (real, substantial, reviewable work, not scratch) rather than deleted.

RECONCILED 2026-08-11 (conductor scheduled agent sweep, state reconciliation): Silas answered option (a) himself -- kind_robots PR #1668 ("security: gate Character and Reward reads by visibility", canView()/viewablePackIds() wired into characters/[id].get.ts, characters/index.get.ts, rewards/[id].get.ts, rewards/index.get.ts) was merged_by silasfelinus directly at 2026-08-11T21:06:24Z. Per docs/state-reconciliation.md's closing-human-gates-safely list, "Silas directly merged the gated implementation PR" is objective evidence the requested outcome happened -- approved_by_human: true.
Residual gap found on reconciliation: PR #1668 covered 4 of the 5 originally-flagged routes. server/api/rewards/random.get.ts was not touched and still ran prisma.reward.count()/findFirst() with no where clause at all -- the identical unfiltered-access gap, just on the random-pick route. Since the access-policy decision is already made (evidenced by the merge), finishing this one route with the exact same isActive/isPublic/own/admin/Pack-Grant where clause rewards/index.get.ts already uses is ordinary reversible software work, not a new security decision. Fixed in kind_robots PR #1787 (bundled with an unrelated model-builder/t-029 fix on the same session branch).
DONE 2026-08-11: PR #1787 merged (squash ed49516), all 21 checks green. All five originally-flagged Character/Reward read routes (characters/[id], characters/index, rewards/[id], rewards/index via #1668; rewards/random via #1787) now gate on canView()/viewablePackIds(). Gap fully closed.
<!-- note:end t-005 -->

## t-008 — Audit the existing kind_robots giftshop/Stripe surface and write STORE-AUDIT.md

<!-- note:begin t-008 -->
DONE 2026-07-15 (claude-burst-2026-07-15-0105): wrote projects/digital-storefront/STORE-AUDIT.md. Findings: Stripe checkout/subscribe routes work end to end up to the Stripe redirect but no webhook exists anywhere, so nothing downstream of a successful payment happens (no order record, no mana credit, no isMember flip). The Stripe checkout route prices against stores/seeds/cartItems.ts (7 items) as the real trusted catalog, but giftshop-interact.vue shows a different, unrelated 2-item demo array -- UI and real catalog are out of sync. Roughly half the giftshop components are wired to real state (shopping-cart.vue, mana-wallet.vue, social-publisher.vue, subscription-manager.vue's start path) and half are alert()/console.log() stubs that look finished but do nothing (cart-interact.vue, credit-purchase.vue, print-swag.vue, subscription-manager.vue's cancel path). No Order/Purchase/Cart/Subscription/Product Prisma models exist; commerce state lives only on User.stripeCustomerId. No POD vendor integration exists (0% wired). Neither Stripe route has an auth guard (contrast mana/social routes, which do) -- low severity today since nothing downstream trusts the session, but must be closed before a webhook starts crediting mana/membership off userId. Recommended next task: a Stripe webhook handler is the single highest-leverage next step -- every v1 catalog item (PDF delivery, subscription isMember flip, mana top-ups) needs it and none have it. No code changes made; audit only. ADDENDUM 2026-07-26 (t-027): the social-publisher.vue row above described this audit's original 2026-07-15 findings. It was removed from kind_robots three days later (migration 20260718200000_remove_social_publishing) and no longer exists — see STORE-AUDIT.md §2/§6 for the corrected, dated write-up. No other finding in this note is affected.
<!-- note:end t-008 -->

## t-009 — Write the digital giftshop v1 design brief (SPEC.md)

<!-- note:begin t-009 -->
DONE 2026-07-15 (claude-burst-2026-07-15-0105, same session as t-008 which unblocked this): wrote projects/digital-storefront/SPEC.md. Design covers Stripe architecture (keep Checkout Sessions, add the missing webhook handler with raw-body signature verification and idempotency via stored event/session ids), new Product/Order/OrderItem/Entitlement Prisma models replacing the imported stores/seeds/cartItems.ts as the trusted price source, secure PDF/DLC delivery via an Entitlement-gated download route (file stays outside public/ and outside the repo, per Silas's hard rule), mana crediting (first writers for the already-defined but unused PURCHASE/SUBSCRIPTION_GRANT ManaReason values), subscription handling (webhook-driven isMember flip + a real soft-cancel route replacing cartStore.cancelSubscription's stub), and admin vs user views. Flagged as a soft needs-human per the 2026-07-04 rule -- this is a proposal to refine, not an approved spec; Silas's hard rules (no live Stripe products/webhooks in production, no POD account creation, no publishing, no spend without approval) are treated as non-negotiable regardless. Suggested a 6-step independently-landable build order ending with the Mermaids of Venice PDF go-live after step 3. Did not claim this via claim_task.py's live origin/main check -- it was status:waiting on origin/main until this same session's t-008 completion unblocked it (via resolve_deps.py), so a live check would have failed until t-008's PR merges; doing both in one session/PR was faster than a blocked round-trip and collision risk is effectively zero since nothing could see this as ready before now.
<!-- note:end t-009 -->

## t-010 — Novel PDF intake: Silas supplies the file and sets the price

<!-- note:begin t-010 -->
CLEARED by Silas in session 2026-07-05. File: projects/mermaids-of-venice/ manuscript/"mermaids of venice second edition.pdf". PRICE: $9.99. IMPORTANT correction to earlier assumption: this is the SECOND EDITION file (not a "third printing") — sale copy must label it second edition. Silas's edition-3 revision is in progress ("Mermaids of Venice Revision.doc", same folder) and is NOT for sale and NOT to be edited by agents. The PDF is served from authenticated server-side storage only, never public/. Unblocks t-011 (purchase flow, Stripe TEST MODE only — going live to real buyers remains a separate hard needs-human).
<!-- note:end t-010 -->

## t-011 — kind_robots: Product/Order/OrderItem/Entitlement schema + seed (Mermaids PDF product) — step 1 of the original Mermaids purchase-flow task

<!-- note:begin t-011 -->
SPLIT 2026-07-17 (autonomous hourly conductor cycle): the original t-011 ("Build the Mermaids of Venice PDF product page and purchase flow") bundled SPEC.md's build-order steps 1-3 (schema migration, webhook, secure download route + product page) into one task carrying gate_human:true/ stakes:outward-facing for the whole customer-facing purchase capability. That's too large for one pass and conflates a pure internal schema migration (zero live behavior, nothing customer-facing) with the actual gated feature. Split before attempting the monolith — no pass burned, no failed attempt: t-011 now covers ONLY step 1 (schema + seed), which has zero user-facing surface and is genuinely `stakes: reversible` on its own merits (additive-only migration: 4 CREATE TABLE + FK constraints, no drops; new tables are inert until the follow-on tasks wire code to them). DONE via kind_robots PR #351: added Product/Order/OrderItem/Entitlement models + ProductType/OrderStatus enums to prisma/schema.prisma (Int autoincrement ids matching house style, not SPEC.md's cuid() draft; metadata as String?/@db.LongText per the repo's no-native-Json-column convention), a purely-additive migration generated offline via `prisma migrate diff --from-schema/--to-schema` (no live DB needed), and scripts/seed_products.ts (idempotent, upsert-by-slug, seeds only mermaids-of-venice-pdf at $9.99 per t-010's cleared price). Also added prisma/migrations/migration_lock.toml, which was missing entirely from the migrations directory (only the 2026-07-15 squashed baseline existed) — scripts/vercel-build.mjs -> scripts/prisma-migrate-deploy.mjs runs real `prisma migrate deploy` on production deploys and needs this file to identify the provider; flagged for Silas in the PR in case it's a known gap. Verified: prisma validate/generate clean, seed dry-run validates, eslint/prettier clean, full-project vue-tsc 0 errors, verifyNoPrismaJsonCast passes. Deliberately did NOT touch checkout.post.ts, add a webhook, or build any UI — zero behavior change to any live route. Remainder split into t-022 (webhook) and t-023 (secure download + product page + purchase flow), both carrying the original gate_human:true/ stakes:outward-facing forward since THEY are the actual customer-facing purchase capability. EGRESS: api.stripe.com reachability (needed by t-022/ t-023, not this step) is tracked in EGRESS-BLOCKERS.md — blocked again as of this same cycle's recheck (varies by session; re-run scripts/recheck_egress_blocks.py before claiming either).
<!-- note:end t-011 -->

## t-012 — Wire one-time mana top-ups to Stripe checkout (test mode)

<!-- note:begin t-012 -->
DONE 2026-07-17 via kind-robots/t-012 (same underlying task, tracked in both roadmaps — see that task's note for the full PR history: kind_robots PR #345 built the Stripe webhook + topup.post.ts + cartStore.topup() flow, PR #347 mounted credit-purchase.vue into giftshop-manager.vue's mana tab and fixed its dead-end success/cancel redirect). Tiers landed: small ($5), medium ($10), large ($25), mana amount via usdToMana's existing $0.001/mana peg — a first-cut pricing pass, not final; Silas can retune server/api/stripe/topup.post.ts's MANA_TOPUP_TIERS anytime, it's the single source of truth. Live Stripe objects remain hard-gated per the standing rule. Original note: Cross-repo (kind_robots). A few reasonable tiers per Silas; reuse credit-purchase.vue / mana-wallet.vue where sensible. Webhook credits mana. Test mode only; live activation is gated.
<!-- note:end t-012 -->

## t-013 — Wire the monthly supporter subscription to Stripe (test mode)

<!-- note:begin t-013 -->
DONE 2026-07-17 (conductor hourly cycle): kind_robots PR #361 merged (98190504). Webhook now activates isMember/memberUntil/stripeSubscriptionId on checkout.session.completed (mode: subscription) and keeps it in sync via customer.subscription.updated/deleted; added a real cancel-subscription endpoint (cartStore.cancelSubscription was a stub); fixed success/cancel redirect URLs (pointed at nonexistent /members/* pages) to /sanctuary?subscription=...; added a notice banner mirroring the mana top-up one. Test mode only; live activation stays gated per CONTROL.md. Reviewed: migration purely additive (nullable ADD COLUMN), no live/hardcoded Stripe keys, cancel-subscription endpoint authenticates via requireApiUser and takes no client-supplied user/subscription id (cannot cancel another user's subscription), webhook signature verification (constructEvent) untouched and still required. All 3 CI checks green.
<!-- note:end t-013 -->

## t-014 — Build the Against Malaria giving page

<!-- note:begin t-014 -->
DONE 2026-07-15 (claude-burst-2026-07-15-hourly): kind_robots PR #271 merged (2ebf8a9). New dedicated /giving route (content/giving.md + components/pages/giving-page.vue) whose primary CTA links directly to https://www.againstmalaria.com/amibot; the existing $1-net cart item remains as a secondary convenience via the existing cartStore action (no new payment code). Added a sidebar/channel nav tab (content/channels/sanctuary/giving.md) and a "Give directly" CTA banner in giftshop-interact.vue. Verified: eslint + prettier clean on all changed source files, vue-tsc shows no new errors, and a local dev-server run (temporary local DB/JWT env stand-ins, since this sandbox has no configured MySQL/JWT secret) confirmed GET /giving returns HTTP 200 with the expected headline and direct AMF link rendered. Did not click-verify the giftshop-interact.vue CTA banner in a live browser (no Playwright/Cypress harness wired up for an authenticated dashboard route in this sandbox) — checked via lint/typecheck only, reusing already-imported NuxtLink/Icon. PR #271's "TypeScript" CI check failed but was confirmed pre-existing and unrelated (identical 82-error set reproduced in a clean worktree at origin/main with no PR changes applied) -- see kind-robots/t-020, whose scope was also refreshed this cycle (grew from 2 to 82 errors since 2026-07-14). Merged with that context documented on the PR. Original spec: Cross-repo (kind_robots). A dedicated giving page whose PRIMARY call to action urges users to donate directly at https://againstmalaria.com/amibot (Silas's AMF fundraiser page) — we should not be the middleman for charity money. The existing $1-net cart item may remain as a convenience, but the page leads with the direct link. Content + layout only; no payment code changes required.
<!-- note:end t-014 -->

## t-015 — Recommend a print-on-demand provider for the KR-logo item

<!-- note:begin t-015 -->
DONE 2026-07-15 (claude-hourly-2026-07-15-0448): wrote projects/digital-storefront/research/pod-provider.md. Recommendation: Printful first (single-supplier fulfillment consistency + more mature webhook-signing docs for a serverless Nuxt/Vercel integration), first orderable item = a die-cut/kiss-cut sticker (lowest base cost and shipping on either platform, single flat print area, no size/color variant matrix, fastest turnaround — smallest financial and integration risk to validate the whole order pipeline before adding a mug or tee SKU). Compared Printful vs Printify on API auth, webhook support, fulfillment model, docs maturity, and per-item base cost for mug/tee/sticker; margins were close enough on all three that supplier integration complexity, not a few dollars of base cost, should decide the first item. No account created, no API keys requested, nothing published — account creation and going live remain needs-human per the task's own constraint and CONTROL.md's standing POD rule. This unblocks t-016 (gallery- to-swag pipeline design) via resolve_deps.py. Original task spec: Compare Printful vs Printify (and any clearly better option) specifically for: API-driven order automation from a Nuxt/Vercel app, per-order economics on a mug/tee/sticker, and putting the Kind Robots logo on whatever item is easiest to make orderable FIRST. Builds on research/stores.md. Output: research/pod-provider.md with a single recommendation. NO account creation — that is a needs-human step for Silas.
<!-- note:end t-015 -->

## t-016 — Design the gallery-to-swag pipeline (user art on POD products)

<!-- note:begin t-016 -->
Design doc for automating swag orders printed with art created on the site (or uploaded), coordinating with the kind_robots art gallery: art selection UX, print-file requirements, POD API order flow, moderation/rights considerations, plus curated "specifically chosen pieces" support. Doc first; implementation tasks get created from it.
UPDATE 2026-07-15 (t-015 landed): design against Printful specifically — research/pod-provider.md recommends Printful (single-supplier fulfillment, more mature webhook-signing docs) with a sticker as the first orderable item; extend the gallery pipeline to that same account/product family rather than re-evaluating providers.
DONE 2026-07-15T07:10Z (reviewer-burst-2026-07-15-0708): wrote docs/gallery-to-swag-pipeline.md. Covers art selection UX (three entry points: own-gallery "print this," curated featured pieces, upload), print-file requirements (Printful's 150/300 DPI floor, RGB, PNG-for-diecut), POD API order flow (new `PrintJob` model bridging `OrderItem` to Printful's Order API, reusing SPEC.md's webhook/signature pattern), moderation/rights (flags that the schema has no field today enforcing CONTROL.md's commercial-generation licensing rule — dev-trained LoRAs/Kontext-dev must never reach a printed product — proposes a `Resource.commercialSafe` field and a default-deny policy until it exists; also reuses the existing `isMature`/ownership checks), and curated pieces (two paths: curated ArtImage rows needing a `storefrontFeatured` field, and non-ArtImage static assets like the actual KR logo, which needs neither schema pitch and can ship first). Suggested build order puts the KR-logo sticker (t-015's real next step) on the unblocked static-asset path, and gates the general self-service "print my gallery art" flow on two new kind-robots pitches (commercialSafe, storefrontFeatured) filed per BOUNDARY.md rather than edited directly. No schema changes, no Printful account, no code shipped — design doc only.
<!-- note:end t-016 -->

## t-017 — Design DLC unlock purchase mechanics (packmaker packs as store items)

<!-- note:begin t-017 -->
RESOLVED 2026-07-18 (session claude-conductor-scheduled-20260718T0411Z): both cross-project note-level blockers this task carried (packmaker/t-003 draft pack contents, kind-robots/t-008 private-but-shared content/ACL design) are now confirmed `status: done` on origin/main, so this design was unblocked and written: `projects/digital-storefront/docs/dlc-unlock-design.md`. It reconciles the two designs that already existed for half of this problem each — digital-storefront SPEC.md's `Entitlement` (proves purchase) and kind-robots SHARING-SPEC.md's `Grant` (proves per-item visibility) — via a new `Pack` model that joins them: one `Product`/ `Entitlement` per purchase as already designed, plus one `Grant` (`subjectType: PACK`) created alongside it in the same webhook fulfillment step, with each pack-generated `Dream`/`Facet`/`Character`/ `Reward` row carrying a nullable `packId` FK that `canView()` checks against an active `Grant` instead of packmaker SPEC.md §4's interim all-or-nothing `isPublic` switch. Design only — no schema, no migration, no code, per `BOUNDARY.md`. Filed two follow-on implementation tasks: digital-storefront/t-026 (webhook DLC fulfillment) and kind-robots/t-037 (the actual `Pack`/`packId`/`GrantSubject.PACK` migration + `canView()` extension). Original task note (now resolved) preserved below.
RESOLVER NOTE 2026-07-17: t-011 (this task's only in-file depends_on) is now `status: done`, so `scripts/resolve_deps.py` and `scripts/audit_roadmaps.py`'s WAITING_WITH_SATISFIED_DEPS check will both flag/promote this to `ready` — that promotion is WRONG. Do not accept it without first checking packmaker/t-003's live status (still `needs-human` as of this note); if still not `done`, revert this back to `waiting` rather than claiming it. Neither script can see cross-project note-level blockers, per CONTROL.md's 2026-07-17 cross-project-collision note. Entitlement design tying purchased DLC packs (packmaker project: Uncanny Valor, Arcane Whimsy) to per-user unlocks of generated content (locations, genres, characters, rewards). ALSO BLOCKED ON (cross-project, note-level dependency — resolver only sees in-project deps): kind-robots t-008 private-but-shared content/ACL design, and packmaker t-003 draft pack contents. This slice is the deliberate low-stakes security proving ground for private-but-shared content.
<!-- note:end t-017 -->

## t-018 — Add digital + POD coloring books from the coloring-book project to the catalog plan

<!-- note:begin t-018 -->
New 2026-07-10 per Silas: whatever coloring pages the coloring-book project creates are storefront inventory, digital (set unlocks / PDF downloads) and physical (print-on-demand paper coloring books). Cross-project blockers noted here previously (coloring-book t-006/t-007/t-009, superseded 2026-07-17 by t-022/t-024/done respectively) are all resolved as of 2026-07-18 — verified coloring-book t-009 done (Lulu POD research) and t-006/t-007 done (superseded by t-022/t-024, current production tasks, not blockers). Deliverable: extend the catalog plan/SPEC with the two coloring book products — pricing approach, digital vs physical variants, and the exact hard-gated go-live steps (POD account, listings, live Stripe products remain needs-human for Silas).
<!-- note:end t-018 -->

## t-019 — Re-verify the api.stripe.com and museum-site sandbox-egress blockers before re-flagging them as blocked again

<!-- note:begin t-019 -->
Kaizen from t-015's close (2026-07-15). t-012, t-013, and ai-art-academy's generation/download tasks have all been logged across multiple sessions as blocked by a 403 policy denial from this sandbox's egress proxy (confirmed via curl to api.stripe.com and metmuseum.org, and /__agentproxy/status). That denial could be sandbox-specific and could change between sessions/environments. The next session that would otherwise skip these tasks on the strength of a prior TALKBACK note should first re-curl the relevant host(s) and only re-log "still blocked" (or start the real build) based on a fresh check, not an inherited assumption. If genuinely still blocked, note the recheck date so this doesn't need repeating every single cycle.
DONE 2026-07-15T07:08Z (reviewer-burst-2026-07-15-0708): re-curled api.stripe.com, www.metmuseum.org, and upload.wikimedia.org directly and checked /__agentproxy/status. All three still show a fresh `connect_rejected` / "gateway answered 403 to CONNECT (policy denial or upstream failure)" entry timestamped this session — the block is still live, not stale info. Stamped a RECHECKED note with this timestamp on digital-storefront t-011/t-012/t-013 and ai-art-academy t-004/t-008/t-013 so the next few cycles can skip the re-curl and just check whether the recheck timestamp is recent, rather than re-verifying from scratch every single burst cycle.
<!-- note:end t-019 -->

## t-020 — kind_robots pitch: add Resource.commercialSafe (or licenseClass) so gallery-to-swag can gate on generation-backend licensing

<!-- note:begin t-020 -->
FOR SILAS: Pitch written at pitches/2026-07-15-resource-commercial-safe-field.md. It proposes an additive `Resource.commercialSafe` (or `licenseClass` enum) Prisma column in kind_robots, defaulted unsafe/unknown, seeded true for FLUX.1 schnell / OpenAI / approved licensed-API rows — the join point digital-storefront's gallery-to-swag print flow needs to stop default-denying almost all gallery art.
TO APPROVE: read the pitch, set its `status: approved` and this task's `approved_by_human: true` + `status: done` if you want it built (or leave a note here with changes first). Unblocks: a small follow-up kind_robots task to add the migration + seed script, then digital-storefront's print-flow gate can read the new field instead of its current default-deny-almost-everything fallback.
APPROVED 2026-07-25: Silas — "approved." Pitch frontmatter flipped to approved; filed the migration as kind-robots/t-045. Note for whoever implements it: this field now has a live connection to this session's engine-default work (queue_monster_recast_art.py etc. switching to krea2) — per creative-engines.md's licensing table, krea2 is "Community (<50 seats, needs content filter)", NOT the "cleanest commercial" tier (that's flux2-klein, Apache-2.0). When seeding this field, krea2 output should almost certainly seed unsafe/unknown by default like everything else, not true — don't assume "recently made the coloring default" implies "commercially safe."
Original spec: Kaizen from t-016's close (docs/gallery-to-swag-pipeline.md §4/§6). The self-service "print my gallery art" flow needs to enforce CONTROL.md's commercial-generation licensing rule (dev-trained LoRAs/Kontext-dev never touch commercial output) before a real user can print a real ArtImage, but `Resource` (the LoRA/checkpoint registry `ArtImage.checkpointResourceId` points at) has no field recording license class today. Per BOUNDARY.md, this is a kind_robots pitch (backend schema change), not a direct edit: write the pitch (additive `Resource.commercialSafe: Boolean @default(false)` or a `licenseClass` enum, defaulted conservatively) and file it in conductor/pitches/ per the proposal-kind template. Does not block the KR-logo-sticker build (static-asset path, no ArtImage involved).
<!-- note:end t-020 -->

## t-021 — kind_robots pitch: add ArtImage.storefrontFeatured (or a StorefrontFeaturedArt join) for curated swag pieces

<!-- note:begin t-021 -->
FOR SILAS: Pitch written at pitches/2026-07-15-storefront-featured-art.md. It proposes an additive `ArtImage.storefrontFeatured` boolean (or a `StorefrontFeaturedArt { artImageId, sortOrder, addedAt }` join table if curation ordering matters) so the storefront's swag rail can point at a curated set of gallery pieces instead of a hand-maintained docs/ allow-list.
TO APPROVE: read the pitch, set its `status: approved` and this task's `approved_by_human: true` + `status: done` if you want it built (pick boolean vs join table, or leave a note here). Unblocks: a small follow-up kind_robots task to add the migration, then digital-storefront's swag-rail query can read the new field instead of a static allow-list.
APPROVED 2026-07-25: Silas — "approved." Went with the pitch's own suggested-first-task default (boolean, not the join table) since no ordering requirement was raised; easy to upgrade to a join table later if the rail grows. Filed as kind-robots/t-047. Pitch frontmatter flipped to approved.
Original spec: Kaizen from t-016's close (docs/gallery-to-swag-pipeline.md §5/§6). Curating specific ArtImage rows for the storefront's featured-prints rail (separate from the KR-logo item, which uses the static-asset path and doesn't need this) needs either a boolean flag on ArtImage or a small join table (`StorefrontFeaturedArt { artImageId, sortOrder, addedAt }` if curation ordering is wanted). Per BOUNDARY.md, write and file the pitch in conductor/pitches/ rather than editing the shared schema directly. A flat allow-list in this project's docs/ is an acceptable stand-in until the pitch lands.
<!-- note:end t-021 -->

## t-022 — kind_robots: Stripe webhook (checkout.session.completed, payment mode) credits an Entitlement for the Mermaids PDF product

<!-- note:begin t-022 -->
FOR SILAS: kind_robots PR #426 (branch worker/digital-storefront-t-022) adds handleProductPurchase to server/api/stripe/webhook.post.ts.
What it does: when a checkout.session.completed event arrives with session.mode === 'payment' and metadata.productSlug set, it looks up the Product by slug, then creates an Order + OrderItem + Entitlement in one transaction. Idempotent on Order.stripeSessionId (unique constraint from t-011's migration) — a redelivered webhook event finds the existing Order and returns without double-crediting, same shape as the existing handleManaTopup handler for mana top-ups.
TO APPROVE: review kind_robots PR #426, confirm the metadata.productSlug contract it defines is what you want the (not-yet-built) checkout-session route in t-023 to set, then merge the PR yourself or tell an agent to merge it. Set approved_by_human: true and status: done here once merged — this stays gate_human because it's real purchase-fulfillment logic (grants a paid Entitlement) even though it has no UI of its own, per this task's original stakes:outward-facing marking.
What unblocks: t-023 (product page + checkout session + secure download route) depends on this task and can then set metadata.productSlug / metadata.userId on its checkout session to match this handler's contract.
CONFIRMED 2026-07-25: Silas — "that pr was approved and merged LOOOOOOOOONG ago." Verified via GitHub MCP: kind_robots PR #426 merged 2026-07-18T18:41:54Z by silasfelinus. This roadmap task was simply never flipped after the merge — a week of stale needs-human noise for already-done work. Flipping to done, approved_by_human: true. t-023 is unblocked.
Verified before opening the PR: full-project `npm run test` (vue-tsc --noEmit) exit 0, eslint clean, prettier clean, `prisma validate` clean (confirms Order/OrderItem/Entitlement/Product and the stripeSessionId unique constraint already exist). No live Stripe call made; could not exercise a real signed webhook event from this sandbox (no egress/test secrets here) — CI is the verification gate before merge, same as how t-011 step 1 and PR #345's mana_topup handler were verified.
ORIGINAL TASK NOTE (unchanged below): Step 2 of the original t-011 (SPEC.md §2, §4): new server/api/stripe/webhook.post.ts event (or extend one if t-012's mana webhook already created a shared handler — check kind_robots PR #345 first) verifying the Stripe signature via readRawBody + stripe.webhooks.constructEvent, handling checkout.session.completed (mode: payment) for the mermaids-of-venice-pdf Product: create an Order + OrderItem row, then an Entitlement row for the buyer. Idempotent on stripeSessionId (unique constraint already in the t-011 migration) so a redelivered webhook event can't double-fulfill. TEST MODE ONLY. This is real purchase-fulfillment logic (credits a paid entitlement) — stays gate_human like the original t-011, even though it has no UI of its own. EGRESS: api.stripe.com — re-run scripts/recheck_egress_blocks.py before claiming (blocked as of 2026-07-17T10:5xZ, varies by session).
<!-- note:end t-022 -->

## t-023 — kind_robots: Mermaids of Venice PDF product page + Stripe checkout + secure entitlement-gated download route

<!-- note:begin t-023 -->
Step 3 of the original t-011 (SPEC.md §5): a Checkout Session creation route for the mermaids-of-venice-pdf Product (mode: payment, reuse the topup.post.ts/checkout.post.ts pattern), a product page linked from the mermaids landing page (components/pages/mermaids-page.vue) and giftshop, and a secure download route that checks the caller's Entitlement (via t-022's webhook) before streaming the PDF from server-side storage — never public/. Going live (real charges, live Stripe keys) stays a separate hard needs-human for Silas regardless of this task's own completion.
DONE (implementation) 2026-07-27 (conductor scheduled burst-mode session, claimed as burst-20260727T170948Z-ds-t023): kind_robots PR #1056 (merged by Silas same day — see CLEARED note below). Delivered all three remaining pieces: (1) server/api/store/product-checkout.post.ts, a generic single-Product Stripe Checkout Session route for Entitlement- granting products (mirrors the existing pod-checkout.post.ts pattern, blocks repurchase if an active Entitlement already exists); (2) server/api/store/entitlements.get.ts, returning the caller's own active entitlement slugs; (3) server/api/store/download/[slug].get.ts, streaming the file from a new private storage root only after re-checking Entitlement on every request. Front end: components/giftshop/ mermaids-pdf-purchase.vue (Buy $9.99 / Download / log-in-to-buy states) replaces the old "coming to the giftshop" placeholder badge in mermaids-page.vue. The webhook, schema, and Product seed row already existed from t-011/t-022 — this closes the remaining gap.
SECURITY FLAG 2026-07-26 RESOLVED this session: the flag below (public, unauthenticated public/mermaids.pdf) is fixed as part of this same PR — git mv'd to storage/products/mermaids-of-venice-pdf.pdf (outside Nitro's public/ static server), behind a new server/utils/productStorageRoot.ts (env-overridable root, same pattern as imageStorageRoot.ts) so the file is no longer publicly reachable at any URL. Confirmed via full-repo grep that nothing else referenced the old public/ path before moving it.
Verified before opening the PR: eslint clean, prettier clean, full-project `npm run test` (vue-tsc --noEmit) exit 0. Could NOT exercise a live Stripe checkout/webhook round-trip or a real file download in-sandbox (no STRIPE_SECRET_KEY / reachable DATABASE_URL) — this is the "verification still needed" gap a human/live environment closes, not a known defect.
Original security-flag text (2026-07-26), preserved for the record: kind_robots' `public/mermaids.pdf` (2.6MB, present in the current checkout) already sits at a public, unauthenticated URL — this task's own spec line above ("stream from server-side storage — never public/") already anticipated exactly this risk. `mermaids-page.vue` does not currently link to it, so it's not linked-and-discoverable from the site UI today, but it is not access-controlled either — anyone who guesses/finds the URL can download the full product for free, regardless of whether this task's paywall ever ships.
FOR SILAS: kind_robots PR #1056 (https://github.com/silasfelinus/kind_robots/pull/1056) adds real Stripe checkout + a gated download for the Mermaids of Venice PDF, and fixes the public/mermaids.pdf security flag in the same change. It contains: a $9.99 "Buy PDF" button on /mermaids that creates a real Stripe Checkout Session, a secure download route that only serves the file to someone who actually paid, and moves the PDF out of public/ so it can no longer be downloaded for free by anyone who finds the URL. CLEARED 2026-07-27T17:56:21Z: kind_robots PR #1056 merged directly by Silas (merged_by: silasfelinus, confirmed via the GitHub API) — the explicit human clearance this hard gate was waiting for. Per AGENTS.md's "Human gate clearance rule," setting approved_by_human: true and status: done on that basis. Still open (not this task's job, and not reversible/agent-doable): confirming STRIPE_SECRET_KEY/ STRIPE_WEBHOOK_SECRET are live-ready and that `npx tsx scripts/seed_products.ts --write` has run against the production DB so the mermaids-of-venice-pdf Product row exists — the checkout route 404s otherwise. Flagging this in case it wasn't already part of the merge decision, but it's a deploy/ops step outside this roadmap task's scope.
<!-- note:end t-023 -->

## t-024 — subscribe.post.ts: require authenticated user instead of trusting client-supplied userId

<!-- note:begin t-024 -->
Kaizen from t-013's close (2026-07-17): reviewing kind_robots PR #361 surfaced that the pre-existing server/api/stripe/subscribe.post.ts accepts a client-supplied userId in the request body with no auth check (not introduced or worsened by #361 -- cancel-subscription.post.ts was built correctly, deriving the user from requireApiUser instead). Fix subscribe.post.ts to derive the user the same way, matching the new cancel-subscription endpoint's pattern. DONE 2026-07-17 (conductor hourly cycle, claude-conductor-hourly-20260717d): kind_robots PR #373 (branch claude/keen-fermat-k0alew) merged after all 3 checks (GitGuardian, TypeScript, Contract verifiers) passed -- subscribe.post.ts now derives the user via requireApiUser(event) instead of trusting a client-supplied userId, mirroring cancel-subscription.post.ts. Conductor PR #728 (roadmap claim->review) merged first, then this task flipped to done here after confirming the implementation PR's merge.
<!-- note:end t-024 -->

## t-025 — Drop the now-unused userId param cartStore.ts's subscribe() still sends to /api/stripe/subscribe.post.ts

<!-- note:begin t-025 -->
Kaizen from t-024's merge (kind_robots PR #373): subscribe.post.ts now derives the user from requireApiUser(event) and no longer reads userId from the request body, but cartStore.ts's subscribe(userId) and its one caller (subscription-manager.vue) still pass one. Harmless today (the server ignores it) but confusing for the next reader — drop the now-dead parameter from both the store action and its caller. kind_robots-repo change.


Claiming the highest-priority reversible ready task after skipping claimed or inactive projects.

FOR SILAS: The two-line cleanup is identified in kind_robots stores/cartStore.ts and components/giftshop/subscription-manager.vue. The current Worker connector can read but only fully replace these large files, and this run has no local GitHub network path for a surgical patch. TO RESUME: return this task to ready when a local checkout or patch-capable GitHub tool is available. No implementation was started and no kind_robots branch was created.

RESOLVED 2026-07-25 (Silas: "you should have that access, fix it"): checked the actual current code with a local kind_robots checkout available this session -- `stores/cartStore.ts`'s `subscribe()` (line 305) already takes zero parameters and its `performFetch` call sends no body at all, and its one caller in `components/giftshop/subscription-manager.vue` (line 99) already calls `cartStore.subscribe()` with no arguments. The dead `userId` param described here no longer exists in either file -- this was already cleaned up (git blame points at the same bulk commit #902 touching both files, likely incidental to an unrelated pass rather than a dedicated fix, but the end state is clean either way). No new kind_robots PR needed. Closing as done.
<!-- note:end t-025 -->

## t-026 — kind_robots: extend Stripe webhook fulfillment to DLC products (Entitlement + Grant)

<!-- note:begin t-026 -->
FOR SILAS: kind_robots PR #1018 (branch `claude/confident-bardeen-qxqhun`) extends `server/api/stripe/webhook.post.ts`'s per-product purchase handler so a `Product.type = DLC` checkout also grants Pack access, not just a bare Entitlement. All CI checks pass (TypeScript, eslint, prettier); no live Stripe keys or deploy involved — this only changes application code.
What it does: on a DLC product's `checkout.session.completed`, it reads `packId` from the product's metadata (same JSON convention already used for POD's artImageId/printfulVariantId) and creates a `Grant` row (subjectType: PACK, level: VIEW, source: PURCHASE, status: ACTIVE) alongside the existing Entitlement, inside the same transaction as the Order/OrderItem.
ONE DECISION FOR YOU: the original task note (2026-07-18) specified `level: UNLOCK`, but `GrantLevel` only defines VIEW/ADMIN (kind-robots/ t-037's schema, approved 2026-07-25/26) — no UNLOCK value exists. I used VIEW, since `server/utils/contentAccess.ts` already gates Pack-owned content on an active PACK Grant at GrantLevel.VIEW, so VIEW is what actually unlocks a pack's content today. If you'd rather add a distinct UNLOCK level (via a new additive migration) instead of reusing VIEW, say so and a future task can do that migration + this file's one-line update.
ALSO NOTE: no checkout-creation route yet produces a DLC-type Product with packId metadata (packmaker/t-004's admin generator builds Pack content bundles, not a priced Product row), so this fulfillment path is real but currently unexercised end-to-end — a follow-up task to wire a DLC product-page/checkout-creation route (mirroring t-023's Mermaids PDF page) would let it actually run in test mode.
TO APPROVE: review kind_robots PR #1018, and if the VIEW-vs-UNLOCK choice and the code look right, merge it and set `approved_by_human: true` + `status: done` here. What unblocks: nothing else currently depends on this — it's the last open piece of the original DLC-unlock design (projects/digital-storefront/docs/dlc-unlock-design.md) besides the product-page/checkout-creation follow-up noted above.


Closed during Silas's explicit human-gate pertinence audit. Silas directly merged Kind Robots PR #1018, which implemented the proposed DLC Entitlement plus PACK Grant fulfillment using GrantLevel.VIEW. That merge is the completed human decision the roadmap was waiting for; leaving this at needs-human after the approved implementation reached main was stale state.
<!-- note:end t-026 -->

## t-027 — Correct STORE-AUDIT.md / t-008 — social-publisher.vue was removed 2026-07-18, three days after being documented as the most-wired giftshop component

<!-- note:begin t-027 -->
Filed 2026-07-26 (weekly site audit, global-ui/SITE-AUDIT-AGENT.md). t-008's DONE note and STORE-AUDIT.md §6 both describe `components/giftshop/social-publisher.vue` as "Wired (most mature piece)" with real CRUD and a real Discord integration, dated 2026-07-15. It no longer exists in kind_robots: removed by migration `prisma/migrations/20260718200000_remove_social_publishing` ("abandoned SocialPost/SocialTarget publishing prototype"), 2026-07-18 — three days after the audit. Confirmed via Glob against the current kind_robots checkout; no other giftshop component from the audit's "wired" list is affected. TO DO: update STORE-AUDIT.md §6 and t-008's note (or add a short addendum) to record the removal, so this project's audit trail doesn't keep describing a component that no longer exists as the giftshop's most mature piece. No code change implied — purely a documentation-accuracy fix; confirm with Silas only if social publishing should be rebuilt rather than left removed.
<!-- note:end t-027 -->

## t-028 — Wire ArtImage.storefrontFeatured into the swag-rail query now that the field exists (kind_robots PR #994)

<!-- note:begin t-028 -->
Kaizen follow-up from kind-robots/t-047 (kind_robots PR #994, merged 2026-07-26): `ArtImage.storefrontFeatured Boolean @default(false)` now exists (additive migration, indexed). Per the approved pitch (conductor/pitches/2026-07-15-storefront-featured-art.md), digital-storefront's swag-rail query is the first real consumer — query `ArtImage` filtered on `storefrontFeatured: true` to populate the curated swag rail described in docs/gallery-to-swag-pipeline.md §5, instead of the flat allow-list fallback mentioned there. No admin UI to toggle the flag exists yet either (Silas would need to set it directly via DB/admin tooling until one is built) — note that gap in this task's PR rather than silently assuming a toggle UI already exists.

UPDATE 2026-07-26 (claude-conductor-burst-20260726T0410Z-ds-t028): kind_robots PR #995 open (branch claude/dazzling-ptolemy-cpiydi) — adds GET /api/art/storefront-featured (mirrors art/image/index.get.ts's public visibility where-clause: isPublic/isActive/!isMature, plus storefrontFeatured: true) and wires a "Featured prints" rail into giftshop-interact.vue that renders from it, replacing the previous flat showcaseItems-only placeholder as the intended real consumer. eslint, vue-tsc --noEmit (full project), and prettier --check all clean locally (via provision_kind_robots_deps.sh); no live DB in-sandbox to exercise the query end-to-end, so CI is the verification gate before merge. Left the admin-toggle-UI gap noted in the PR body per this task's own instruction.

DONE 2026-07-26: kind_robots PR #995 merged (squash 05f0fd8) — all 5 checks (TypeScript, verify, Contract verifiers, facet-catalog, GitGuardian) green, no review comments. Rail stays empty in production until Silas flags at least one ArtImage.storefrontFeatured=true via DB/admin tooling.
<!-- note:end t-028 -->

## t-029 — Wire the print-eligibility gate to read Resource.commercialSafe instead of the checkpointResourceId-IS-NULL default-deny

<!-- note:begin t-029 -->
RENUMBERED from t-028 during merge-conflict resolution (conductor PR #1095): t-028 was independently taken by a concurrent live session's kaizen task (kind-robots/t-047 follow-up, kind_robots PR #995 in progress) before this PR merged — see root TALKBACK.md 2026-07-26 for detail. This task's own content is unaffected by the renumber. Kaizen from t-020's close: kind-robots/t-045 (kind_robots PR #993, merged 2026-07-26) added `Resource.commercialSafe Boolean @default(false)` plus `scripts/seed_resource_commercial_safe.ts` (dry-run by default, `--write` to apply), seeding known-clean backends (OpenAI, FLUX.1 schnell, Flux.2 Klein, BFL Kontext pro/max, fal.ai/Replicate — NOT Flux.1 dev, Kontext dev, or krea2). The field exists now but nothing reads it yet: the actual "print my gallery art" eligibility check (docs/gallery-to-swag-pipeline.md §4/§6) still needs to join `ArtImage.checkpointResourceId -> Resource.commercialSafe` and gate on it, replacing (or narrowing) the current conservative `checkpointResourceId IS NULL` default-deny. Before wiring this: run `npm run seed:resource-commercial-safe` (no --write) against real data and sanity-check the classification output first — the seed script's backend matching is regex-based heuristics, not a verified mapping (flagged in kind-robots' TALKBACK 2026-07-26).
REVIEWED AND MERGED 2026-07-26 (Reviewer, conductor sweep session): kind_robots PR #996 implements `checkPrintEligibility()` in `server/api/art/utils/printEligibility.ts` exactly per the pipeline doc's §4 rule (not-mature, owned-or-public, base-model-or- commercialSafe-checkpoint) plus `GET /api/art/image/:id/print-eligibility` (auth/ visibility gate mirrors `facets.get.ts`) and wired the gate into `print-swag.vue`'s Add-to-Cart button. No schema changes, no live checkout/payment logic touched. All CI green (verify, TypeScript, Contract verifiers, facet-catalog). Merged as-is.
<!-- note:end t-029 -->

## t-030 — Design a PrintJob model + real POD checkout flow to replace print-swag.vue's UI mock

<!-- note:begin t-030 -->
DONE 2026-07-26 (claude-conductor-scheduled-20260726T070532Z-ds-t030): kind_robots PR #1004 merged (Silas). Added the additive `PrintJob` model + `PrintJobStatus` enum (migration `20260726070000_add_print_job`, matching the real schema's `Int` autoincrement ids rather than the design doc's `cuid()` draft), a new `server/api/store/pod-checkout.post.ts` route that re-checks `checkPrintEligibility` server-side and creates a real Stripe test-mode Checkout Session, and a `webhook.post.ts` branch that creates a `PrintJob` (status PENDING) instead of an `Entitlement` when `Product.type === 'POD'` — using the session's real line-item quantity instead of the previous hardcoded 1. `print-swag.vue` now calls the real route instead of its `console.log`/`alert()` mock; dropped its freeform "paste an image URL" field since there's no `ArtImage` row to gate eligibility against or attach a `PrintJob` to for an arbitrary URL. Verified: `prisma validate`/`generate`, full-project `vue-tsc --noEmit` (0 errors), `eslint`/`prettier` clean, `verifyNoPrismaJsonCast.ts` and `verifyFetchGenericPinning.ts` both pass. Could not exercise a live Stripe test-mode click-through from this sandbox (no `STRIPE_SECRET_KEY`/ `STRIPE_WEBHOOK_SECRET`). Submitting to a POD vendor (Printful) is deliberately unimplemented — no vendor account/API key exists yet (t-015, needs-human); `PrintJob` rows stay PENDING until that lands. Found but did NOT fix (flagged in the PR, kaizen task t-031 filed): the general multi-item cart checkout (`checkout.post.ts`) sets `metadata.kind: 'giftshop_checkout'`, which matches no branch in the webhook's dispatch, so a cart purchase of a print/shirt/sticker/mug today succeeds at Stripe but never creates an Order — out of this task's scope (t-030 was specifically print-swag.vue's own single-item flow). Original spec follows: Kaizen from t-029's close (kind_robots PR #996, merged 2026-07-26): the new print-eligibility gate has a real, tested check but no live consumer — the self-service print entry point (`print-swag.vue`) is still a UI mock (`addToCart` just `console.log`s + `alert`s, no Stripe/order integration). Per gallery-to-swag-pipeline.md §7 steps 2/5/6: define a `PrintJob` model (additive migration) and a real checkout flow that calls the eligibility endpoint before allowing a print/swag order. Payment/checkout integration is outward-facing — expect this to reach `needs-human` once it touches a live payment provider or order fulfillment API; the schema/plumbing work up to that point is reversible software.
MERGED 2026-07-26 (~08:45 UTC, Reviewer, scheduled agent run): kind_robots PR #1004 squash-merged (`e3d921b`). Dispatched a subagent to audit the migration.sql line-by-line and the Stripe webhook billing logic before merging, given the note's own outward-facing caution above. Findings: the migration is additive-only (1 CREATE TABLE `PrintJob` + 2 ADD CONSTRAINT FKs, no drops/rewrites) -- passes the migration-audit rule. `PrintJob` creation in `handleProductPurchase` is atomic (same `$transaction` as the Order/OrderItem it's keyed to) and mutually exclusive with the existing Entitlement-creation branch, so no double-crediting risk. Stripe line-item quantity is read from the trusted server-side `session.id` (not client input), with a safe quantity=1 fallback on API failure. `getStripeClient()` reads `STRIPE_SECRET_KEY` from env with no hardcoded key or forced live-mode flag -- same pattern as every other already-merged Stripe route in this codebase, so this stays reversible/test-mode plumbing, not a live payment action; confirmed `pod-checkout.post.ts` re-validates print eligibility server-side (fresh DB fetch + `checkPrintEligibility`) rather than trusting client state. All 6 kind_robots CI checks green (TypeScript, Contract verifiers, facet-catalog, verify, Preserve Components/Reactions, GitGuardian). Actual Printful vendor submission stays out of scope / needs-human per the PR's own disclosure (no vendor API key exists) -- not a blocker for this schema/plumbing task.
<!-- note:end t-030 -->

## t-031 — kind_robots: wire checkout.post.ts's general multi-item cart flow to actually fulfil (Order/OrderItem/Entitlement/PrintJob) whatever it sells

<!-- note:begin t-031 -->
Kaizen from t-030 (kind_robots PR #1004, merged 2026-07-26). The general multi-item cart checkout (`server/api/stripe/checkout.post.ts` -- corrected 2026-08-05 per t-036; this note originally said `server/api/store/checkout.post.ts`, which does not exist) sets Stripe session `metadata.kind: 'giftshop_checkout'`, which the webhook's `handleProductPurchase` dispatch doesn't match against any branch -- so a cart purchase of a `print`/`shirt`/`sticker`/`mug` today succeeds at Stripe but never creates an `Order`, `OrderItem`, `Entitlement`, or (for POD items) a `PrintJob`. Only the single-item, dedicated-route flows (`topup.post.ts`, the new `pod-checkout.post.ts`) get real fulfillment today; the shared cart checkout is decorative past the Stripe redirect. Wire the general checkout's webhook branch to create the same Order/OrderItem/Entitlement/PrintJob rows per line item that the dedicated routes already create, reusing `handleProductPurchase`'s per-product-type branching rather than duplicating it. Reversible, Stripe test-mode only, no live payment/vendor action.
DONE 2026-07-26 (claude-conductor-scheduled-20260726T083600Z-ds-t031): research first (before writing code) surfaced two real complications the note above didn't anticipate: (1) `handleProductPurchase` is a single-Product-per-session handler keyed on `metadata.productSlug` -- a multi-item cart session needs a materially different shape, since Stripe Checkout Sessions carry one flat `metadata` object at the *session* level but multiple line items. Confirmed via the Stripe SDK's own type definitions (`node_modules/stripe/esm/resources/LineItems.d.ts`) that the `LineItem` objects returned by `checkout.sessions.listLineItems()` carry their own per-line `metadata` field (distinct from `price_data.product_data`), which `checkout.sessions.create()`'s line item params also accept -- so `checkout.post.ts` now stamps `metadata: { giftshopType: id }` on each line item, and a new `handleGiftshopCartPurchase` webhook handler (dispatched on `session.metadata?.kind === 'giftshop_checkout'`, kept separate from `handleProductPurchase` rather than overloading it) reads it back per line via `listLineItems` with no `expand` needed. (2) The client cart already tracks a real `artImageId` per item (`stores/cartStore.ts`'s `CartItem` interface), but `checkout()` drops it before sending to the server (`{ id: item.type, quantity }` only) -- so there is no real, eligibility-checked art to attach a `PrintJob` to for print/shirt/sticker/mug/book today. Filing a `PrintJob` against a fabricated `artImageId` would misrepresent what art is being printed, so split that off as `t-033` rather than guessing. Implemented for this task: every general-cart purchase now creates a real `Order` + one `OrderItem` per line item (upserting a generic `Product` row per `stores/seeds/cartItems.ts` catalog type, server-trusted price, same idempotency pattern as `handleProductPurchase` -- check-then- transact, backstopped by `Order.stripeSessionId`'s unique constraint). Additionally credits real mana for `tokens`-type line items (mirroring `handleManaTopup`'s `PURCHASE`-reason pattern) since an audit-only record would mean charging real money for "100 Boost Tokens" and granting nothing -- extended `applyMana` (`server/utils/mana.ts`) to accept an optional `tx` parameter so this commits atomically inside the same transaction as the Order/OrderItem write, rather than opening a second, non-atomic transaction (a small, backward-compatible signature change; every other existing caller is unaffected). `donation` and POD-mapped types (print/shirt/sticker/mug/book) get Order/OrderItem only in this pass -- correct behavior, not a gap, for donation (nothing to "own"); genuinely deferred for the POD types, tracked as `t-033`. Verified: `npx eslint` clean on all three changed files; `npx prettier --check` clean; full-project `vue-tsc --noEmit` (0 errors, after fixing one real type error -- the `GIFTSHOP_TYPE_TO_PRODUCT_TYPE` map's keys must match `stores/seeds/cartItems.ts`'s narrower `CartItem['type']`, not the similarly-named but broader client-side `stores/cartStore.ts` `CartItem` interface, which also declares `extra`/`subscription` types that don't exist in the trusted server catalog); `verifyNoPrismaJsonCast.ts` and `verifyFetchGenericPinning.ts` both pass. Could not exercise a live Stripe test-mode click-through from this sandbox (no `STRIPE_SECRET_KEY`/ `STRIPE_WEBHOOK_SECRET`) -- the per-line `metadata` round-trip is verified against the Stripe SDK's own type definitions rather than a live call. kind_robots PR #1007: all 4 CI checks green (TypeScript, Contract verifiers, facet-catalog, GitGuardian) -- merged squash `b29064d9`. No review comments. Follow-up split off as `t-033` (real artImageId/PrintJob wiring for the four physical-goods cart types).
<!-- note:end t-031 -->

## t-032 — kind_robots: re-check ArtImage takedown/eligibility at Stripe webhook time for POD purchases, not just at checkout-creation time

<!-- note:begin t-032 -->
Kaizen from reviewing kind_robots PR #1004 (merged 2026-07-26, digital-storefront/t-030). `server/api/store/pod-checkout.post.ts` correctly calls `checkPrintEligibility` (ownership, isMature hard-deny, isActive, Resource.commercialSafe default-deny) before creating the Stripe Checkout Session -- but the webhook's POD branch in `server/api/stripe/webhook.post.ts` (`handleProductPurchase`, ~lines 194-224) does not re-run that check when the session actually completes. It only reads `artImageId`/ `printfulVariantId` back out of `Product.metadata` and creates the `PrintJob` row directly. This matches gallery-to-swag-pipeline.md's design doc section 4 (Takedown path): "an image flagged between 'added to cart' and 'payment completed' should fail the webhook's PrintJob creation step ... rather than silently shipping it." Currently inert (no Printful vendor account/API key exists yet, so PrintJob rows stay PENDING with printfulOrderId: null regardless -- nothing physically ships from this gap today), but it should be closed before or alongside whatever task wires the real Printful order-submission call, so a moderation action taken during the Stripe checkout window isn't silently ignored once that call exists. Fix: call `checkPrintEligibility` again inside the webhook's POD branch using the ArtImage's *current* state (re-fetch, don't trust anything cached from checkout-creation time); on failure, create the `PrintJob` with `status: FAILED` (or skip creation) and surface the reason rather than proceeding silently. Reversible, Stripe test-mode only, no live vendor action.
DONE 2026-07-26 (claude-conductor-scheduled-20260726T090000Z-ds-t032): kind_robots PR #1008 (squash bc592f5) re-fetches the ArtImage's current isMature/isActive/isPublic/userId/ checkpointResourceId + CheckpointResource.commercialSafe inside handleProductPurchase's POD branch and calls the existing checkPrintEligibility() again before creating the PrintJob -- same gate pod-checkout.post.ts already applies at session-creation time, now also applied at fulfillment time. On failure (or if the ArtImage row no longer exists), the PrintJob is still created but with status: FAILED instead of the default PENDING, so the failure is visible/ auditable rather than either silently shipping it or silently dropping the record. Verified: npx eslint/prettier clean, full-project vue-tsc --noEmit 0 errors, both contract verifiers (verifyNoPrismaJsonCast, verifyFetchGenericPinning) pass. All 4 kind_robots CI checks green (TypeScript, Contract verifiers, facet-catalog, GitGuardian); no review comments. Could not exercise a live Stripe test-mode click-through (no STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET in this sandbox) -- verified by type-checking and matching pod-checkout.post.ts's existing call shape exactly (same select fields, same checkPrintEligibility signature).
<!-- note:end t-032 -->

## t-033 — kind_robots: wire real artImageId through the general giftshop cart's print/shirt/sticker/mug/book checkout so those types can get a real PrintJob

<!-- note:begin t-033 -->
Split off from t-031 during implementation (2026-07-26) rather than guessing at a fabricated artImageId. `stores/cartStore.ts`'s client-side `CartItem` interface already tracks a real `artImageId` per item (populated correctly for at least the "Featured print" flow in `giftshop-interact.vue`'s `addFeaturedPrint`, which sources it from `/api/art/storefront-featured`), but `cartStore.ts`'s own `checkout()` function drops it before sending to the server -- the payload it builds is `{ id: item.type, quantity: item.quantity }`, discarding `artImageId`/`imageUrl` entirely. `server/api/stripe/ checkout.post.ts`'s `parseCart` only ever accepted `{id, quantity}` too, so the server never had anywhere to put it even if the client sent it. t-031 wired real Order/OrderItem (+ mana crediting for tokens) fulfillment for every general-cart line item using only `type`+`quantity`+the trusted server-side `stores/seeds/cartItems.ts` price/label -- it deliberately does NOT create a `PrintJob` for print/shirt/sticker/mug/book line items, since doing so would need a real, eligibility-checked `artImageId` that doesn't exist anywhere in the current payload. To close this: (1) extend `cartStore.ts`'s `CartItem` payload sent to `/api/stripe/checkout` to include `artImageId` per line; (2) extend `checkout.post.ts`'s `parseCart` to accept and validate it (integer, required for print/shirt/sticker/mug/book types, matching `pod-checkout.post.ts`'s validation shape); (3) re-run `checkPrintEligibility` (`server/api/art/utils/printEligibility.ts`, already reused unmodified by t-030's `pod-checkout.post.ts`) server-side per print-type line item before creating the Stripe session -- decide and document what happens to the *rest* of a multi-item cart if one line item fails eligibility (reject the whole checkout vs. drop just that item vs. some other UX -- this is a real product decision, not just plumbing); (4) carry the validated `artImageId` through the same per-line-item Stripe `metadata` mechanism t-031 already established (`metadata: { giftshopType, artImageId }`) so `handleGiftshopCartPurchase` in `server/api/stripe/webhook.post.ts` can create a real `PrintJob` for those four types instead of stopping at Order/OrderItem. Note the demo showcase items in `giftshop-interact.vue` (`addDemoItem`, "Butterfly Print" / "Jellybean Token Pack") and `giving-page.vue`'s donation button use placeholder `artImageId` values (1, 2, 0) that don't correspond to real `ArtImage` rows -- these will fail a real eligibility check once (3) lands, which is correct behavior, not a bug to work around; they're demo/placeholder UI, not real product listings, and may need their own follow-up to either wire real featured art or get removed. Reversible, Stripe test-mode only, no live vendor action.


DONE 2026-07-26: kind_robots PR #1011 merged as squash commit 10d22619f08ef2fa220456669a94a31b2cdc6f02. The general giftshop cart now carries a real artImageId per physical-goods line, validates it and re-runs print eligibility before creating the Stripe session, preserves separate same-type items by (type, artImageId), carries artImageId through Stripe line metadata, and creates a real PrintJob during webhook fulfillment. Ineligible art rejects the whole checkout so customers are never charged for a silently reduced cart. Book remains Order/OrderItem-only because it is a fixed product rather than an ArtImage print. All implementation verification and CI passed on the merged PR; no live vendor action was performed.
<!-- note:end t-033 -->

## t-034 — kind_robots: handleProductPurchase's POD 'ArtImage not found' branch inserts a PrintJob referencing a nonexistent FK, which can throw and roll back the whole Order

<!-- note:begin t-034 -->
DONE 2026-07-26: kind_robots PR #1014 merged. `handleProductPurchase`'s POD branch no longer inserts a `PrintJob` referencing a nonexistent `ArtImage` id when the eligibility re-check finds it gone -- it skips the insert entirely (mirroring the existing `fulfillGiftshopPrintJob` pattern from t-033), so the `Order`/`OrderItem` created earlier in the same `$transaction` survive as the audit trail instead of being rolled back by the FK violation. Verified with `eslint` + a full `vue-tsc --noEmit` typecheck (both clean); live-DB rollback verification wasn't possible in this sandbox (no reachable DATABASE_URL), noted as a flag on the PR. Kaizen: extract a shared `createPrintJobIfEligible` helper so the two POD-fulfillment paths (`handleProductPurchase` vs. `handleGiftshopCartPurchase`) stop reimplementing the same missing-ArtImage guard independently -- filed as new task below.
<!-- note:end t-034 -->

## t-035 — kind_robots: extract a shared createPrintJobIfEligible helper for the two POD-fulfillment webhook paths

<!-- note:begin t-035 -->
DONE 2026-07-26: kind_robots PR #1016 merged. Extracted `createPrintJobIfEligible(tx, { orderItemId, artImageId, printfulVariantId, userId }, context)` and switched both `handleProductPurchase`'s POD branch and `fulfillGiftshopPrintJob` to call it, removing ~90 lines of duplicated eligibility-recheck/FK-safety logic. Verified with `eslint` + a full `vue-tsc --noEmit` typecheck (both clean); behavior preserved exactly (same eligibility check, same FK-safety guard, same FAILED-status handling, same log wording via a per-call-site `context` string).
<!-- note:end t-035 -->

## t-036 — Correct t-031's note — the general cart checkout handler lives at server/api/stripe/checkout.post.ts, not server/api/store/checkout.post.ts

<!-- note:begin t-036 -->
Filed 2026-08-02 (weekly site audit, global-ui/SITE-AUDIT-AGENT.md). t-031's title and note both refer to the general multi-item cart checkout as `server/api/store/checkout.post.ts`. Confirmed via Glob/Grep against the current kind_robots checkout: no such file exists under server/api/store/ (which holds pod-checkout.post.ts, product-checkout.post.ts, entitlements.get.ts, download/). The code t-031 actually describes -- the handler that sets Stripe session `metadata.kind: 'giftshop_checkout'` -- lives at `server/api/stripe/checkout.post.ts`. Functionality is not affected (the file exists and works, just under a different path than documented); this is a small doc-accuracy fix so a future reader searching server/api/store/ for this handler doesn't come up empty.
DONE 2026-08-05 (claude-scheduled-20260805T032756Z-ds-t036): corrected t-031's note in place -- the one wrong `server/api/store/checkout.post.ts` reference (t-031's title never contained the wrong path) now reads `server/api/stripe/checkout.post.ts`, with a parenthetical noting the correction and what the note originally said, so the history stays legible. No code change; doc-accuracy only, exactly as scoped.
<!-- note:end t-036 -->

## t-037 — Audit the live storefront against the agreed catalog and real implementation state

<!-- note:begin t-037 -->
DONE (conductor sweep, 2026-08-08). Full read-only code audit of kind_robots @ main (bbb1dd8) against the v1 catalog, written up in projects/digital-storefront/STOREFRONT-REALITY-AUDIT-2026-08-08.md (supersedes STORE-AUDIT.md 2026-07-15 and PRODUCT-GAP-AUDIT.md 2026-06-30 as current source of truth). Findings by classification: BUILT-AND-VISIBLE — Mermaids PDF (full checkout->webhook-> Entitlement->gated-download chain, file confirmed on disk), monthly subscription (checkout, webhook activation/lifecycle, real cancel-subscription.post.ts, no longer a stub), mana top-ups (checkout, idempotent webhook credit via applyMana()), Against Malaria giving page (real link to againstmalaria.com/amibot + working $1 add-on), general-cart POD via featured art (giftshop-interact.vue's storefrontFeatured rail -> real PrintJob). BUILT-BUT-UNREACHABLE — DLC fulfillment mechanism (webhook creates Grant correctly) but zero DLC Product ever seeded and no admin path to create one; dedicated pod-checkout.post.ts POD flow is real but its only UI (print-swag.vue) was moved to components/abandonware/ (excluded from Nuxt auto-import, formally retired) so nothing live points at it. MISSING — no KR-logo Product/SKU exists anywhere (podCatalog.ts variant IDs are literal placeholder strings). DELEGATED — coloring-book confirmed fully separate (zero storefront references), boundary is real. Cross-cutting: every user-initiated Stripe/store route now requires auth (closes the 2026-07-15 no-auth-guard gap); webhook is idempotent (Order.stripeSessionId unique) and re-checks POD eligibility again at fulfillment time via a shared createPrintJobIfEligible() helper (confirms t-032/t-035); but the general cart (checkout.post.ts) still prices off the old stores/seeds/cartItems.ts array while product-checkout.post.ts/pod-checkout.post.ts price off the real Product table -- two parallel pricing sources coexist. t-040's scope confirmed precisely: no real POD vendor (Printful) call exists anywhere, printfulOrderId is never set. Full gap list (feeds t-038 directly) is in the audit file's "Real gaps remaining" section. Not verifiable from a code-only audit: whether the webhook actually processes live Stripe events in production, or whether STRIPE_WEBHOOK_SECRET/ STRIPE_SECRET_KEY/STRIPE_PRICE_ID are correctly configured in the deployed environment -- that is t-039's scope, unchanged.
<!-- note:end t-037 -->

## t-038 — Implement every missing storefront-owned catalog item found by the reality audit

<!-- note:begin t-038 -->
Close t-037's storefront-owned gaps -- see STOREFRONT-REALITY-AUDIT-2026-08-08.md's "Real gaps remaining" section for full detail. Original concrete scope: (1) seed a real KR-logo Product/POD SKU (currently MISSING entirely -- no artImageId, no catalog row); (2) give DLC a live catalog -- an admin route/script that turns a Packmaker Pack into a Product with metadata.packId (the webhook fulfillment side already works correctly, it just has nothing to sell), plus wire contentAccess.ts's packId check into the actual Dream/Facet/Character/Reward content routes (explicitly not done yet per that file's own comment); (3) decide whether to restore a live entry point for the orphaned pod-checkout.post.ts flow (e.g. an art-detail "Print this" action) or formally retire it in favor of the general-cart featured-art POD path that already works; (4) decide whether stores/seeds/cartItems.ts should be retired in favor of the real Product table now that two pricing sources coexist; (5) replace/remove giftshop-interact.vue's two hardcoded demo showcase items (guessed artImageId 1/2) now that the real storefrontFeatured rail supersedes them; (6) optionally harden giftshop-manager.vue's tab-switch to explicitly handle activeTab === 'mermaids' in-place (currently masked, not confirmed to cause a live bug). Do not duplicate products whose production is explicitly owned by coloring-book/packmaker/ Mermaids; integrate their finished outputs instead.
Slice 1 (conductor sweep, 2026-08-08): closed items 5 and 6, the two self-contained UI fixes that needed no DB seeding or vendor/design decisions. giftshop-interact.vue's "Butterfly Print" demo card (guessed artImageId: 1, never a real ArtImage) is dropped -- the real Featured prints grid above it already covers that exact use case with genuine storefrontFeatured-backed art. Its "Jellybean Token Pack" sibling is kept (no other UI purchases boost tokens) but now sources label/price/description straight from stores/seeds/cartItems.ts's real 'tokens' entry -- checkout.post.ts's own trusted pricing source -- instead of a second hand-maintained guess, using artImageId: 0 for the needsArt: false item per giving-page.vue's existing donation-add convention. giftshop-manager.vue's activeTab now explicitly redirects to /mermaids when navStore's persisted dashboard tab is 'mermaids' but the component mounts on /sanctuary directly (a stale-tab-selection case that previously fell through to the catch-all "misplaced tab" warning instead of reaching real content) -- the fix sends the visitor to the same destination the tab's own route config already points at. kind_robots PR: silasfelinus/kind_robots#1603 (merged 2026-08-08T08:52Z, squash 111bfee). Verified: eslint (0 new problems, ratchet unchanged at 392/27), vue-tsc --noEmit clean, prettier clean. Post-merge, the PR's own deployment-triggered responsive-layout-audit run (31246882294) was read RAW rather than trusted on pass/fail alone: /sanctuary (giftshop) and /mermaids both passed cleanly at phone, tablet, and desktop -- zero regressions from this change. The run's one reported failure (desktop /taskmaster CRUSHED) is the pre-existing, already-tracked interface-vision/t-113 regression, unrelated to this diff and fixed separately in kind_robots PR #1600.
Items 1-4 (KR-logo Product/POD SKU, DLC live catalog + admin route + contentAccess wiring, and the two pricing/POD-entry-point retirement decisions) are genuinely larger and judgment-heavy -- split into t-003 as this task's remaining_scope_task per AGENTS.md's umbrella-sweep convention. Keeping this task at review rather than done until t-003 closes; a task-events "done" transition should NOT fire for this id until then.
Reconciled (conductor sweep, 2026-08-08): t-003 reached status: done, but t-003 itself closed only items 1/3/4 and split its own remaining item 2 (DLC live catalog + contentAccess.ts wiring -- the same item 2 this task's original scope listed) into t-004, still open. t-038's remaining_scope_task pointer is updated from t-003 to t-004 so this umbrella does not prematurely auto-close to done while its real remaining scope (t-004) is still in progress.
Reconciled done (conductor sweep, 2026-08-09): remaining_scope_task t-004 (Give DLC a live catalog and wire contentAccess.ts's packId check into the Dream/Facet content routes) reached status: done, merged as kind_robots PR #1630 (merged 2026-08-08T23:55:08Z). t-004's own two deferred follow-ons -- the Character/Reward "no access check at all" security gap and the Dream/Facet LIST-route Grant-awareness work -- were filed as their own new tasks (t-005 hard needs-human, t-006 ready) rather than left as further remaining_scope_task chaining on t-038, so t-038's umbrella scope (items 1-6 of the original reality-audit gap list) is now fully closed across t-038 slice 1 (items 5/6), t-003 (items 1/3/4), and t-004 (item 2's DLC-catalog and packId-wiring half). Verified via GitHub MCP: no merged close-out PR exists titled "digital-storefront/t-038" (search on both silasfelinus/conductor and silasfelinus/kind_robots returned zero results), so this is a genuine reconciliation, not duplicate close-out work.
<!-- note:end t-038 -->

## t-043 — Wire contentAccess.ts's packId Grant into the Dream and Facet LIST routes

<!-- note:begin t-043 -->
Split off digital-storefront/t-004 (conductor sweep 2026-08-08) -- t-004 wired the packId Grant fallback into the Dream and Facet SINGLE-ITEM read routes (dreams/[id].get.ts, dreams/[id]/facets.get.ts, facets/[id].get.ts, kind_robots PR #1630) but explicitly left the LIST routes for a follow-on, since they are structurally different: dreams/index.get.ts and facets/index.get.ts build a set-based Prisma `where` clause (isPublic/own/admin baked into `andFilters`), and facets/catalog.get.ts's loadFacetCatalogEntries (server/utils/facetCatalog.ts) is a third, independent implementation of the same pattern. None of the three have a per-row check to swap a canView() call into -- adding Pack-Grant awareness means prefetching the caller's active PACK Grant subjectIds first, then OR-ing `packId IN (...)` into the existing `where`.
This is NOT a security gap like t-005 -- these list routes already correctly exclude non-public/non-owned rows for every existing caller; they simply don't yet let a PACK-Grant holder see Pack-gated Dreams/Facets in list/catalog views the way the single-item routes now do. Reversible, ordinary software work, safe for normal Worker pickup once picked up.


Salvaged only the reversible Dream/Facet list-route Pack Grant visibility slice from the stranded t-004 branch onto fresh Kind Robots main; Character/Reward access-policy changes remain behind digital-storefront/t-005's hard human gate.

DONE: Kind Robots #1643 merged as d9f955b52d2d0b4553e05b92a96d57730dd364de. Added active/unexpired VIEW-or-better PACK Grant ID prefetch and made Dream list, Facet list, and Facet catalog visibility include packId matches without changing anonymous, owner, admin, mine, inactive, or maturity filters. Exact PR head 25d1f39c248b42a6f4e374b72d88ecdc8b103bbc passed all 13 observed PR workflows, including TypeScript #31298673219, Contract Tests #31298673175, and Facet Catalog Contract #31298673167. Character/Reward access-policy changes remain untouched behind hard-gated digital-storefront/t-005.
<!-- note:end t-043 -->

## t-044 — Fix 4 stale digital-storefront milestone status fields (m2/m3/m4/m5 all lag their fully-completed task sets)

<!-- note:begin t-044 -->
Kaizen from the 2026-08-23 weekly site audit. Flipped 4 milestones to `done`: m3 "Support & donations" (all 6 tasks done), m4 "Print-on-demand & gallery coordination" (all 12 tasks done), and m5 "DLC website unlocks" (both tasks done) were all flipped from `not-started`. m2 "First sellable item live" was `in-progress` with its 4 other tasks done and only this task itself outstanding under it -- now that this task is closing as `done` too, m2's own task set is complete, so it's flipped to `done` in this same commit rather than left `in-progress` for a separate follow-up. m1 and m6 were already correctly stamped and are untouched. No other task content changed.
<!-- note:end t-044 -->
