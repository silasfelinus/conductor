# kindrobots-unraid — task history archive

Full `note:` prose for completed kindrobots-unraid tasks, moved out of `roadmap.yaml` so the
Kind Robots projection payload (`scripts/sync_kind_robots_projection.py`, hard limit
4,000,000 bytes) stays well clear of its ceiling. See conductor/t-158.

Each note below is the **verbatim** original — nothing is summarized or trimmed. The
`roadmap.yaml` task keeps its own first sentence plus a pointer here. The HTML comment
markers delimit each note exactly; `archive_done_task_notes.py --verify-only` re-extracts
every note and checks it byte-for-byte against what the roadmap recorded.

## t-003 — Tune ProxySQL and switch Kind Robots database traffic

<!-- note:begin t-003 -->
Completed the production cutover. ProxySQL uses backend hostgroup 10 with mariadb-kindrobots2:3306, a 40-connection backend cap, the Kind Robots application user and schema, monitoring, and verified frontend TLS. The certificate now identifies acrocatranch.com as well as the local endpoints. Vercel strict hostname verification passed, Prisma checked all 43 migrations with none pending, the final clean production deployment reached READY, the homepage returned HTTP 200, and a database-backed /api/dreams request returned HTTP 200 with live records. Kind Robots retains a two-connection application pool limit per Vercel runtime instance.
<!-- note:end t-003 -->

## t-005 — Normalize the existing PortOS Unraid deployment into the catalog

<!-- note:begin t-005 -->
Inspected silasfelinus/PortOS: it has no app-level Dockerfile (runs as a native Node/PM2 process per its CLAUDE.md single-user, multi-machine-federation model). The only Unraid-relevant piece is docs/features/network-postgres.md, which documents pointing PGMODE=network at a pgvector/pgvector:pg17 container (matching PortOS's own docker-compose.yml) so multiple PortOS machines can share one database. Packaged that as templates/portos-postgres.xml (mirroring proxysql.xml's shape: Host Port 5432, POSTGRES_DB/USER/PASSWORD config vars, masked password, persistent /mnt/user/appdata/portos-postgres/data path, Tailscale/LAN-only Requires note) plus docs/portos-postgres.md (install steps, per-machine .env wiring, quick checks, backup/upgrade policy with a dump-and-restore-before-major-version-bump warning since it can serve several machines at once). Added assets/portos-postgres-icon.svg and updated projects/catalog.yaml (portos: discovery -> template) and README.md. Also updated kindrobots-unraid's own in-repo projects/roadmap.yaml (app-packaging/portos-template: planned -> done) to keep both trackers in sync. Deliberately scoped to the database only -- containerizing the PortOS app itself (multi-service Node/PM2/browser-CDP packaging) is a separate, much larger effort not implied by this task's existing-Unraid-files framing. Verified: XML passes the same required-element/duplicate-Config checks as .github/workflows validate-catalog.yml; catalog.yaml and roadmap.yaml both parse clean with pyyaml.
<!-- note:end t-005 -->

## t-006 — Package persistent Kind Robots Nitro/API deployment for Unraid

<!-- note:begin t-006 -->
Design the long-term topology where Vercel serves frontend traffic and a stable Nitro/API service near MariaDB owns the controlled database pool.


Silas explicitly redirected this task in-session on 2026-08-08: Kind Robots is moving from Vercel to a full self-hosted Unraid production deployment at kindrobots.org. Package the complete Nuxt/Nitro app for Unraid, keep configuration editable through the Unraid Docker UI, load the repo .env instead of duplicating the full environment list, mount /mnt/user/pc/kindrobots/images for persistent media, and document the DNS plus Google OAuth cutover as human-gated follow-up steps. The old Vercel-frontend/local-API topology is superseded.

Implementation is ready for review across companion branches in silasfelinus/kind_robots and silasfelinus/kindrobots-unraid. The app container now builds on Node 24 without baking .env into the image, reads the host .env at runtime, persists the requested media directory, and keeps deployment overrides compatible with DockerMan. The catalog adds the Kind Robots user template and migration/cutover runbook. DNS, reverse-proxy publication, and Google OAuth account changes remain human actions and were not performed.

Completed and merged the full self-hosted Kind Robots packaging. silasfelinus/kind_robots#1637 merged as 63478f728c9ccfdf76ada097c7bf1283d26d493d after all 12 PR workflows completed successfully. silasfelinus/kindrobots-unraid#6 merged as 635bddc9f344b1b4ed3dbbcbb4eb51a1e292da3b after the Unraid catalog validator passed. The runtime now keeps .env out of the image, loads the existing host file at container startup, allows DockerMan variables to override deployment-specific values, persists /mnt/user/pc/kindrobots/images, and ships a DockerMan template/runbook. Actual Unraid installation, reverse-proxy publication, Google OAuth account changes, and DNS cutover remain human-gated deployment steps and were not performed.
<!-- note:end t-006 -->

## t-012 — Observe ProxySQL production pooling and record steady-state thresholds

<!-- note:begin t-012 -->
FOR SILAS: after normal production traffic has accumulated, capture stats_mysql_connection_pool plus frontend connection and query statistics from the ProxySQL admin interface. Record ConnUsed, ConnFree, ConnOK, ConnERR, Queries, latency, and failure behavior; then define alert and capacity thresholds. This is a soft gate because Alexandria's ProxySQL admin interface is intentionally available only through local docker exec and is not reachable by the Worker sandbox.


SENT BACK by silasfelinus via Kind Robots For You. You haven't given me information on this thread to do the work so I do not know how to get it.

Reconciliation (conductor scheduled sweep, 2026-08-10T11:47Z): claim from session
2026-08-10T092116Z-kindrobots-unraid-t012-q6n4 had gone stale (claimed 09:21Z, past
the 90-minute CLAIM_TTL_MINUTES with no further progress -- the claiming session
ended after the "SENT BACK" note above without releasing it). No agent sandbox can
do this work regardless of TTL: the ProxySQL admin interface genuinely requires
local docker exec access. Corrected status from stale `claimed` to `needs-human`
(soft) to reflect that reality accurately -- this was already effectively a soft
gate per this note's own first paragraph, just not reflected in `status`.

Silas supplied a fresh Alexandria ProxySQL admin snapshot after normal production traffic. Hostgroup 10 (mariadb-kindrobots2:3306) is ONLINE at 3 ConnUsed / 4 ConnFree, MaxConnUsed 18 against the configured 40-connection backend cap, 157763 queries, ConnERR 0, and measured backend latency 92us. Hostgroup 30 is also ONLINE with ConnERR 0. Global stats show 0 active transactions at capture, 0 server connections aborted, 0 delayed server connections, 0 ConnPool_get_conn failures, 0 killed backend connections/queries, and only 4 slow queries across 338077 Questions. The monitor reports 19233 successful / 0 failed connect checks and 19568 successful / 0 failed ping checks; the latest 20 connect checks are all successful (~350-533us) and latest 20 pings are all successful (~66-170us).
This establishes a healthy steady-state baseline with substantial connection headroom. Recommended operational thresholds: warn if hostgroup 10 ConnUsed sustains >=28/40 (70%) or MaxConnUsed reaches >=30 between restarts; critical if ConnUsed sustains >=36/40 (90%), any ConnERR/ConnPool_get_conn_failure begins increasing, or monitor connect/ping errors appear. Treat backend latency sustained above ~5ms as an investigation signal relative to this ~0.1ms local baseline rather than tuning from isolated query max-times. The 8 wrong-password/access- denied counters are authentication noise rather than pool-health failures; track only if they continue increasing unexpectedly.
Human action complete. No ProxySQL capacity change is warranted from this snapshot.
<!-- note:end t-012 -->

## t-013 — Publish Kind Robots production image to GHCR and automate Unraid updates

<!-- note:begin t-013 -->
Silas explicitly requested automatic deployment updates after moving kindrobots.org to Alexandria. kind_robots PR #1680 adds the GHCR publish workflow: every main push builds linux/amd64 and publishes latest plus immutable sha tags with OCI revision metadata. kindrobots-unraid PR #10 switches DockerMan to the GHCR image and documents Force Update, CA Application Auto Update, version inspection, and rollback. The first GHCR package is private by GitHub default. Human gate: after #1680 merges and the first package is published, change the kind_robots container package visibility to Public; then verify anonymous pull and merge catalog PR #10. Public package visibility is irreversible on GitHub, so the catalog PR remains unmerged until that action is done.

Silas explicitly confirmed the GHCR Kind Robots package was made public in-session. The companion Unraid catalog PR #10 is merged, so DockerMan can now consume ghcr.io/silasfelinus/kind_robots:latest without registry credentials.
<!-- note:end t-013 -->

## t-014 — INCIDENT: kindrobots.org has returned 502 since 13:25 UTC on 2026-09-04, right after the first auto-deploy of the day's Storybook image

<!-- note:begin t-014 -->
RESOLVED (recovery only -- approved_by_human intentionally left false; Silas has not reviewed this in a live session, see docs/state-reconciliation.md's incident-recovery closure pathway, which closes on met recovery criteria without requiring a policy decision). `https://kindrobots.org/` and `/api/health/database` both answered 200 as of this session's sweep (2026-09-04T18:2x UTC), and creation-burst art jobs queued against the live API completed successfully as late as 18:08:16Z (ArtImage 22381, entity ferrywake-cliffhanger-mooring) -- recovery happened sometime before 17:40 UTC based on the earliest post-outage ArtImage timestamp (22376, 17:40:49Z), narrowing the outage window to roughly 13:25:47-17:40 UTC (~4h15m), not self-clearing within an hour as an earlier, unrelated 502 had. Closing per this task's own TO CLOSE criteria (`/api/health/database` 200 again) rather than leaving a recovered incident open on unknown root cause. Root cause remains UNCONFIRMED -- this session has no Alexandria/ Unraid access to inspect `docker ps`/User Script output directly, so the container-recreate-without-restart theory in the original note is plausible but not verified. All 20 creation-burst ledger rows in projects/art-prompts.yaml confirmed art-attached via live API spot-check (artImageId/imagePath set on every entity, ids 22362-22381) and marked done in this same PR. FOR SILAS: if you want the outage mechanism actually confirmed (not just the symptom cleared), say so and a follow-up root-cause/prevention task can be filed -- see kindrobots-unraid roadmap notes_from_silas or the suggested external-health-probe idea in root TALKBACK.md 2026-09-04.
<!-- note:end t-014 -->

## t-015 — INCIDENT: kindrobots.org is returning 502 as of 02:56 UTC on 2026-09-08

<!-- note:begin t-015 -->
FOR SILAS: kindrobots.org is down. `https://kindrobots.org/` and `https://kindrobots.org/api/health/database` both answered 502 Bad Gateway on every attempt (7 total, spread over ~35 seconds via direct curl -- egress to this host is unrestricted from this sandbox, so this is a real reading of production, not a sandbox artifact) during this session's routine sweep, first observed 2026-09-08T02:5x UTC. Same signature as t-014 (2026-09-04, recovered on its own/via an unobserved fix in ~4h15m, root cause never confirmed -- container-recreate-without-restart was the leading but unverified theory). This session has no Alexandria/Unraid access to inspect `docker ps` or the Unraid UI directly, so it cannot attempt a Force Update or otherwise intervene -- only observe and report.
Timing note: several unrelated kind_robots PRs merged to main in roughly the same window this session observed (#2504-#2509, ~00:2x-02:5x UTC, none of them mine), including at least one (#2509, "Redesign Storybook setup...") still open as of this writing -- t-014's own leading theory was that an auto-deploy of a new image triggers this, so a recent/in-flight deploy is circumstantially plausible as the trigger again, but exactly as unconfirmed as t-014's original theory was. Do not treat this note as confirming which deploy (if any) caused it.
TO APPROVE: once `https://kindrobots.org/api/health/database` answers 200 again (check yourself, or ask the next session to confirm), set status: done and this session's completion criteria are met -- per docs/state-reconciliation.md's incident-recovery closure pathway, recovery can close on its own criteria without a live policy decision (approved_by_human may stay false, as t-014's did). If the outage clears before the next sweep, the next session should apply this same closure pattern itself rather than waiting for you.
RECOVERY CONFIRMED 2026-09-08 ~05:53 UTC (scheduled conductor sweep session): https://kindrobots.org/ and https://kindrobots.org/api/health/database both answered 200 cleanly on 10/10 attempts (5 each, spread over the session), after having been confirmed still 502 as recently as this same session's earlier facet-coverage check (~05:20 UTC). Total outage duration ~2h57m (02:56 UTC start per this task's own note -> ~05:53 UTC recovery), shorter than t-014's ~4h15m. Root cause remains unconfirmed, same as t-014 -- this session has no Alexandria/Unraid access to inspect docker ps or the Unraid UI directly. Per docs/state-reconciliation.md's incident-recovery closure pathway (and this task's own note: "If the outage clears before the next sweep, the next session should apply this same closure pattern itself"), closing now on the documented recovery criterion alone; approved_by_human intentionally left false, matching t-014's precedent. Recommend the standing follow-up (an external health-probe/alerting task) if Silas wants the recurring-outage mechanism actually root-caused rather than repeatedly observed-and- cleared.
<!-- note:end t-015 -->

## t-017 — PRODUCTION INCIDENT: kindrobots.org running with 1 failed + 1 unapplied migration since ~2026-09-12T11:45 UTC

<!-- note:begin t-017 -->
FOR SILAS: `https://kindrobots.org/api/health/database` has returned HTTP 503 (`"Database is reachable but the schema is not current for this build."`, `pendingMigrations: 1, failedMigrations: 1`) continuously from at least 2026-09-12T12:45 UTC through this sweep at 2026-09-12T20:27 UTC (8+ hours, still ongoing as of this note). First surfaced by `check_container_log_drift.py` reading today's Alexandria digest (a brand-new `[migration-drift]` signature on the `KindRobots` container, generated_at 2026-09-12T14:00 UTC) and independently confirmed by curling the health endpoint directly. t-016 (still `ready`, unclaimed) would have caught this the moment it started instead of it sitting undetected for 8+ hours -- worth prioritizing now that a third occurrence has happened, this time not self-clearing.
Likely cause: kind_robots#2662 ("storybook: server-side story runs on ending decks, for every shape", merged 2026-09-12T11:45:40Z) ships two migrations in one deploy -- `20260912120000` (additive) and `20260912123000`, which the PR's own description calls out as "the ONE non-additive step": it drops the global unique constraint on `LifeEnding.outcomeKey` in favor of a composite `(deckId, outcomeKey)` unique and makes `deckId` NOT NULL. If any existing `LifeEnding` row had a null `deckId` or the composite-unique backfill hit a real collision, that migration would fail exactly like this while the additive one either also failed or never got the chance to apply -- matching "1 failed, 1 pending." `docs/runbooks/migration-credential-boundary.md` (kind_robots) says the canonical `scripts/deploy-unraid.sh` path is supposed to leave the OLD container running if migration fails, so either that guard didn't hold here or the container was updated via a different path after a partial manual migration attempt -- worth checking Alexandria's Unraid User Scripts log for the actual deploy sequence around 11:45-12:45 UTC.
Effects observed this session: `auto-art-generate.yml`'s Kind Robots API health check has failed on every scheduled run since (10:31, 15:23, 20:26 UTC), which skips ArtJob repair, missing-project-art refresh, Mandarin Tutor corpus submission, and BOTH art-request/project-art-queue consumption steps every single run -- this is why cthulhuquarium/t-073's re-queued 32-fish art batch has sat un-submitted all day (separately noted on that task). Any other DB-backed API route on the live site is presumably degraded or erroring too, not just the art pipeline; this note only speaks to what this session could observe from outside.
TO APPROVE / TO FIX: this needs direct Alexandria access this sandbox does not have (no `MIGRATION_DATABASE_URL`, no Alexandria shell). From Alexandria, per the credential-boundary runbook: run `cd /mnt/user/appdata/kind_robots && bash scripts/deploy-unraid.sh` (the guarded path -- pulls latest, applies pending migrations via the protected migration lane, only then updates the container) rather than a bare Force Update. If `20260912123000` is genuinely failing on a data conflict (a pre-existing null `deckId` or duplicate `outcomeKey` under the new composite key), that needs a one-off data-repair `prisma db execute` under `MIGRATION_DATABASE_URL` before `migrate deploy` will succeed -- read the actual Prisma error first (the runbook's "Reading what is pending" section has the `_prisma_migrations` diagnostic query) rather than guessing further from here. Once the health endpoint returns 200 with `schemaCurrent: true`, set `approved_by_human: true` and `status: done` here (or note the real root cause if it differs from the guess above) -- t-016 stays `ready` as the follow-on so the next occurrence doesn't need a lucky sweep either.
Escalation update (scheduled conductor sweep, ~2026-09-12T23:30 UTC): the outage has gotten worse than the 503/schema-drift state described above. Both this session's own direct curl against https://kindrobots.org/api/health/database and https://kindrobots.org/ (repeated retries) now get a TLS-level connection reset (agent-proxy: "ws_closed_mid_exchange", 7 consecutive failures) rather than any HTTP response at all -- not a 503, no response at all. Independently corroborated by kind_robots#2686's own CI run (unrelated PR, still open) failing verifyPopulationDraftQuality.ts with a raw TypeError: fetch failed / ECONNREFUSED 75.111.66.70:443 at 2026-09-12T23:02:35Z, and verifyAcademyStarterManifest.ts failing the same way at 23:03:48Z -- ECONNREFUSED means the port actively refused the connection, not a timeout, so the service itself (or the box it is on) looks down, not merely degraded. Ruled out a sandbox-wide network problem: https://github.com and https://example.com both returned normally from the same shell in the same minute. This is now ~11-12 hours since the first 503 at ~11:45 UTC and the state has regressed rather than self-cleared. Not independently reconciled with Alexandria (no access from this sandbox) -- recorded here as corroborating evidence for whoever next has hands-on access, per t-016's still-open kaizen.
Recovery confirmed (scheduled conductor sweep, 2026-09-13): https://kindrobots.org/api/health/database now returns 200 {"success":true,"message":"Database is reachable and the schema is current for this build.","schemaCurrent":true,"databaseAheadMigrations":0} (checked 3x, ~2s apart, all 200). https://kindrobots.org/ also returns 200 with real SSR markup -- no TLS reset/ECONNREFUSED like the 2026-09-12T23:37Z escalation note described. Root cause confirmed (not just guessed): Silas merged silasfelinus/kind_robots@5ebaa76 ("fix(db): repair failed Storybook ending-deck migration", 2026-09-12T22:56:50Z) directly to main -- prepares LifeEnding's deck FK for the required-deck migration, adds the deck-scoped uniqueness constraint safely, repairs the already-failed migration, reorders the deploy to run migration-repair before deploy, adds a regression test for this exact failed- migration-recovery path, and replays real migration history against MariaDB in CI. This matches this task's original guess (kind_robots#2662's non-additive LifeEnding.outcomeKey/deckId migration) closely enough to count as confirmed, not speculative. Closing per docs/state-reconciliation.md's 'Closing human gates safely' criteria (Silas directly merged the fix; the task's own recovery bar -- 200 + schemaCurrent:true -- is met and verified) and CLAUDE.md's session-end rule against leaving a recovered incident open pending root-cause paperwork. approved_by_human intentionally left false, matching t-014/t-015 precedent for this incident class. t-016 (external health probe/alert) stays ready as the standing prevention follow-on -- third occurrence of this outage class, so worth prioritizing.
<!-- note:end t-017 -->

## t-018 — PRODUCTION INCIDENT: kindrobots.org returning 502 Bad Gateway as of ~09:29 UTC on 2026-09-13

<!-- note:begin t-018 -->
FOR SILAS: `https://kindrobots.org/` and `https://kindrobots.org/api/health/database` both returned a plain `HTTP/2 502` ("Bad Gateway", 11-byte body, no JSON) at this session's sweep, confirmed on 6 separate curls spread over roughly 90 seconds (09:29:53 UTC through the final recheck) -- a sustained outage, not a single blip. This is the 502-Bad-Gateway signature (matching t-014 2026-09-04 and t-015 2026-09-08), NOT the 503-schema-drift signature t-017 just closed as recovered earlier today (2026-09-13T00:32 UTC) -- so this is a fresh, fourth occurrence of the outage class, not a recurrence of t-017's migration issue. `check_project_scaffold_drift.py` and `check_live_facet_coverage.py` both failed outright on the same 502 this session (every Kind Robots API read attempted came back 502).
TO APPROVE / TO FIX: this needs direct Alexandria/Unraid access this sandbox does not have. Per the t-014/t-015 precedent, the leading (still unconfirmed) theory is a container-recreate-without-restart on auto-deploy; check the Unraid User Scripts log and `docker ps` for the KindRobots container's actual state, and use `cd /mnt/user/appdata/kind_robots && bash scripts/deploy-unraid.sh` (the guarded migration-safe path) rather than a bare Force Update if a restart is needed. Once `https://kindrobots.org/api/health/database` returns 200 again, set `status: done` here (`approved_by_human` may stay `false` per the t-014/t-015 recovery-only precedent if no live policy decision was made) -- t-016 (external health probe/alert, still `ready`/unclaimed) remains the standing prevention follow-on; this is its fourth unrelated occurrence (t-014, t-015, t-017, this one) sitting undetected until a scheduled sweep happened to notice, worth prioritizing.
RECOVERED: confirmed by this session at 2026-09-13T11:27:53Z UTC -- https://kindrobots.org/ and /api/health/database returned sustained healthy 200s (8 consecutive checks over ~20s, schemaCurrent: true, latencyMs 3-4) after the earlier 502 outage. approved_by_human left false per t-014/t-015 recovery-only precedent (no live policy decision made). Root-cause prevention remains tracked separately at t-016 (external health probe/alert, still ready/unclaimed) -- this is its fourth unrelated occurrence.
<!-- note:end t-018 -->

## t-019 — HARDWARE: Alexandria reported Machine Check Events (mcelog) -- identify the failing part and check it against the four unexplained outages

<!-- note:begin t-019 -->
FOR SILAS: Unraid notified you that Alexandria detected Machine Check Events and logged mcelog output. A triage runbook is at projects/kindrobots-unraid/docs/t-019-mce-triage.md -- it has the exact commands to run (none of them print a credential), a table for reading the result, and the one comparison worth doing first.
WHY THIS IS NOT ROUTINE: an MCE is the CPU reporting a hardware fault -- bad RAM, a failing memory controller, or a CPU/bus error. This host has closed FOUR production incidents in nine days with root cause unconfirmed (t-014 2026-09-04 502, t-015 2026-09-08 502, t-017 2026-09-12 failed migration, t-018 2026-09-13 502), plus healthcheck.ps1 stopping silently on 2026-09-01. Until now the only theory was container-recreate-without-restart, never verified. A failing DIMM would explain all of it, and would explain a mid-flight schema write failing (t-017) better than the container theory does.
TO ACT: (1) capture the evidence BEFORE any reboot -- Unraid keeps /var/log in RAM unless syslog mirroring to flash is on, so a restart erases the record; (2) run the step 1-2 commands in the runbook to get the MCE class and which DIMM slot; (3) compare the MCE timestamps (local PDT) against the incident windows in the runbook's step 4 table (UTC). Correlation there means the recurring-502 mystery is hardware, and t-016's external probe drops from lead to nice-to-have; no correlation rules hardware out, which is also worth knowing.
DO NOT post the Unraid diagnostics zip to the forums as the notification suggests without opening it first -- the bundle carries your dockerMan template XMLs, which hold container env VALUES (MariaDB credentials, KR_API_TOKEN). The forum only needs the mcelog/dmesg lines, the per-DIMM counters, and your CPU/board model; runbook step 5 lists exactly what is safe to paste.
Blocked here only because no agent session has shell or UI access to Alexandria -- soft gate, other work continues. Set status: done once the part is identified and either replaced/scheduled or confirmed a benign one-off, and record in this note whether the timestamps correlated (that answer feeds t-016 and the open root-cause question).
RESOLVED 2026-09-13, same session -- Silas ran the diagnostics and the MCE is benign. Full decode in projects/kindrobots-unraid/docs/t-019-mce-triage.md (RESULT section). Summary: one event, Sat 2026-09-12 15:44:38 PDT, Bank 5 = SMCA_EX (Execution Unit, not memory -- IPID 500b000000000 gives HWID 0xB0 / McaType 0x5). Status bea0000000000108 sets PCC, but TSC 0 means it was read out by the boot-time machine_check_poll() of all banks rather than raised as a live exception, and EDAC initialises 45s after it, placing it ~1 min into a boot. A genuine uncorrected PCC error would have panicked the box. This is the widely-reported spurious Zen 2/Zen 3 boot signature. CPU decodes as AMD family 0x17 model 0x71 (Matisse / Ryzen 3000).
CORRELATION: NEGATIVE. Nothing lands in an incident window; nearest is t-017 at ~11h earlier on a previous boot. Stronger: dmesg covers the current boot (started ~15:43 on 09-12) and therefore contains t-018 in full, with no MCE anywhere near 09-13 02:29 PDT. Machine checks did not cause t-018. For t-014, t-015, t-017 and the 09-01 healthcheck stop the evidence is unrecoverable (see t-020).
The container-recreate-without-restart theory is NOT confirmed by this -- it is just no longer competing with a hardware-MCE explanation. What the diagnostics did expose is three real blind spots, now tracked at t-020.
<!-- note:end t-019 -->

## t-021 — ComfyUI render box down again: CLIPTextEncode "hostbuf_file_reader_read failed" across every project

<!-- note:begin t-021 -->
FOR SILAS: the render box is down again -- confirmed live this session via `python scripts/check_render_box.py` (exit 1: "36 render(s) failed and none completed in the last 6h") and a direct `GET /api/art/queue/stats` pull (132 FAILED in the last 24h; the 25 most recent failures are 100% the identical `node 3 (CLIPTextEncode): hostbuf_file_reader_read failed` signature, hitting cthulhuquarium and coloring-book jobs indiscriminately). This blocks ALL art rendering across every project, not just one -- three coloring-book/t-022 render requests this session (mr-001, mr-013, mr-023) failed with this exact error and are correctly left `status: pending` rather than forced through.
This is the same hardware signature ai-art-academy/t-067 and t-068 diagnosed and fixed (a failing SATA cable on Alexandria's disk13, swapped, error count confirmed dropped to 0) -- and then t-077 and t-078 each caught it recurring roughly 24h-ish later, same box, same symptom. ai-art-academy is now `finished` so this incident has no active project home; filing here since kindrobots-unraid is the active infra project that owns Alexandria. Full diagnostic trail is in that project's now-closed roadmap history (search `hostbuf_file_reader_read`) if useful context.
TO APPROVE / TO DO: this needs physical hands on Alexandria -- reseat or re-swap the disk13 cable/drive, or check whichever drive is now exhibiting the identical symptom (the prior fix may not have held, or a sibling drive/cable in the same array is failing the same way). No sandbox can check hardware. Once fixed, nothing else needs to be re-run by hand -- every project's pending render queue (this task's three coloring-book entries included) drains on its own once the box is healthy; confirm with `python scripts/check_render_box.py` (should exit 0).
Separately, NOT blocking on this and filed as its own task: conductor/t-156 tracks that `scripts/recheck_render_queue.py` classified this exact live outage as "healthy" (see RENDER-BACKLOG.md's 2026-09-14 entry from this session) even with 25/25 recentFailed saturated by a known-bad signature, while `scripts/check_render_box.py` correctly caught it as DOWN -- the two scripts disagree and the ledger-writing one is the one that's wrong.
UPDATE 2026-09-14 (session review of merged kind_robots#2719/conductor#4314): a possible SOFTWARE contributor was found and may be worth trying before -- or alongside -- the physical disk13 re-check. Ferngrotto's live D:\comfy\comfy-fast\extra_model_paths.yaml maps the local clip/text_encoders keys to models/text_encoders, but the 4.88 GB Qwen/Krea text encoder actually lives at models/clip on that box. That makes the local encoder invisible to ComfyUI and lets it fall through to the Alexandria SMB copy instead -- which is exactly the code path that produces hostbuf_file_reader_read failed when Alexandria's share/disk is degraded. If that path mapping is the real trigger, fixing it would stop new jobs from touching the flaky share at all, independent of whether disk13's hardware is still marginal.
TO DO (new, optional, low-risk): on Ferngrotto, run scripts/repair-comfy-local-paths.ps1 from kind_robots main (merged). It only edits extra_model_paths.yaml after writing a timestamped backup and verifying the file's shape; it does not restart ComfyUI or touch anything else. Review its printed diff, then restart ComfyUI yourself if it looks right. This does not replace the disk13 hardware check above -- both are worth doing.
Separately, scripts/check_hostbuf_failure.py (conductor#4314, merged) now distinguishes a truly recovered incident (a render completed after the last hostbuf failure) from one that merely aged out of the old 2h alert window, so the hourly sentinel will stop going quiet on an incident that never actually resolved.
RECOVERED (scheduled Conductor session, 2026-09-14T10:29Z): render box confirmed back up -- check_render_box.py exit 0 ("render box UP ... Engine heartbeat: Comfy healthy 0.8 minutes ago"), and check_hostbuf_failure.py independently confirmed genuine recovery (a real DONE render at 2026-09-14T10:28:49Z landed after the latest hostbuf failure at 2026-09-14T08:35:46Z, not just an aged-out alert window). Closing per this task's own stated recovery bar ("confirm with check_render_box.py, should exit 0") and the state- reconciliation policy that an unknown root cause does not keep a recovered incident open. Root cause is NOT confirmed fixed -- this is the third recurrence of the same disk13/cable hostbuf signature (ai-art-academy/t-067/t-068 first fixed it, t-077/t-078 caught two recurrences ~24h apart each) and the possible ComfyUI local-path software contributor documented in this note (Ferngrotto extra_model_paths.yaml) has not been confirmed applied or ruled out either. If it recurs again, re-open a fresh task rather than reusing this one, and check both the physical disk13/cable path and the Ferngrotto path-mapping fix before assuming hardware alone.
<!-- note:end t-021 -->
