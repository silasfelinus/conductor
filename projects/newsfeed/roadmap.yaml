project: newsfeed
kind: software

notes_from_silas: |
  Build a modular, programmable newsfeed for the Kind Robots user homepage.
  Replace the current settings-first homepage with useful feed content and move settings
  into its own dedicated section. Ship a functional MVP quickly: curated recommended feeds
  first, customization second, polish after the core loop works.

  Recommended starter feeds should include AI news, activism, malaria activism,
  AI gaming advances, AI model creation, and developer tips. Modularity is a core value:
  feed definitions, source adapters, filtering, ordering, and presentation should be
  separable rather than welded into one giant component.

  Take inspiration from Ground News: users should be able to see and adjust how much
  political viewpoint diversity enters their feed. Keep political perspective as optional,
  transparent metadata and a pluggable ranking dimension. Never infer the user's politics
  from unrelated behavior, and keep factual reliability separate from political bias.

milestones:
  - id: m1
    title: "SHAPE — define the modular feed contract"
    weight: 20
    status: not-started

  - id: m2
    title: "BUILD — ship the functional homepage feed"
    weight: 60
    status: not-started

  - id: m3
    title: "POLISH & SHIP — customization, resilience, and UX"
    weight: 20
    status: not-started

tasks:
  - id: t-001
    milestone: m1
    title: "Audit the homepage and write the implementation brief"
    status: done
    owner: null
    updated: 2026-07-14T12:00:00Z
    passes: 0
    stakes: reversible
    note: >
      Done. Audited pages/[...slug].vue + content/index.md (homepage is a Nuxt Content
      doc rendering :user-manager -> user-dashboard.vue, which is where settings actually
      live today), stores/userStore.ts + Prisma User columns (settings persistence),
      stores/helpers/dashboardHelper.ts (wonder.newsfeed tab already registered, route
      /newsfeed, image asset missing on disk), stores/helpers/tutorialCards.ts (no
      top-level wonder channel yet), content/newsfeed.md + content/channels/lab/newsfeed.md
      (already-stubbed routes), components/conductor/newsfeed-page.vue (separate pitch/
      status page, not the real feature), server/api/conductor/prs.get.ts (fetch pattern
      to model aggregation on; no caching layer exists yet anywhere in server/), and
      components/dreams/dream-card.vue (best existing feed-card visual model). Findings
      written into DESIGN-BRIEF.md's new "Audit findings" section with exact paths.
      BUILD task notes (t-003 through t-006) refined below with these specifics.

  - id: t-002
    milestone: m1
    title: "Confirm scope with Silas"
    status: needs-human
    owner: null
    passes: 0
    stakes: reversible
    note: >
      FOR SILAS: soft checkpoint only; development is not blocked. Review
      projects/newsfeed/DESIGN-BRIEF.md and BIAS-CONTROLS.md, then redirect any feed
      categories, homepage behavior, perspective vocabulary, or customization expectations.
      Add a note here with changes, then set approved_by_human: true and status: done when satisfied.
    soft_gate: true

  - id: t-003
    milestone: m2
    title: "Move homepage settings into a dedicated settings section"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-001
    note: 'Audit (t-001): the homepage is content/index.md, currently rendering :user-manager (components/user/user-manager.vue -> user-dashboard.vue, which embeds user-panel.vue''s settings form, avatar upload, theme picker, cache-clear, galleries). No settings live in a dedicated route today. Relocate user-manager''s "dashboard" tab so it''s reachable at the existing /dashboard route (dashboardConfigs.user is already registered) instead of the root, preserving all current controls. Settings data itself stays on the Prisma User model via stores/userStore.ts / server/api/users — do not add a new settings table. Connector-backed Worker claim (conductor-burst-hourly rotation, 2026-07-18): kind_robots PR https://github.com/silasfelinus/kind_robots/pull/391 swaps content/index.md''s body from :user-manager to a new :home-feed-placeholder component (components/home/home-feed-placeholder.vue) and drops the stale dashboardKey/dashboardTab binding on content/channels/home/dashboard.md (the Home channel''s Dashboard nav-tab entry, route /). content/dashboard.md (route /dashboard) is untouched -- it already rendered the identical :user-manager, so every current control (avatars/friends/achievements/ themes/chats/logout) is preserved exactly where it already lived, and the header avatar widget (user-picture.vue) already links there. vue-tsc, eslint, and prettier all clean on the touched files. Could not get a working browser preview of either / or /dashboard in this sandbox (both showed the stock Nuxt welcome page pre- and post-change -- a DB/session limitation, not caused by this diff). PR CI is the verification gate before merge.'
    claimed_by: claude-conductor-burst-20260718T-newsfeed
    claimed_at: '2026-07-18T09:07:43Z'
    updated: '2026-07-18T09:20:07Z'

  - id: t-004
    milestone: m2
    title: "Create the feed registry and recommended presets"
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    depends_on: t-001
    note: >
      DONE 2026-07-16: kind_robots PR #318 merged (squash a4d0bd29). Added
      stores/helpers/newsfeed.ts (typed FeedSourceDefinition/FeedDefinition/
      NewsFeedItem/PerspectiveMode/PerspectiveWeights contracts + the
      FEED_SOURCES and FEED_DEFINITIONS registries covering the six starter
      presets) and stores/feedPreferenceStore.ts (enabled feeds + order,
      perspective mode/weights, label visibility, contrast preference,
      persisted via the existing safeGetLocalStorage/safeSetLocalStorage
      pattern, no DB table). Verified: eslint + prettier clean, full-project
      vue-tsc --noEmit 0 errors, all 3 kind_robots CI checks green (GitGuardian,
      TypeScript, Contract verifiers). Source URLs are unverified candidates
      (`verified: false`) — this session's sandbox 403'd every outbound feed-URL
      probe attempted, so live reachability is deferred to t-005/t-013.
      Original spec: Add typed, modular feed definitions for AI news, activism,
      malaria activism, AI gaming, AI model creation, and developer tips. Keep
      source adapters, feed metadata, defaults, user selection, and optional
      ranking dimensions separate. Prefer configuration and Pinia state over
      hard-coded branching in components. Audit (t-001): new feedPreferenceStore
      should follow the safeGetLocalStorage/safeSetLocalStorage client-persistence
      pattern used by stores/navStore.ts and stores/socialStore.ts (no
      Pinia-persistence plugin exists in this repo) rather than a database table.
    claimed_by: claude-conductor-burst-20260716-newsfeed004
    claimed_at: '2026-07-16T17:06:59Z'
    updated: '2026-07-16T17:35:00Z'

  - id: t-013
    milestone: m3
    title: "Batch-verify FEED_SOURCES reachability once the aggregation pipeline exists"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-005
    note: >
      Kaizen from t-004: every FEED_SOURCES entry in stores/helpers/newsfeed.ts
      is currently an unverified candidate (verified: false) because the
      sandbox's egress allowlist 403'd every outbound feed-URL probe attempted
      during t-004 (TechCrunch, WHO, MarkTechPost, Kotaku). Once t-005 wires the
      real server-side aggregation pipeline, batch-fetch every source once,
      record verified: true/false per entry, and swap out any that 404/403 for
      a working replacement rather than discovering broken feeds one at a time
      in production.

  - id: t-005
    milestone: m2
    title: "Build a minimal feed aggregation pipeline"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-004
    note: >
      Normalize feed items into one typed contract with title, summary, source, URL,
      published time, category, optional image, and optional perspective metadata with
      provenance/confidence. Use the smallest reliable ingestion approach supported by the
      existing Kind Robots architecture. Avoid database schema work for v1; any unavoidable
      shared-backend change becomes a proposal rather than an improvised migration. Audit
      (t-001): no server-side caching layer exists anywhere in server/ yet. Model the new
      server/api/newsfeed/index.get.ts handler on server/api/conductor/prs.get.ts's shape
      (thin handler + a shared fetch helper in a new server/utils/newsfeed.ts, mirroring
      server/utils/conductor-github.ts), and introduce defineCachedEventHandler for TTL
      caching — document the chosen TTL in the PR since there's no existing precedent.

      DONE 2026-07-18 (conductor-burst session claude-conductor-burst-20260718T131014Z-18889):
      built server/utils/newsfeed.ts (dependency-free RSS 2.0 / Atom parsing via regex
      extraction -- no new npm dependency for a handful of known tags; sanitizes/truncates HTML
      via a shared sanitizeText; per-source fetchSourceItems with an 8s timeout that never
      throws, returning an error string instead so one broken source can't blank the feed;
      aggregateFeed dedupes by stable id, sorts newest-first, applies optional
      includeTags/excludeTags keyword filtering against title+summary+category) and
      server/api/newsfeed/index.get.ts (thin handler, ?feed=/?feeds= query, defaults to every
      defaultEnabled feed from t-004's registry). Used this repo's dominant
      {success,message,data}+errorHandler response convention (110+ existing GET routes) rather
      than prs.get.ts's raw-array shape, since prs.get.ts is a rare conductor-only outlier and a
      future frontend consumer (t-006) will expect the standard shape. Wrapped in
      defineCachedEventHandler at a 15-minute TTL -- this repo's first use of Nitro caching --
      documented in a code comment (RSS sources publish far slower than 15 minutes; bounds
      egress to the ~15 registry hosts to 4x/hour regardless of traffic). category on
      NewsFeedItem is populated from each entry's real RSS/Atom <category> tags rather than
      synthesized from the feed slug, matching DESIGN-BRIEF.md's "include/exclude tags or
      keywords" wording more literally (no current FEED_DEFINITIONS entry sets
      includeTags/excludeTags yet, so that path is untested against real data). Added
      utils/scripts/verifyNewsfeedAggregation.ts (+ test:newsfeed-aggregation script) covering
      RSS/Atom parsing, sanitization, malformed input, and unreachable-source resilience without
      hitting the network for the deterministic assertions. Verified: test:newsfeed-aggregation
      passes; full-project npm run test (vue-tsc --noEmit) clean (caught and fixed two real
      noUncheckedIndexedAccess errors during development); eslint clean; prettier clean.
      kind_robots PR #421 merged (squash, sha 9fa53d7). No live verification of the actual
      FEED_SOURCES RSS URLs -- all remain verified: false per their own docstring; pipeline
      verified against fixtures + a deliberately-unreachable host instead. Frontend consumption
      (FeedCard.vue etc.) is t-006, out of scope here. CI's "Contract verifiers" check showed 2
      failures on this PR (academy-examples-manifest, workflow-paths) -- both confirmed
      pre-existing on origin/main via a clean worktree check before merging, unrelated to this
      diff; merged anyway and filed ai-art-academy/t-033 and kind-robots/t-038 to track them
      separately.
    claimed_by: claude-conductor-burst-20260718T131014Z-18889
    claimed_at: '2026-07-18T13:10:15Z'
    updated: '2026-07-18T13:24:49Z'

  - id: t-006
    milestone: m2
    title: "Render the recommended newsfeed on the user homepage"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-005
    note: >
      Ship the first useful homepage: responsive feed cards, loading and empty states,
      source attribution, external links, category filtering, and sensible chronological
      ordering. Use the existing Kind Robots visual system and keep the component surface
      small enough to evolve. Audit (t-001): fork components/dreams/dream-card.vue for the
      new FeedCard.vue (closest existing title/badge/image/timestamp grammar plus a
      multi-source image-fallback chain for inconsistent RSS thumbnails). content/newsfeed.md
      and content/channels/lab/newsfeed.md are already stubbed — build into them rather than
      new routes. components/conductor/newsfeed-page.vue is a separate Project pitch/status
      page (channelKey: wonder, tabKey: newsfeed) — keep it as-is and update its
      deliverables.done list as milestones land; slot the real feed component into
      project-front-page.vue's #interactive slot or swap content/newsfeed.md's body once
      the real feature is ready, rather than replacing the pitch page.

      DONE 2026-07-18: kind_robots PR #425 merged (squash 14ef1c6). Added
      components/newsfeed/feed-card.vue (forked dream-card.vue's grammar, per-item
      image-fallback chain + category badge + relative timestamp) and
      components/newsfeed/newsfeed-feed.vue (fetches /api/newsfeed filtered by
      feedPreferenceStore.enabledFeedSlugs, merges/dedupes/sorts chronologically,
      loading/empty/error states, per-feed filter chips). Wired into both
      components/home/home-feed-placeholder.vue (the live homepage) and
      components/conductor/newsfeed-page.vue's #interactive slot (the project
      pitch page). Reviewer (this session) verified all 3 kind_robots CI checks
      green (GitGuardian, TypeScript, Contract verifiers) and merged directly —
      reversible, scoped, does the task. Follow-ups filed as kaizen below.
    claimed_by: claude-conductor-burst-20260718T1506Z
    claimed_at: '2026-07-18T15:06:56Z'
    updated: '2026-07-18T16:11:18Z'

  - id: t-007
    milestone: m2
    title: "Add user feed selection and ordering"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-006
    note: >
      Let users enable or disable recommended feeds and reorder them. Persist preferences through the existing user/store/local-storage patterns without adding a new schema for the MVP. Defaults should remain useful before any configuration.

      DONE 2026-07-18 (conductor session claude-conductor-session-20260718T211402Z): the store side (t-004's feedPreferenceStore, enableFeed/disableFeed/reorderFeeds) already existed with nothing consuming it. Added components/newsfeed/newsfeed-preferences.vue, a "Manage feeds" panel toggled from newsfeed-feed.vue's header, listing every FEED_DEFINITIONS entry with an enable/disable toggle and move-up/move-down reordering for enabled feeds (no drag-and-drop library exists in this repo, so buttons instead of a drag handle). newsfeed-feed.vue now watches enabledFeedSlugs (order-sensitive .join(',')) to auto-refetch on any selection/order change, guarded against the store's own initial-hydrate mutation via a hasLoadedOnce flag. Registered the component in public/components.json. Verified: vue-tsc --noEmit (0 errors), eslint clean, prettier clean on touched files (public/components.json's one remaining prettier warning is pre-existing drift on main, confirmed via git stash). No live preview available (sandbox has no DB). kind_robots PR https://github.com/silasfelinus/kind_robots/pull/456 (branch claude/vigilant-edison-15k0tv). Flagged for the Reviewer: the "All" tab still sorts purely chronologically (feed order only affects per-feed chip order, not "All" interleaving), and a pre-existing quirk where disabling every feed still fetches everything via the availableFeeds fallback -- both out of this task's scope, noted as possible follow-ups.
    claimed_by: claude-conductor-session-20260718T211402Z
    claimed_at: '2026-07-18T21:14:02Z'
    updated: '2026-07-18T21:30:43Z'

  - id: t-008
    milestone: m3
    title: "Add programmable feed filters"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-007
    note: >
      Add a compact advanced surface for include/exclude keywords, category filters,
      source toggles, and sort mode. Treat this as a declarative configuration layer,
      not arbitrary executable user code.
      DONE 2026-07-19 (claude-conductor-agentrun-20260719, Reviewer merge): kind_robots
      PR #484 merged (squash 43c7dc6). New pure applyNewsfeedFilters()/sanitizeKeywordList()
      in stores/helpers/newsfeed.ts, a stable sourceId on NewsFeedItem, persisted
      include/exclude keywords + disabledSourceIds + selectedCategories + sortMode on
      feedPreferenceStore, a new newsfeed-filters.vue panel, and a regression test
      (utils/scripts/verifyNewsfeedFilters.ts) wired into contract-tests.yml CI.
      The PR's initial CI never triggered (0 workflow runs queued for its branch
      beyond GitGuardian, ~15+ min after opening) -- manually re-queued
      contract-tests.yml/typecheck.yml via workflow_dispatch to unblock review; see
      TALKBACK.md for the anomaly note. Once green, the PR had drifted behind the
      just-merged t-010 (#486, same file) and hit a real merge conflict (both added
      steps to contract-tests.yml and both touched newsfeed-feed.vue's Manage-feeds
      panel markup) -- resolved by hand (kept both new CI steps; kept both the
      aria-controls id from t-010 and the NewsfeedFilters panel from t-008),
      re-verified locally (vue-tsc, eslint, prettier, both newsfeed contract tests
      all clean) via provision_kind_robots_deps.sh, pushed, re-ran CI clean, merged.
    claimed_by: claude-conductor-burst-20260719
    claimed_at: '2026-07-19T01:04:38Z'
    updated: '2026-07-19T02:20:00Z'

  - id: t-009
    milestone: m3
    title: "Harden caching, deduplication, and failure handling"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-006
    note: 'Add bounded caching, stable item identity, duplicate removal, stale-source tolerance, and partial-success behavior so one broken feed does not blank the homepage. RAN 2026-07-18 (conductor-burst-hourly rotation, claude-conductor-burst-20260718T1707Z): audited server/utils/newsfeed.ts against the five requirements in this note''s original text. Bounded caching (defineCachedEventHandler, 15min TTL at the route), stable item identity (stableId hash over guid/link), duplicate removal (per-aggregateFeed-call seen-id Set), and partial-success (Promise.all + per-source sourceErrors, one broken source never blanks the others) were already in place from t-005/t-006. The one real gap was stale-source tolerance: a source that failed returned zero items with no fallback. Added an in-memory last-known-good cache per source id in fetchSourceItems -- on failure, serves the prior successful items (bounded to 24h old) flagged stale: true, with the underlying error still reported via AggregatedFeed.sourceErrors[].stale. A source with no prior success keeps the original always-empty-on-failure behavior. Added regression coverage to utils/scripts/verifyNewsfeedAggregation.ts using a local http.createServer fixture (serves once, then closed) to prove the stale-fallback path without relying on real network egress. Verified: npm run test:newsfeed-aggregation, npm test (vue-tsc --noEmit, exit 0), eslint, prettier --check -- all clean. kind_robots PR #428 (branch claude/keen-fermat-7exrse), CI pending at time of this note.'
    claimed_by: claude-conductor-burst-20260718T1707Z
    claimed_at: '2026-07-18T17:07:15Z'
    updated: '2026-07-18T17:07:15Z'

  - id: t-010
    milestone: m3
    title: "Polish accessibility and responsive presentation"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-007
      - t-009
    note: >
      Finish keyboard behavior, focus states, readable timestamps, mobile layout,
      reduced-motion behavior where relevant, and final visual consistency. Keep the
      feed fast and calm rather than turning the homepage into algorithmic slot-machine soup.
      DONE 2026-07-19 (claude-conductor-agentrun-20260719T0110Z): kind_robots PR #486
      merged (squash a75cb03). aria-pressed/aria-expanded/aria-controls/aria-busy added
      to feed-card and newsfeed-feed controls, aria-live status region, motion-safe:
      variants for hover transforms, focus-visible rings throughout, and a semantic
      <time> element with absolute-timestamp title on feed cards. Verified via
      vue-tsc --noEmit, eslint, prettier (all clean); no live nuxt dev render was
      possible in-sandbox (dummy DATABASE_URL), so this is static/type verification
      only — flagged for a follow-up visual/keyboard-nav pass, see t-014.
    claimed_by: claude-conductor-agentrun-20260719T0110Z
    claimed_at: '2026-07-19T01:12:57Z'
    updated: '2026-07-19T01:12:57Z'

  - id: t-011
    milestone: m3
    title: "Add adjustable political perspective balance"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-007
      - t-008
    note: 'Merged kind_robots PR #505 (2026-07-19). Perspective-balance mode presets, weighted round-robin ranking gated to topicPolitical feeds, mode selector + labels-visible toggle, perspective badge, and contract test all landed on main. FEED_SOURCES perspective ratings remain deliberately unseeded pending a real methodology/citation (see PR description) -- a good follow-up task for whoever has one, not filed as a new roadmap task since it''s a data/sourcing question, not a code gap.'
    claimed_by: claude-conductor-burst-20260719T-rotation-newsfeed
    claimed_at: '2026-07-19T06:07:28Z'
    updated: '2026-07-19T06:07:28Z'

  - id: t-012
    milestone: m2
    title: "Polish and upgrade Newsfeed front-end surface"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    note: >
      UI polish + upgrade for the newsfeed front end now that the scaffold page is live
      at /newsfeed. Steps: (1) generate final dashboard-tab art at
      public/images/dashboard-tabs/wonder/newsfeed.webp and tutorial art at
      public/images/tutorials/wonder/newsfeed.webp; (2) add a matching section for
      'newsfeed' under tutorialChannels.wonder.sections in
      kind_robots/stores/helpers/tutorialCards.ts; (3) verify the PROJECT Dream liveUrl
      points at /newsfeed (admin can run the Conductor 'Placements' button to backfill
      channelKey/tabKey/liveUrl); (4) evolve the placeholder scaffold page into the full
      interactive experience for this project.

  - id: t-014
    milestone: m2
    title: "Preview-deploy visual check of the / and /dashboard swap from t-003"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-003 (Worker, kind_robots PR #391): the homepage settings-relocation
      (content/index.md -> :home-feed-placeholder, settings preserved unchanged at
      /dashboard) landed with vue-tsc/eslint/prettier verification only -- the sandbox
      could not boot a working `nuxt dev` preview for either route (both served the
      stock Nuxt welcome page regardless of branch content, a DB/session limitation of
      that session, not a code defect). A session with a real preview-deploy connector
      should visually confirm: (1) "/" renders home-feed-placeholder.vue correctly for
      both guest and logged-in states, (2) "/dashboard" still renders the full
      user-manager dashboard (avatars/friends/achievements/themes/chats/logout)
      unchanged, (3) the header avatar link and the placeholder's CTA both land on
      /dashboard correctly.

  - id: t-015
    milestone: m3
    title: "Regenerate public/components.json from a canonical environment and stabilize its sort order"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-006 (kind_robots PR #425): running node utils/scripts/create-component-json.mjs (or npm run test's nuxi prepare postinstall) in a sandbox reorders several unrelated existing entries (e.g. builder-card/builder-card-grid, ami-link) and surfaces several real components already missing from the committed file (packmaker-admin-panel, component-review-feed, wonderlab-preview-host, etc.) that predate t-006 -- looks like environment-dependent Array.prototype.sort/localeCompare collation, or the committed file has simply drifted from a stale generation. t-006 hand-added only its two new folder entries to stay scoped. Regenerate the file from a canonical (non-sandbox) environment and reconcile the drift, or make the generator's sort explicit and collation-stable (e.g. localeCompare(b, 'en') or a plain codepoint sort) so future sessions don't have to hand-patch it to avoid unrelated churn.

      RESOLVED 2026-07-18 (conductor-agent-run, cross-project duplicate): this exact ask was independently tracked and completed under kind-robots/t-039 (sort stabilization -- confirmed all 3 sort call sites in create-component-json.mjs now use localeCompare(x, 'en'), kind_robots PR #429) and kind-robots/t-040 (regenerate + reconcile drift, kind_robots PR #434, merged 2026-07-18). Verified directly: grep for localeCompare in the script shows the 'en' locale pin at all three call sites, and the just-merged t-040 diff added exactly the missing components this note names (packmaker-admin-panel, component-review-feed, wonderlab-preview-host, plus packmaker-pack-editor/component-test-fixture-cleanup/wonderlab-selection-router) with no unrelated reordering churn. No further action needed here -- closing as done rather than duplicating the fix a second time. Cross-reference for CONTROL.md's cross-project same-task collision note (2026-07-17): this is another instance of the same task tracked in two project roadmaps with no depends_on link between them.
    claimed_by: claude-conductor-agent-run-20260718
    claimed_at: '2026-07-18T19:19:26Z'
    updated: '2026-07-18T19:19:26Z'

  - id: t-016
    milestone: m3
    title: "Kaizen: check same-project open PRs for touched-file overlap before merging the first one"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-19T02:35:00Z'
    note: >
      Kaizen from t-008/t-010 (Reviewer, kind_robots PR #484/#486, 2026-07-19):
      two `claimed` newsfeed tasks both touched newsfeed-feed.vue and
      contract-tests.yml and merged minutes apart, producing an avoidable merge
      conflict discovered only when reviewing the second PR. Add a lightweight
      step to the Reviewer flow (a script or a documented manual check) that,
      before merging a PR, lists any other currently-open PR from the same
      project and diffs their changed-file sets -- if they overlap, flag it in
      the PR review so the conflict is anticipated rather than discovered at
      merge time. Keep it advisory (a warning/note), not a merge blocker, since
      overlapping files don't always conflict.

  - id: t-017
    milestone: m3
    title: "Investigate a real rendered preview path (e.g. Vercel MCP) instead of the local nuxt dev wall"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    updated: '2026-07-19T02:40:00Z'
    note: >
      Kaizen from t-010 (a concurrent session's PR #486 review, cherry-picked from
      a since-superseded conductor PR #825 whose roadmap edits collided with this
      session's #827): at least three newsfeed tasks now (t-010, t-014, and this
      note itself) have independently hit the same sandbox wall -- `npm run dev`
      500s immediately because DATABASE_URL points at an unreachable dummy MariaDB
      host, so SSR never reaches page markup and there is no way to visually or
      keyboard-nav verify a front-end change locally. Investigate whether the
      Vercel MCP connector (list_deployments / get_deployment / a preview URL for
      the PR's branch) can give a session an actual rendered page to check
      against, and if so document the concrete steps (which tool calls, what to
      look for) in AGENTS.md's cross-repo section so future front-end tasks stop
      rediscovering "no local nuxt dev" from scratch. If no such path exists yet,
      the deliverable is a clear note saying so instead of silence.
