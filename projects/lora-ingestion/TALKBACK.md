# lora-ingestion — TALKBACK

## 2026-09-22 | Reviewer → Worker | lora-ingestion/t-013 | pattern

type: pattern

**Subject:** Clean cross-repo companion PR pair, merged as reviewed.

**Detail:**
- kind_robots#2992 (canonical scanner/import/preview source) and conductor#5059 (ops
  home-server vendor copy + kr-download routing) were both scoped, well-tested (23-51
  checks green each), and matched the roadmap task's stated scope exactly — no drift,
  no scope creep.
- The PR bodies included specific, dated root-cause narration (Resources 2427/2429
  mislabeling from Civitai collection-version titles bleeding into triggerWords) rather
  than generic "fixed a bug" language — this made the diff easy to verify against the
  claimed bug without re-deriving it from scratch.
- Both PRs correctly identified and preserved the existing conservative
  non-classification behavior for CHARACTER LoRAs (no keyword-guessing), which matches
  this project's prior notes about false positives in this exact space.

**Suggested action:** Keep doing this — task notes and PR bodies that name the specific
prior-bug symptom and Resource ids make Reviewer verification meaningfully faster.
Filed t-014 (parity check between the vendored home-server scanner and its kind_robots
source) as the kaizen follow-up so this class of silent-drift bug doesn't recur.

## 2026-09-23 | Agent (Claude, scheduled Conductor Agent run) | lora-ingestion/t-014 | resolution

**Decision:** merged (silasfelinus/conductor#5063, squash).

**Failure category:** n/a — clean cycle, no CI issues.

**What was good:**
- Followed PROVENANCE.md's own stated invariant ("straight file copy, pure stdlib, no
  kind_robots imports") to derive the check's strictness: byte-for-byte comparison rather
  than a fuzzier "same logic surface" heuristic, since the vendored files are supposed to be
  an exact copy and any difference at all is drift by that file's own documented contract.
- Verified all three outcome paths live before opening the PR: real clean run against the
  actual repos, a manually-introduced one-line diff to confirm the drift path reports
  correctly (then reverted, confirmed via `git status`/`git diff --stat`), and an unset-token
  run to confirm the unresolved/exit-2 path — not just unit tests in isolation.
- Matched the repo's established check_*.py conventions closely (module docstring shape,
  scan()/render()/main() split, --json flag, exit-code semantics) and made scan() accept an
  injectable fetcher/vendor_dir for testability rather than monkeypatching module globals,
  after confirming the more obvious `fetcher=fetch_kind_robots_file` default-argument approach
  silently breaks monkeypatch-based test injection (default args bind at def time, not call
  time).
- Ran the full local suite (2292 passed) before pushing, not just the new test file.

**What to improve:**
- No CI job runs this on every push in either repo (documented explicitly in the PR body as a
  deliberate choice, matching the session-sweep-only pattern the repo's other check_*.py
  scripts already use) — worth revisiting if vendored-scanner drift turns out to recur faster
  than the session cadence catches it.

**Kaizen task:** lora-ingestion/t-015 — automate the PROVENANCE.md re-sync step (still a
manual `cp` + hand-edited note) using the same GitHub Contents fetch this task added.

**Pattern note:** this is the second time in two cycles (t-013, t-014) that a cross-repo
vendored-copy pattern needed both a fix and a standing check added — worth keeping in mind if
another vendored/duplicated-logic pair turns up elsewhere in the repo (ops/home-server or
otherwise): the fix alone reliably regresses without the check.

---
_Generated by [Claude Code](https://claude.ai/code)_

## 2026-09-23 | Reviewer → Worker | lora-ingestion/t-016 | critique

**Decision:** merged (silasfelinus/conductor#5074, squash).

**Failure category:** n/a — clean cycle; the one blocking CI check was a template-format gap, not a code defect.

**What was good:**
- Correctly extended `resync_vendored_scanner.py` with a `--from-parity-report` mode that
  reuses the existing `resync()` per-file primitive rather than duplicating its fetch/write
  logic — `resync_from_parity_report` just loops and delegates.
- Refused-state handling is conservative and correct: any unresolved parity-check entry or
  untracked filename aborts the whole batch rather than partially applying it.
- Preserves the parity report's own `ref` by default (so a resync pulls from the exact commit
  the checker audited) while still allowing an explicit `--ref` override — avoids a TOCTOU gap
  where `main` moves between the parity check and the resync.
- New tests are genuinely offline (fake fetcher, no network/token) and cover the real edge
  cases: dedup, ref propagation, unresolved/untracked refusal, clean-report no-op.

**What to improve:**
- The PR body only had `## Summary`/`## Verification`/`## Flags for Reviewer`/
  `### Kaizen suggestion` — missing the required `### Task`, `### What changed / what I
  produced`, `### How I verified`, `### Stakes`, and `### Notes for reviewer` headings from
  AGENTS.md's handoff template, which tripped the "Validate Worker PR handoff" CI check (the
  one required check that started red). Fixed here by editing the PR body in place to the full
  template rather than sending it back for a retry pass, since the code itself was sound and
  every other check was already green — no `passes` increment, this was a review-side fix, not
  a quality rejection.

**Kaizen task:** lora-ingestion/t-017 — print a byte-delta summary per file for
`--from-parity-report --dry-run`, so a human/agent can tell a trivial change from a
substantial one before applying a multi-file resync.

**Pattern note:** this is at least the second known case in this repo where a PR body used a
non-standard section layout (`## Summary` instead of `### Task` etc.) and only surfaced at CI
time via `check_pr_handoff_template.py`; worth keeping an eye on whether it recurs from the
same `openai-scheduled-*` claim-id family before treating it as a one-off.

---
_Generated by [Claude Code](https://claude.ai/code)_
