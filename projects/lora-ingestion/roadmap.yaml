project: lora-ingestion
kind: infrastructure
goal: >
  Keep LoRA/checkpoint files automatically represented as canonical Kind Robots Resource
  records, browsable/downloadable from the unified library, and prove Resource-localPath
  selection reaches real generation end-to-end before closing this now-nearly-complete project.
notes_from_silas: |
  Auto-import LoRAs: drop a file into Lora/import on the home box
  and have it detected, sorted into <Base>/<SFW|NSFW>/, and registered as a
  kind_robots Resource with no manual input. Then a front-end browser (My Library
  + Discover tabs) to browse/manage owned LoRAs and download new ones from
  Civitai/CivArchive, with maturity + base-model filters, hide-owned, and
  update-to-new-version. Builds on the completed lora-catalog sort/import work.
milestones:
- id: m1
  title: ArtJob LoRA path resolution (loraResourceId/name -> localPath)
  weight: 20
  status: done
- id: m2
  title: Watched-folder import agent (drop -> sort -> upsert Resource)
  weight: 30
  status: in-progress
- id: m3
  title: Resource schema — Civitai version columns + mutation-select echo
  weight: 10
  status: done
- id: m4
  title: Front-end My Library browser (images, filters, edit)
  weight: 20
  status: done
- id: m5
  title: Discover tab — Civitai/CivArchive browse + download queue + versioning
  weight: 30
  status: done
tasks:
- id: t-001
  milestone: m1
  title: Resolve enqueue LoRA to Resource.localPath (never bare name)
  status: done
  owner: worker
  stakes: reversible
  note: 'Landed on kind_robots main as server/utils/artLoraResource.ts
    (resolveEnqueueLoraResource) — resolves loraResourceIds/loraName to a LoRA
    Resource, sets loraName = localPath, and 409s when a Resource has no
    localPath. This is the server side of the ume_classic_impressionist bug.'
- id: t-002
  milestone: m2
  title: Build the watched-folder LoRA import agent
  status: done
  owner: claude
  stakes: reversible
  note: >-
    conductor/ops/home-server/lora_import_agent.py — stdlib poller; on a stable drop it runs kind_robots scan_loras.py (--organize move) then import_catalog.py --upsert.

    Full history: projects/lora-ingestion/HISTORY.md#t-002
- id: t-003
  milestone: m2
  title: "Reconcile later Resource-backed generation evidence; run one current smoke only if proof is still missing"
  status: needs-human
  stakes: reversible
  gate_human: false
  note: >-
    Import half CONFIRMED 2026-07-28: standalone run detected ume as Flux, moved it, and upserted the Resource (created:1). Remaining: (1) run it as the embedded kr-relay thread on the pm2 box (pm2 restart kr-relay after setx CIVITAI_TOKEN), and (2) confirm a krea2/flux2 render selecting that Resource succeeds — closes the path bug end-to-end. Note ume sorted NSFW because Civitai flags the model nsfw; override to SFW if wrong.

    SENT BACK by silasfelinus via Kind Robots For You. this should have been fixed. if not, please report back.

    Reconciled repository evidence in projects/lora-ingestion/docs/t-003-resource-backed-generation-evidence.md. Application-side Resource.localPath resolution is strongly supported by merged Kind Robots fixes, but no durable job-specific DONE + ArtImage evidence was found for a Resource-backed smoke. KR_API_TOKEN is absent in this run, so no production smoke was queued.

    FOR SILAS: Evidence reconciliation is merged in projects/lora-ingestion/docs/t-003-resource-backed-generation-evidence.md (Conductor #2023). It shows the Resource.localPath application plumbing is fixed, but repository evidence does not prove a Resource-backed ArtJob reached DONE and produced an ArtImage. TO UNBLOCK: no product decision is needed; a later token-equipped agent should queue exactly one Krea2/Flux2 Resource-backed smoke and record Resource id/localPath, ArtJob id, terminal DONE state, and ArtImage id/path. KR_API_TOKEN was absent in this run, so production was left untouched.

    Silas reproduced a live compatibility bug on the image generator: selecting Krea 2 still offers FLUX LoRAs. Reopen this Resource-backed Krea2/Flux2 verification task so the picker and enqueue guard can enforce model-family compatibility before the smoke is considered complete.

    Implemented the Krea/Flux LoRA compatibility split on the Kind Robots worker branch. The shared picker now keys Krea 2 compatibility from Resource.generation, FLUX/KONTEXT resources score zero for Krea, and the enqueue resolver reuses the same check so stale/direct selections are rejected. Added contract coverage while preserving the existing Flux.2 policy.

    Compatibility fix merged to kind_robots main as PR #2416 (merge commit 501fb31abbbbe6310cf504aad3f2b22066e9a37a). Krea 2 LoRA selection now uses Resource.generation lineage, FLUX/KONTEXT Resources are excluded for Krea, and the enqueue resolver rejects stale/direct mismatches. All PR-triggered GitHub Actions completed successfully, including TypeScript and the full Contract Tests suite; the new Art generator preset contract passed. Vercel had no PR preview deployment to inspect, so no preview was fabricated. The broader t-003 runtime Resource-backed DONE + ArtImage smoke evidence remains genuinely unresolved and should be handled separately without holding this code fix claim.

    FOR SILAS: Full evidence in projects/lora-ingestion/docs/t-003-resource-backed-generation-evidence.md (2026-09-05 section, appended this session). Short version: confirmed both original candidate ArtJobs (2615, 2621) from the 2026-08-10 evidence are CANCELLED, not DONE -- they never proved anything. Scanned the 700 most recent DONE ArtJobs for any Krea2/Flux2 + LoRA combination: zero matches, so real proof was still missing.

    Two separate findings, both need you specifically: (1) Krea2 cannot be smoke-tested at all right now -- zero Resources in the whole library carry a "Krea" generation tag (checked all 2344 rows via the API); not a code bug, just needs a Krea2 LoRA ingested before this half can ever be tested. (2) Queued one Flux2 Resource-backed LoRA smoke (ArtJob 21616); the application-side wiring was confirmed correct (LoRA Resource resolved to its exact localPath, wired into the right ComfyUI node), but the relay (Silas-PC) claimed the job and then went completely silent -- never completed it, never claimed anything else, and its own last-seen heartbeat stayed frozen at 18:46:46 UTC for 40+ minutes. Cancelled the job server-side to clear the DB state, but that can't un-wedge a relay process that's actually stuck locally. Filed as conductor/t-147 with full detail and two unconfirmed guesses at the cause.

    TO APPROVE: this needs your eyes on the actual relay machine, which nothing in this sandbox can reach. Once you've restarted/checked the relay and resolved conductor/t-147 with what you found, t-003 can either get a fresh Flux2 smoke attempt or wait on a Krea2 Resource before it's closeable. Set approved_by_human: true and status: done (or ready, if you want another smoke attempted) once conductor/t-147 is resolved.

    Release under Silas's 2026-09-07 human-gate simplification policy. No product decision is required. conductor/t-147 has been closed as mitigated; a token-equipped agent should now queue exactly one fresh Flux2 Resource-backed LoRA smoke and record Resource id/localPath, ArtJob id, terminal DONE state, and ArtImage id/path. Krea2 proof may remain deferred until a genuinely Krea2-tagged LoRA Resource exists; absence of such a Resource is not a Silas gate.

    Cycle (conductor scheduled sweep, 2026-09-15T10:30Z): re-verified the Flux2 LoRA
    compatibility gate landed correctly since the 2026-09-05 evidence -- POST
    /api/art/enqueue now returns 409 "LoRA Resource 1055 is marked FLUX, not
    FLUX2-compatible" when the payload uses a Flux.1-generation Resource on the flux2 engine
    (this is flux2LoraCompatibilityRank() in utils/loraSelection.ts requiring generation to
    match /FLUX[ ._-]*2/), closing the ambiguity the 2026-09-05 doc left open about whether
    the earlier relay hang was a Resource/path bug or a generation mismatch -- it was gated
    correctly by the app, not a hang cause. Found a genuine Flux.2-generation LoRA Resource
    that did not exist in the 2026-09-05 search: Resource 1123 (Flux_2-Turbo-LoRA_comfyui,
    localPath Flux/SFW/Flux_2-Turbo-LoRA_comfyui.safetensors, generation "Flux.2 D",
    isActive true). Queued ArtJob 22837 with it via the real POST /api/art/enqueue endpoint;
    confirmed via GET /api/art/queue/22837 that the LoraLoaderModelOnly node correctly wired
    lora_name to this Resource's exact localPath onto the flux2_dev_fp8mixed.safetensors
    checkpoint -- Resource-path resolution proven correct again at the application layer.
    Job did not reach DONE this cycle: it sat PENDING for the full session behind ArtJob
    22836 (claimedBy Silas-PC, claimed 10:15:52Z), which GET /api/art/queue/stats flagged
    staleRunning past the 15-minute threshold for the whole ~25 minutes this session polled
    it. Did not cancel 22836 -- unlike the 2026-09-05 incident, this job is not this
    session's own, its designer/projectSlug are unset so its owner and purpose are unknown,
    and this backend's DONE-job history includes legitimate 70+ minute renders, so a mid-
    length RUNNING job is not on its own proof of a second relay hang. Not resubmitting --
    ArtJob 22837 is valid, queued, and will be picked up once the relay's single concurrent
    slot frees. Re-arming to ready rather than needs-human: this is a transient queue/relay
    wait, not a blocked task -- next actionable step is simply to poll GET
    /api/art/queue/22837 to a terminal state (DONE or FAILED) and record the Resource
    id/localPath, ArtJob id, terminal state, and ArtImage id/path per the task's standing
    instruction; only escalate to needs-human if 22837 itself later shows the same
    staleRunning hang pattern once it actually starts running.

    Second reproduction of the identical Flux.2 Klein (flux2_dev_fp8mixed.safetensors) relay
    hang, five weeks after the first (2026-08-10, ArtJob 21616 / conductor/t-147). This time
    ArtJob 22837, 67+ min staleRunning, 3 jobs queued behind it -- cancelled to restore
    throughput; relay resumed healthy within a minute, so it's checkpoint-specific, not a
    dead relay. Filed conductor/t-165 (hard needs-human, physical/console access to Silas-
    PC) for the hands-on diagnosis this needs. Full evidence in projects/lora-
    ingestion/docs/t-003-resource-backed-generation-evidence.md (2026-09-15 update). t-003
    stays soft needs-human pending t-165; Krea2 remains untestable (no compatible Resource)
    for the same reason as before.
  approved_by_human: false
  updated: '2026-09-15T11:55:17Z'
  outcome_note: >
    The original July 28 gate is stale in scope. Since then Kind Robots made Resource.localPath
    authoritative for LoRA resolution across image/video engines (including PR #1090), fixed
    art-styler path handling (#1136), unified the model library/download flow (#1152/#1166),
    and added retry-time Resource path refresh (#1163/#1164). This task now has one job: inspect
    later production/ArtJob evidence for a successful Resource-backed LoRA render. If that proof
    already exists, document it and finish the project. Only queue one fresh smoke render if the
    existing evidence is genuinely insufficient. No human product decision is required.
  owner: reviewer
  claimed_by: 2026-09-15T115306Z-lora-t003-relay-recur
  claimed_at: '2026-09-15T11:53:06Z'
  soft_gate: true

- id: t-004
  milestone: m3
  title: Add civitaiModelId/civitaiModelVersionId + fix resourceMutationSelect
  status: done
  stakes: reversible
  note: >-
    Landed on kind_robots branch claude/lora-organization-catalog-coyhav (commit f1a9585).

    Full history: projects/lora-ingestion/HISTORY.md#t-004
  updated: '2026-08-03T07:15:03Z'
- id: t-005
  milestone: m4
  title: Front-end My Library LoRA browser
  status: done
  stakes: reversible
  depends_on: [t-004]
  note: >-
    Landed on kind_robots branch claude/lora-organization-catalog-coyhav (commit c4bdb60).

    Full history: projects/lora-ingestion/HISTORY.md#t-005
  updated: '2026-08-16T05:29:00Z'
- id: t-006
  milestone: m5
  title: Discover tab + DownloadRequest queue + version-update flow
  status: done
  stakes: outward-facing
  depends_on: [t-005]
  note: >-
    Landed + gaps closed.

    Full history: projects/lora-ingestion/HISTORY.md#t-006
  updated: '2026-08-03T07:15:05Z'

- id: t-007
  milestone: m5
  title: Correct t-005/t-006's stale file-path claims (front end was later rebuilt content-driven)
  status: done
  owner: reviewer
  passes: 0
  stakes: reversible
  note: >-
    Kaizen from the 2026-08-16 weekly site audit.

    Full history: projects/lora-ingestion/HISTORY.md#t-007
  claimed_by: sched-conductor-lora-t007-20260816T052922Z
  claimed_at: '2026-08-16T05:29:23Z'
  updated: '2026-08-16T05:35:00Z'

- id: t-008
  milestone: m4
  title: Add temporary admin LoRA maturity triage page with local review progress
  status: done
  passes: 0
  stakes: reversible
  note: >-
    Silas-directed 2026-08-24 cleanup tool.

    Full history: projects/lora-ingestion/HISTORY.md#t-008
  claimed_by: 2026-08-25T003700Z-lora-ingestion-t-008-9c71
  claimed_at: '2026-08-25T00:37:00Z'
  updated: '2026-08-25T00:55:00Z'

- id: t-009
  milestone: m2
  title: Generalize model discovery/download and watched imports beyond LoRA-only routing
  status: done
  stakes: reversible
  passes: 1
  updated: '2026-09-19T20:30:52Z'
  note: |-
    Silas-directed 2026-09-19: audit the downloader against the full Resource model taxonomy, preserve automatic Lora/import ingestion, add equivalent checkpoint/import ingestion, and stop routing every non-CHECKPOINT download into the LoRA directory. Keep Civitai discovery aligned with the Resource types it can actually supply and with ComfyUI's canonical model directories.

    Generalized Civitai discovery and download routing across file-backed Resource types, preserved the existing Lora/import watched ingestion path, added checkpoints/import watched ingestion using the generic model scanner, removed the duplicate DownloadRequest consumer from the filesystem watcher, and added focused contracts for routing and import behavior. Implementation branches are reconciled with current main and ready for PR review.

    2026-09-19 Reviewer: rejected pass 1 -- conductor PR #4737 has 3 real Python test
    failures from an unfinished lora-import->model-import/checkpoints-path rename, plus a
    PR-handoff-template violation. See retry_context. kind_robots#2877 companion PR is green
    and unaffected.

    Follow-up from Silas on the merged downloader work: the Discover base-model filter was still hardcoded to five families. Replaced it with a grouped taxonomy covering every generation/base family recognized by the LoRA/model catalog, including Krea 2, Flux.2, ZImage, Qwen, video/multimodal families, and all SD/SDXL variants. Added a contract that cross-checks the UI list against scan_loras.py BASEMODEL_MAP and scan_models.py FOLDER_BASE_HINTS so future catalog families cannot disappear from Discover silently.

    Completed after the original downloader/import pair landed as kind_robots#2877 and conductor#4737, then Silas spotted the remaining base-filter drift. kind_robots#2882 replaced the five hardcoded Discover base choices with the shared catalog-aligned taxonomy and added CI coverage against both model scanners. All 47 checks passed, including TypeScript and the Model download taxonomy contract.

    Silas follow-up: the exhaustive base-model menu is too noisy. Reopen to condense Discover to base families, surface the bases Kind Robots actually supports, and keep exact Civitai variant coverage behind the family choice.

    Condensed Discover from exact Civitai base-version entries to 14 useful families. Kind Robots-supported LoRA bases are grouped first with checkmarks: Krea 2, FLUX.2, SDXL, Pony, SD 1.5, and Z-Image. Family choices expand server-side to Civitai's exact BaseModel enum strings, so SDXL still covers Turbo/Lightning/etc. without showing them all. Legacy exact-value URLs remain accepted. Also corrected several exact Civitai enum spellings used by the prior exhaustive list.

    2026-09-19 closed: kind_robots#2894 merged (family-based Civitai base filter, retry pass
    addressing pass-1 rejection). All CI green, mergeable_state clean.
  retry_context: 'pass 1 failed (quality): kind_robots/conductor#4737 renamed the lora-import watcher thread/log tag to ''model-import'' and changed KR_CHECKPOINT_DIR''s default from Z:/ai/models/Stable-diffusion to Z:/ai/models/checkpoints (ops/home-server/lora_import_agent.py, relay_download_agent.py) but did not update three pre-existing tests that still assert the old values: tests/test_home_server_share_root.py::test_default_paths_are_unchanged, tests/test_lora_watcher_share_resilience.py::SupervisorTests::test_start_lora_watcher_uses_the_supervisor_not_the_raw_loop, and tests/test_relay_log_timestamps.py::test_log_line_is_one_readable_local_time -- Python test suite fails with 3 real assertion errors on conductor PR #4737. The PR body also fails Validate Worker PR handoff: it uses ### What changed instead of the required ### What changed / what I produced heading and omits ### How I verified and ### Kaizen suggestion entirely. Do differently: update the three listed tests to assert the new model-import tag and the new Z:/ai/models/checkpoints KR_CHECKPOINT_DIR default (matching the intentional rename already in this diff), and rewrite the PR body to use the exact AGENTS.md handoff headings before re-requesting review. kind_robots#2877 (the companion PR) is green; only conductor#4737 needs the retry.'
  owner: worker
  claimed_by: 2026-09-19T131700-0700-lora-t009-family-filter-4c91
  claimed_at: '2026-09-19T13:17:00-07:00'
  implementation_pr: 'silasfelinus/kind_robots#2894'

- id: t-010
  milestone: m4
  title: Type every LoRA by what it is FOR, and randomize art batches from those pools
  status: done
  owner: claude
  stakes: reversible
  note: >-
    Silas-directed 2026-09-22: "identify their type, such as character, style,
    setting, action, clothing, etc, and then be able to randomize them for art
    generation, for example, asking for '{character} running in {style}, batch
    10', should give ten different characters running in ten random styles for
    the selected checkpoint." Auto-configure from Civitai where possible, and
    make it editable from the Resource card or the LoRA editor. Also asked
    whether random selection already works for other objects (character,
    location, facet) -- it did not, for art generation.

    Landed as one vertical slice on kind_robots: Resource.loraCategory +
    loraCategorySource (additive migration, ten categories), a shared
    classifier (utils/loraCategory.ts) mirrored into scan_loras.py so the
    Civitai tags are read at the one moment they exist, category editing in
    the LoRA editor and the triage page (with a suggest pass and bulk apply),
    and a new POST /api/art/randomize that rolls a templated prompt into a
    batch plan. server/utils/promptVariants.ts became the shared roll engine
    for both the Challenge Center and art, dealing WITHOUT replacement so ten
    variants genuinely give ten different values, with a replayable seed.
    Facets, Characters and Scenarios answer placeholders too, so the
    "other objects" half of the ask is covered.

    SCHEMA-AFFECTING: prisma/migrations/20260922120000_add_lora_category.
    Additive (two nullable columns + one index), so it is safe for the
    unattended Alexandria deploy window.

    FOR SILAS, after deploy: the pools start EMPTY -- every existing LoRA row
    is unclassified, so {character} rolls nothing until the catalog is
    classified. Run
    `npm run backfill:lora-categories` (dry run first, then --apply; add
    --fetch-tags with CIVITAI_TOKEN set to reach the Civitai tags the rows
    never stored). Then use the triage page's "Suggest for all unclassified"
    for whatever the heuristics could not reach. Hand corrections are stamped
    HUMAN and are never overwritten by a later scan or backfill.

    2026-09-22 closed: kind_robots#2967 merged. All 62 checks on the final head passed
    except the pre-existing tests/python/test_powershell_syntax_contract.py::test_the_paths_
    filter_covers_every_ps1_in_the_repo, which reproduces identically with the whole branch
    stashed and is documented on the PR with a one-line proposed patch (its helper matches
    only double-quoted .ps1 globs while powershell-syntax-contract.yml writes single quotes,
    so it reads a coverage hole that does not exist). Green on this PR's own ground:
    TypeScript, Contract verifiers (383 tests, including the three new steps), Replay
    migrations on MariaDB, Failed-migration repair, Generator + importer regression, Build
    production image, GitGuardian.

    Independent reviewer close-out (same session, conductor scheduled sweep): re-verified the
    above before this task's own close-out landed -- reproduced the powershell-syntax-
    contract failure directly against origin/main (confirmed pre-existing/unrelated) and
    audited migration.sql line-by-line (two nullable ADD COLUMN statements plus one CREATE
    INDEX, no DROP/data rewrite) per AGENTS.md's Reviewer migration-audit requirement. Filed
    the regex fix as lora-ingestion/t-011 (kaizen, ready).
  updated: '2026-09-22T06:04:56Z'
  claimed_by: 2026-09-22T053000Z-lora-t010-type-randomization
  claimed_at: '2026-09-22T05:30:00Z'
  implementation_pr: 'silasfelinus/kind_robots#2967'
- id: t-011
  milestone: m4
  title: Fix the PowerShell-syntax-contract Python test's quote-only regex so it recognizes single-quoted workflow path globs
  status: done
  passes: 0
  stakes: reversible
  note: |-
    Kaizen from t-010's merge (kind_robots PR #2967). CI on that PR was red on
    tests/python/test_powershell_syntax_contract.py::test_the_paths_filter_covers_every_ps1_in_the_repo
    even though the diff touched no .ps1 file and no PowerShell workflow -- confirmed
    independently on origin/main itself before merging past it (see t-010's note).

    Root cause: utils/scripts/../tests/python/test_powershell_syntax_contract.py's
    _ps1_path_patterns() only matches double-quoted globs:
    `re.findall(r'^\s*-\s*"([^"]*\.ps1)"', text, re.M)`. .github/workflows/powershell-
    syntax-contract.yml writes its `paths:` filter in single quotes
    (`- 'scripts/**.ps1'`), so the regex finds nothing and the test wrongly asserts a
    coverage hole that does not exist. It has stayed invisible because
    python-scripts.yml only runs on scripts/**.py changes, and t-010 (scan_loras.py)
    was the first thing in a while to touch one.

    Fix: make the quote character optional in _ps1_path_patterns(), e.g.
    `re.findall(r"""^\s*-\s*['"]([^'"]*\.ps1)['"]""", text, re.M)`. Verify by running
    the test against main both before (red) and after (green) the fix, and confirm it
    still correctly fails if a real .ps1 is added without updating the paths filter
    (don't let the fix accidentally make the test unconditionally pass).

    Fixed the PowerShell syntax contract parser to recognize both single- and double-quoted .ps1 workflow path globs, with a focused regression test covering both quote styles. Implementation branch worker/lora-ingestion-t-011-openai-scheduled-2026-09-22T061509Z-lora-ingestion-t011-a11.

    Reconciled 2026-09-22T07:10Z: this task's own PR (kind_robots#2969, opened
    2026-09-22T06:18:52Z by an OpenAI Worker cycle) was superseded by kind_robots#2970, an
    independent same-bug fix from a different session that merged first
    (2026-09-22T06:45:31Z) with broader coverage (accepts double-quoted, single-quoted, and
    bare YAML scalars, plus a quote-backreference guard, vs. #2969's single/double-only
    fix). #2969 showed mergeable_state=dirty against current main as a result; closed it as
    redundant (comment posted) rather than resolving the conflict, since #2970 already fully
    covers the fix. Closing done with implementation_pr pointing at the PR that actually
    landed.
  updated: '2026-09-22T06:58:48Z'
  owner: worker
  claimed_by: openai-scheduled-2026-09-22T061509Z-lora-ingestion-t011-a11
  claimed_at: '2026-09-22T06:15:09Z'
  implementation_pr: 'silasfelinus/kind_robots#2970'

- id: t-012
  milestone: m4
  title: Polish art-generator random pools, facet picker placement, image sizes, and Krea 2 preset
  status: done
  passes: 1
  stakes: reversible
  gate_human: false
  note: |-
    Silas-directed follow-up to t-010 on 2026-09-22. The newly merged random-batch
    surface needs a usability pass: the Facet picker opens below the bottom of the
    scrollable generator and is clipped; random choices should visibly expose LoRA
    categories, Facet types, and Kind Robots objects instead of hiding Facet/object
    fallbacks behind duplicate placeholder names; object picks should contribute a
    compact visual clause derived from their existing artPrompt rather than only a
    name/title or an enormous context dump; width and height should be selects with
    common model-family-appropriate dimensions; and the Random batch helper text must
    stop repeating the original "{character} running in {style}" implementation prompt.

    Also audit the Krea 2 Detailed preset. Krea 2 Turbo is the repo's 8-step default,
    while the current Detailed button asks for 16 steps even though the distilled
    engine contract caps Krea 2 below that. Remove or replace the misleading preset
    rather than promising quality from unsupported extra steps.

    Prefer existing artPrompt fields plus a deterministic compacting rule before adding
    new schema columns. Add focused contracts for the source-explicit random placeholders
    and model-size choices, preserve the existing random-batch/LoRA compatibility and
    maturity rules, and keep this as a reversible front-end/server utility change.

    Implementation is scoped on the Kind Robots worker branch: random source-explicit LoRA/Facet/object controls, compact persisted artPrompt context, upward Facet picker, checkpoint-aware dimension selects, and removal of the misleading 16-step Krea 2 preset.

    Retry completed and merged as kind_robots#2972 at 2026-09-22T08:43:15Z. The lint-ratchet regression from pass 1 was repaired before merge; exact-head GitHub Actions on ca670db0e5a0d35643ef2ebbcb40808b8ab303a4 completed successfully, including TypeScript Type Check 35705843124, Contract Tests 35705843079, Layout Contract 35705843239, Art Prompt + Icon Safety 35705842988, Facet Catalog Contract 35705843228, Project Architecture Contract 35705843150, Generated Client Parity 35705843226, and Schema Migration Parity 35705843156. No deploy was triggered.
  updated: '2026-09-22T10:17:45Z'
  claimed_by: openai-scheduled-2026-09-22T081655Z-lora-ingestion-t012-a11
  claimed_at: '2026-09-22T08:16:55Z'
  retry_context: 'pass 1 failed (quality): kind_robots#2972''s ESLint lint-ratchet check went from 331->332 problems (no-useless-assignment: 9 -> 10), specifically new useless assignments in server/utils/artRandomPools.ts:293:7 and stores/storybookStore.ts:808:9. Everything else on the PR (all other contract checks, TypeScript, tests) is green. Do differently: remove the two useless assignments (or restructure so the assigned value is actually read) and re-run ''npm run test:lint-ratchet'' locally before pushing; do not touch unrelated ratchet counts.'



- id: t-013
  milestone: m2
  title: Align LoRA/checkpoint imports through one enriched Resource path and queue a custom preview ArtJob
  status: done
  owner: worker
  stakes: reversible
  note: |-
    Silas-directed 2026-09-22. Audit both watched-folder imports and Discover/DownloadRequest
    imports so LoRAs and checkpoints follow the same lifecycle: move into their canonical
    local model directories, upsert the canonical Resource with Civitai provenance, classify
    LoRAs from Civitai tags into character/style/setting/etc. without overwriting HUMAN
    corrections, and queue one durable custom preview ArtJob whose completed ArtImage attaches
    back to the Resource. Re-scans/retries must not create duplicate preview jobs.

    Implementation is in review as kind_robots#2992 and conductor#5059. The paired change unifies watched and DownloadRequest LoRA/checkpoint imports through the canonical scanners, restores Civitai tag category classification in the home-server vendor copy, preserves checkpoint Civitai ids, queues attached Resource preview ArtJobs, and deduplicates in-flight resource previews.

    REVIEWED & MERGED (Reviewer, 2026-09-22): both companion PRs audited and merged —
    kind_robots#2992 (canonical scanner/import/preview source, 51/51 checks green) and
    conductor#5059 (ops home-server vendor copy + kr-download routing, 23/23 checks green).
    Diff is scoped to the scanner/import/preview surface plus regression tests; fixes a real
    mislabeling bug (Civitai collection versions bleeding model-level title into
    triggerWords, e.g. Resources 2427/2429) and adds LoRA category classification from
    Civitai tags without guessing CHARACTER. No schema/secrets/deploy changes. Merged
    kind_robots#2992 first (canonical source), then conductor#5059.
  claimed_by: 2026-09-22T221600Z-openai-lora-t013-a61f
  claimed_at: '2026-09-22T22:16:00Z'
  updated: '2026-09-22T22:51:57Z'
  implementation_pr: 'silasfelinus/conductor#5059'
