project: lora-ingestion
kind: infrastructure
goal: >
  Keep LoRA/checkpoint files automatically represented as canonical Kind Robots Resource
  records, browsable/downloadable from the unified library, and prove Resource-localPath
  selection reaches real generation end-to-end before closing this now-nearly-complete project.
notes_from_silas: |
  Auto-import LoRAs: drop a file into Lora/import on the home box
  and have it detected, sorted into <Base>/<SFW|NSFW>/, and registered as a
  kind_robots Resource with no manual input. Then a front-end browser (My Library
  + Discover tabs) to browse/manage owned LoRAs and download new ones from
  Civitai/CivArchive, with maturity + base-model filters, hide-owned, and
  update-to-new-version. Builds on the completed lora-catalog sort/import work.
milestones:
- id: m1
  title: ArtJob LoRA path resolution (loraResourceId/name -> localPath)
  weight: 20
  status: done
- id: m2
  title: Watched-folder import agent (drop -> sort -> upsert Resource)
  weight: 30
  status: done
- id: m3
  title: Resource schema — Civitai version columns + mutation-select echo
  weight: 10
  status: done
- id: m4
  title: Front-end My Library browser (images, filters, edit)
  weight: 20
  status: done
- id: m5
  title: Discover tab — Civitai/CivArchive browse + download queue + versioning
  weight: 30
  status: done
tasks:
- id: t-001
  milestone: m1
  title: Resolve enqueue LoRA to Resource.localPath (never bare name)
  status: done
  owner: worker
  stakes: reversible
  note: 'Landed on kind_robots main as server/utils/artLoraResource.ts
    (resolveEnqueueLoraResource) — resolves loraResourceIds/loraName to a LoRA
    Resource, sets loraName = localPath, and 409s when a Resource has no
    localPath. This is the server side of the ume_classic_impressionist bug.'
- id: t-002
  milestone: m2
  title: Build the watched-folder LoRA import agent
  status: done
  owner: claude
  stakes: reversible
  note: 'conductor/ops/home-server/lora_import_agent.py — stdlib poller; on a
    stable drop it runs kind_robots scan_loras.py (--organize move) then
    import_catalog.py --upsert. Runs embedded as a daemon thread inside kr-relay
    (relay_media_agent.start_lora_watcher) — one process/token/log, watcher and
    render loop cannot stall each other. Host: the pm2 render box (alexandria is
    a locked-down NAS; SMB is case-insensitive so Windows moves are safe post
    case_merge). Verified live 2026-07-28 with ume_classic_impressionist
    (detected Flux, moved, Resource created:1); Civitai flagged it nsfw so it
    sorted NSFW — override to SFW via scan --overrides if wrong.'
- id: t-003
  milestone: m2
  title: "Reconcile later Resource-backed generation evidence; run one current smoke only if proof is still missing"
  status: needs-human
  stakes: reversible
  gate_human: false
  note: |-
    Import half CONFIRMED 2026-07-28: standalone run detected ume as Flux, moved it, and upserted the Resource (created:1). Remaining: (1) run it as the embedded kr-relay thread on the pm2 box (pm2 restart kr-relay after setx CIVITAI_TOKEN), and (2) confirm a krea2/flux2 render selecting that Resource succeeds — closes the path bug end-to-end. Note ume sorted NSFW because Civitai flags the model nsfw; override to SFW if wrong.

    SENT BACK by silasfelinus via Kind Robots For You. this should have been fixed. if not, please report back.

    Reconciled repository evidence in projects/lora-ingestion/docs/t-003-resource-backed-generation-evidence.md. Application-side Resource.localPath resolution is strongly supported by merged Kind Robots fixes, but no durable job-specific DONE + ArtImage evidence was found for a Resource-backed smoke. KR_API_TOKEN is absent in this run, so no production smoke was queued.

    FOR SILAS: Evidence reconciliation is merged in projects/lora-ingestion/docs/t-003-resource-backed-generation-evidence.md (Conductor #2023). It shows the Resource.localPath application plumbing is fixed, but repository evidence does not prove a Resource-backed ArtJob reached DONE and produced an ArtImage. TO UNBLOCK: no product decision is needed; a later token-equipped agent should queue exactly one Krea2/Flux2 Resource-backed smoke and record Resource id/localPath, ArtJob id, terminal DONE state, and ArtImage id/path. KR_API_TOKEN was absent in this run, so production was left untouched.
  approved_by_human: false
  updated: '2026-08-10T09:41:20Z'
  outcome_note: >
    The original July 28 gate is stale in scope. Since then Kind Robots made Resource.localPath
    authoritative for LoRA resolution across image/video engines (including PR #1090), fixed
    art-styler path handling (#1136), unified the model library/download flow (#1152/#1166),
    and added retry-time Resource path refresh (#1163/#1164). This task now has one job: inspect
    later production/ArtJob evidence for a successful Resource-backed LoRA render. If that proof
    already exists, document it and finish the project. Only queue one fresh smoke render if the
    existing evidence is genuinely insufficient. No human product decision is required.
  claimed_by: 20260810T093057Z-lora-t003-a50cb6
  claimed_at: '2026-08-10T09:30:57Z'
  soft_gate: true

- id: t-004
  milestone: m3
  title: Add civitaiModelId/civitaiModelVersionId + fix resourceMutationSelect
  status: done
  stakes: reversible
  note: |-
    Landed on kind_robots branch claude/lora-organization-catalog-coyhav (commit f1a9585). Migration 20260728120000 adds civitaiModelId + civitaiModelVersionId (Int?, indexed, ADD COLUMN IF NOT EXISTS); create.ts validates/emits them and allows them through the field guard; batch.post.ts carries them in the upsert update; resourceMutationSelect now also echoes triggerWords/defaultTrigger/hash/previewImageUrl + the two new columns; scan_loras.py captures modelId/modelVersionId (Civitai + CivArchive) and emits them from to_resource(); prisma client regenerated. Needs: a PR opened + merged to kind_robots main, then migrate deploy on kindblank_fresh.

    Reconciled during the open-roadmap drift audit. Kind Robots PR #1124 merged the Civitai model/version columns, migration, mutation-select fields, and catalog import support described by this task. The implementation is on main, so review is complete.
  updated: '2026-08-03T07:15:03Z'
- id: t-005
  milestone: m4
  title: Front-end My Library LoRA browser
  status: done
  stakes: reversible
  depends_on: [t-004]
  note: |-
    Landed on kind_robots branch claude/lora-organization-catalog-coyhav (commit c4bdb60). pages/lora.vue (My Library + Discover-stub tabs) + components/lora/{lora-card,add-lora,lora-gallery}.vue built on reactable-card; resourceStore.visibleLoras adds LORA/LYCORIS + maturity gating. Search + base-model + maturity + sort filters, client-side pagination, add/edit via updateResource (PATCH) / batch upsert. All SFCs compile clean. Ships with t-004 in the same PR.

    Reconciled during the open-roadmap drift audit. Kind Robots PR #1124 merged the My Library LoRA browser, filtering, maturity handling, pagination, and add/edit surfaces described by this task. The implementation is on main, so review is complete.
  updated: '2026-08-03T07:15:04Z'
- id: t-006
  milestone: m5
  title: Discover tab + DownloadRequest queue + version-update flow
  status: done
  stakes: outward-facing
  depends_on: [t-005]
  note: |-
    Landed + gaps closed. kind_robots (PR #1124): DownloadRequest model + migration 20260728133000; /api/lora/download-request (dedupes owned + in-flight), /download/claim (atomic claim + reaper), /download/[id]/complete; browse.get.ts is source-aware — Civitai keyword search AND CivArchive by-id/URL lookup (recovers removed models), owned-flag + updatable (own-a-different-version) + maturity clamp; lora-discover.vue with source toggle + Update action + Discover tab. conductor (088ebfdc): lora_import_agent.py download loop (claim -> fetch, Civitai token redacted, .part atomic write -> complete; direct URLs cover CivArchive downloads). Verified: npm run test (vue-tsc) clean + resource-ownership contract passes; gallery/mutation selects echo the new columns. Remaining nicety: resourceId back-link on complete is best-effort null (owned-detection reads Resource.civitaiModelVersionId set by the catalog).

    Reconciled during the open-roadmap drift audit. Kind Robots PR #1124 merged the Discover browser and DownloadRequest queue, while the Conductor import agent landed the matching claim/download/complete loop. The task note already records the implementation and verification; review is complete. End-to-end relay verification remains separately and correctly tracked by t-003.
  updated: '2026-08-03T07:15:05Z'

- id: t-007
  milestone: m5
  title: Correct t-005/t-006's stale file-path claims (front end was later rebuilt content-driven)
  status: ready
  owner: null
  passes: 0
  stakes: reversible
  note: >
    Kaizen from the 2026-08-16 weekly site audit. t-005/t-006 (both done)
    describe the front end as `pages/lora.vue` (My Library + Discover tabs)
    plus `components/lora/{lora-card,lora-gallery}.vue`. None of those three
    exact files exist on current kind_robots main. The actual shipped
    surface is content-driven: `content/resources.md` (route `/resources`,
    channelKey `play`, tabKey `resources`) mounting
    `components/resources/resource-manager.vue`, which itself embeds the
    Library/Discover tabs, backed by `resource-gallery.vue` and
    `resource-card.vue` (`components/lora/add-lora.vue` and
    `components/lora/lora-discover.vue` do still exist and match). This is a
    live, functionally-equivalent rename/rebuild with no correction note
    anywhere recording it -- unlike ruler-hooked/t-013 or alexa-
    integration/t-019, which self-corrected the same class of drift. Add a
    short dated note to t-005 and/or t-006 recording the actual current
    file paths.
