project: scene-animator
kind: software

goal: >
  An admin-only Kind Robots workflow that turns folders of still scene images into short
  generated videos automatically using the existing Comfy video pipeline, with durable
  progress, safe resume/deduplication, maturity controls, presets, and a browsable result
  gallery.

notes_from_silas: |
  Give the tool a folder of still images and make each scene alive without writing a unique
  motion prompt for every image. Reuse Kind Robots' general video generation infrastructure
  and presets. The operator chooses the source folder, desired clip duration, and maturity
  setting; the system remembers progress and exposes a gallery. This belongs on an admin
  surface in Kind Robots. Source stills may live on the existing media server in a dedicated
  sibling tree such as kindrobots/animate rather than kindrobots/images.

milestones:
  - id: m1
    title: "SHAPE — source contract and automation design"
    weight: 20
    status: done

  - id: m2
    title: "BUILD — durable folder-to-video pipeline"
    weight: 50
    status: done

  - id: m3
    title: "POLISH & SHIP — gallery, recovery, and operator UX"
    weight: 30
    status: done

tasks:
  - id: t-001
    milestone: m1
    title: "Build the resumable Scene Animator MVP in Kind Robots"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: 'Merged silasfelinus/kind_robots#2072 (squash 0e823d3). Reviewer fixed 6 TypeScript errors the PR CI caught before merging: server/api/scene-animator/index.get.ts included a non-existent ArtJob->ArtImage Prisma relation (ArtJob only has a plain artImageId int column, no declared relation) -- replaced with a batch findMany by id; server/api/scene-animator/source.get.ts passed Content-Length as a string where h9s typed header expects number; stores/sceneAnimatorStore.ts durationSeconds ref was inferred as the literal union from the readonly VIDEO_PRESETS array instead of number, and the first-folder lookup was not guarded against an empty array. All 39 CI checks green after the fix, mergeable_state clean, merged via squash.'
    claimed_by: 2026-08-24T131000Z-scene-animator-t-001-g56s
    claimed_at: '2026-08-24T13:10:00Z'
    updated: '2026-08-24T13:38:19Z'
    implementation_pr: 'silasfelinus/kind_robots#2072'

  - id: t-002
    milestone: m1
    title: "Confirm the Scene Animator scope with Silas"
    status: done
    passes: 0
    stakes: reversible
    note: |-
      FOR SILAS: soft checkpoint only; development is not blocked. Review DESIGN-BRIEF.md after the MVP lands and redirect naming, defaults, or workflow if desired. The project is intentionally scoped to internal/admin batch animation, not public publishing.


      Closed under Silas's 2026-09-07 human-gate simplification instruction. Accept the implemented internal/admin batch-animation scope and current naming/defaults as the baseline. Future reversible workflow polish is agent-owned; public publishing or outward distribution remains a separate genuine gate if ever added.
    updated: '2026-09-07T11:32:43+00:00'

  - id: t-003
    milestone: m2
    title: "Harden media-root deployment and source-folder operations"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-001
    note: >-
      After the MVP establishes the code contract, verify the self-hosted app and media
      container expose the dedicated kindrobots/animate source tree safely. Prefer a single
      ANIMATE_PATH/ANIMATE_MEDIA_ORIGIN contract with traversal protection and clear health
      diagnostics. Any physical mount/container-template change remains an operator step;
      code and docs should make the required mapping explicit.

      RESOLVED: Merged silasfelinus/kind_robots#2108 (squash 6e02168). Admin-only GET
      /api/scene-animator/health reports which source (ANIMATE_PATH / IMAGES_PATH-derived
      sibling / local /animate fallback) is in effect plus folder/image counts, or a specific
      unavailable reason; GET /api/scene-animator no longer hardcodes rootAvailable: true;
      sceneAnimatorStore surfaces the specific reason through its error banner. Traversal
      protection was already solid and untouched. 41/41 CI checks green, mergeable_state
      clean, merged via squash.
    claimed_by: claude-scheduled-20260825T143104Z-scn3
    claimed_at: '2026-08-25T14:31:05Z'
    updated: '2026-08-25T15:28:08Z'
    implementation_pr: 'silasfelinus/kind_robots#2108'

  - id: t-004
    milestone: m3
    title: "Polish batch recovery, filtering, and gallery ergonomics"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-001
    note: >
      Add operator-quality recovery controls after real batches exercise the MVP: failed-job
      retry, source/result filters, clearer runtime estimates, per-folder completion summary,
      and responsive gallery polish without replacing the underlying ArtJob ledger.
    claimed_by: claude-sa004-20260825T172946Z
    claimed_at: '2026-08-25T17:29:58Z'
    updated: '2026-08-25T17:44:43Z'
    implementation_pr: 'silasfelinus/kind_robots#2111'

  - id: t-005
    title: "FOR SILAS: confirm the admin Scene Animator surface to close out this finished project"
    status: ready
    passes: 0
    stakes: reversible
    note: |-
      FOR SILAS: all 4 roadmap tasks across all three milestones (shape, build, polish/
      ship) are done -- nothing agent-actionable remains in the queue. Reconciled
      2026-09-07 (scheduled Agent sweep) per AGENTS.md's rule that a user-facing
      software project isn't `finished` on task count alone. This one is scoped
      intentionally as internal/admin-only (per t-002's own note), so the bar is lighter
      than a public-facing surface, but the same closing step still applies: you should
      look at it once before it's marked done.
      What I could verify from this sandbox: `https://kindrobots.org/admin/scene-animator`
      returns 200 with real SSR markup (confirmed live "Scene Animator" content in the
      response, not the bare SPA shell). What I could NOT verify: actual layout,
      spacing, or a real folder-to-video batch run (that needs real source media and the
      Comfy video pipeline, not a sandbox check).
      TO APPROVE: open /admin/scene-animator yourself, and if you have a moment, run one
      real batch against real source folders. If it looks and works right, set
      `approved_by_human: true` and flip project-overrides.yaml's scene-animator entry to
      `status: finished`. If something needs fixing first, note what and I'll file it as
      a fresh `ready` task.
      What unblocks when you do: nothing else is waiting on this -- it's the project's
      own closing step, and it also clears a currently-failing CI assertion
      (`test_current_project_lifecycle.py`) that flags any `active` project sitting with
      zero open tasks.

      Silas tested the Scene Animator surface on 2026-09-08 and rejected the closeout: the UI reports "Scene Animator source root is unavailable: /app/animate" and the animator does not work. Reopen this acceptance task for repair. Treat the observed production source-root failure as the concrete bug to fix before asking for acceptance again.
    updated: '2026-09-09T05:50:06Z'
