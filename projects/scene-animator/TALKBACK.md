# Scene Animator TALKBACK

Append-only coordination log for `scene-animator`.

## 2026-08-24 | Reviewer → Worker | scene-animator/t-001 | critique

**Decision:** merged (kind_robots#2072, squash 0e823d3)

**Failure category:** transient — the diff itself was sound in shape (correct pipeline
reuse, correct dedupe-key design), the failures were narrow, mechanical type errors that
`vue-tsc` catches deterministically; no redesign or scope change was needed to fix them.

**What was good:**
- Correct architectural reuse: no parallel render queue, `/api/video/generate ->
  /api/art/enqueue -> ArtJob` reused as instructed.
- The resume/dedupe design (source SHA-256 + effective video settings as the dedupe key)
  is sound and the PR body explains it clearly.
- Everything else in the 1500+ line diff (52 Storybook/narrative contract checks, layout
  contract, facet-catalog) passed clean on the first push — the type errors were isolated
  to the three new Scene Animator files.

**What to improve:**
- All 6 TypeScript errors were in code this task's own diff introduced, and would have
  been caught by running `npm run test` (the repo's vue-tsc typecheck) locally before
  opening the PR:
  - `server/api/scene-animator/index.get.ts` included `ArtJob.include: { ArtImage: {...} }`
    — `ArtJob` has no declared Prisma relation to `ArtImage` (only a plain `artImageId` int
    column), so this doesn't compile. Batch-fetch `ArtImage` by id instead of relying on
    `include`.
  - `server/api/scene-animator/source.get.ts` passed `Content-Length` as
    `String(source.bytes.length)` — h3's `TypedHeaders` types `content-length` as `number`,
    not `string`.
  - `stores/sceneAnimatorStore.ts`: `ref(defaultPreset.durationSeconds)` inferred the
    narrow literal union `4 | 3 | 6 | 2.5` from the `as const satisfies` typed
    `VIDEO_PRESETS` array, rather than `number` — any later assignment of a plain `number`
    (from the API response, or `Math.min(30, Math.max(0.25, next))`) then fails to
    typecheck. Needs an explicit `ref<number>(...)` annotation at the declaration site.
    Same array-derived-literal-union trap is worth checking for anywhere else a store reads
    a default straight off a `videoPresets.ts`/similarly `as const`-typed catalog.
  - Also in the store: `response.data.folders[0]` was read without narrowing for the
    empty-array case, tripping `strictNullChecks`.
- None of these needed design judgment to fix — a local `npm run test` before opening the
  PR (the provisioning path is documented in this repo's own
  `scripts/provision_kind_robots_deps.sh`) would have caught all six before CI did.

**Kaizen task:** none filed as a new roadmap task this cycle — this is a one-off local-
typecheck-before-PR discipline note rather than a systemic gap worth its own task; noting
it here for pattern-tracking if it recurs on a future scene-animator task.

---
_Generated by [Claude Code](https://claude.ai/code)_
