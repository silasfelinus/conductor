project: animation-manager
kind: software
autonomous: true

goal: >
  A durable creative pipeline that researches surprising passive desktop animations,
  pitches them before implementation, ships the strongest candidates into Kind Robots,
  and improves or retires builds over time using real user Reactions.

notes_from_silas: |
  Create surprising and new opening desktop wallpapers. Every animation must work as a
  passive experience with no interaction required. Optional interactions are encouraged
  when they deepen the surprise without blocking the page: bubbles can be popped, a cursor
  may leave a wake, and clicks may create harmless flourishes while the underlying click
  still works.

  Treat this as an evolving Animation Manager program, not a pile of unrelated effects.
  Research modern screensavers and browser animation techniques, keep a queue of planned
  animation pitches, build new effects, polish old ones, and track multiple attempts over
  time with Kind Robots Reactions so the best versions rise and weak experiments can be
  improved or retired.

  A new animation should be registered once and then automatically appear in Screen FX,
  explicit startup preferences, and the random opening animation pool when eligible.
  Front-load the project with pitches, then ship the first new addition end-to-end.

milestones:
  - id: m1
    title: "Research, quality contract, and animation pitch queue"
    weight: 20
    status: done
  - id: m2
    title: "Single-source animation registry and first shipped wallpaper"
    weight: 25
    status: done
  - id: m3
    title: "Attempt museum and Reaction-driven quality history"
    weight: 25
    status: not-started
  - id: m4
    title: "Animation Manager front end and promotion workflow"
    weight: 20
    status: done
  - id: m5
    title: "Daily research, build, and polish cadence"
    weight: 10
    status: not-started

tasks:
  - id: t-001
    milestone: m1
    title: "Write the passive-animation research and quality contract"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Added projects/animation-manager/RESEARCH.md and SPEC.md. The contract requires a
      satisfying passive loop, optional non-blocking interaction, requestAnimationFrame
      timestamp motion, surface-aware sizing, capped device-pixel ratio, cleanup, reduced-motion
      behavior, transparent layering, and a documented performance budget.

  - id: t-002
    milestone: m1
    title: "Front-load a diverse queue of animation pitches"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Added projects/animation-manager/PITCHES.yaml with twelve distinct candidates spanning
      bioluminescence, paper craft, fluid sand, stained glass, botanical machinery, clouds,
      moths, koi, weather, and impossible architecture. Bioluminescent Tide is the first build.

  - id: t-003
    milestone: m2
    title: "Centralize animation registration and ship Bioluminescent Tide"
    status: done
    owner: worker
    updated: "2026-07-13T23:03:00-07:00"
    passes: 0
    stakes: reversible
    note: >
      Shipped through kind_robots PR #237, squash commit
      afb88af80c2ae3816c6698830e13169e1b805ebf. stores/animationCatalog.ts is now the
      single metadata registry; effect ids are derived from it and Nuxt lazy components are
      resolved by filename convention in Screen FX and startup-animation. Added
      components/screenfx/bioluminescent-tide.vue: a passive, generationSafe wallpaper eligible
      for random startup selection, with cursor wake and click ripples that do not capture input,
      reduced-motion behavior, capped DPR, ResizeObserver sizing, and full listener/RAF cleanup.
      TypeScript Type Check and Contract Tests passed before merge.

  - id: t-004
    milestone: m3
    title: "Track animation attempts as Component records with Reactions"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-003
    note: >
      Use the existing Component model exactly for its stated museum-of-development-attempts
      purpose; no schema change. Define one Component row per animation build using folderName
      screenfx, a stable componentName such as bioluminescent-tide@v1, title, working/broken/
      construction flags, and structured notes with animation slug, build number, commit/PR,
      technique, quality checklist, and supersedes/supersededBy links. Reactions already support
      reactionCategory COMPONENT and componentId. Add store helpers and API-backed list/create/
      update/reaction flows without front-end components calling routes directly.

      IMPLEMENTED 2026-07-19 (conductor-burst session, kind_robots PR #564): found the
      Component/Reaction CRUD infra (componentStore.ts, componentHelper.ts, reactionStore.ts)
      already fully built and unused by any caller -- reactionCategory COMPONENT was already
      wired end-to-end. The actual gap was the animation-specific naming/tag convention, so
      added stores/helpers/animationComponentHelper.ts (componentName "<slug>@v<build>",
      folderName screenfx, listAnimationAttempts/getLatestAnimationAttempt/nextBuildNumber)
      plus componentStore.ts's recordAnimationAttempt/linkSupersededAnimationAttempt actions.
      Two deviations from this note's literal wording, both documented in SPEC.md's Attempt
      records section: (1) "working/broken/construction flags" map to the existing ComponentStatus
      enum (WORKING/UNDER_CONSTRUCTION/BROKEN/...) -- the boolean fields were already retired
      from the live schema before this task started; (2) the structured ledger (slug, build,
      commit/PR, technique, quality checklist, supersedes/supersededBy) lives in the `tags` JSON
      column, not `notes` -- `notes` is an uncapped VARCHAR(191) with no app-layer length limit,
      real risk of silent truncation for a growing JSON blob. `tags` existed on the model already
      (component_canonical_expand migration) but no route exposed it; added support to both
      server/api/components/index.post.ts and [id].patch.ts (also widened the PATCH componentName
      regex to allow "@"). Added utils/scripts/verifyAnimationComponentAttempts.ts (npm run
      test:animation-component-attempts, wired into contract-tests.yml) covering every one of
      the 30 catalog slugs against the real server-side componentName validation, plus
      listing/supersession logic. Verified clean: the new contract test, the existing
      verifyNoPrismaJsonCast.ts guard (confirmed the new `tags` cast doesn't trip the
      InputJsonValue ban -- it's a legitimate native-Json-column cast, kind-robots/t-033
      territory), full vue-tsc typecheck, and eslint on every touched file. No live DB in this
      sandbox, so did not attempt to backfill historical builds (bioluminescent-tide@v1,
      gravity-garden@v1) or exercise the new store actions end-to-end -- left as a follow-up
      once someone has DB access; t-007 (which depends on this task) will create the first
      real attempt record on its next build regardless.
    claimed_by: claude-conductor-burst-20260719T2100Z
    claimed_at: '2026-07-19T21:04:59Z'
    updated: '2026-07-19T21:25:18Z'

  - id: t-005
    milestone: m4
    title: "Build the Animation Manager gallery and promotion workspace"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-004
    note: >
      Add a Kind Robots Animation Manager surface linked from Screen FX. Show catalog effects,
      conductor pitch status, Component build history, aggregate Reaction/rating signals,
      working/construction/broken state, version lineage, and actions to preview, compare,
      promote, polish, or retire. Use an animationManagerStore for API and local state. Do not
      create a second effect registry; the UI reads stores/animationCatalog.ts.

      DONE 2026-07-20 (conductor-burst session, kind_robots PR #590, squash ad145742): shipped
      a new /animation-manager tab in the wonder dashboard, linked from Screen FX's header
      ("Manage builds"). stores/animationManagerStore.ts is a thin view-model composing the
      existing componentStore (t-004's recordAnimationAttempt convention) + animationStore
      (live preview trigger) + animationCatalog.ts -- no duplicate CRUD layer, no second
      registry. components/animation/animation-manager.vue reuses components/wonderlab/
      component-card.vue (already wired to Reaction ratings) for the gallery grid, a detail
      sidebar with full version history, and a two-up compare view. Actions: Preview (triggers
      the effect live), Compare, Promote (-> WORKING), Polish (-> UNDER_CONSTRUCTION), Retire
      (-> RETIRED), all as Component status transitions on the attempt row.

      Two deviations from the note's literal wording, both flagged for Reviewer in the PR: (1)
      "conductor pitch status" is not deeply wired -- no kind_robots API exposes this project's
      PITCHES.yaml (it lives only in this conductor repo), so rather than fabricate a second
      source of truth the UI links out to the existing /conductor page instead; (2) promote
      doesn't yet auto-supersede a prior WORKING build of the same effect (componentStore's
      linkSupersededAnimationAttempt exists but wasn't wired in -- left as the kaizen task).

      CI caught a real gap on first push: content/animation-manager.md declared channelKey lab
      / tabKey animation-manager but nothing registered that tab, so verifyChannelContent.ts
      failed with "references unknown tab lab/animation-manager". Fixed by adding
      content/channels/lab/animation-manager.md (mirrors screen-fx.md's shape). Verified: npm
      run test (vue-tsc, clean), eslint (clean), and every relevant test:* contract script
      (channel-content, channel-resolver, tutorial-channel-resolver, wonderlab-*,
      animation-catalog, animation-component-attempts) all green before and after the fix. No
      live DB in this sandbox, so promote/polish/retire and initialize() were not exercised
      against real data -- left as a live-smoke follow-up once DB/Vercel-preview access is
      available in a session (same limitation t-004 hit).
    claimed_by: claude-conductor-burst-20260720T000606Z
    claimed_at: '2026-07-20T00:06:06Z'
    updated: '2026-07-20T00:29:00Z'

  - id: t-006
    milestone: m5
    title: "Research and pitch one genuinely new passive animation"
    owner: worker
    passes: 0
    stakes: reversible
    recurring: true
    note: |-
      Daily creative-research loop. Produce at most one new pitch per Pacific calendar day and no-op/re-arm if one already exists for the day or the buildable queue is above twelve. Avoid cosmetic clones of existing effects. Every pitch needs a passive-loop hook, optional interaction, implementation technique, reduced-motion plan, performance risk, novelty comparison, and acceptance criteria. Append to PITCHES.yaml; never overwrite history.
      RAN 2026-07-19 (~19:46 UTC, conductor scheduled session): PITCHES.yaml's `updated` timestamp was stale at 2026-07-13, and the buildable (status: pitched) queue was at 11, so neither no-op condition applied. Added `ant-farm-excavation` (a cross-section ant-farm sand column with agent-driven tunnel digging/collapse), checked against all 12 existing pitch titles/themes for overlap -- distinct from magnetic-sand-garden (raked static patterns, no agents/excavation) and every other shipped/pitched concept. Brings the buildable queue to exactly 12, at the cap but not above it. No kind_robots code touched (pitch-only, per this task's scope) -- re-armed to ready per the recurring-task convention.
      RAN 2026-07-20 (~07:17 UTC, conductor agent session, claude-conductor-agent-20260720T0713Z): new Pacific calendar day (last run was 2026-07-19T19:45 -07:00) and the buildable queue was exactly 12 (not above twelve), so neither no-op condition applied. Added `marble-run-contraption` (a perpetual Rube Goldberg marble circuit -- fixed track segments, seesaw/bell/pinwheel contact triggers, bucket-elevator return), checked against all 13 existing pitches. Distinct from clockwork-greenhouse (organic growth state machines, not track-position physics), celestial-mobile (free-hanging balance physics, not a fixed circuit), and ant-farm-excavation (grid-based digging sim, not track traversal). Ran `scripts/check_animation_novelty.py --strict` -- no collisions at or above the 0.2 threshold across all 14 pitches. Brings the buildable queue to 13 (above the cap for the *next* cycle's no-op check, consistent with how the prior cycle read the same rule going from 11 to exactly 12). No kind_robots code touched (pitch-only) -- re-armed to ready per the recurring-task convention.
      RAN 2026-07-21 (~11:15 UTC, conductor scheduled session, claude-conductor-scheduled-20260721T1113Z): new Pacific calendar day (last run was 2026-07-20T07:17 UTC / 2026-07-20T00:17 -07:00) and the buildable queue was at 11 (two prior pitches -- paper-lantern-weather and magnetic-sand-garden -- had since moved to status: candidate, pulling the pitched count back down from the prior cycle's 13), so neither no-op condition applied. Added `strandbeest-migration` (a herd of wind-powered, Jansen-linkage wire-legged walker sculptures migrating across a dune horizon; wind gusts scale gait speed, click spawns a new walker), checked against all 14 existing pitches. Distinct from marble-run-contraption (fixed track-position circuit, not leg locomotion), ant-farm-excavation (grid-based digging, not walking), and celestial-mobile (free-hanging balance physics, not linkage-driven legs) -- no existing pitch or shipped effect uses procedural leg-linkage walking. Ran `scripts/check_animation_novelty.py --strict` -- no collisions at or above the 0.2 threshold across all 15 pitches. Brings the buildable queue to exactly 12, at the cap but not above it. No kind_robots code touched (pitch-only) -- re-armed to ready per the recurring-task convention.
      RAN 2026-07-22 (~16:42 UTC / 09:42 -07:00, conductor scheduled agent session): new Pacific calendar day (last run was 2026-07-21T11:15 UTC / 2026-07-21T04:15 -07:00) and the buildable queue was at 11 (t-007's 2026-07-22 build moved stained-glass-rain to status: candidate, pulling the pitched count back down from the prior cycle's 12), so neither no-op condition applied. Added `shadow-puppet-theater` (backlit rod-puppet cutouts performing short wordless vignettes with joint-driven walk/flight cycles and a slow-drifting backlight), checked against all 15 existing pitches. Distinct from bioluminescent-tide (ambient particle drift, not articulated figures), moth-constellation and every other entry -- no existing pitch or shipped effect uses backlit rod-puppetry or narrative vignette rotation. Ran `scripts/check_animation_novelty.py --strict` -- no collisions at or above the 0.2 threshold across all 16 pitches. Brings the buildable queue to exactly 12, at the cap but not above it. No kind_robots code touched (pitch-only) -- re-armed to ready per the recurring-task convention.


      Connector-native hourly Worker claim for the daily animation research cycle.

      Added Kintsugi Weather as the 2026-07-25 passive-animation research pitch on worker/animation-manager-t-006-20260725-8f2c. Reviewed and merged conductor PR #1043 (all 22 CI checks green, additive-only pitch YAML) -- re-armed to ready per the recurring-task convention.

      FIXED 2026-07-27 (~10:14-10:20 UTC, conductor scheduled burst-mode rotation, reviewer/20260727T0940Z-am-t006-pitch-consolidation): PR #1043's Kintsugi Weather pitch was never actually consolidated into PITCHES.yaml -- its "Flags for Reviewer" note said as much explicitly (stored as a dated additive file under projects/animation-manager/pitches/ to avoid an unsafe connector-side full-file rewrite, "a later consolidation pass can fold dated pitch artifacts into the canonical queue once the repository exposes a safe append processor"). Confirmed via `git log -- PITCHES.yaml` that no commit since #1021 (2026-07-22) had touched the file, while `projects/animation-manager/pitches/2026-07-25-kintsugi-weather.yaml` sat orphaned -- kintsugi-weather was invisible to check_animation_novelty.py and to t-007's "highest-priority unbuilt pitch" pick, even though PR #1043 reported success. Built the missing processor per the kaizen suggestion: new `scripts/consume_animation_pitches.py` drains dated `projects/animation-manager/pitches/*.yaml` artifacts into PITCHES.yaml as a targeted text insertion (not a full YAML re-dump, which would reformat every existing entry's folded-scalar style), authoritatively renumbers `priority` to max+1 (never trusts the artifact's own value, so two artifacts queued out of order can't collide), validates the merged result against `check_animation_novelty.py --strict` on a temp copy before writing anything real, and deletes the artifact once consumed. Dry-run by default, `--live` to apply. Added `tests/test_consume_animation_pitches.py` (10 cases: no-op, dry-run leaves files untouched, live run consolidates + deletes, priority is recomputed not trusted, `created` field dropped from the canonical entry, duplicate id is skipped and its stale artifact removed, invalid artifact is skipped not fatal, a genuine novelty collision aborts with nothing written, folded vs plain field formatting). Ran it live: kintsugi-weather (priority 17, no novelty collisions across all 17 pitches) is now in PITCHES.yaml and the orphaned artifact file is gone. Full `pytest` suite green (632 passed, 1 skipped). Not spending this cycle's daily-pitch pass on a new pitch -- today's Pacific slot is better used consolidating the backlog than adding to it while the drain gap existed. Re-arming to ready per the recurring-task convention.

      ROADMAP-ACCURACY FIX 2026-07-27T17:05:00Z (conductor scheduled Agent run, ai-art-academy/t-010 lane 2): `status:` was left at `review` by conductor PR #1216 even though that same PR's own note text says "Re-arming to ready per the recurring-task convention" -- classic note/field drift (AGENTS.md's task-events-race pattern, just via a manual PR edit instead of a race). Confirmed via `pull_request_read` that PR #1216 is merged and its diff shows `status: claimed -> review` with no later commit correcting it to `ready`; no open PR references this task (`list_pull_requests` on conductor returned none) and `claimed_by`/`claimed_at` were 7+ hours stale (past `CLAIM_TTL_MINUTES: 90`). Corrected `status` to `ready` and cleared the stale claim fields so the daily-pitch cadence isn't silently stuck.
      RAN 2026-07-27 (~19:20 UTC, conductor burst-mode rotation, claude-conductor-burst-20260727T1920Z-am-t006): new Pacific calendar day (last actual pitch addition was 2026-07-25's kintsugi-weather; the 2026-07-27T10:14 UTC session only consolidated that backlog and explicitly deferred its own pitch slot) and the buildable (status: pitched) queue was at 9 (not above twelve), so neither no-op condition applied. Added `tapestry-loom` (a hand loom weaving a deterministic motif row-by-row via a moving shuttle, then fully unraveling top-down onto a bobbin before rewatching a new palette/motif), checked against all 17 existing pitches. Distinct from clockwork-greenhouse (organic growth state machine, no weaving), marble-run-contraption (fixed physical track, no fabric or full-reset lifecycle), and kintsugi-weather (repairs existing damage rather than progressively constructing and fully unraveling a pattern) -- no existing pitch or shipped effect uses a textile/loom motif. Queued as a dated artifact (`projects/animation-manager/pitches/2026-07-27T19-20-00Z-tapestry-loom.yaml`) and drained it with `scripts/consume_animation_pitches.py --live` rather than hand-editing PITCHES.yaml's folded-scalar formatting -- assigned priority 18, `check_animation_novelty.py --strict` reported no collisions at or above the 0.2 threshold across all 18 pitches, and the artifact was deleted on consolidation. Brings the buildable queue to 10. No kind_robots code touched (pitch-only) -- re-arming to ready per the recurring-task convention.
      RAN 2026-07-28 (~21:09 UTC, conductor agent run): new Pacific calendar day (last run was 2026-07-27T19:20 UTC / 2026-07-27T12:20 -07:00) and the buildable queue was at 8 (below twelve), so neither no-op condition applied. Added `hourglass-cascade` (a glass hourglass with grains streaming through the neck and piling into a self-leveling cone via a folded height-map with occasional angle-of-repose slumps, then flipping a half-turn to reverse once the source bulb empties), checked against all 18 existing pitches. Distinct from magnetic-sand-garden (continuous flow-field combing of a static, never-emptied sand bed), ant-farm-excavation (agent-driven tunnel digging, not gravity-fed accumulation), marble-run-contraption (discrete objects on a fixed track, no pile state), and tapestry-loom (additive-then-unraveled weaving, not gravity accumulation with a flip) -- no existing pitch uses falling-grain accumulation or a physical flip-to-reverse mechanic. Queued as a dated artifact (`projects/animation-manager/pitches/2026-07-28T21-09-38Z-hourglass-cascade.yaml`) and drained it with `scripts/consume_animation_pitches.py --live` -- assigned priority 19, `check_animation_novelty.py --strict` reported no collisions at or above the 0.2 threshold across all 19 pitches, and the artifact was deleted on consolidation. Brings the buildable queue to 9. No kind_robots code touched (pitch-only) -- re-arming to ready per the recurring-task convention.
      NO-OP 2026-07-28 (~23:12 UTC, conductor scheduled Agent run): same Pacific calendar day as the 21:09 UTC run above, which already added today's one pitch (hourglass-cascade). Per this task's own daily cap ("Produce at most one new pitch per Pacific calendar day and no-op/re-arm if one already exists for the day"), skipped adding a second pitch to avoid the exact daily-cap collision the root TALKBACK.md 2026-07-28 "four-way rotation collision" entry already flagged as a live risk today. No files changed. Re-arming to ready per the recurring-task convention.
      RAN 2026-07-29 (~19:10 UTC, conductor scheduled burst-mode rotation, claude-conductor-scheduled-20260729T190600Z-am-t006): rotation landed on animation-manager this cycle (ai-art-academy/t-010 and model-builder/t-029/t-031 had claims in flight from concurrent sessions within the same minute window, and t-007 -- this project's own build lane -- had already built tiny-weather-front earlier today, 2026-07-29T13:33 UTC). New Pacific calendar day since the last pitch (hourglass-cascade, added 2026-07-28T21:09 UTC / ~14:09 PDT, Pacific day 07-28) and the buildable (status: pitched) queue was at 7 (not above twelve), so neither no-op condition applied. Added `geode-bloom` (a hollow geode cavity where hexagonal-lattice-constrained crystal clusters nucleate, grow facet-by-facet, hold at full bloom, then recede to free room for the next formation), checked against all 19 existing pitches. Distinct from kintsugi-weather (fracture-then-gold-seam repair -- geode-bloom never fractures, only grows/recedes additively), impossible-terrarium (disagreeing gravity-pocket rooms, not crystal accretion), and clockwork-greenhouse (organic mechanical-plant phase state machine, not hexagonal lattice geometry) -- no existing pitch or shipped effect uses lattice-constrained faceted mineral growth. Queued as a dated artifact (`projects/animation-manager/pitches/2026-07-29T19-10-00Z-geode-bloom.yaml`) and drained it with `scripts/consume_animation_pitches.py --live` rather than hand-editing PITCHES.yaml's folded-scalar formatting -- assigned priority 20, `check_animation_novelty.py --strict` reported no collisions at or above the 0.2 threshold across all 20 pitches, and the artifact was deleted on consolidation. Brings the buildable queue to 8. Full `pytest` suite green (773 passed, 1 skipped; had to reinstall the sandbox's isolated pytest tool with `--with pyyaml` first -- it was missing the `yaml` dependency the repo's scripts and tests rely on). No kind_robots code touched (pitch-only) -- re-arming to ready per the recurring-task convention.
      RAN 2026-08-12 (~11:31 UTC, conductor scheduled Agent run, session 20260812T1131-am-t006): task had gone stale (no pitch added since 2026-07-29 -- an almost-two-week gap the note history doesn't explain; nothing indicates a skipped no-op day, this cycle just wasn't picked up). New Pacific calendar day since the last pitch, well past 07-29, and the buildable (status: pitched) queue was at 8 (not above twelve), so neither no-op condition applied. Added `candlelit-reliquary` (a row of tapers on a stone ledge whose melted wax drips and pools into a growing, softening accretion that slowly relaxes between drips, with each candle burning down to a stub, guttering, and silently relighting on a fresh full taper), checked against all 20 existing pitches. Distinct from hourglass-cascade (dry discrete grains that only level past an angle-of-repose threshold and never soften/decay between events, not a continuous viscous pool with passive decay), geode-bloom (faceted crystal lattice growth/recession, not a viscous drip-and-pool dynamic), kintsugi-weather (repairs existing fractures rather than depositing new material), and tapestry-loom (additive-then-fully-unraveled weaving with no pooling/decay) -- no existing pitch or shipped effect uses continuous soft-material drip accumulation with passive decay paired with a burn-down/relight replacement cycle. Queued as a dated artifact (`projects/animation-manager/pitches/2026-08-12T11-31-44Z-candlelit-reliquary.yaml`) and drained it with `scripts/consume_animation_pitches.py --live` rather than hand-editing PITCHES.yaml's folded-scalar formatting -- assigned priority 21, `check_animation_novelty.py --strict` reported no collisions at or above the 0.2 threshold across all 21 pitches, and the artifact was deleted on consolidation. Brings the buildable queue to 9. No kind_robots code touched (pitch-only) -- re-arming to ready per the recurring-task convention.
      RAN 2026-08-14 (~10:43 UTC, conductor scheduled Agent run, session claude-scheduled-20260814T1040Z-am-t006): new Pacific calendar day since the last pitch (candlelit-reliquary, added 2026-08-12T11:31 UTC / ~04:31 PDT, Pacific day 08-12) and the buildable (status: pitched) queue was at 8 (not above twelve), so neither no-op condition applied. Added `frost-window-etching` (a windowpane where diffusion-limited-aggregation frost crystals branch inward from the edges, then a periodic warm breath patch erases and slowly regrows a clear region), checked against all 21 existing pitches. Distinct from geode-bloom (faceted hexagonal-lattice crystal growth, not freeform branching walkers), kintsugi-weather (repairs pre-existing fractures rather than growing new branching structure), candlelit-reliquary and hourglass-cascade (both accumulate into a height-map from falling/dripping primitives, not randomly-walking freeze-on-contact points), and tapestry-loom (fixed woven motif, not a randomized branching pattern) -- no existing pitch or shipped effect uses diffusion-limited aggregation, edge-seeded branching growth, or a breath-fogged erase/regrow cycle. Queued as a dated artifact (`projects/animation-manager/pitches/2026-08-14T10-43-13Z-frost-window-etching.yaml`) and drained it with `scripts/consume_animation_pitches.py --live` rather than hand-editing PITCHES.yaml's folded-scalar formatting -- assigned priority 22, `check_animation_novelty.py --strict` reported no collisions at or above the 0.2 threshold across all 22 pitches, and the artifact was deleted on consolidation. Brings the buildable queue to 9. No kind_robots code touched (pitch-only) -- re-arming to ready per the recurring-task convention.
    status: ready
    claimed_by: null
    claimed_at: null
    updated: '2026-08-14T10:45:00Z'

  - id: t-007
    milestone: m5
    title: "Build or polish one animation candidate from reaction evidence"
    passes: 0
    stakes: reversible
    recurring: true
    depends_on:
      - t-003
      - t-004
    note: 'Reconciliation (conductor scheduled sweep, state-reconciliation duty): task was left at status: review (claimed 2026-08-12T17:30:11Z by 20260812T1727-am-t007) even though kind_robots PR #1821 (Ant Farm Excavation screen FX) merged over 6 hours ago -- merged_at 2026-08-12T20:58:06Z, verified via GitHub MCP, all CI green. Rearmed to ready per recurring-task convention -- t-007 never reaches done; no code change, bookkeeping only.'
    status: ready
    updated: '2026-08-13T03:31:49Z'
    claimed_by: null
    claimed_at: null
    owner: null
    implementation_pr: 'silasfelinus/kind_robots#1821'

  - id: t-008
    milestone: m3
    title: "Add automated animation catalog and lifecycle verification"
    status: done
    owner: Reviewer
    passes: 0
    stakes: reversible
    depends_on: t-003
    note: 'Done via kind_robots PR #251 (merged, squash sha 683dd2e3): added utils/scripts/verifyAnimationCatalog.ts (npm run test:animation-catalog) and docs/architecture/animation-catalog-smoke-matrix.md. Script checks unique catalog ids, filename-to-lazy-component resolution (handles dotted filenames), valid preferredSurface values, a generationSafe/blocksInput invariant, and no stale preference/fallback ids (DEFAULT_PREFERENCES.startupEffect, pickRandomEffect''s fallback literal, every narratorAnimationAliases target). Follow-up t-010 filed for an unrelated stale ''bubble-effect'' id found in displayStore''s legacy (pre-t-003) EffectId system.'
    updated: '2026-07-14T14:02:41Z'

  - id: t-009
    milestone: m1
    title: "Add a novelty-collision check to the pitch pipeline"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: 'Merged: conductor PR #836. scripts/check_animation_novelty.py ships the Jaccard keyword-overlap novelty check (technique+surprise, --pitch/--threshold/--json/--strict), advisory by default per SPEC.md''s not-an-auto-reject intent. Also fixed a latent YAML syntax bug in PITCHES.yaml (unquoted colon in tiny-weather-front''s surprise field) that had made the file unparseable to any loader -- caught only because this task''s script was the first thing to ever read it. 13 new tests, full suite green (374 passed, 1 skipped).'
    claimed_by: claude-conductor-burst-20260719-rotation-animation-manager
    claimed_at: '2026-07-19T07:10:26Z'
    updated: '2026-07-19T08:12:54Z'

  - id: t-010
    milestone: m3
    title: "Remove or fix the stale 'bubble-effect' reference in displayStore's legacy animation system"
    status: done
    owner: worker
    passes: 1
    stakes: reversible
    note: >
      Found while building t-008's verification script (kind_robots PR #251). kind_robots
      stores/displayStore.ts's EffectId type and animationOptions array (a separate,
      pre-centralization debug-only system used only by components/screenfx/animation-tester.vue
      and components/admin/screen-debug.vue) include the id 'bubble-effect', which does not
      exist in stores/animationCatalog.ts — the single source of truth since t-003. There is an
      orphaned components/screenfx/bubble-effect.vue file, never registered in the catalog.
      Options: (a) register bubble-effect properly in animationCatalog.ts if the effect is worth
      keeping, or (b) retire displayStore's whole legacy EffectId/animationOptions/currentAnimation
      path (and animation-tester.vue) in favor of the centralized animationStore, if it's fully
      superseded. Out of scope for t-008 since it's a different, non-catalog system; scoped here
      as its own task. Not registered against verifyAnimationCatalog.ts since that script
      intentionally only covers the animationCatalog.ts-based system.

      RESOLVED 2026-07-18 (conductor-burst session): went with option (a) — registered
      bubble-effect in stores/animationCatalog.ts (kind_robots PR #390, merged). Confirmed
      bubble-effect.vue is a real, visually distinct effect (colorful per-bubble-color SVG
      circles with a pop-on-leave animation) worth keeping, not a duplicate of fizzy-bubbles
      (white glowing bubbles), and that animation-loader.vue's effectMap was already fully
      wired to render it — it was purely a missing catalog registration, not orphaned code.
      Verified with npm run test:animation-catalog (30 effects, unique ids, all screenfx
      components resolved) and npm run test (vue-tsc --noEmit), both clean.
    claimed_by: claude-conductor-burst-20260718T081118Z-838175
    claimed_at: '2026-07-18T08:11:21Z'
    updated: '2026-07-18T08:11:21Z'

  - id: t-011
    milestone: m1
    title: "Wire check_animation_novelty.py into CI as an advisory check on PITCHES.yaml changes"
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    note: >
      Kaizen from Reviewer's t-009 review (conductor PR #836): the novelty check exists but
      is manual-invocation-only, so a near-duplicate pitch can still land silently if nobody
      remembers to run it. Add a CI step (advisory, non-blocking — do not pass --strict) that
      runs scripts/check_animation_novelty.py whenever PITCHES.yaml changes and surfaces any
      flagged pairs in the job output/PR, matching the "not an auto-reject" intent already in
      the script and its originating task.


      DONE 2026-07-19 (conductor-burst rotation, claude-conductor-burst-20260719T1400Z): added
      .github/workflows/animation-novelty-check.yml, triggered on `pull_request` when
      projects/animation-manager/PITCHES.yaml or scripts/check_animation_novelty.py changes.
      Runs `python scripts/check_animation_novelty.py` (no `--strict`), matching the script's
      existing advisory intent — the job always exits 0 and any flagged pairs print straight
      into the check's log output for a reviewer to see, without failing the PR. Modeled on
      roadmap-audit.yml's path-triggered pattern already used elsewhere in this repo. Verified
      the workflow YAML parses (`yaml.safe_load`) and the existing 13-test
      tests/test_check_animation_novelty.py suite still passes unchanged (no script logic
      touched). No new test added for the workflow file itself — GitHub Actions workflows
      aren't unit-testable from this sandbox; correctness was checked by YAML validation plus
      matching the exact CLI invocation (`python scripts/check_animation_novelty.py`, no args)
      already covered by the script's own tests and its documented advisory default. Merged
      conductor PR #856 (squash edba0494), all 23 checks green, no review comments.
    claimed_by: claude-conductor-burst-20260719T1400Z
    claimed_at: '2026-07-19T14:08:14Z'
    updated: '2026-07-19T14:16:41Z'

  - id: t-012
    milestone: m4
    title: "Auto-supersede the prior WORKING build when Animation Manager promotes a new one"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-005's review (kind_robots PR #590): stores/animationManagerStore.ts's
      promoteAttempt() sets the chosen build's status to WORKING but does not call
      componentStore's existing linkSupersededAnimationAttempt() against the effect's
      previous WORKING build (if any), so "promote" can leave more than one WORKING attempt
      for the same catalog effect. Wire promoteAttempt() to look up the prior WORKING attempt
      for that slug (componentStore.allComponents filtered via listAnimationAttempts, status
      WORKING, excluding the one being promoted) and call linkSupersededAnimationAttempt on it
      before/after setting the new status, so promoting always leaves exactly one WORKING
      attempt per effect. Verify with a focused addition to
      utils/scripts/verifyAnimationComponentAttempts.ts or a small new store-level test if one
      exists; full vue-tsc + eslint as usual.


      DONE 2026-07-20 (conductor burst cycle claude-conductor-burst-20260720T0500Z):
      extracted the lookup as a pure findAttemptToSupersede() helper in
      stores/helpers/animationComponentHelper.ts (finds the other WORKING attempt for the
      same slug, excluding the one being promoted, or null if unparseable/none found) and
      wired promoteAttempt() to call componentStore.linkSupersededAnimationAttempt() on it
      before setting the promoted attempt's own status. Covered slug-matching,
      self-exclusion, no-prior-WORKING, and unparseable-componentName cases in
      verifyAnimationComponentAttempts.ts. Verified via npm run test:animation-component-attempts,
      full npm test (vue-tsc, clean), and eslint on all changed files (clean). kind_robots
      PR #613, squash f276662.
    claimed_by: claude-conductor-burst-20260720T0500Z
    claimed_at: '2026-07-20T04:05:35Z'
    updated: '2026-07-20T04:05:35Z'

  - id: t-013
    milestone: m6
    title: "Clear the four deferred live-smoke builds once DB access is available (or find a sandbox workaround)"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-007 (Reviewer, kind_robots PR #887, 2026-07-22): four consecutive
      animation-manager builds -- paper-lantern-weather (PR #627), magnetic-sand-garden
      (PR #785), stained-glass-rain (PR #838), and clockwork-greenhouse (PR #887) -- all
      carry the identical deferred item in PITCHES.yaml's builds: record: "Live browser
      smoke test (docs/architecture/animation-catalog-smoke-matrix.md) -- no reachable
      DATABASE_URL in the sandbox; server/utils/prisma.ts throws on every SSR request"
      plus the matching deferred Component attempt record (SPEC.md "Attempt records").
      Four repeats of the same paragraph is the threshold this file's own convention
      uses to promote a recurring note into a real task instead of a fifth PR carrying
      the same caveat.

      Two ways to close this out, either is acceptable:
      (a) A session with real DATABASE_URL/Vercel preview access (see AGENTS.md's
      "Visually verifying a front-end change" section for the Vercel MCP path) runs the
      smoke matrix against all four candidates on a live deployment, creates each
      effect's Component attempt record, and promotes each PITCHES.yaml builds: entry
      from candidate to shipped.
      (b) If no such access materializes for a while, investigate whether a throwaway
      SQLite or local Postgres the sandbox *can* reach would let server/utils/prisma.ts
      not throw on SSR for just enough of a smoke pass to satisfy the matrix, without
      touching any real database -- and if that's not practical either, fold "deferred,
      same sandbox limitation" into this task's own formal acceptance bar so future PRs
      stop re-explaining it per diff.

      RESOLVED 2026-07-26 (burst-mode cycle, path (b)): re-verified both blockers before
      falling back to the formal-acceptance-bar route. (1) Local DB workaround: no Docker
      daemon is reachable in this sandbox (`docker info` fails, no `/var/run/docker.sock`),
      so a throwaway local MySQL is not practical here; kind_robots'
      `provision_kind_robots_deps.sh` dummy `DATABASE_URL` only unblocks `vue-tsc`/`prisma
      generate`, never a running server, confirming the note above. (2) Vercel path:
      confirmed Vercel MCP access works (`list_teams`/`list_projects`/`list_deployments`,
      production `READY`) and its `web_fetch_vercel_url` can fetch the live homepage's
      SSR markup server-side, but it is a one-shot static fetch -- no click/resize/
      `prefers-reduced-motion` emulation -- so it cannot drive the smoke matrix's
      interactive rows. Also tried a real headless Chromium (the sandbox's pre-installed
      browser) through the sandbox's own egress proxy: `ERR_CONNECTION_RESET` on direct
      HTTPS to `kind-robots.vercel.app` (and to a plain `example.com` control case),
      confirming the egress policy -- not a cert/config bug -- blocks arbitrary
      interactive browser sessions to the live site from here.
      Formalized the fix per option (b): added a "Sandbox verification gap" section to
      SPEC.md documenting both findings once and defining a standing acceptance-bar
      exception -- a candidate that passes CI (TypeScript, contract tests,
      test:animation-catalog, test:animation-component-attempts) and satisfies the
      Non-negotiable experience contract by code review may ship with its live smoke
      test and Component attempt record tracked under one stable tag
      (`sandbox-access-gap`) instead of a fresh explanation per PR. Ran both verification
      scripts fresh against current kind_robots main (via
      provision_kind_robots_deps.sh) -- both pass. Applied the exception to all five
      builds that had accumulated the identical deferred note (paper-lantern-weather,
      magnetic-sand-garden, stained-glass-rain, clockwork-greenhouse, and a fifth that
      landed since this task was filed, moth-constellation #1010) plus cloud-city-drift
      (#949, same blocker, same fix, otherwise would have re-triggered this same kaizen
      next cycle) -- promoted PITCHES.yaml status: candidate -> shipped and normalized
      each `deferred:` list to the one-line tag. Component attempt records themselves
      remain genuinely un-created (still require live DB write access) -- that
      backfill work is tracked by the `sandbox-access-gap` tag on each build per the new
      SPEC.md section, to be cleared by whichever future session has real DB/interactive
      browser access, oldest first.
    updated: '2026-07-26T17:16:00Z'
    claimed_by: claude-conductor-burst-20260726T180000Z-am-t013
    claimed_at: '2026-07-26T17:06:28Z'

  - id: t-014
    milestone: m6
    title: "Wire npm run test:animation-catalog into contract-tests.yml CI"
    status: done
    passes: 0
    stakes: reversible
    note: |-
      Kaizen from t-007 (Reviewer, kind_robots PR #949, 2026-07-25): while building cloud-city-drift this cycle, found utils/scripts/verifyAnimationCatalog.ts had been throwing on current kind_robots main for three days (since commit b3d75ab, 2026-07-22) -- DEFAULT_PREFERENCES.startupEffect legitimately resolves to the 'random' sentinel, but the script asserted it must always be a literal catalog id. Fixed in PR #949, but it only surfaced because this session happened to run the script locally while verifying: .github/workflows/contract-tests.yml only runs test:animation-component-attempts, never test:animation-catalog, so the regression was invisible to CI the whole time. Add a "Animation catalog contract" step running `npm run test:animation-catalog` alongside the existing animation-component-attempts step in contract-tests.yml, so a future regression in catalog/id/surface/generationSafe invariants fails CI immediately instead of waiting for a session to notice locally.


      Added the animation catalog contract to kind_robots contract-tests.yml so catalog regressions fail CI alongside animation attempt conventions.

      DONE 2026-07-25: kind_robots PR #965 merged as squash c12401e9aaaa8ec789f388f56c82d936e96e6108. Added `npm run test:animation-catalog` to `.github/workflows/contract-tests.yml` beside the animation attempt contract. Contract Tests, TypeScript Type Check, and Facet Catalog Contract all passed; the new Animation catalog contract step itself completed successfully. Diff was CI-only: 3 additions, no runtime or production changes.
    updated: '2026-07-25T20:25:00Z'
    claimed_by: 2026-07-25T201035Z-animation-manager-t-014-7c4e
    claimed_at: '2026-07-25T20:10:35Z'

  - id: t-015
    milestone: m6
    title: "Request a terrarium/jar/glass-box icon for the animation catalog"
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-007 (Worker, kind_robots PR #1069, 2026-07-27): impossible-terrarium's
      catalog entry had to fall back to icon kind-icon:cube since assets/icons/ has no
      terrarium/jar/glass-box icon, a loosely-fitting match. Flagged as worth a dedicated
      icon-request pass rather than each future pitch picking the nearest existing icon
      ad hoc. Generate (or otherwise source) a terrarium/jar/glass-box icon, add it to
      assets/icons/, and swap impossible-terrarium's catalog entry (and any other pending
      candidate that reuses cube/box as a placeholder) to reference it.

      IMPLEMENTED 2026-07-28 (conductor burst-mode cycle): added a hand-drawn line-art
      assets/icons/terrarium.svg matching the existing kind-icon style (24x24, currentColor
      stroke), swapped impossible-terrarium's stores/animationCatalog.ts entry from
      kind-icon:cube to kind-icon:terrarium (confirmed it was the only catalog entry
      reusing cube/box as a placeholder), and regenerated stores/seeds/validIcons.ts via
      the existing utils/scripts/updateKindIcons.js.

      DONE 2026-07-28T08:05Z: kind_robots PR #1086 merged. Closing out.
    updated: '2026-07-28T08:05:33Z'
    claimed_by: claude-conductor-burst-20260728T0800Z-am-t015
    claimed_at: '2026-07-28T07:16:45Z'

  - id: t-016
    milestone: m6
    title: "Flag placeholder-icon reuse in animationCatalog.ts automatically instead of relying on manual kaizen passes"
    status: done
    passes: 0
    stakes: reversible
    note: |-
      IN REVIEW 2026-07-28 (session 2026-07-28T12-25-00Z-am-t016-placeholder-icon): added a GENERIC_PLACEHOLDER_ICONS check (kind-icon:cube, kind-icon:box) to utils/scripts/verifyAnimationCatalog.ts (already wired into contract-tests.yml as test:animation-catalog) -- fails with a message pointing at the terrarium.svg precedent (assets/icons/, node utils/scripts/updateKindIcons.js) if a future ANIMATION_EFFECTS entry reuses a placeholder icon. Verified: npm run test:animation-catalog passes against the current 38-entry catalog; manually confirmed the check fails with a clear message when temporarily forced to kind-icon:cube (reverted before commit); eslint clean; prettier clean; vue-tsc --noEmit exit 0. kind_robots PR #1104 open.
      Original spec follows: Kaizen from t-015 (Reviewer, kind_robots PR #1086, 2026-07-28): t-015 existed only because t-007 happened to notice impossible-terrarium was reusing kind-icon:cube as a loosely-fitting placeholder. Add a lightweight structural check (e.g. a small addition to utils/scripts/updateKindIcons.js or a standalone script run in CI) that flags any stores/animationCatalog.ts entry whose icon is a generic placeholder (kind-icon:cube, kind-icon:box, or similar) so a dedicated-icon request surfaces on its own the next time this happens, instead of depending on a future kaizen note to catch it by chance.


      Merged kind_robots PR #1104 as e710f91437cdd503a9ffb47ea70b09010991babb and Conductor PR #1325 as 9d9a479397ca0ccd96aae8b45f7f8d489b6b45fd. The animation catalog contract now rejects generic cube and box placeholder icons and directs contributors to add and regenerate a dedicated icon.
    updated: '2026-07-28T13:11:42Z'
    claimed_by: 2026-07-28T12-25-00Z-am-t016-placeholder-icon
    claimed_at: '2026-07-28T12:27:06Z'

  - id: t-017
    milestone: m6
    title: "Document the sandbox pytest/pyyaml gap so future sessions don't rediscover it"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    claimed_by: claude-conductor-scheduled-20260729T200653Z-am-t017
    claimed_at: '2026-07-29T20:06:54Z'
    updated: '2026-07-29T21:00:43Z'
    note: >
      Kaizen from t-006 (Reviewer, conductor PR #1449, 2026-07-29): the session's
      isolated `pytest` tool (installed via `uv tool install`) had no `yaml`
      dependency, so `pytest` failed to collect any test touching PyYAML-based
      scripts (consume_animation_pitches, check_animation_novelty) even though the
      system `python3` interpreter the scripts themselves use has yaml installed
      fine. Fixed locally that cycle via `uv tool install pytest --with pyyaml
      --force`, but nothing records this for the next session that hits the same
      unexplained `ModuleNotFoundError: yaml` under plain `pytest` (distinct from
      `python3 -m pytest`'s own separate "no pytest module" failure). Add a short
      note to AGENTS.md's cross-repo/tooling section (or a scripts/README) so
      future sessions recognize the symptom and apply the fix immediately instead
      of re-deriving it.

      RESOLVED 2026-07-29T20:06:54Z (conductor scheduled burst-mode rotation,
      claude-conductor-scheduled-20260729T200653Z-am-t017): added a "Sandbox
      `pytest` is missing PyYAML" subsection to AGENTS.md, right after the
      Cross-repo tasks section, documenting the `ModuleNotFoundError: yaml`
      symptom under bare `pytest`, how it differs from `python3 -m pytest`'s
      separate missing-module failure, and the fix (`uv tool install pytest
      --with pyyaml --force`). Re-ran that exact command in this session to
      confirm it still resolves the gap (pytest 9.1.1 + pyyaml 6.0.3 installed
      cleanly). No script/roadmap-tooling changes needed beyond the doc note.
