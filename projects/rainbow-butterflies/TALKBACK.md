# Rainbow Butterflies — TALKBACK

## 2026-08-30 | Reviewer → Worker | rainbow-butterflies/t-023 | critique

**Decision:** merged (kind_robots PR #2225, squash 64cf403)

**Failure category:** n/a — clean first-pass merge

**What was good:**
- Extensible typed reference shape (`{ kind, id }`) instead of one field per object
  kind — matches the task's explicit design constraint ("more KR object kinds can be
  added without a new forum-post schema each time").
- Reused existing `Chat.artImageId`/`Chat.projectId` relations with no migration, kept
  the forum from cloning object state, and re-checks visibility on every serialize so a
  later-privatized/deactivated object silently drops from previews rather than leaking.
- Maturity/ownership enforcement applied consistently across create-thread, create-reply,
  and PATCH, with `attachments: []` vs. omitted field given distinct, documented semantics.
- Shipped the additive OpenAPI 1.1 contract, docs, and both new and updated tests in the
  same PR — no separate follow-up needed to make the contract discoverable.

**What to improve:**
- Task status sat at `claimed` through PR open instead of flipping to `review` first (see
  AGENTS.md step 7) — cosmetic here since Reviewer caught the open PR directly, but worth
  tightening so a later sweep doesn't have to hand-check the open-PR list.

**Kaizen task:** t-030 — Add a third canonical forum-attachment kind (e.g. Bot or
Character) reusing the existing typed reference resolver and maturity checks.

**Pattern note:** One CI check (`comment-contract` / `verifyPopulationDraftQuality`) was
red on this PR but is unrelated base-branch drift (character-ID mismatches in comment
population batch files vs. current production data) — confirmed failing on kind_robots
`main` since 2026-08-30T03:32 UTC across 5 consecutive runs before this PR even opened,
and the PR touches none of the flagged batch files. Merged past it per the CI-red base-
branch-broken rule; the underlying drift is a separate, pre-existing issue worth its own
task if it doesn't self-resolve (likely a stale snapshot of "production" character IDs
baked into the verifier's expectations, or a recent character reseed).

## 2026-08-30 | Agent (Claude, scheduled conductor run) | rainbow-butterflies/t-030 | resolution

**Decision:** merged

**Failure category:** n/a — clean first pass

**What was good:**
- Confirmed `Chat.characterId` and the `Character` relation already existed on the Prisma
  schema before writing any code — no migration needed, and it validated CHARACTER as the
  right pick over the note's other suggestion (Bot), since `Chat` already has a differently-
  scoped `botId`/`Bot` relation (the authoring bot) that a naive reuse would have collided
  with semantically.
- Traced the actual in-app deep-link idiom (`character-manager.vue`'s `characterId ?? character`
  numeric-id resolution, `content/characters.md` as the existing top-level content page) before
  picking the canonical URL, rather than guessing a path — mirrors exactly how `/art?art=<id>`
  and `/conductor?project=<id>` are consumed elsewhere.
- Set every touched surface in one pass: resolver, all three write routes (thread create, reply
  create, patch connect/disconnect), OpenAPI enum + version bump, docs, and both contract test
  scripts — no follow-up needed to make the extension discoverable or tested.

**What to improve:**
- Nothing new this cycle; t-023's own TALKBACK entry already flagged the one process gap
  (flip to `status: review` before opening the PR) and this session did that correctly.

**Kaizen task:** deferred — a natural next slice is a `BOT`-kind attachment (a second,
differently-named FK on `Chat` for an *attached* Bot showcase, distinct from the existing
authoring-bot relation), but that's a real design decision (new column) rather than a
one-sentence reversible task, so left for Silas/a future task to scope rather than
auto-created here.

**Pattern note:** none new — the `comment-contract` / `verifyPopulationDraftQuality`
pre-existing base-branch failure (tracked in t-023's entry above) was still red on this
PR's CI and still confirmed non-required / unrelated (6 consecutive failing runs on
`main` as of this session, still none touching files either PR changed).

---
_Generated by [Claude Code](https://claude.ai/code)_

## 2026-08-30 | Agent (Claude, scheduled conductor run) | rainbow-butterflies/t-025 | resolution

**Decision:** merged

**Failure category:** n/a — clean first pass

**What was good:**
- Read the existing forum implementation (`server/utils/forumApi.ts`, `utils/forumApiContract.ts`,
  the write/read routes) before designing anything, and found report/flag handling already shipped
  — only the remaining five buckets in the task note needed new work.
- Found the project's own existing "restriction" primitive (`User.isRestricted` /
  `server/utils/restriction.ts`'s `notInRestricted`, used elsewhere to shadow-ban a user's
  existing content) and discovered it was never wired into the forum at all — neither write nor
  any of the four forum read paths checked it. Closing that gap (write-time: force `isPublic:
  false` rather than reject, to preserve shadow-ban semantics; read-time: exclude via
  `notInRestricted` everywhere) was more valuable than inventing a new restriction mechanism.
- Kept every new control additive/no-migration: rate limiting and duplicate rejection are
  plain `Chat` queries scoped by actor+window; health-claim escalation reuses the existing
  `Reaction`-based flag rows (parses the same JSON `comment` shape `flag.post.ts` already
  writes) and the `Log` model via a new `logSystemAction` sibling to the existing
  `logAdminAction`; edited/removed states are derived from existing `createdAt`/`updatedAt`/
  `isActive` columns.
- Caught a real prettier-tooling gap before it became repo damage: this sandbox's installed
  prettier reformats even *unrelated, pre-existing* lines in every file it touches (confirmed
  via two `git checkout --hard` + re-edit cycles after `prettier --write` initially blew up
  the diff on both `forumApi.ts` and `forumApiContract.ts`). Settled on hand-wrapping only the
  lines actually added, verified against `npx prettier <file>` (no `--write`) diffed line-by-line
  against `git diff` hunks, so the final diff is purely additive and prettier's complaints are
   confirmed identical to `main`'s pre-existing state file-by-file.

**What to improve:**
- Tombstoning removed posts (rendering `"[removed]"` instead of 404/vanishing) is a real behavior
  change to two read routes (`posts/[id].get.ts`, `threads/[id].get.ts`'s reply listing) that I
  judged in-scope from the task note's "clear removed states" phrase — worth Silas's eyes if the
  UX isn't what he pictured, since a quieter reading of that phrase would have been "just expose
  `edited`/`removed` booleans" without changing what 404s.

**Kaizen task:** t-031 — Add an admin-facing endpoint (or extend an existing admin dashboard) to
list posts currently auto-hidden by health-claim escalation, so a human moderator has a queue to
review rather than only a `Log` entry to grep for.

**Pattern note:** the `comment-contract` / `verifyPopulationDraftQuality` pre-existing
base-branch failure (tracked in t-023's and t-030's entries above) was still red on this PR's
CI — confirmed via `list_workflow_runs` still failing on every `main` run since at least
2026-08-30T03:32 UTC, unrelated to any file this PR touches. Commented once on the PR per the
CI-red convention and merged past it without a second occurrence of the same investigation.

---
_Generated by [Claude Code](https://claude.ai/code)_
