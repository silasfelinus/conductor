project: brainstorm
kind: proposal
notes_from_silas: >
  Generate ideas for me to vote on. I want to log in and see things like: 5 STL
  project ideas, 5 comic pitches under the genre guidelines I gave, lists of
  potential revenue streams, and ways to improve AI_Networker. QUALITY OVER
  QUANTITY — a few strong, specific, buildable pitches beat a pile of vague ones.
  Respect my genre and direction guidance in CONTROL.md. Tie product ideas to our
  approved product types where relevant. Most importantly, do not pitch work that
  already exists as a project, roadmap task, shipped feature, archived pitch, or
  obvious extension of an active project. Existing work belongs in that project's
  roadmap or kaizen queue, not back in the pitch inbox wearing a fake moustache.
pitch_policy:
  max_new_pitches_per_cycle: 3
  max_open_awaiting_pitches: 6
  dedup: true
  dedup_sources:
    - pitches/*.md regardless of status
    - projects/*/roadmap.yaml project goals, waypoints, tasks, and notes
    - STATUS.md active project inventory
    - known kind_robots routes, stores, APIs, and shipped dashboard features
  existing_work_action: >
    If an idea overlaps an existing project or shipped feature, do not create a new
    pitch. Either skip it, or add a clearly scoped task/kaizen to the existing
    project when the idea contains a genuinely new increment.
  required_pitch_sections:
    - The idea
    - Why it's worth doing
    - Rough effort
    - Suggested first task
    - Existing-work check
  categories:
    - new-products
    - content-series
    - revenue-streams
    - ai-networker-upgrades
    - cross-project
milestones:
  - id: m1
    title: Idea engine running and producing votable pitches
    weight: 0
    status: in-progress
tasks:
  - id: t-001
    milestone: m1
    title: Generate this cycle's pitches
    status: ready
    owner: null
    updated: '2026-07-28T09:09:13Z'
    passes: 0
    stakes: reversible
    recurring: true
    note: >
      2026-07-28 cycle (conductor burst rotation, claude-conductor-burst-20260728T0909Z-bs-t001):
      queue was at 3/6 awaiting-silas before this cycle, leaving room for up to 3 new. Wrote 2
      new pitches (kept to 2, not the full 3, on quality grounds — a third candidate around
      Silas's "5 comic pitches under the genre guidelines" note was dropped because no genre
      guidance actually exists in CONTROL.md's brainstorm section to write against; see the
      dedup/gap note below rather than fabricating genres). (1)
      pitches/2026-07-28-kind-robots-figurine-stl-line.md (new-products/cross-project) —
      connects two already-designed-but-never-run halves: digital-storefront's pre-approved
      `stl-3d` product type (unused by any current catalog item) and model-builder/t-012's
      spec'd Hunyuan3D-to-STL pipeline (designed, never executed for a real Reward). (2)
      pitches/2026-07-28-kind-robots-branch-janitor.md (ai-networker-upgrades) — ports
      conductor's proven scripts/branch_janitor.py + workflow to kind_robots, which has no
      equivalent; grounded in two independent 2026-07-28 TALKBACK entries that each hit the
      identical missing capability this week. Checked both against pitches/*.md (all statuses),
      every projects/*/roadmap.yaml goal/task, STATUS.md's active inventory, and the relevant
      kind_robots-side folders (scripts/, .github/workflows/) before writing either — no
      overlap found with existing pitches, shipped features, or roadmap tasks.

      GAP NOTED (not fixed this cycle, scope creep to fix inline): brainstorm/roadmap.yaml's
      own notes_from_silas references "genre and direction guidance in CONTROL.md" for comic
      pitches, but CONTROL.md's `### brainstorm` section currently has no genre guidance at
      all (checked the full file). This is the same class of prose-drift CONTROL.md itself
      warns about elsewhere (see the kindrobots-unraid priority.yaml precedent, 2026-07-14) —
      either the guidance was never written down, or it lives somewhere this pass didn't find.
      Left as `needs-human` bait for Silas rather than guessing at genres and shipping
      possibly-misaligned comic pitches; flagging here so a future cycle doesn't rediscover
      this cold.

      2026-07-12 hygiene run: reviewed the six lingering awaiting-silas pitches.
      Archived the Kind Robots coloring pack and Conductor agent-health dashboard as
      duplicates, and superseded the AI session-credits subscription because the
      subscription, Stripe, and mana surfaces already exist in kind_robots. Three
      distinct product pitches remain for human review: pet-business guide,
      Patchwork Worlds RPG zine, and Narrative Prompt Deck. Before every future
      cycle, count awaiting pitches and stop when the queue is at the cap. Then scan
      every pitch file regardless of status, every project roadmap goal/waypoint/task,
      STATUS.md, and relevant kind_robots routes/stores/components. A title rewrite
      does not make existing work new. Each new pitch must include an
      `## Existing-work check` section naming the closest projects/features inspected
      and explaining why the proposal is still distinct. If the useful part belongs
      inside an existing project, add a task or kaizen there instead of creating a
      pitch. Open one PR with all truly new pitches, return this recurring task to
      ready, and never set it to done.
