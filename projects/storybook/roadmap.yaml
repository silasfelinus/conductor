project: storybook
kind: software
goal: >
  Ship the collaborative Storybook experience as a polished story-first product: guided and
  limited-visibility play, reusable story state/artifacts, the in-progress casting/setup
  redesign, and a responsive front end that has been visually accepted on phone/tablet/desktop.
notes_from_silas: 'Build the collaborative storytelling experience layer on top of the existing Kind Robots models. Support guided adventure and limited-visibility collaboration modes with async-first play, branching choices, custom inputs, rewards, inventory, and reusable story state. Boundaries with Da Vinci (see projects/davinci/docs/storybook-boundary-comparison.md): no shared run/session tables, no columns added to Life* models — shared behavior only through existing Kind Robots models (Bot, Chat, Character, Dream, Scenario, ArtCollection, Milestone) or extracted pure utilities. Respect this when designing the session schema.'
milestones:
- id: m1
  title: Session model and boundaries
  weight: 30
  status: done
- id: m2
  title: Turn engine and branching state
  weight: 30
  status: in-progress
- id: m3
  title: Player rewards and reusable artifacts
  weight: 20
  status: in-progress
- id: m4
  title: Prototype UX
  weight: 20
  status: in-progress
tasks:
- id: t-001
  milestone: m1
  title: Draft Storybook session data model
  status: done
  owner: worker
  updated: '2026-07-01T00:00:00Z'
  passes: 0
  stakes: reversible
  gate_human: true
  approved_by_human: true
  note: 'Session data model written and approved. It covers participants, narrator Bot, Scenario variables, narrative state, branch history, turn ownership, and persistence between turns.'
- id: t-002
  milestone: m2
  title: Define Guided Adventure turn lifecycle
  status: done
  owner: worker
  updated: '2026-07-02T19:14:32-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-001
  note: 'Written projects/storybook/docs/guided-adventure-turn-lifecycle.md. Spec only — no schema, API, live data, deploy, or publishing changes.'
- id: t-003
  milestone: m2
  title: Define limited-visibility collaboration rules
  status: done
  owner: worker
  updated: '2026-07-02T20:18:03-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-001
  note: 'Written in PR #99. It defines async turn order, bounded visibility, contribution rules, mutation permissions, and session-scoped story merging. Spec only — no schema, API, live data, or publishing changes.'
- id: t-004
  milestone: m3
  title: Map rewards, locations, items, and characters back to profiles
  status: done
  owner: worker
  updated: '2026-07-02T21:20:00-07:00'
  passes: 0
  stakes: reversible
  depends_on: t-001
  note: 'Written and merged in PR #100: projects/storybook/docs/profile-artifact-mapping.md. Spec only — no schema, API, live data, deploy, or publishing changes.'
- id: t-005
  milestone: m4
  title: Sketch first Storybook UX flow
  status: done
  owner: worker
  updated: '2026-07-03T00:00:00Z'
  passes: 0
  stakes: reversible
  depends_on:
  - t-002
  - t-003
  note: 'Written and merged in PR #106: projects/storybook/docs/first-ux-flow.md. Covers session start, narrator and seed choice, turn screens, artifact review, resume states, component boundaries, and MVP/deferred scope. Spec only — no schema, API, live data, deploy, or publishing changes. The needs-human status the Worker left this at was a soft escalation (connector could not merge), not a content gate — Reviewer merged and closed it out.'
- id: t-007
  milestone: m4
  title: Add a script that prints the first unblocked ready task across roadmaps
  status: done
  owner: worker
  updated: '2026-07-03T09:00:00-07:00'
  passes: 0
  stakes: reversible
  note: 'Merged in PR #110: scripts/next_ready_task.py. The needs-human the Worker left this at was a soft escalation (connector could not run shell to verify) not a content gate, matching the t-005 pattern. Reviewer tested it in two local worktrees against real roadmap state (PR branch and pre-merge main) and confirmed it correctly applies priority order, project-overrides.yaml active-status filtering, and depends_on/gate_human resolution before merging.'
- id: t-008
  milestone: m4
  title: Add unit tests for scripts/next_ready_task.py covering dependency chains, paused/retired projects, and gate_human tasks
  status: done
  owner: worker
  updated: '2026-07-03T02:13:53-07:00'
  passes: 0
  stakes: reversible
  note: 'Merged in PR #116: tests/test_next_ready_task.py covers priority order, paused/retired project skipping, dependency chains, unapproved gate_human dependencies, and approved gate_human unblocking for scripts/next_ready_task.py.'
- id: t-006
  milestone: m1
  title: Audit kind_robots Bots, Characters, Scenarios, and Chats models for session compatibility
  status: done
  owner: worker
  updated: '2026-06-30T00:00:00Z'
  passes: 0
  stakes: reversible
  note: 'Wrote docs/kr-model-audit.md. All 5 models audited. No MVP blockers in existing schema.'
- id: t-009
  milestone: m1
  title: Cross-link the Da Vinci boundary rules before designing session schema
  status: done
  owner: worker
  passes: 0
  stakes: reversible
  note: >
    Kaizen from davinci/t-007: read and reference
    projects/davinci/docs/storybook-boundary-comparison.md in the Storybook
    session-schema work. Add a short "boundaries with Da Vinci" section to the
    session data model doc (or a pointer in notes_from_silas) so schema design
    respects: no shared run/session tables, no columns added to Life* models,
    shared behavior only via existing KR models or extracted pure utilities.
    DONE: no standalone "session data model doc" file exists (t-001's model was
    approved via its roadmap note only, no doc artifact), so used the task's own
    fallback — added a "Boundaries with Da Vinci" pointer to this roadmap's
    notes_from_silas, summarizing the concrete rules from
    projects/davinci/docs/storybook-boundary-comparison.md's "Concrete boundary
    rules" section: no shared run/session tables, no columns on Life* models,
    shared behavior only via existing KR models or extracted pure utilities.
    Any future session-schema design task will read notes_from_silas first per
    AGENTS.md's picking-order rules, so this is in the right place to actually
    be seen before schema work starts.
  claimed_by: claude-conductor-scheduled-20260720T0511Z
  claimed_at: '2026-07-20T05:13:44Z'
  updated: '2026-07-20T05:21:14Z'
- id: t-010
  milestone: m4
  title: Polish and upgrade Storybook front-end surface
  status: ready
  passes: 0
  stakes: reversible
  note: |-
    UI polish + upgrade for the storybook front end now that the scaffold page is live at /storybook. Steps: (1) generate final dashboard-tab art at public/images/dashboard-tabs/scenario/storybook.webp and tutorial art at public/images/tutorials/scenario/storybook.webp; (2) add a matching section for 'storybook' under tutorialChannels.scenario.sections in kind_robots/stores/helpers/tutorialCards.ts; (3) verify the PROJECT Dream liveUrl points at /storybook (admin can run the Conductor 'Placements' button to backfill channelKey/tabKey/liveUrl); (4) evolve the placeholder scaffold page into the full interactive experience for this project.
    PROGRESS 2026-07-20 (conductor agent session claude-conductor-agentrun-20260720-storybook, kind_robots PR #640 merged squash b6adafd8): step (2) done -- added a 'storybook' section under tutorialChannels.scenario.sections in stores/helpers/tutorialCards.ts, mirroring the existing serendipity entry, resolving via tutorialImage('scenario', 'storybook') with a placeholder fallback until dedicated art lands. Step (4) partially advanced rather than confirmed already-covered: the scaffold's #interactive slot was a static card linking out to /stories with no real data, so wired it to scenarioStore (initialize() on mount, cheap local/seed data only, no live fetch, matching the SSR-safe pattern already used in scenario-gallery.vue/scenario-interact.vue/dream-manager.vue) to show a live scenario count plus up to 3 scenario titles linking to /stories, alongside the existing CTA. Not a full dedicated Storybook UI -- that would be materially larger scope than a polish pass; left as a genuinely open step for a future cycle if Silas wants a bespoke flow rather than reuse of the Stories engine. Remaining and kept ready: step (1) dashboard-tab + tutorial .webp art (needs the generation backend/egress -- relay confirmed still down this session, see ai-art-academy/t-035) and step (3) the liveUrl backfill (an admin Placements click, not an agent action). Verified: eslint + prettier clean on both changed files, full-project vue-tsc --noEmit exit 0. All 3 kind_robots PR checks green (TypeScript, Contract verifiers, GitGuardian).
     PROGRESS 2026-07-21 (~04:29 UTC, conductor scheduled agent run, claude-conductor-scheduled-20260721T0410Z): step (3) DONE -- this project's Project row liveUrl/channelKey/tabKey are NOT an admin-only action after all. Discovered kind_robots utils/projectPlacements.ts, a hardcoded canonical map the front end reads at runtime for every project's channelKey/tabKey/route (storybook: channelKey 'play', tabKey 'storybook', route '/storybook' -- confirmed by directly fetching the file, not just a subagent's paraphrase). Mirrored those exact values into project-overrides.yaml and ran scripts/sync_projects.py; confirmed live via GET /api/projects that the Project row (id=10) now carries all three fields matching the canonical map exactly. NOTE: caught and fixed a real mistake in the same cycle -- serendipity/t-012's equivalent backfill (done just before this task) had initially used channelKey: scenario, guessed from the tutorialChannels.scenario grouping, which is a DIFFERENT and unrelated concept from PROJECT_PLACEMENTS' channelKey; corrected to 'play' once projectPlacements.ts was found and verified. Given this map is authoritative and already covers 26 project slugs, also bulk-backfilled the same fields for every other ACTIVE project appearing in it that was still null in the DB (22 projects: coloring-book, wishmaster, appmaker, model-builder, sketchy, packmaker, davinci, media-watchlist, coat-dance, ruler-hooked, newsfeed, humboldt-scoop, humboldt-scoop-cms, conductor-app, alexa-integration, superkate-services-calculator, superkate-hairstyle-ai, digital-storefront, ai-art-academy, brainstorm, mural-design, conductor) -- see root TALKBACK.md for the full account. Their own project roadmap notes are left untouched (out of scope for one task's diff); any of their 'polish' tasks can now skip the Placements step entirely on next touch. Step (1) art still blocked -- relay confirmed down again this session (see serendipity/t-012's note for the corrected recheck method).
    PROGRESS 2026-07-22 (~05:06-05:40 UTC, conductor burst-mode cycle, claude-conductor-scheduled-20260722T0506Z-story-t010): step (4) follow-on -- the interactive card's only action was a link out to the general /stories studio, leaving visitors to find the create flow themselves. Added a primary "Start a new scenario" CTA. Naive approach (call navStore.setDashboardTab('scenario', 'add') before navigating) was tried first and found broken: /stories's own content frontmatter (dashboardTab: scenarios) re-resolves and overwrites the dashboard tab on every page load, clobbering any preset tab the instant the target page mounts. Fixed properly by deep-linking to /stories?scenario=new and reading that query param in scenario-manager.vue's onMounted (after the frontmatter resolution runs), overriding the tab to 'add' -- mirrors the existing ?manaTopup/?subscription query-flag pattern already used in giftshop-manager.vue. Verified: eslint clean on both changed files; full-project npm run test (vue-tsc --noEmit) exit 0; prettier --check clean on storybook-page.vue, and confirmed via git-stash that scenario-manager.vue's prettier drift pre-dates this change (same warning without the diff, consistent with the prettier-version-mismatch noted elsewhere in this repo's rotation notes) -- no blanket reformat. kind_robots PR #857 (branch claude/keen-fermat-pvflnj) MERGED (squash 75555b4a) after all 4 checks green (Contract verifiers, TypeScript, verify, GitGuardian). Live browser smoke of the full deep-link flow deferred (no reachable DATABASE_URL in this sandbox, same class of blocker as model-builder/t-031 and davinci/t-014's notes). Steps (1) and (3)'s remaining half (dashboard-tab/tutorial art) stay blocked on the relay as documented above; not touched this cycle. Kept at ready (not done) since step (1) is still outstanding, same pattern as model-builder/t-029's handling of a genuinely blocked substep.
    PROGRESS 2026-07-27 (~15:09-15:35 UTC, conductor scheduled burst-mode rotation, claude-conductor-burst-20260727T150921Z-story-t010): step (4) follow-on -- the recent-scenarios list's links all pointed at the generic /stories browse view, so clicking one didn't actually reopen that scenario; the visitor had to re-find it in the gallery. Wired openScenario(id) to call scenarioStore.selectScenario(id) before navigating -- storyStore.phase reacts to scenarioStore.selectedScenario, so this now lands the visitor directly in that scenario's configure phase, mirroring the existing ?scenario=new deep-link pattern already used by the "Start a new scenario" CTA. Verified: eslint clean, prettier --check clean, full-project npm run test (vue-tsc --noEmit) exit 0 (node_modules were not present in this sandbox this cycle -- ran npm ci + nuxi prepare first, unlike prior cycles that had them already). Live browser smoke still deferred -- no reachable DATABASE_URL in this sandbox, same class of blocker as every prior entry on this task. kind_robots PR #1053 (branch claude/dazzling-ptolemy-gmxoaz) -- open, CI running. Set status: review; will flip to done once CI is green and the PR merges. Step (1) art and step (3)'s liveUrl note both already resolved by prior cycles; only step (4)'s remaining polish and step (1)'s art stay open. Kept at review (not done) pending CI.
    PROGRESS 2026-07-27 (~15:45 UTC, conductor Reviewer sweep, claude-conductor-reviewer-20260727T1545Z): kind_robots PR #1053 CI finished all 5 checks green (verify, TypeScript, Contract verifiers, facet-catalog, GitGuardian); merged squash d0d3c590. Reverting status to ready rather than done -- step (1)'s dashboard-tab/tutorial art is still the sole outstanding substep (blocked on the art-generation relay per every prior cycle's note), same precedent as the 2026-07-22 entry above.
    PROGRESS 2026-07-27 (~19:30 UTC, conductor burst-mode rotation, claude-conductor-burst-20260727T1930Z-story-t010): step (4) follow-on -- scenarioStore.initialize() already tracks a lastError on failure (stores/scenarioStore.ts), but storybook-page.vue never read it, so a failed init was visually indistinguishable from the legitimate "no scenarios yet" empty state. Added a computed reading scenarioStore.lastError plus an alert block, reusing the exact pattern challenge-center-page.vue already uses (role="alert", alert alert-warning rounded-2xl text-sm, kind-icon:warning) -- no store changes needed. Verified: eslint clean, prettier --check clean, full-project npm run test (vue-tsc --noEmit) exit 0 (ran provision_kind_robots_deps.sh first; node_modules/.nuxt were not present this cycle). Live browser smoke deferred -- no reachable DATABASE_URL in this sandbox, same class of blocker as every prior entry on this task. kind_robots PR #1060 (branch claude/dazzling-ptolemy-3pziqi) -- open, CI running. Set status: review; will flip to ready (not done -- step (1)'s art substep remains open) once CI is green and the PR merges.

    kind_robots PR #1060 merged as a6ba6a912b8cb2bdb1f4db8f1772fcb0cc96fe89 and Conductor PR #1255 merged as eedd4a2b22a51fcfb952651c59dc2ab2c58b6913. Scenario initialization failures are now surfaced on the Storybook page. Rearmed because the recurring polish task still retains its separately blocked tutorial-art substep.
    PROGRESS 2026-07-28 (~08:00-08:20 UTC, conductor burst-mode rotation, claude-conductor-burst-20260728T0810Z-story-t010): step (4) is now genuinely DONE, not by this task's own doing -- Silas committed a full dedicated Storybook studio directly to kind_robots main this session (PR #1084, squash b00fa19: reusable narrative-ingredient-multi-picker.vue, an independent storybookStore.ts with its own persisted sessions/story bible/narrator, a progressive premise/cast/world/review setup flow, and its own CI contract check utils/scripts/verifyStorybookStudio.mjs). This supersedes the "reuse the Stories engine" scope every prior PROGRESS entry on this task assumed -- Storybook is no longer a thin wrapper around scenarioStore. Reviewed the new studio for a follow-on polish gap (same posture as every prior cycle on this task) and found one: the header's "New story" button called store.resetSession() on a single click with no confirmation, discarding the current session -- including a finished tale -- whose only backup is localStorage, which the reset itself clears. Fixed by applying this repo's existing arm-on-first-click/confirm-on-second pattern (already used by kind_robots art-interact.vue's delete button, per its no-window.confirm convention) instead of inventing a new one. Verified: eslint clean; prettier --check drift on the file confirmed pre-existing via git-stash (unrelated to this diff, consistent with the prettier-version-mismatch noted elsewhere in this rotation); full-project npm run test (vue-tsc --noEmit) exit 0; node utils/scripts/verifyStorybookStudio.mjs (the new studio's own CI contract check) still passes. kind_robots PR #1089 (branch claude/dazzling-ptolemy-m33fmb) -- open, CI running. Remaining open scope on this task, now narrower: step (1)'s dashboard-tab + tutorial .webp art, still blocked on the art-generation relay (spot-checked relay-status this session: one agent listed with a fresh lastSeenAt, but per every prior cycle's finding a listed agent alone doesn't prove it's claiming jobs, and no storybook art-prompts.yaml request exists to test against -- not re-litigated further this cycle to stay in scope). Set status: review; will flip to ready (not done, art substep still open) once CI is green and the PR merges, matching this task's established precedent.
    PROGRESS 2026-07-28 (~09:00 UTC, conductor Reviewer sweep, claude-conductor-agentrun-20260728-storybook): kind_robots PR #1089 (arm-confirm New story button) reviewed -- all 7 checks green, diff matches the note's description exactly (arms on first click, fires on second, mirrors art-interact.vue's existing pattern, no window.confirm), scoped to one file. Merged squash 6d5e7a9. Conductor PR #1298 merged squash 64e7dfc. Reverting status to ready rather than done -- step (1)'s dashboard-tab/tutorial art remains the sole outstanding substep, same precedent as the 2026-07-27 and 2026-07-22 entries above.
  updated: '2026-07-28T09:00:00Z'
  claimed_at: '2026-07-28T08:14:47Z'
  claimed_by: claude-conductor-burst-20260728T0810Z-story-t010
  owner: reviewer
- id: t-011
  milestone: m4
  title: Arrange the casting board by role instead of a uniform grid
  status: done
  passes: 0
  stakes: reversible
  note: |-
    components/narrative/narrative-role-assigner.vue is a card board as of kind_robots 2026-08-06 (character art at 2:3 through kr-art-plate, the assigned part worn as a badge, parts offered as pressable chips), but the cards sit in a uniform auto-fill grid so POSITION CARRIES NO MEANING. Silas: "I'm imagining a layout that lets the user feel like they are selecting cards and creating a playboard that turns into a story." A real playboard arranges BY role -- protagonist given prominence, antagonist set opposite, ensemble ranked behind -- rather than in reading order. This is a layout design question rather than a mechanical one and wants Silas's eye on the arrangement before it is built. The cast is capped at five, which bounds the problem usefully.

    Reviewed and merged silasfelinus/kind_robots#1727 as 5beb132da03f516d8e673e73b6ad0332c03d3db1. The casting board now arranges protagonist/antagonist leads opposite each other, supporting roles in a middle tier, and ensemble/unassigned cast in a receded back tier while retaining pressable role chips as the accessible fallback. Exact-head TypeScript, Contract Tests, Layout Contract, Narrative Accessibility Acceptance, and attached Storybook/Narrative workflows completed successfully.
  claimed_by: 20260810T192800Z-storybook-t011-9f3a
  claimed_at: '2026-08-10T19:29:41Z'
  updated: '2026-08-10T20:15:20Z'
  implementation_pr: 'silasfelinus/kind_robots#1727'
- id: t-012
  milestone: m4
  title: Drag a character card onto a role slot
  status: ready
  owner: worker
  passes: 0
  stakes: reversible
  depends_on: t-011
  note: |-
    Casting is currently a chip press -- fast, accessible, keyboard-friendly, and not very playful. Dragging a card onto a role slot is the interaction that would actually feel like a playboard. Depends on t-011, since slots only exist once the board has an arrangement. Two constraints: keep the chip path as the accessible fallback rather than replacing it, and support touch properly -- a drag that only works with a mouse is worse than the chips it replaced.
    t-011 (kind_robots PR #1727, merged 2026-08-10) landed the lead/support/back tier grouping this task's drop slots should target -- protagonist/antagonist in the lead row, everyone else in support/back. Now formally unblocked (depends_on was prose-only before; encoded here since t-011 is done).
- id: t-013
  milestone: m4
  title: Bring the other three Storybook setup steps up to the board
  status: ready
  owner: worker
  passes: 0
  stakes: reversible
  note: |-
    The casting board sits inside a four-step wizard whose other steps -- premise, world/flavor, review -- still read as a form, so the "assembling a story" feeling exists on exactly one screen. The world-and-flavor step already uses card-based ingredient pickers and is closest; premise and review are plain. The open design question is whether the whole wizard becomes one board or the steps stay distinct while sharing the card language.
- id: t-014
  milestone: m4
  title: Add "Start a story with this" links on the object surfaces
  status: done
  passes: 0
  stakes: reversible
  note: |-
    Storybook now seeds its setup draft from ?scenario= / ?character= / ?facet= / ?reward= / ?location= (kind_robots 2026-08-06) -- seeding on top of the restored draft so a link never destroys a story in progress, and clearing the query so a reload cannot re-add an ingredient the author removed. NOTHING LINKS TO IT YET: the consuming half shipped without the entry points, so the feature is currently unreachable by a user. Silas: "what we should do is have links to these elements that start the process to tell a storybook story with that element as part of it" -- explicitly INSTEAD of giving Facets and Rewards conversations of their own, since both are tools that feed other interactions rather than things you talk to. Facets and Rewards are the priority; each now has exactly one detail pane to host the affordance.

    Reviewed and merged kind_robots PR #1706 as squash 8a457281cd6f0e03c341786f8b1ddb71827c2381. The Facet and Reward detail surfaces now link into Storybook with their object slug. Review repaired two contract failures on the existing branch: kept facet-interact as a thin router by extracting facet-profile, and made the extracted profile grid container-width responsive without carrying forward the temporary layout-baseline mutation. Exact head 000f87f5e940bb4355e9125dccc5027360beac98 passed all 13 observed workflows, including Layout Contract #31366156627, Contract Tests #31366156525, and TypeScript #31366156531.
  claimed_by: 20260810T063100Z-story-t014
  claimed_at: '2026-08-10T06:44:48Z'
  updated: '2026-08-10T07:34:46Z'
- id: t-015
  milestone: m4
  title: Decide whether stage roles and narrative roles converge
  status: needs-human
  owner: silas
  passes: 0
  stakes: reversible
  gate_human: true
  approved_by_human: false
  note: |-
    FOR SILAS: this is a subjective product-boundary decision, not an agent tooling block.
    TO APPROVE: decide whether Stage presets become Storybook frames sharing narrative roles, or stay a separate system; record the choice here.

    Two role systems now exist. stores/helpers/stageCards.ts holds the older one -- min/max cardinality, badge art, promptDescriptor, and assignCharacter() already resolving real Character records -- with a per-format vocabulary describing a SHOW (host, bailiff, goblin-investor, musical-guest). utils/narrativeRoles.ts is the new one, borrowing that shape with a story vocabulary that holds across premises (protagonist, antagonist, love-interest, mentor, foil, ally, wildcard, ensemble). They were kept separate deliberately: merging before either was integrated would have invented a third thing neither side asked for. A stage preset is arguably a story frame with its cast pre-filled, so convergence is plausible -- but whether the Stage system becomes a Storybook frame or stays its own thing is Silas's call, not an agent's.

- id: t-016
  milestone: m4
  title: "FOR SILAS: visually accept the redesigned Storybook setup and reading experience"
  status: waiting
  owner: silas
  passes: 0
  stakes: reversible
  gate_human: true
  approved_by_human: false
  depends_on: [t-011, t-012, t-013, t-014]
  note: >
    FOR SILAS: once the current casting-board/setup/deep-link redesign tasks are complete, review
    Storybook at phone/tablet/desktop widths with a real scenario/cast. TO APPROVE: confirm the
    front end feels like assembling and reading a story rather than nested admin panels, or leave
    concrete visual/interaction notes. This is separate from t-015's stage-role architecture choice.
- id: t-017
  milestone: m4
  title: Add a contract for Storybook object-entry links
  status: done
  owner: worker
  passes: 0
  stakes: reversible
  depends_on: t-014
  note: 'Merged kind_robots PR #1721 (squash 74f9e6c). Added utils/scripts/verifyStorybookObjectEntryLinks.mjs + .github/workflows/storybook-object-entry-links-contract.yml, asserting facet-profile.vue and reward-encounter.vue navigate to /storybook with the object''s slug in ?facet=/?reward=, and that storybook-page.vue''s seedFromQuery() still consumes both into facetSlugs/rewardSlugs -- without asserting button markup. Contract check passed on the PR (green CI) and stays green on main.'
  claimed_by: 20260810T143624-conductor-agent-run
  claimed_at: '2026-08-10T14:36:25Z'
  updated: '2026-08-10T14:52:24Z'
  implementation_pr: 'silasfelinus/kind_robots#1721'
- id: t-018
  milestone: m4
  title: Give Scenario, Character, and Location surfaces the same "Start a story with this" entry point Facet and Reward have
  status: done
  passes: 0
  stakes: reversible
  note: |-
    Kaizen from t-017 (kind_robots PR #1721): storybook-page.vue's seedFromQuery() already reads ?scenario=, ?location=, and ?character= (in addition to ?facet=/?reward=) and seeds the setup draft's scenarioSlug/locationSlug/castSlugs accordingly, but only the Facet and Reward detail surfaces (facet-profile.vue, reward-encounter.vue) actually have a CTA that navigates there -- a quick grep of components/characters/character-manager.vue and components/scenarios/scenario-manager.vue found no outgoing "Start a story with this" link, only unrelated matches. Add the equivalent CTA to the Scenario and Character (and Location, if it has an analogous detail surface) working views, mirroring startStoryWithFacet/startStoryWithReward exactly (navigateTo /storybook with the right query key, no assumptions about button markup), then extend utils/scripts/verifyStorybookObjectEntryLinks.mjs to cover the new handlers the same narrow way.

    Implemented Storybook entry CTAs for selected Character and Scenario working surfaces and extended the object-entry contract; no analogous standalone Location manager surface was found.

    Merged kind_robots PR #1726 as 52393a6fa4d1f56c93460d0dbb766ad510756211. Character and Scenario selected-object surfaces now start Storybook with ?character= or ?scenario=; the required object-entry contract covers both. No analogous standalone Location surface exists, so no speculative route or manager was added.
  claimed_by: 20260810T182152Z-91159b
  claimed_at: '2026-08-10T18:21:52Z'
  updated: '2026-08-10T18:36:40Z'
- id: t-019
  milestone: m4
  title: Add a contract asserting the casting board's role tiers group members correctly
  status: review
  passes: 0
  stakes: reversible
  note: |-
    Kaizen from t-011 (kind_robots PR #1727): narrative-role-assigner.vue now groups cast members
    into three tiers purely from their assigned role -- protagonist/antagonist into the lead row,
    love-interest/mentor/foil/ally/wildcard into support, ensemble and unassigned into back -- but
    nothing asserts that grouping in CI. eslint/vue-tsc/verifyNarrativeRoles.ts all pass without
    ever checking tier placement, so a future edit could silently move a role to the wrong tier (or
    drop the branching entirely, collapsing back to one flat grid) and nothing would fail. Add a
    source-level contract (same narrow style as verifyStorybookObjectEntryLinks.mjs -- assert the
    grouping logic, not button markup or CSS classes) that feeds a synthetic member of each role
    through the component's tier-selection logic and checks it lands in the expected row.
  owner: worker
  claimed_by: 20260811T003009Z-storybook-t019
  claimed_at: '2026-08-11T00:30:09Z'
  updated: '2026-08-11T00:30:09Z'
