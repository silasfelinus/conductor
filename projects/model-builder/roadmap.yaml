project: model-builder
kind: software

goal: >
  A reusable Kind Robots front end that turns any source record — Project, Character,
  Bot, Facet, Dream, Reward, or Scenario — into a resumable, human-gated build run:
  pick a recipe, generate fields and art, and commit upgraded or newly created records
  with full provenance. Marketing Deck is one recipe, not the root product.

notes_from_silas: |
  Build a reusable front-end Model Builder for Kind Robots schema records. Marketing Deck
  is one recipe, not the root product. A user selects a source model — Project, Character,
  Bot, Facet, Dream, Reward, or Scenario — then selects a recipe and individual outputs.

  The front end is a resumable, human-gated recipe runner. Each Build Item advances through
  PITCH, FIELDS_AND_PROMPTS, GENERATE_ASSETS, and COMMIT. Users can stop after any stage,
  edit results, rerun only stale stages, or resume later. Editing upstream work marks
  downstream work stale without deleting revision history.

  Support upgrading existing rows and creating related rows. New rows may be deferred until
  final commit or created early as private/inactive drafts after field approval when a durable
  ID is required. Final activation, relationship writes, and canonical asset promotion remain
  separate approvals.

  Reuse existing Kind Robots model APIs, generation specs, ArtImage/ArtCollection provenance,
  ExpressionMedia, LTX video, Hunyuan3D, and Comfy workflows. Components use modelBuilderStore
  and never call API routes directly. Provider credentials remain server-side. Generation may
  use Silas-owned team tokens with usage visible.

  No unattended publishing, deployment, printing, fabrication, customer contact, account
  creation, ad spend, paid provider use, or destructive canonical replacement.

milestones:
  - id: m1
    title: "Model Builder contract, recipes, and endpoint audit"
    weight: 20
    status: done
  - id: m2
    title: "Persistent gated build runs and front-end workflow"
    weight: 25
    status: done
  - id: m3
    title: "Deck, art-upgrade, expansion, and 3D recipes"
    weight: 25
    status: done
  - id: m4
    title: "Idempotent API execution, linking, and asset promotion"
    weight: 20
    status: done
  - id: m5
    title: "Reference builds and reusable production release"
    weight: 10
    status: in-progress
  - id: m6
    title: "Usage-driven refinements: smart fields, async art, auto-build"
    weight: 15
    status: in-progress

tasks:
  - id: t-001
    milestone: m1
    title: "Write the Model Builder product and orchestration specification"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Write projects/model-builder/SPEC.md. Define SourceModel, Recipe, BuildRun, BuildItem,
      BuildStage, BuildArtifact, revision/provenance, usage, and final commit boundaries.
      Specify PITCH, FIELDS_AND_PROMPTS, GENERATE_ASSETS, and COMMIT; stale-stage invalidation;
      reruns; cancellation; retries; partial failure; resume; DEFERRED and DRAFT_EARLY creation.
      The orchestrator must not duplicate durable fields owned by canonical domain models.

  - id: t-002
    milestone: m1
    title: "Audit source schemas, write endpoints, generation specs, and Comfy workflows"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Produce projects/model-builder/API-INVENTORY.md grounded in the current kind_robots schema
      and routes. Cover Project, Character, Bot, Facet, Dream, Reward, Scenario, ArtImage,
      ArtCollection, ExpressionMedia, relationship writes, batch dry-runs, image generation,
      LTX video, and /api/comfy/hunyuan3d. Record exact methods, payloads, auth, idempotency,
      missing routes, and the gap between generated model files and verified STL export.

  - id: t-003
    milestone: m1
    title: "Define the model-to-recipe matrix and selectable output catalog"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-001
      - t-002
    note: >
      Write projects/model-builder/RECIPES.md. Define editable defaults: Project ->
      marketing-deck; Character/Bot -> character-deck; Reward -> reward-deck; Facet, Dream,
      and Scenario -> art-upgrade. Add relationship-expansion. Each output needs a stable key,
      eligible sources, prerequisites, quantity rules, target type, sizes, generation method,
      gates, relationship plan, and review requirements.

  - id: t-004
    milestone: m2
    title: "Design persistent BuildRun, BuildItem, artifact, and revision state"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-001
      - t-003
    note: >
      Propose the smallest durable orchestration schema for resume across devices and sessions.
      Track source snapshot, recipe version, outputs, quantities, CREATE/UPDATE/ASSET_ONLY,
      target IDs, stage state, approvals, editable drafts, artifacts, provider/workflow metadata,
      token or compute use, stale reasons, errors, idempotency keys, and cleanup rules.

  - id: t-005
    milestone: m2
    title: "Design the front-end source picker and gated progress matrix"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-004
    note: >
      Specify the Kind Robots UI: source picker, recipe editor, selectable outputs, quantities,
      related models, row-by-stage matrix, pitch/field/relationship/prompt editors, candidate
      asset review, and final diff. Include run, rerun, pause, cancel, approve, reject, save,
      resume, and final commit. modelBuilderStore owns API and persistence interactions.

  - id: t-006
    milestone: m2
    title: "Define normalized source adapters for supported models"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-003
    note: >
      Define adapters for Project, Character, Bot, Facet, Dream, Reward, and Scenario. Return
      identity, editable fields, canonical art, collections, relationships, constraints,
      maturity/ownership, eligible recipes, and valid target relations. Preserve source
      snapshots and keep final writers model-specific.

  - id: t-007
    milestone: m3
    title: "Specify Marketing Deck as a Project recipe"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-006
    note: >
      Preserve the existing Marketing Deck scope as one recipe: business card, logo application,
      lawn sign, banner, flyer, website/app mockups, photo/video shoot plans, static ads,
      commercial treatment/storyboard, optional Comfy execution, and weekly launch plan.
      Support humboldt-scoop plus humboldt-scoop-cms. Existing brand assets and screens win.

  - id: t-008
    milestone: m3
    title: "Specify Character Deck for Character and Bot"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-003
      - t-006
    note: >
      Define identity and field pitches, artPrompt, portrait candidates, canonical avatar,
      icon, card, hero shots, cutout/model-sheet reference, selectable ExpressionMedia emotions
      and actions, custom keys, optional clips/transitions, and optional 3D reference/model.
      Preserve identity anchoring, owner XOR, dry-run, paths, and provenance conventions.

  - id: t-009
    milestone: m3
    title: "Specify Reward Deck with type-aware art and optional 3D"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-003
      - t-006
    note: >
      Define Reward fields, icon, card, hero/use scene, collection placement, and relationship
      proposals. ITEM offers 3D by default; PET may offer a figurine; abstract types default
      to art-only. Separate reference image, generated GLB/source model, mesh QA, verified STL
      conversion/export, and print-readiness review.

  - id: t-010
    milestone: m3
    title: "Specify the general Art Upgrade recipe"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-006
    note: >
      Offer only assets valid for the selected model: primary image, icon, card, hero,
      inspiration set/ArtCollection, key scene, profile/avatar, or repair. Preserve old
      canonical files as inspirations before promotion. Asset-only runs do not rewrite text.

  - id: t-011
    milestone: m3
    title: "Specify relationship-aware new-model expansion recipes"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-006
    note: >
      Support Dream -> X Characters/Rewards/Scenarios and optional Narrator Bot; Project ->
      Manager Bot; Scenario -> cast Characters/Rewards; Facet -> fitting records; Character ->
      signature Rewards. Each child has an independent BuildItem, gates, retry path, and final
      relationship diff. Do not invent unsupported universal combinations.

  - id: t-012
    milestone: m3
    title: "Design Comfy image, expression, video, and 3D orchestration"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-008
      - t-009
      - t-010
    note: >
      Map asset keys to licensed-safe workflows, requests, polling, output parsing,
      cancellation, usage, and provenance. Reuse expression and LTX conventions. Audit
      Hunyuan3D input/output and create explicit gaps for STL conversion, mesh repair,
      persistence, or export. Commercial output follows CONTROL.md.

  - id: t-013
    milestone: m4
    title: "Design idempotent draft-early and final-commit execution"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-004
      - t-006
      - t-011
    note: >
      Define DEFERRED and DRAFT_EARLY server execution. Draft rows remain private/inactive.
      Final commit promotes only approved fields, assets, and relationships. Show before/after
      diffs, use model APIs and dry-runs, stable idempotency keys, collision handling, and
      compensating cleanup. Replaying commit must not duplicate records or relations.

  - id: t-014
    milestone: m4
    title: "Implement the Model Builder API, Pinia store, and first UI slice"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-004
      - t-005
      - t-013
    note: >
      Shipped the Kind Robots front end: the existing model-builder page component wired into a public
      scaffold-based page at /model-builder, registered as the 'model-builder' tab in the builder channel
      with channelKey/tabKey. Reusable project-front-page scaffold in kind_robots/components/conductor/.
      kind_robots PR #204 merged 2026-07-12.

  - id: t-015
    milestone: m4
    title: "Implement model creation, relationship linking, and batch expansion"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-011
      - t-013
      - t-014
    note: >
      Extend to CREATE BuildItems and quantity-based child sets. Prove deferred and draft-early
      creation, approved relationship writes, ownership/maturity preservation, and per-item
      retry without rerunning an entire batch.

  - id: t-016
    milestone: m5
    title: "Produce the Humboldt Scoop Solutions Marketing Deck reference run"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    gate_human: false
    note: >
      Marketing Deck reference run produced as a full autonomous example set at
      projects/model-builder/reference-runs/hss-marketing-deck.md — humboldt-scoop source +
      humboldt-scoop-cms reliability angle, all 13 marketing-deck outputs (copy + art specs)
      grounded in Silas's real data (logo_clean.jpg, pricing, service area, the mandatory
      "Professional Pet Waste Removal" descriptor, billed the 1st), also folded into the
      CONTENT-BRIEF. The image pieces are specified for Flux (which renders text) at
      print/platform-correct shapes in reference-runs/hss-marketing-deck.generate.yaml —
      full text-bearing marketing pieces, ready to generate when Silas decides to spend
      (~$5k total op budget; shapes are the deliverable, not spend). The real logo is
      composited into a reserved corner (no rebrand); logo sheet + site/app mockups are
      assembled from real assets. (Marketing does NOT gatekeep text — Flux handles it.)
      Gate note (Silas, 2026-07-12): the four gates are OPTIONAL front-end pauses a user can
      place on a request — not a backend block that waits on a repo file. Default backend
      behavior is to create full objects. This corrects the earlier over-cautious
      needs-human framing. Live spend/print/posting/real-customer-contact remain out of
      scope for an internal set, but nothing here waits on approval.

  - id: t-017
    milestone: m5
    title: "Produce one complete Character Deck reference run"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    gate_human: false
    note: >
      Autonomous Character Deck run for the flagship Bot AMIb0t at
      reference-runs/character-deck-amibot.md (+ .generate.yaml). Every gate decided with
      reasonable defaults, no human input: identity pitch + field proposal, canonical
      NEUTRAL avatar (identity anchor generated first, seed/checkpoint locked and reused),
      icon/card/hero, a 5-key expression subset (joyful/loving/surprised/cheering/thinking,
      Bot-owned, proven before any full 20-key set), model sheet, provenance; 3D deferred.
      Assets specified for Flux/Comfy; COMMIT column is exactly what the executor writes
      (Bot art promotion + ExpressionMedia rows, no destructive replacement).

  - id: t-018
    milestone: m5
    title: "Prove Dream expansion with exactly three gated Characters"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    gate_human: false
    note: >
      Autonomous Dream expansion at reference-runs/dream-expansion-lantern-greenhouse.md
      (+ .generate.yaml): the real "Lantern Greenhouse" LOCATION dream (id 37) -> exactly
      three fitting Characters (Cogswaithe the robot philosopher, Zippa Emberwing the
      hyperactive butterfly, Elder Vellum the wise moth), each a full field set + portrait.
      Demonstrates the t-018 proof points against the executor: independent items, unique
      slugs, ownership (userId 10, private/inactive draft-early), Dream links via Characters
      m2m, partial-failure isolation (a failed child releases its idempotencyKey), and no
      duplicate creation on COMMIT replay (idempotencyKey claim).

  - id: t-019
    milestone: m5
    title: "Create the matching PROJECT Dream and expose Model Builder navigation"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-014
    note: >
      Identity via the canonical sync path (scripts/sync_projects.py -> POST/PATCH
      /api/projects, keyed on conductorSlug): added model-builder to project-overrides.yaml
      (active/high) so it syncs at all, added goal + waypoints to this roadmap's top matter,
      and extended build_project_payload to transmit goal (text) and waypoints (serialized
      pipe-delimited, KR's format) only when present — no clobbering. Slug model-builder is
      the single identity (no model-generator, no conflicting record). Navigation: appended
      a Model Builder directory SmartIcon (link /model-builder) to kind_robots
      stores/seeds/smartIcons.json — the fallback nav navStore imports; the content page
      already qualifies for the generatesmart path. kind_robots nav PR + conductor sync PR.

  - id: t-020
    milestone: m3
    title: "Register a grounded model-builder suggest sheet for AI-drafting"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Registered server/utils/suggest/sheets/modelBuilderSuggest.ts (builder
      'model-builder') with a Model-Builder system prompt, per-field prompts for
      pitch/fields/artPrompt, and a buildContext that surfaces the source record's canon
      (type, recipe, output, action + a name/class/personality/backstory/pitch digest).
      Also fixed the store: draftText was force-sending extra.instruction, which overrode
      any sheet fieldPrompt — now it only sends one when explicitly passed, so the sheet
      drives. No more adventure-sheet fallback. kind_robots PR #193.

  - id: t-021
    milestone: m2
    title: "Add a Model Builder run-history panel (list and reopen past runs)"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Kaizen from durable persistence (PR #188): runs now persist via
      /api/model-builder/runs, but the UI only surfaces the single active run. Add a
      history panel that lists the user's recent runs (GET /api/model-builder/runs) with
      source, recipe, and committed/total progress, and lets them reopen one into the
      progress matrix or cancel it. Front-end only; reuse modelBuilderStore. No schema.
      DONE — kind_robots PR #189.

  - id: t-022
    milestone: m5
    title: "Live post-deploy smoke: promote an asset and replay a Dream expansion commit"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      RAN 2026-07-25 (~21:10-21:15 UTC, claude-conductor-scheduled-20260725T2110Z-mb-t022,
      scheduled agent run): live-tested the COMMIT executor (server/api/model-builder/
      items/[id]/commit.post.ts) against kind-robots.vercel.app prod using the
      BETA_ADMIN_TOKEN (KR_API_TOKEN, sent as x-api-key). Both required flows pass clean —
      no bug found, no fix task filed.

      CREATE (Dream -> 3 Characters): created a private/inactive throwaway Dream (id 3821),
      then POST /api/model-builder/runs with sourceType=Dream/sourceId=3821 and 3 items
      (outputKey=expand-characters, action=CREATE). Dry-ran item 6's commit first (plan
      correctly showed action=CREATE, targetType=Character) then committed all 3 items for
      real: each returned `target.created=true, target.linked=true` with new Character ids
      1895/1896/1897. Verified via GET /api/characters/{id} and GET /api/dreams/3821 that
      all three Characters were created with isPublic=false/isActive=false (the `priv`
      override in commit.post.ts's createRecord, not just a schema default) and that
      Dream.Characters contained exactly the 3 new ids — no more, no fewer. Re-POSTed
      commit on item 6 with no dryRun: got `alreadyCommitted: true` with the same target
      id 1895 and no new Character was created — idempotencyKey guard confirmed.

      ASSET_ONLY (promote a Character hero): built a second run with sourceType=Character/
      sourceId=1896 (one of the freshly created test characters) and one ASSET_ONLY item,
      PATCHed a real public ArtImage id (12198) onto it, dry-ran (plan correctly showed
      field=artImageId/value=12198/targetId=1896), then committed for real — response
      confirmed `target.created=false, target.id=1896`, and GET /api/characters/1896 showed
      artImageId=12198 with isPublic/isActive still false. Re-POSTed the same commit and got
      `alreadyCommitted: true` — no double-write.

      One correction to how this task was scoped: ASSET_ONLY promotes onto the *run's
      source record* (whatever sourceType/sourceId the run was created against), not
      implicitly "whichever Character a CREATE step produced" — so a literal "promote a
      Character hero" test needs its own Character-sourced run, which is what this cycle
      did (using one of the CREATE step's own output characters as the target, to stay
      fully self-contained). Also worth noting for future callers: GET /api/characters
      does NOT filter by a `?id=` query string (it silently returns the full unfiltered
      list) — the working lookup is the path param route GET /api/characters/{id}. Not
      filing a fix task for this since it isn't part of the COMMIT executor under test and
      didn't block verification, but flagging here in case another live-smoke task hits
      the same surprise.

      Cleanup: deleted all throwaway records afterward (DELETE /api/dreams/3821, then
      DELETE /api/characters/1895, 1896, 1897) — both endpoints returned clean 200s: no
      residue left in prod.

      Conclusion: the COMMIT executor's CREATE, ASSET_ONLY, and idempotency-guard paths
      all work correctly against a live deployment, matching PR #190's CI-only claims.
      This is the live proof behind the gated reference runs t-016/t-017/t-018.
    claimed_by: claude-conductor-scheduled-20260725T2110Z-mb-t022
    claimed_at: '2026-07-25T21:06:46Z'
    updated: '2026-07-25T21:15:00Z'

  - id: t-023
    milestone: m6
    title: "Source thumbnails and run source-context panel"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      From live testing (Silas): the source picker now shows each record's existing art as
      a thumbnail, and the build-run view shows a source-context card (art + description/
      pitch/backstory) sourced from the run's sourceSnapshot so it survives resume.
      kind_robots PR #194.

  - id: t-024
    milestone: m6
    title: "Model-aware auto-fields and richer drafting"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      From live testing (Silas): FIELDS_AND_PROMPTS should populate automatically — we know
      each model type and what it needs — and the AI draft for a Reward came back as a bland
      "enhance the project" sentence instead of a real reward. Reuse the single source of
      per-model field truth (stores/registerBuilderStore.ts + stores/helpers/*Cards.ts:
      defaultSheet, required/optional card keys, choice pools) to auto-fill the required
      fields per source/target model and only surface optional toggles. Feed that same
      per-model schema + choice pools into the suggest sheet so drafts produce a full,
      specific record (a Reward gets name/rewardType/rarity/effect/flavor), not filler.

  - id: t-025
    milestone: m6
    title: "Async art generation via the ArtJob queue"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      From live testing (Silas): the normal path is create the object, then send the prompt
      to ArtJob (async) and handle it when done — synchronous generate is the interactive
      special case. Add an async path: enqueue via POST /api/art/enqueue (returns jobId),
      store the jobId on the build item, poll GET /api/art/queue/:id (~5s) until DONE, then
      promote the resulting artImageId onto the item/target. Reuse artStore.enqueueAndRender
      pattern. Show queued/rendering/done state per item. Keep sync generate as the "art
      first, interactively" option.
    claimed_by: claude-conductor-burst-20260719T1259Z
    claimed_at: '2026-07-19T13:07:21Z'
    updated: '2026-07-19T13:07:21Z'

  - id: t-026
    milestone: m6
    title: "Auto-build run mode + relationship-expansion art toggle"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      From live testing (Silas): a user who chooses a run should be able to just create the
      art/objects directly without walking every gate. Add an "auto-build" action that runs
      a build to completion with defaults — auto-draft fields (t-024), queue art (t-025) or
      skip it, and commit — for minimal-interaction production, while the gated flow stays
      available. Include a relationship-expansion "generate art for the created records?"
      toggle (create text-only records vs create + queue art).

  - id: t-027
    milestone: m6
    title: "Batch editor for quantity outputs"
    status: done
    owner: reviewer
    passes: 0
    stakes: reversible
    updated: '2026-07-17T11:00:00Z'
    note: >
      From live testing (Silas): batch generations should be edited together, not one item
      at a time. For a quantity output (e.g. 10 rewards), give a batch editor that sets which
      fields to automate vs fine-tune across all N items at once, with per-item overrides for
      the ones you want to hand-tune. Builds on the model-aware fields (t-024).

      Done 2026-07-17 (Silas-directed session, kind_robots PR #353 squash 4878064a): added
      components/model-builder/model-builder-batch-editor.vue, shown above the single-item
      panel whenever the selected item belongs to a quantity/expansion group (items sharing
      an outputKey). It (a) automates a stage across all N (batch-draft pitch/fields/art-prompt
      with an only-empty guard, approve FIELDS, or auto-build the group), (b) sets one model
      field to the same value on all N (choice fields render as dropdowns, e.g. rarity=RARE
      across 10 rewards), and (c) delegates per-item fine-tuning to the existing item panel via
      a select-item event. New store surface: itemGroups computed +
      batchDraftField/batchSetField/batchApproveStage/batchAutoBuild looping the existing
      per-item primitives; parse/read/setFieldLine helpers for the FIELDS key:value blob.
      vue-tsc 0 errors; all 3 CI checks green. The AI-draft/auto-build paths call /api/suggest
      + art/commit, so they were type-checked but not exercised live (DB-less sandbox); they
      reuse the already-shipped, already-exercised per-item primitives.

  - id: t-028
    milestone: m6
    title: "Executor: persist drafted structured fields on commit"
    status: done
    owner: Reviewer
    passes: 0
    stakes: reversible
    note: 'Done via kind_robots PR #250 (merged, squash sha 53f0d1be): commit executor now parses the FIELDS-draft key:value lines and applies known, typed, choice-validated columns per model on CREATE/UPDATE (Character/Bot/Reward/Dream/Scenario/Project/Facet); added Facet.kind choices to modelBuilderFields.ts. Also fixed a pre-existing, unrelated TS2531 null-check bug in artjob-manager.vue that was blocking main''s required TypeScript check for every PR.'
    updated: '2026-07-14T13:33:57Z'

  - id: t-029
    milestone: m6
    title: "Polish and upgrade Model Builder front-end surface"
    status: ready
    recurring: true
    passes: 0
    stakes: reversible
    note: |-
      UI polish + upgrade for the model-builder front end now that the scaffold page is live at /model-builder. Steps: (1) generate final dashboard-tab art at public/images/dashboard-tabs/builder/model-builder.webp and tutorial art at public/images/tutorials/builder/model-builder.webp; (2) add a matching section for 'model-builder' under tutorialChannels.builder.sections in kind_robots/stores/helpers/tutorialCards.ts; (3) verify the PROJECT Dream liveUrl points at /model-builder (admin can run the Conductor 'Placements' button to backfill channelKey/tabKey/liveUrl); (4) evolve the placeholder scaffold page into the full interactive experience for this project.
      PROGRESS 2026-07-17 (Silas-directed session, kind_robots PR #353 squash 4878064a): step (2) done -- added a 'model-builder' section under tutorialChannels.builder.sections in stores/helpers/tutorialCards.ts (image resolves via tutorialImage('builder','model-builder'), falls back to a placeholder until the art lands). Step (4) is effectively covered: the /model-builder mount (content/model-builder.md -> :model-builder-manager) is already the full interactive experience -- source picker, recipe selector, gated progress matrix, run history, and now the t-027 quantity batch editor -- not a placeholder scaffold. Remaining and kept ready: step (1) dashboard-tab + tutorial .webp art (needs the generation backend/egress) and step (3) the liveUrl backfill (an admin Placements click, not an agent action).
      PROGRESS 2026-07-23 (conductor scheduled agent run, claude-conductor-agentrun-20260723T164245Z-t029): step (4) follow-on -- dispatched an Explore subagent to read every model-builder component and the store in full, with the exclusion list of every bug class already fixed by prior cycles of this task (run-history cancel confirm, item-panel per-field textarea watches, state-free async poll). Found a real, previously-unfixed gating race: `canApproveAssets` in `model-builder-item-panel.vue` only checked `isLocked('GENERATE_ASSETS')` and truthiness of `item.artImageId` -- it never consulted `isGenerating`/`isQueued`, both already computed in the same file and already used to disable the generate/regenerate buttons a few lines above. Since neither `generateItemAsset` nor `generateItemAssetAsync` clears `artImageId` while a (re)render is in flight, "Keep this asset" stayed clickable throughout, so a user could approve (and then commit) a stale candidate moments before the in-flight regeneration silently overwrote `artImageId` and reset the just-approved stage back to `ready` underneath them. Fixed by adding the same `isGenerating`/`isQueued` gate to `canApproveAssets`. Verified: `npx eslint` clean; `npx prettier --check` shows the same pre-existing drift as `main` (confirmed via `git stash` before this change, matching how prior cycles handled the same file's pre-existing formatting drift) -- kept the diff to a minimal 3-line hand-applied addition rather than running `--write` on the whole file; full-project `vue-tsc --noEmit` exit 0. kind_robots PR #900: all 4 CI checks green (TypeScript, Contract verifiers, verify, GitGuardian) -- merged squash `04433b8e`. Steps (1) and (3) remain genuinely blocked as documented above; not touched this cycle. Rearmed to `ready`.
      PROGRESS 2026-07-20 (conductor-burst rotation, claude-burst-rotation-20260720T1810Z): step (4) follow-on -- found a real gap while re-reading the interactive experience components: model-builder-run-history.vue's "Cancel run" trash-icon button fired store.cancelRun(run.id) (a PATCH to CANCELLED, irreversible, and it drops the run from local history) on a single click with zero confirmation -- the only destructive action anywhere in the model-builder surface with no guard. Added the arm-on-first-click/fire-on-second-click/disarm-on-blur confirm pattern already established in components/art/art-interact.vue's delete button, so it now takes a deliberate second click. Verified: eslint clean, prettier clean, full-project `npm run test` (vue-tsc --noEmit) exit 0. kind_robots PR #671 (branch claude/keen-fermat-96x6u6) -- MERGED. Steps (1) and (3) remain genuinely blocked as documented above; not touched this cycle. Kept at `ready` (not `done`) since those two steps are still outstanding, matching how other "Polish and upgrade X" tasks with a genuinely blocked substep handled the same situation this week (e.g. media-watchlist/t-006).
      PROGRESS 2026-07-21 (conductor-burst rotation, claude-conductor-burst-20260721T0204Z): step (4) follow-on -- re-read the interactive components again and found another real gap adjacent to the t-671 cancel-confirm fix: cancelRun() removed the run from local state but never signalled pollAsyncArtJob (the async GENERATE_ASSETS poll loop, keyed on `item.artJobId === jobId`) to stop. A job that finished rendering after the user cancelled the run would still POST an artifact and PATCH the item via /api/model-builder/items/{id}/artifacts and /api/model-builder/items/{id}, and pop a misleading "Generated a candidate for X" success toast for work the user had just told the app they no longer wanted. Fixed by clearing artJobId/queueState on every item of the cancelled run inside cancelRun(), which the poll loop already checks at its next iteration (or immediately after its next getArtJobStatus await resolves) and exits cleanly instead of writing to a dead run. Verified: eslint clean on the changed file except 2 pre-existing no-empty errors in unrelated catch{} blocks (confirmed pre-existing via git stash, same 2 errors with and without this diff); full-project `npm run test` (vue-tsc --noEmit) exit 0. Note: `prettier --check` on this file reports drift that pre-dates this change too (same git-stash confirmation) -- likely a prettier-version mismatch between this sandbox and whatever last formatted the file; did not run a blanket --write since that would balloon a 14-line fix into an ~80-line unrelated reformat, flagged as a Kaizen suggestion instead of fixed inline. kind_robots PR #735 (branch worker/model-builder-t-029) -- MERGED (squash 9f24085). Steps (1) and (3) remain genuinely blocked as documented above; not touched this cycle. Kept at `ready` (not `done`), same pattern as the 2026-07-20 cycle -- those two steps are still outstanding.
      PROGRESS 2026-07-21 (~10:25 UTC, claude-conductor-scheduled-20260721T1012Z): step (3) correction -- this note's "an admin Placements click, not an agent action" framing is stale. `project-overrides.yaml` already carries `liveUrl: /model-builder`, `channelKey: build`, `tabKey: model-builder` for this slug (added in a prior bulk cross-repo sweep, per root `TALKBACK.md` 2026-07-21), and a live `GET https://kind-robots.vercel.app/api/projects` this cycle confirms the kind_robots Project row already reflects the same three values (verified directly, not just trusted the overrides file). Step (3) is done. Step (1) (dashboard-tab + tutorial .webp art) is still genuinely blocked: confirmed via GitHub `get_file_contents` that neither `public/images/dashboard-tabs/builder/model-builder.webp` nor `public/images/tutorials/builder/model-builder.webp` exist yet, and this cycle's fresh live probe of the art-generation home relay (ai-art-academy/t-010, job 1229) timed out unclaimed -- same shared-relay-down signature, so re-queuing this project's own art request would just waste a slot. Conductor-docs-only change; no kind_robots PR needed. Kept at `ready` -- step (1) is the only remaining blocker now.
      PROGRESS 2026-07-22 (conductor burst-mode cycle, claude-conductor-scheduled-20260722T030537Z): step (4) follow-on -- re-read the interactive components again (continuing the 07-20/07-21 pattern) and found another real gap in model-builder-item-panel.vue: the three local textarea refs (pitch/fields/prompt) were kept in sync via one combined `watch` over `[item.pitch, item.fieldsDraft, item.promptDraft]`. Because the callback unconditionally reset all three refs whenever *any* single one changed in the store, drafting one field with AI (or auto-build's sequential per-field drafting) silently clobbered unsaved edits the user was mid-typing in the *other* two textareas of the same item -- no warning, no conflict indicator, just silent data loss. Fixed by splitting into three independent per-field watches so each only reacts to its own value. Verified: eslint clean; full-project `npm run test` (vue-tsc --noEmit) exit 0; `prettier --check` drift on this file confirmed pre-existing via git-stash (same warning without the diff), consistent with the prettier-version-mismatch note from the 07-21 cycle -- no blanket reformat. kind_robots PR #850 (branch claude/keen-fermat-a76onp) -- MERGED. Steps (1) and (3) remain as documented in the prior entry; not touched this cycle. Kept at `ready` (not `done`), same pattern as the 07-20/07-21 cycles -- those two steps are still outstanding.
      PROGRESS 2026-07-25 (conductor scheduled agent run, claude-conductor-agentrun-20260725T121559-t029): step (4) follow-on -- dispatched an Explore subagent to read the full model-builder surface (stores/modelBuilderStore.ts, modelBuilderRecipes.ts/modelBuilderFields.ts, and every components/model-builder/*.vue file) against the exclusion list of every bug class already fixed by prior cycles of this task. Found a real, previously-unfixed cross-item race: `generateItemAsset`'s `finally` block unconditionally cleared the store-wide `state.generatingItemId` singleton, unlike the per-item `artJobId`/`queueState` pattern the async generation path already uses correctly. Selecting a different row and clicking "Generate candidate" while another item's `generateItemAsset` call is still awaiting `artStore.generateCurrentArt()` lets the new item's start overwrite `generatingItemId`; when the first call resolves, its `finally` then nulls the flag out from under the second item -- clearing its spinner/disabled state and defeating `canApproveAssets`' `isGenerating` guard (the exact guard t-029's 2026-07-23 cycle added to fix the sibling bug class), letting a user approve a stale/about-to-be-overwritten `artImageId` or fire a second concurrent generation for the same item. Fixed by guarding the clear with an ownership check (`if (state.generatingItemId === item.id) state.generatingItemId = null`), mirroring `pollAsyncArtJob`'s existing `item.artJobId === jobId` pattern. Confirmed the race is reachable by reading `model-builder-progress-matrix.vue`'s row-selection handler directly -- row clicks (`@click="selectedItemId = item.id"`) carry no guard tied to `generatingItemId`. kind_robots PR #948: all 5 CI checks green (TypeScript, Contract verifiers, verify, facet-catalog, GitGuardian) -- merged squash `cc6102b0`. Note on this cycle's own process: an earlier push attempt to the PR branch was corrupted by hand-typed/hallucinated base64 content (a mis-encoded multi-byte `×` character, some dropped indentation) -- caught by fetching the pushed file back and diffing against the local source before opening the PR, and fixed by re-pushing with the tool's plain-text `content` parameter (no manual base64 needed) instead. Steps (1) and (3) remain genuinely blocked/pending as documented in prior entries; not touched this cycle. Rearmed to `ready`.
      PROGRESS 2026-07-25 (conductor scheduled burst rotation, reviewer/claude-conductor-burst-20260725T140608-mb-t029): step (4) follow-on -- re-verified steps (2)/(3)'s plumbing is still intact (tutorialCards.ts 'model-builder' section, dashboardHelper.ts builder-channel tab entry, and content/channels/build/models.md channel-tab content all correctly wired to route /model-builder; step (1) art generation remains genuinely blocked, public/images/** is gitignored so it isn't a git-committable deliverable from this sandbox). Dispatched a general-purpose subagent to read the full store (1532 lines), the recipe catalog, all 6 components, and all 11 server/api/model-builder routes against the exclusion list of every bug class already fixed by prior cycles (t-671 cancel-confirm, t-20260721 poll-cancel signal, PR #948 generatingItemId ownership fix, PR #900 canApproveAssets gating). Found 3 more instances of the same store-wide-singleton unconditional-clear race PR #948 fixed for generatingItemId, in three siblings that share the identical shape but weren't covered by that fix: (a) autoBuildItem's autoBuildingItemId -- clicking "Auto" on a different row while one item's auto-build is still running let the finishing call null out the still-in-flight row's guard, allowing a duplicate concurrent auto-build of the same item; (b) loadSources -- switching source-type tabs quickly had no correlation check, so a slower response for a previously-selected tab could land after a newer one and silently overwrite the currently-displayed source list with records of the wrong type; (c) draftText -- the pitch/fields/prompt textarea stays editable while an AI draft is in flight, so a manual edit committed via @change during that window was silently clobbered when the draft resolved (draftingField is also a store-wide singleton with the same overwrite-then-clear race as autoBuildingItemId). Fixed all three following #948's exact pattern (only clear a store-wide singleton if it's still ours) plus a "still current" value check for the draft-overwrite case. kind_robots PR #951: all 5 CI checks green (TypeScript, Contract verifiers, verify, facet-catalog, GitGuardian) -- merged squash `84bc9209`. Steps (1) and (3) remain genuinely blocked/pending as documented above; not touched this cycle. Rearmed to `ready` since steps (1)/(3) are still outstanding, same pattern as every prior cycle of this task.


      Connector-native claim for the Model Builder front-end polish cycle.

      Added keyboard-accessible item selection controls to the Model Builder stage matrix on worker/model-builder-t-029-keyboard-matrix-6d91.

      PROGRESS 2026-07-25 (connector Worker closeout, worker-conductor-20260725T151232Z-mb-t029-6d91): kind_robots PR #954 merged as squash `2b19d4a1bfa50bbc61e01edd37cacfedee38db18`. Added a real keyboard-focusable button to each Model Builder progress-matrix item row, exposed selection with aria-pressed and a descriptive accessible label, and preserved the existing whole-row pointer interaction. The PR changed one Vue component (10 additions, 2 deletions), had no unresolved review threads, and merged after its checks completed. Step (1) generated dashboard/tutorial art remains the only outstanding original deliverable; rearmed to ready for a later eligible cycle.

      Connector Worker claim for a focused Model Builder run-history accessibility polish pass.

      Improved Model Builder run-history accessibility on worker/model-builder-t-029-run-history-a11y-12a7: announced loading state, hid decorative icons from assistive technology, and gave the icon-only cancel control a run-specific accessible name.

      PROGRESS 2026-07-25 (connector Worker, worker-conductor-20260725T181204Z-mb-t029-12a7): kind_robots PR #957 merged as squash 92f35ef470c3f93f8c654913ed969a9934a75dbc. Improved run-history accessibility by announcing loading state, hiding decorative icons, and naming the icon-only cancel action for its target run. All four PR workflows passed; no reviews or unresolved threads. Original generated-art deliverable remains outstanding, so the finite polish task returned to ready.
      PROGRESS 2026-07-25 (~20:03-20:24 UTC, conductor scheduled burst rotation, reviewer/claude-conductor-burst-20260725T2010Z-mb-t029): step (4) follow-on -- this cycle rotated here after ai-art-academy/t-010 (the usual top-priority pick) was claimed by a concurrent session seconds before this one, and ai-art-academy's own remaining ready tasks (t-019, t-035) were confirmed still genuinely blocked (t-019: verified `public/images/academy/styles/` still absent in a fresh kind_robots checkout; t-035: same home-relay backlog documented in that project's own notes) -- rotated down projects/priority.yaml to the next project with real, unblocked ready work, per this task's own established exclusion-list-driven "read everything, find one new bug" pattern. Dispatched a general-purpose subagent to read the full store (stores/modelBuilderStore.ts), recipe/field catalogs, all components/model-builder/*.vue files, and all server/api/model-builder/ routes in full, against the exclusion list of every bug class already fixed by prior cycles (t-671 cancel-confirm, the async poll-cancel signal fix, PR #900 canApproveAssets gating, PR #948 generatingItemId ownership guard, PR #951's three sibling singleton races, PR #954/#957 accessibility fixes). Found a real, previously-unfixed gap in the *synchronous* generation path: `generateItemAsset` (the "Generate candidate" button, distinct from the async job-queue path `generateItemAssetAsync` that PR #735 already guards) held only its original `BuildItem` reference across the `artStore.generateCurrentArt()` await, with no check against the item's *run* having been cancelled in the meantime. Concrete failure: user clicks "Generate candidate" on an item, then switches to Run History and cancels that run before the synchronous render resolves -- the promise still resolves and unconditionally POSTs the artifact + PATCHes the item, persisting a candidate (and popping a misleading success toast) for a run the user had just abandoned. A plain post-await `findItem()` re-check can't distinguish "this run was cancelled" from "the user merely switched to a different still-active run" -- both leave the item unreachable from the current `state.run`, but only the cancelled case should discard the result. Fixed with a `cancelledRunIds: Set` on the store: `cancelRun` records the cancelled run's id, and `generateItemAsset` captures its run's id up front and bails (no artifact POST, no item PATCH, no toast) only if that specific run was cancelled, while still persisting normally on a mere run-switch. Verified: `npx prettier --check` on the changed lines clean (pre-existing drift on untouched lines of the same file confirmed via `git stash` comparison, consistent with every prior cycle's documented prettier-version-mismatch note); `npx eslint`/`vue-tsc --noEmit` could not run in this sandbox (no `node_modules`, same documented limitation as prior cycles) -- relied on kind_robots CI. kind_robots PR #964 (branch `claude/dazzling-ptolemy-wcahqx`, created via the GitHub MCP `create_branch` workaround since the branch didn't exist on the remote yet): all 5 CI checks passed (TypeScript, Contract verifiers, facet-catalog, verify, GitGuardian) -- merged squash `b36d9ee`. No review comments. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay this sandbox cannot reach. Rearmed to `ready`.
      PROGRESS 2026-07-26 (conductor scheduled burst rotation, claude-conductor-scheduled-20260726T072000Z-mb-t029): step (4) follow-on -- rotated here after ai-art-academy/t-004 and coloring-book/t-022 were both confirmed still genuinely blocked (same art-render-queue backlog, rechecked earlier this same session rather than a third time) and digital-storefront/t-030 was claimed by a concurrent session moments before this one. Dispatched a general-purpose subagent to read stores/modelBuilderStore.ts (1598 lines) in full, both catalog helpers (modelBuilderRecipes.ts/modelBuilderFields.ts), all six components/model-builder/*.vue files, every server/api/model-builder/** route, and cross-checked against prisma/schema.prisma, against the exclusion list of every bug class already fixed by prior cycles (t-671 cancel-confirm, the async poll-cancel signal fix, PR #900 canApproveAssets gating, PR #948 generatingItemId ownership guard, PR #951's three sibling singleton races, PR #954/#957 accessibility fixes, PR #964 sync-path cancelledRunIds check). It also spotted the identical unconditional-clear race in `commitItem`'s `committingItemId` singleton but correctly withheld it as a duplicate of the already-fixed pattern rather than reporting it as new. Found a genuinely new, server-side bug this time: `linkSourceToTarget` in `server/api/model-builder/items/[id]/commit.post.ts` had an explicit `Project`->`Bot` case (sets `managerBotId`) but no `Dream`->`Bot` case, even though the `expand-narrator-bot` recipe output (`CREATE_TARGETS['expand-narrator-bot'] = 'Bot'`, description "Optional narrator bot for a dream") targets Dream sources and `Dream.narratorId`/`Narrator` relation exists specifically for this in prisma/schema.prisma. Committing a narrator-bot output created the Bot row but silently fell through to `return false` -- the Dream's `narratorId` was never set, and the store's plain success toast gave no indication the link failed, leaving an orphaned Bot the user believed was linked. Fixed by adding the missing `Dream`->`Bot` branch, mirroring the existing `Project`->`Bot` pattern exactly. Verified: `npx eslint` clean; `npx prettier --check` drift confirmed pre-existing via `git stash` (identical warning with and without this diff); full-project `npm run test` (`vue-tsc --noEmit`) exit 0 after provisioning deps via `provision_kind_robots_deps.sh` (regenerating the Prisma client was required to get a clean typecheck -- the checked-in `prisma/generated/prisma/*` files were reverted to `main`'s committed state before committing, not left as incidental diff noise from local generation). kind_robots PR #1005: all 4 CI checks green (TypeScript, Contract verifiers, facet-catalog, GitGuardian) -- merged squash `b6373c55`. No review comments. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`.

      PROGRESS 2026-07-27 (conductor scheduled agent run, claude-conductor-scheduled-20260727T0110Z-mb-t029): step (4) follow-on -- picked up the one lead the 2026-07-26 cycle's note left open: it had spotted the identical unconditional-clear race in `commitItem`'s `committingItemId` singleton (same shape as the already-fixed `generatingItemId`/`autoBuildingItemId`) but only logged it as a withheld duplicate rather than fixing it. Dispatched a general-purpose subagent to confirm the code was still unguarded (it was) and apply the fix, mirroring the exact ownership-check pattern and comment style of the `generatingItemId`/`autoBuildingItemId` fixes: `if (state.committingItemId === item.id) state.committingItemId = null` in `commitItem`'s `finally` block. Failure scenario: clicking "Commit" on row B while row A's commit request is still in flight (up to 60s) lets A's `finally` null out B's still-in-flight `committingItemId` the moment A resolves, silently dropping B's committing/disabled UI state and permitting a duplicate click. Verified independently (not just trusting the subagent's report): `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210, confirmed present via `git stash` with and without this change; full-project `npm run test` (`vue-tsc --noEmit`) exit 0 after provisioning deps via `provision_kind_robots_deps.sh`. kind_robots PR #1029 (branch claude/confident-bardeen-chlog4) -- all 5 CI checks green (TypeScript, Contract verifiers, facet-catalog, verify, GitGuardian Security Checks), no review comments, merged squash `d007d08f`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`.

      kind_robots PR #1029 merged as squash d007d08f after all required checks passed. Fixed the commitItem committingItemId ownership race so an earlier commit cannot clear a later row's in-flight UI guard. The generated dashboard/tutorial art remains the only outstanding original deliverable, so this recurring polish task returns to ready.

      PROGRESS 2026-07-27 (conductor agent run, claude-conductor-agentrun-20260727T020530-mb-t029): step (4) follow-on -- dispatched an Explore subagent to read the full store (stores/modelBuilderStore.ts), catalog helpers, all six components/model-builder/*.vue files, and every server/api/model-builder/** route, against the exclusion list of every bug class already fixed by prior cycles (t-671 cancel-confirm, the async poll-cancel signal fix, PR #900 canApproveAssets gating, PR #948 generatingItemId ownership guard, PR #951's three sibling singleton races, PR #954/#957 accessibility fixes, PR #964 sync-path cancelledRunIds check, PR #1005 Dream->Bot commit link, PR #1029 committingItemId ownership guard). Found one more instance of the exact same store-wide-singleton unconditional-clear race, in a fifth sibling that shares the identical shape but wasn't covered by any prior fix: `batchDraftField`/`batchAutoBuild` both write `state.batchingOutputKey` and unconditionally cleared it in their `finally` block. Starting a batch op (draft-field or auto-build) on one group while a different group's batch op is still in flight lets the first-to-finish group's `finally` null out `batchingOutputKey` out from under the still-running group -- `model-builder-batch-editor.vue`'s `batching` computed (gating every batch button off `store.batchingOutputKey === props.outputKey`) then flips to false for the still-running group, re-enabling its buttons and allowing a duplicate concurrent batch operation over the same items. Fixed both functions with the same ownership-check pattern and comment style as the other four singletons: `if (state.batchingOutputKey === outputKey) state.batchingOutputKey = null`. Verified: `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` with and without this diff); full-project `npm run test` (`vue-tsc --noEmit`) shows no new errors -- 3 pre-existing unrelated errors in `server/utils/facetCatalog.ts`/`utils/scripts/seedBotFacetCatalog.ts` (`Property 'botFacet' does not exist`) confirmed present on unmodified `main` too via `git stash` (the checked-in `prisma/generated/prisma/*` predates the `BotFacet` model added to `prisma/facet-catalog.prisma` by a recent unrelated commit); relying on kind_robots CI for those, consistent with how prior cycles handled this same sandbox limitation. Reverted the locally-regenerated `prisma/generated/prisma/*` files to their committed state before committing so the diff stayed scoped to one file. kind_robots PR #1032 (branch `claude/confident-bardeen-9jdwdp`): all 5 CI checks green (TypeScript, Contract verifiers, facet-catalog, verify, GitGuardian Security Checks), no review comments, merged squash `4f9d5323`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay (see ai-art-academy/t-004's standing recheck instruction -- rechecked this same cycle via `recheck_render_queue.py`, still growing: PENDING=112, oldestPending job 2017 ~63.1h old). Rearmed to `ready`.

      Kaizen suggestion from this cycle: the store now has 5 near-identical "store-wide singleton with ownership-check" implementations (generatingItemId, committingItemId, autoBuildingItemId, draftingField, batchingOutputKey). A small createOwnedSingleton() helper (set/clear-if-mine) could collapse the duplicated comment+conditional into one reusable primitive and make it structurally impossible to add a 6th one without the guard.
      PROGRESS 2026-07-27 (conductor scheduled agent run, closing out claude-conductor-burst-20260727T1109Z-mb-t029-singleton): kind_robots PR #1049 (createOwnedSingleton() helper collapsing the 5 store-wide singleton race-guards into one primitive) confirmed merged squash `8e3de5c`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`, matching every prior cycle's pattern for this recurring task.

      CORRECTION 2026-07-27T13:15:00Z: the squash sha recorded above (`8e3de5c`) was wrong -- that's kind_robots PR #1043's squash commit (an unrelated ci-janitor fix seen in the same TALKBACK sweep), not #1049's. #1049's actual squash merge commit is `ab1556e9` (confirmed via kind_robots commit history, authored 2026-07-27T12:13:07Z). Also: this close-out (conductor PR #1219) rearmed `status` to `ready` but never cleared `claimed_by`/`claimed_at`/`owner`, leaving the task looking like a stale claim -- which is exactly what led a concurrent session to open a duplicate close-out (conductor PR #1221, now closed as redundant/superseded by this same PR #1219). Clearing the claim fields now.

      PROGRESS 2026-07-27 (conductor scheduled burst rotation, claude-conductor-burst-20260727T200726Z-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, all six components/model-builder/*.vue files, and the recipe/fields stores against the exclusion list of every bug class already fixed by prior cycles (see the six prior PROGRESS entries above for the full list, most recently the createOwnedSingleton() collapse in PR #1049). Found a real, previously-unfixed run-level race in `autoBuildRun()`: it loops over a snapshot of the active run's items, awaiting `autoBuildItem()` per item, without ever re-checking whether `state.run` is still the same run across those awaits. `state.autoBuilding` is a store-wide (not per-run) flag and nothing in the UI gates switching runs (via History) on it. Failure scenario: user clicks "Auto-build all" on Run A while an item's art generation is in flight, then opens Run B via History mid-loop -- the loop's remaining iterations fail fast against the new run (`findItem` can't locate Run A's item ids in Run B, so no data corruption), but `state.autoBuilding` stays stuck `true`, keeping Run B's own "Auto-build all" button disabled until Run A's abandoned in-flight item resolves, and the eventual `setStatus('success', 'Auto-build finished: X/Y committed.')` then overwrites Run B's status banner with a stale count describing the run the user navigated away from. This is the run-level counterpart of the already-fixed item-level singleton races. Fixed by capturing `state.run.id` before the loop, bailing per-iteration (`finally` still clears `autoBuilding`) if the active run has changed, and gating the completion `setStatus` call the same way. Verified independently: `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` with and without this diff); full-project `npx vue-tsc --noEmit` shows 0 errors both before and after. kind_robots PR #1062 (branch `claude/dazzling-ptolemy-2vi3y3`): all 5 CI checks green (TypeScript, Contract verifiers, facet-catalog, verify, GitGuardian Security Checks), no review comments, merged squash `eb7928cc`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below per the 07-27T13:15 correction's lesson.

      CORRECTION 2026-07-27T23:30:00Z (ai-art-academy/t-010 lane-2 roadmap-accuracy pass, conductor-scheduled-agent-20260727T2325Z-aa-t010-lane2): the 20:26 close-out above cleared `claimed_by`/`claimed_at`/`owner` as intended but left the top-level `status:` field itself stuck at `claimed` -- the exact note/field-drift pattern this same task's 07-27T13:15 correction already documented once, just missing one more field this time. Confirmed via GitHub MCP `list_pull_requests` that kind_robots has zero open PRs (nothing mid-flight that this claimed status could legitimately represent), and the note above already says "Rearmed to ready" in plain text. Flipped `status` to `ready`.

      PROGRESS 2026-07-28 (conductor scheduled burst rotation, claude-conductor-burst-20260728T0730Z-mb-t029): step (4) follow-on -- dispatched an Explore subagent to read the full store, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and cross-check against prisma/schema.prisma, against the exclusion list of every bug class already fixed by prior cycles (see the eight prior PROGRESS entries above for the full list, most recently the autoBuildRun() run-id staleness fix in PR #1062). Found a real, previously-unfixed gap in `linkSourceToTarget` (server/api/model-builder/items/[id]/commit.post.ts): it handled `Scenario -> Character` (and Dream/Reward/Bot siblings) but had no case for the reverse `Character -> Scenario` pairing, even though `prisma/schema.prisma` models a genuine bidirectional many-to-many relation (`Character.Scenarios` / `Scenario.Characters`, `@relation("CharacterToScenario")`). `getOutputsForRecipe` filters `OUTPUT_CATALOG` only by recipe key, not source type, so a `Character` source running `relationship-expansion` with the `expand-scenarios` output reaches this path through the normal UI, not just a crafted request. Committing that item created the new `Scenario` row but silently returned `linked: false`, leaving the relation unpopulated with no indication to the user. Fixed by mirroring the existing `Scenario -> Character` branch's shape. Verified: `npx eslint` on the changed file clean; full-project `npm run test` (`vue-tsc --noEmit`) exit 0 after provisioning deps via `provision_kind_robots_deps.sh`; diff scoped to exactly the one file (`git status --porcelain` showed only this file before commit). kind_robots PR #1079 (branch `claude/model-builder-t029-scenario-link`): all 4 CI checks green (TypeScript, Contract verifiers, facet-catalog, GitGuardian) -- merged squash `8d4deb33`. No review comments. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      Kaizen suggestion from this cycle: add a small dev-time consistency check (or unit test) that walks every `CREATE_TARGETS` entry in `modelBuilderFields.ts` and asserts `linkSourceToTarget` has a matching `(sourceType, targetType)` case wherever a bidirectional Prisma relation exists between the two models. Would have caught this gap (and the earlier `Dream -> Bot` gap fixed in PR #1005) automatically instead of relying on a manual read-through each cycle. Two related, lower-confidence sibling gaps also noticed but not fixed this cycle (need relation verification first): `Reward -> Character` despite `Character -> Reward` and `Reward.Characters` existing; other `CREATE_TARGETS` pairs (`Project`/`Facet`/`Reward -> Scenario`, etc.) also unhandled but unconfirmed against schema relations.

      PROGRESS 2026-07-28 (conductor scheduled burst rotation, conductor-scheduled-20260728T141608Z-mb-t029-burst): step (4) follow-on -- dispatched a general-purpose subagent to read the full store (stores/modelBuilderStore.ts, all ~1655 lines), both catalog helpers, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and cross-checked against prisma/model-builder.prisma, against the exclusion list of every bug class already fixed by prior cycles (all PROGRESS entries above, most recently the Character->Scenario link fix in PR #1079). It independently re-verified all 12 prior fixes are still intact on current main before searching for a new one. Found a real, previously-unfixed UI bug in `previewCommit()`: it always labeled the commit-preview `targetType`/summary with `state.run.sourceType`, correct for UPDATE items but wrong for every CREATE relationship-expansion output (expand-characters, expand-rewards, expand-scenarios, expand-narrator-bot, expand-manager-bot, expand-signature-rewards). Concrete failure: running Relationship Expansion on a Dream and selecting the "Narrator bot" CREATE item, the Commit stage -- whose entire purpose is a human-reviewable preview before an irreversible write -- showed "CREATE Dream from Narrator bot" instead of "CREATE Bot from Narrator bot". The server-side commit itself resolves the target correctly via CREATE_TARGETS, so no data corruption occurred, but the review UI was lying about what was about to be created, defeating the human-gate the COMMIT stage exists for. Fixed by wiring the store's existing `resolveTargetModel()` helper (already used correctly at 3 other call sites: startRun, draftText, itemGroups) into previewCommit instead of hardcoding sourceType. Verified: `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` with and without this diff); full-project `npm run test` (`vue-tsc --noEmit`) exit 0; diff scoped to exactly the one file (8 lines). kind_robots PR #1106 (branch `claude/dazzling-ptolemy-2lidyz`): all 7 CI checks green (TypeScript, Contract verifiers, facet-catalog, Shared narrative UI contract, Dedicated Storybook studio contract, verify, GitGuardian Security Checks) -- merged squash `918f8508`. No review comments. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      PROGRESS 2026-07-28 (conductor scheduled burst rotation, claude-conductor-burst-20260728T190649Z-mb-t029): step (4) follow-on -- rotated here after coloring-book/t-022 (the top-priority active ready task) was confirmed still genuinely blocked, same as many prior cycles (`coloring_queue_status.py` shows 18 pending ArtJobs failing the semantic gate with "ANTHROPIC_API_KEY is required for the production semantic art gate" -- this sandbox has KR_API_TOKEN but not ANTHROPIC_API_KEY, and a concurrent session was independently working that same task's t-036 sub-track this cycle, so did not duplicate). First re-verified the prior cycle's own kaizen suggestion (a CREATE_TARGETS/linkSourceToTarget consistency check) was in fact already fully built and merged across the intervening t-032/t-033/t-034/t-035 tasks: `npx tsx utils/scripts/verifyModelBuilderLinkCoverage.ts` passes cleanly on current main, confirming the two sibling gaps that PROGRESS 2026-07-28T14:16Z's note flagged as unconfirmed (`Reward -> Character` and other unhandled `CREATE_TARGETS` pairs) are already covered by that regression guard (both the missing-case direction and, per t-035, the join-table-only-false-eligibility direction) -- no further action needed there. Dispatched a general-purpose subagent to read the full store, all six components/model-builder/*.vue files, and every server/api/model-builder/** route in full against the complete exclusion list of every bug class fixed by prior cycles. Found a real, previously-unfixed bug: `batchDraftField` and `batchSetField` (the batch-editor's "Draft with AI" / "Apply to all" actions on a quantity group) iterated every item in the group with no check on that item's own PITCH/FIELDS_AND_PROMPTS stage-approval status, silently overwriting a stage the user had already reviewed and approved via the item panel -- the "approved" badge kept showing even though the underlying content had just been rewritten, and the change was persisted to the server via pushItem/batchPushItems. This is the "review gate bypassed by group operation" class, distinct from the item-level singleton-ownership races fixed in prior cycles (those guarded concurrent in-flight state, not stage-approval status). Fixed by adding `isStageEditable(item, stageKey)`, mirroring the item panel's own `isEditable` semantics (editable only when `ready`/`stale`/`rejected`), and gating both batch functions on it so an already-approved item is skipped rather than silently rewritten. Verified independently (not just trusting the subagent's report): `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` with and without this diff); full-project `npx vue-tsc --noEmit` exit 0; `git status --porcelain` showed only the one file touched. kind_robots PR #1111 (branch `claude/dazzling-ptolemy-3emg68`): all 7 CI checks green (TypeScript, Contract verifiers, facet-catalog, Shared narrative UI contract, Dedicated Storybook studio contract, verify, GitGuardian Security Checks), no review comments -- merged squash `c28d1b4c`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      PROGRESS 2026-07-28 (conductor-burst-hourly rotation, reviewer/claude-conductor-burst-20260728T-mb-t029): step (4) follow-on -- coloring-book/t-022 (top-priority active ready task) was skipped this cycle: its semantic art gate remains blocked on the missing ANTHROPIC_API_KEY credential (same known wall the PR #1357 fail-closed preflight now surfaces cleanly), so a retry here would be a no-op. Claimed this task instead. Dispatched an Explore subagent to read stores/modelBuilderStore.ts and all model-builder components in full against the exclusion list of every bug class fixed by prior cycles. Found a real, previously-unfixed race: a GENERATE_ASSETS render (sync generateItemAsset or async pollAsyncArtJob/generateItemAssetAsync) can be reopened upstream while still in flight -- markDownstreamStale correctly marks GENERATE_ASSETS 'stale' at that moment -- but the render's own completion handler holds only its original item reference and has no idea this happened, so it unconditionally overwrote the stage back to 'ready' on success/failure (and re-asserted 'in-progress' on every 5s async poll tick), silently erasing the 'needs re-review' signal for a candidate actually produced from an outdated prompt -- the same 'review gate bypassed' bug class as the canApproveAssets and batch-operation fixes above, just in the render-completion path. Fixed by adding finishGenerateAssets(item, next), which only applies a completion status write if the stage is still 'in-progress' (i.e. nothing else touched it while the call was awaiting), and using it at all six race-prone write sites. Verified: npx vue-tsc --noEmit clean; npx eslint stores/modelBuilderStore.ts shows only the 2 pre-existing no-empty errors at lines 203/210 (confirmed via git stash with and without this diff); prettier drift on this file confirmed pre-existing on main the same way; git status --porcelain showed only the one file touched. No live smoke test of the actual race was possible -- no reachable render backend from this sandbox -- verified by code trace instead. kind_robots PR #1114 (branch claude/dazzling-ptolemy-54n3h5), open, CI pending. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay.

      REVIEWED 2026-07-28 (conductor agent run, claude-conductor-agentrun-20260728T-review): reviewed conductor PR #1359 and the kind_robots PR #1114 it tracks. Re-traced the finishGenerateAssets guard against markDownstreamStale directly in the store source: markDownstreamStale marks an 'in-progress' GENERATE_ASSETS stage 'stale' on upstream edit, so by the time an in-flight render's completion handler runs, the status guard (`=== 'in-progress'`) correctly evaluates false and skips the overwrite -- confirms the fix does what the PR claims. All 9 kind_robots CI checks green. Merged kind_robots #1114 (squash 7629a32c), then conductor #1359 (squash a655af6b). Rearmed to `ready` per the recurring-task rule; claim fields cleared below.

      PROGRESS 2026-07-28 (conductor-burst-hourly rotation, claude-conductor-burst-20260728T2308Z-mb-t029): step (4) follow-on -- coloring-book/t-022 (top-priority active ready task) was confirmed still genuinely blocked (18 pending ArtJobs stuck on the missing ANTHROPIC_API_KEY GitHub Actions secret, per t-036's own credential_gate_error_count check; still unresolved per TALKBACK.md, needs Silas). Also handled an unrelated OPEN kind_robots Todo first per AGENTS.md's todo-before-roadmap rule (#912, ci-janitor flag for a red Cypress run on main at commit 0805576) -- root-caused it as a transient flake (`cy.request()` 30s timeout hitting the live Vercel deployment while POSTing to /api/sheets/by-dream, which then cascaded a second assertion failure in the same spec) rather than a real regression: `git log` between the failing commit and current main touches no sheets/dream-delete code, and the next scheduled Cypress run on main (30405043108, same head_sha as current main) was already green before this session started investigating. Marked Todo #912 DONE, no code change needed.
      Claimed this task instead. Dispatched a general-purpose subagent to read the full store, all seven components/model-builder/*.vue files, every server/api/model-builder/** route, and both static regression-guard scripts, against the exclusion list of every bug class fixed by prior cycles (all PROGRESS entries above, most recently PR #1122's commitItem finishCommit gate). Found a real, previously-unfixed gap: `generateItemAsset`'s success path checks `cancelledRunIds` before persisting a candidate for a run the user has since cancelled (comment explains why), but its `catch` block had no equivalent check -- a render that rejects after the user cancels its run still called `setStatus('error', ...)` unconditionally, popping a stray "Generation failed" banner unrelated to whatever the user is currently looking at (they may already be back on the source picker or a different run). Fixed by mirroring the success path's guard at the top of the catch block. Verified independently: `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` with and without this diff); full-project `npx vue-tsc --noEmit` exit 0; `npx prettier --check` drift on this file confirmed pre-existing via the same stash comparison; `git status --porcelain` showed only the one file touched. kind_robots PR #1123 (branch `claude/dazzling-ptolemy-386aln`): merged (silasfelinus, 2026-07-28T23:17:15Z), 1 file changed (+5/-0). Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      PROGRESS 2026-07-29 (conductor scheduled Agent run, conductor-scheduled-20260729T010536Z-mb-t029): step (4) follow-on -- coloring-book/t-022 (top-priority active ready task) re-confirmed still genuinely blocked via `coloring_queue_status.py --book monster-recast` (credential_gate_error_count 18, recommended_action recover-existing-jobs, but recovery itself also needs ANTHROPIC_API_KEY which this sandbox still doesn't have -- identical state to the 07-28 checks, nothing new to act on). Claimed this task instead. Dispatched a general-purpose subagent to read the full store, all model-builder components, server routes, and existing regression-guard scripts against the complete exclusion list of every bug class fixed by prior cycles. Found a real, previously-unfixed race in the *async* generation path: `pollAsyncArtJob` clears `item.artJobId` as part of normal terminal-status handling *before* awaiting `artStore.finalizeQueuedArtImage()` (a real network round-trip). If the user cancels the run while that await is in flight, `cancelRun()`'s own `item.artJobId` clearing is a no-op (already cleared) -- only `cancelledRunIds` could still distinguish "cancelled" from "completing normally", but `pollAsyncArtJob` never checked it, unlike its synchronous sibling `generateItemAsset` (fixed on both its success and catch paths back on 2026-07-28). So a cancelled run's async render could still get its candidate persisted, an artifact PATCHed/POSTed onto the item, and a misleading success toast popped -- directly contradicting `cancelRun()`'s own doc comment. Fixed by threading `runId` through to `pollAsyncArtJob` and checking `cancelledRunIds.has(runId)` immediately after `finalizeQueuedArtImage` resolves, before any persistence or toast. Added `utils/scripts/verifyModelBuilderCancelledRunGuard.ts` + `.test.ts` (matches the existing static-regression-guard convention) plus two `package.json` script entries. Verified independently: `npx eslint stores/modelBuilderStore.ts` shows only the 2 pre-existing `no-empty` errors at lines 203/210 (confirmed via `git stash` before/after); `npx eslint` on both new files clean; full-project `npx vue-tsc --noEmit` exit 0; `npx prettier --check` drift on the store file confirmed pre-existing via the same stash comparison, new files clean; `git status --porcelain` showed only the 4 intended files touched; the new checker fails against the pre-fix store and passes after (confirmed via git stash), its own self-test passes standalone. kind_robots PR #1128 (branch `claude/confident-bardeen-w454ml`): merged (silasfelinus, 2026-07-29T01:30:17Z), 4 files changed (+295). Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      PROGRESS 2026-07-29T06:43Z (conductor scheduled burst rotation, claude-conductor-burst-20260729T0630Z-mb-t029): step (4) follow-on -- rotated here after ai-art-academy/t-010 hit a genuine rotation collision this same cycle (a concurrent session independently fixed the same art-prompts.yaml pruning bug; both sides landed cleanly, no data lost, see that project's TALKBACK.md) and coloring-book/t-022 re-confirmed still genuinely blocked on the missing ANTHROPIC_API_KEY credential. Dispatched a general-purpose subagent to read the full store, both catalog helpers, every `components/model-builder/*.vue` file, every `server/api/model-builder/**` route, and the three existing regression guards, against the complete exclusion list of every bug class fixed by the ~20 prior cycles of this task. Found a real, previously-unfixed **server-side** gap distinct from every prior client-side race fix: `server/api/model-builder/items/[id]/commit.post.ts` executed the durable CREATE/UPDATE/ASSET_ONLY write without ever reading `item.stageStatuses` -- the entire PITCH -> FIELDS_AND_PROMPTS -> GENERATE_ASSETS -> COMMIT human-approval gate this module's own header comment describes is enforced only client-side (`stores/modelBuilderStore.ts`'s `approveStage`/`isLocked`). A direct `POST /api/model-builder/items/{id}/commit` for an item still `'locked'`/`'ready'`/`'rejected'` on any earlier stage -- a replayed request, a client-state bug, or plain `curl` with a valid session -- would create/update/promote a canonical record straight from an unreviewed draft, contradicting the module's own "must never silently rewrite a canonical model" comment. Fixed by adding a pre-write check (dryRun previews exempt) that walks `BUILD_STAGES` and requires every non-COMMIT stage's persisted status to be `'approved'`, 400ing with the first unapproved stage's label otherwise -- reuses `parseStoredJson` (already imported) and `BUILD_STAGES` (the same ordered list the client's own stage gating is built from), so server and client can't drift. Verified independently: `npx eslint "server/api/model-builder/items/[id]/commit.post.ts"` clean before/after (git stash); full-project `npm run test` (`vue-tsc --noEmit`) clean; confirmed this is distinct from all three existing regression guards (`verifyModelBuilderCompletionGate.ts`, `verifyModelBuilderApprovedAssetGuard.ts`, `verifyModelBuilderCancelledRunGuard.ts`), which all cover client-side store races, none server-side stage enforcement on the commit route. kind_robots PR #1139 (branch `claude/dazzling-ptolemy-e5010718`): all 11 CI checks green, no review comments -- squash-merged `df29e56`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearmed to `ready`; claim fields cleared below.

      Kaizen suggestion from this cycle: `batchApproveStage` in `modelBuilderStore.ts` approves any non-`'locked'` stage with no `isStageEditable`-style check -- currently harmless (only ever called with `'FIELDS_AND_PROMPTS'`, which never reaches `'in-progress'`), but a future caller wiring it to `'GENERATE_ASSETS'` would bypass the same "don't approve mid-render" protection `canApproveAssets` enforces per-item. Worth a guard now, before a caller exists, rather than after.

      PROGRESS 2026-07-29T10:06Z (conductor scheduled burst rotation, claude-burst-20260729T100612Z-t029): step (4) follow-on -- rotated here after coloring-book/t-022 re-confirmed still genuinely blocked on the missing ANTHROPIC_API_KEY credential (checked env directly this cycle). Dispatched an Explore subagent to read the full store and every `components/model-builder/*.vue` file against the exclusion list of every bug class fixed by prior cycles (the finding that landed as PR #1139 just before this claim -- a server-side stage-approval gate bypass in commit.post.ts -- was already present on main by the time this cycle read the file). Found a real, previously-unfixed gap distinct from that one: `commitItem()` is the only stage-completion function in `modelBuilderStore.ts` that awaits a real network round-trip (the commit POST, up to 60s) without checking `cancelledRunIds` afterward, unlike its siblings `generateItemAsset` and `pollAsyncArtJob`. The commit POST durably creates/links/promotes the target server-side regardless (can't be undone from here, same as `generateItemAsset`'s art render), but without the guard, a commit that resolves after the user cancels the run via `cancelRun()` still reattached the detached item locally and popped a misleading "committed" success toast for a run they'd already abandoned. Fixed by capturing `runId` from `state.run` at the top of `commitItem` and checking `cancelledRunIds.has(runId)` right after the POST resolves (both success and catch branches), mirroring the existing guard. Added a narrow textual regression checker (`verifyModelBuilderCommitCancelledRunGuard.ts`) plus self-test, following the exact pattern of the existing `verifyModelBuilderCancelledRunGuard.ts`, and wired both into `contract-tests.yml`. Verified: `npx eslint` -- 2 pre-existing `no-empty` errors confirmed unrelated via `git stash`; `npx prettier --check` -- pre-existing drift confirmed unrelated via `git stash`; full-project `node ./node_modules/vue-tsc/bin/vue-tsc.js --noEmit -p tsconfig.json` exit 0; both new checker scripts pass. kind_robots PR #1146 open, CI pending at time of this note -- watching via PR subscription, will merge once green. Steps (1) and (3) remain genuinely blocked/pending as documented in prior entries; not touched this cycle.

      REVIEWED 2026-07-29T11:03Z (conductor scheduled agent run, claude-reviewer-2026-07-29T110331-sweep): posted a review-claim marker on kind_robots PR #1146 and conductor PR #1412 before starting (no rival claim found). Re-read the commitItem() diff directly against stores/modelBuilderStore.ts on the PR branch: confirms runId is captured from state.run before the await, and cancelledRunIds.has(runId) is checked immediately after the commit POST resolves in both the success and catch branches, mirroring generateItemAsset/pollAsyncArtJob's existing pattern exactly. All 13 kind_robots CI checks green (TypeScript, Contract verifiers, facet-catalog, etc.), no review comments, mergeable_state clean. Merged kind_robots #1146 (squash 96583c90), then conductor #1412 (squash 50e79aa3). kind_robots PR #1149 (unrelated missing-art-suggestion-context change, also open this cycle) was merged directly by Silas moments after the review-claim marker was posted -- no reviewer action needed there. Rearmed to `ready` per the recurring-task rule; claim fields cleared below.

      REVIEWED 2026-07-29T18:41Z (conductor scheduled agent run, claude-conductor-scheduled-20260729T180500Z-review-1161): this cycle's own claim, claude-conductor-scheduled-20260729T181200Z-mb-t029, opened kind_robots PR #1161 -- fixed the exact kaizen suggestion flagged in this note's 2026-07-29T10:06Z entry, extending draftText()'s existing liveValue-changed-while-drafting guard to also check isStageEditable(item, stageForDraftField(field)) after the /api/suggest response resolves, since the Approve button for PITCH/FIELDS_AND_PROMPTS has no isDrafting gate of its own and could approve a stage while its own draft was still in flight, silently overwriting the approved content. Posted a review-claim marker first (no rival claim found); re-read the diff directly (stores/modelBuilderStore.ts, extracted stageForDraftField() shared by draftText/batchDraftField, plus verifyModelBuilderDraftApprovalGuard.ts + self-test following the established narrow-textual-checker convention). All 13 kind_robots CI checks green (TypeScript, Contract verifiers, facet-catalog, facet-alias-smoke, GitGuardian, etc.), no review comments. Merged kind_robots #1161 (squash 97e768d). Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay in this sandbox. Rearmed to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-07-29T21:40Z (conductor scheduled Agent run, session claude-conductor-scheduled-20260729T2140Z-mb-t029): dispatched a general-purpose subagent to check the specific unticketed lead from the immediately-prior entry (batchApproveStage isStageEditable guard) before reading anything else. Confirmed it's already safe: batchApproveStage only flips stage status via approveStage and never writes drafted content, so the only real overwrite hazard is already covered by draftText()'s own guard (PR #1161) regardless of what triggered the approval -- no fix needed there. Found a distinct new bug instead: autoBuildItem() discarded draftText()'s success/failure result and called approveStage(itemId, 'PITCH')/approveStage(itemId, 'FIELDS_AND_PROMPTS') unconditionally afterward, so a failed draft (network error, empty model response, or draftText()'s own race guards) could leave the field empty while still marking the stage 'approved' -- a state the manual Approve button itself refuses (:disabled="isLocked('PITCH') || !pitch.trim()"). The GENERATE_ASSETS branch a few lines below already guarded this correctly; PITCH/FIELDS_AND_PROMPTS now follow the same shape. Added verifyModelBuilderAutoBuildDraftGate.ts + self-test following the established narrow-textual-checker convention, wired into package.json/contract-tests.yml. Verified: self-test passes, checker flags 3 violations on the pre-fix store (git stash) and passes clean post-fix, vue-tsc --noEmit exit 0, eslint/prettier clean on all changed files (2 pre-existing no-empty errors in the store confirmed via git stash to predate this change). Opened kind_robots PR #1165 (squash 6974f89), merged (all required checks green). Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-07-31T20:39Z (conductor Agent run, session claude-quirky-curie-20260731T2035Z-mb-t029): dispatched an Explore subagent to read stores/modelBuilderStore.ts, modelBuilderRecipes.ts, modelBuilderFields.ts, and every components/model-builder/*.vue and server/api/model-builder/** file in full, against the exclusion list of every bug class already fixed by prior cycles. Found a real, previously-unfixed gate bug distinct from PR #900's isGenerating/isQueued addition: canApproveAssets()'s isQueued check read item.artJobId, but generateItemAssetAsync only sets artJobId *after* two awaited network round-trips (prepareArtGenerator(), enqueueCurrentArt()) resolve -- item.queueState is set to 'queued' synchronously, one line above GENERATE_ASSETS.status = 'in-progress', well before artJobId exists. In that window, isQueued read false, so "Keep this asset" stayed enabled against the item's stale prior artImageId during a genuine in-flight async regenerate -- exactly what the function's own adjacent comment says must be blocked. Fixed by switching isQueued to read item.queueState, the field the store's own comment documents as the async-progress signal; this also fixed a secondary cosmetic bug where the "Queued..." indicator didn't appear during that same initial window (same computed). Verified: read generateItemAssetAsync and pollAsyncArtJob in full to confirm queueState's set/clear lifecycle and that nothing else in the panel depended on the old artJobId-based semantics; eslint clean on the changed file; vue-tsc --noEmit exit 0 (also confirms the unrelated pre-existing videoStore TS2459 errors noted by parallel sessions this cycle are already fixed on main). kind_robots PR #1216: all 12 checks green (TypeScript, Contract verifiers, facet-catalog, GitGuardian, etc.), merged squash a9e44185. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-07-31T22:30Z (conductor Agent run, session claude-quirky-curie-20260731T2226Z-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, both catalog helpers, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and all five existing verifyModelBuilder*.ts regression guards, against the exclusion list of every bug class already fixed by prior cycles (all PROGRESS/RAN entries above, most recently the canApproveAssets queueState fix in PR #1216 merged ~2 hours earlier this same day). Found a real, previously-unfixed gap: generateItemAsset (the synchronous "Generate candidate" path) had no check for `item.stages.GENERATE_ASSETS.status === 'approved'` before writing item.artImageId/imagePath, unlike its async sibling pollAsyncArtJob, which already has this exact guard (added for the regenerate-while-queued race). Two calls into generateItemAsset can be in flight for the same item concurrently -- an item's own "Auto" button (autoBuildItem) and a run-level "Auto-build all" (autoBuildRun) both reaching the same item -- because generatingItemSingleton (createOwnedSingleton) only guards a *different* owner value overwriting the current one, not two calls sharing the identical item id. Concrete failure: call A resolves first and its image gets approved via "Keep this asset"; call B (still in flight) then resolves and unconditionally overwrote artImageId/imagePath with its own different image, leaving the "approved" badge showing while the underlying image had silently changed to something never reviewed. Fixed by adding the identical guard pollAsyncArtJob already has. Extended verifyModelBuilderApprovedAssetGuard.ts (+ .test.ts) to check both functions instead of just pollAsyncArtJob. Verified independently: read generateItemAsset, pollAsyncArtJob, autoBuildItem, autoBuildRun, and batchAutoBuild directly to confirm the concurrent-entry-point path is real and reachable through normal UI actions; npx eslint stores/modelBuilderStore.ts shows only the 2 pre-existing no-empty errors (confirmed via git stash); npx prettier --check drift on the store confirmed pre-existing via the same stash comparison; npx vue-tsc --noEmit -p tsconfig.json exit 0; new/updated regression test passes (flags pre-fix shape in either function independently, a partially-fixed state, the fixed shape, and either function absent). kind_robots PR #1221: all 12 CI checks green (TypeScript, Contract verifiers, facet-catalog, Storybook/narrative contracts, GitGuardian, etc.), merged squash 4332c140. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Kaizen suggestion (deferred to a future cycle, not fixed here): the three auto-build entry points (autoBuildItem, batchAutoBuild, autoBuildRun) have no reentrancy check against each other for the same item id -- a real fix would give autoBuildItem an early-return guard so a second concurrent path skips an item already being processed, rather than relying on this cycle's completion-time discard. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      Fixed same-item auto-build reentrancy in kind_robots: autoBuildItem now rejects a second concurrent call for an item already being auto-built, preventing overlap between item, batch, and run-level entry points. Extended the existing auto-build regression guard with an isolated reentrancy fixture.

      REVIEWED 2026-07-31T23:28Z (conductor Agent run, session claude-quirky-curie-20260731T2328Z-mb-t029-review): reviewed kind_robots PR #1223. Confirmed the diff matches the note's description -- `autoBuildItem()` now returns false immediately when `state.autoBuildingItemId === item.id`, before claiming the singleton, closing the reentrancy gap deferred from PR #1221. `verifyModelBuilderAutoBuildDraftGate.ts`'s self-test was extended with an isolated fixture proving the checker flags the guard's absence and clears once present. All 12 CI checks green (TypeScript, Contract verifiers, facet-catalog, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian), mergeable_state clean, no review comments beyond the review-claim marker. Merged kind_robots #1223 (squash 2da5de5a). Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. No new kaizen task: this cycle's PR explicitly closed the one deferred kaizen from #1221 rather than raising a new one, and the prior createOwnedSingleton() kaizen already landed in PR #1049. Rearmed to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-08-01T00:28Z (conductor Agent run, session claude-quirky-curie-20260801T002819Z-mb-t029): step (4) follow-on -- dispatched an Explore subagent to read the full t-029 note history plus the store and every components/model-builder/*.vue file, against the exclusion list of every bug class already fixed by prior cycles including #1223's same-item autoBuildItem reentrancy guard. Found a real, previously-unfixed sibling gap: that guard only blocked a second `autoBuildItem` call for the same item -- it never checked whether a *manual* single-stage action (Generate candidate/`generatingItemId`, Draft with AI/`draftingField`, Execute commit/`committingItemId`) was already in flight for that item. Clicking "Auto" while a manual "Generate candidate" was still awaiting `artStore.generateCurrentArt()` let `autoBuildItem` see GENERATE_ASSETS still `in-progress` (not yet approved) and fire `generateItemAsset` a second, concurrent time for the same item -- a real duplicate call to the paid art-generation backend, both racing to write `artImageId`/`imagePath`. The "Auto" button itself also only disabled on `isAutoBuilding`, unlike every other button in the same panel, so the UI gave no feedback that the click was unsafe. Fixed by adding the same three-singleton check to `autoBuildItem` before it claims `autoBuildingItemSingleton`, and extending the item panel's Auto button `:disabled` with a matching `isManualActionInFlight` computed. Extended `verifyModelBuilderAutoBuildDraftGate.ts` + its self-test with a new check/fixtures for the added guard, mirroring how PR #1223 extended the same checker for its own guard. Verified: `npx eslint`/`npx prettier --check` clean on all 4 changed files (the store's 2 pre-existing `no-empty` errors and both files' pre-existing prettier drift confirmed unrelated via `git stash`); `npx vue-tsc --noEmit -p tsconfig.json` exit 0, full project; `npx tsx utils/scripts/verifyModelBuilderAutoBuildDraftGate.test.ts` passes with the new assertions; the checker also passes clean against the real store. kind_robots PR #1224: all 12 CI checks green (TypeScript, Contract verifiers, facet-catalog, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian) -- merged. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Kaizen suggestion: `batchAutoBuild`/`autoBuildRun` still have no cross-check against a *manual* action on an item they haven't started processing yet (only `autoBuildItem`-vs-itself and now `autoBuildItem`-vs-manual are covered) -- filed as t-037. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      (An automated task-event processor also rearmed this task to `ready` with a shorter note between this session's PR merge and this close-out commit -- consumed above rather than left as a separate duplicate entry; the field values it set, status: ready and cleared owner/claim fields, matched what this commit already sets.)

      RAN 2026-08-01T05:50Z (conductor Agent run, session claude-quirky-curie-20260801T0500Z-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, both catalog helpers, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and all seven existing verifyModelBuilder*.ts regression guards, against the exclusion list of every bug class already fixed by prior cycles (all PROGRESS/RAN entries above, most recently the manual-action-vs-auto-build guard in PR #1224). Found a real, previously-unfixed gap distinct from t-037's still-open auto-build-vs-manual-action kaizen: `prepareItemUpdate()` in `server/api/model-builder/runs/index.ts` -- the shared helper behind both `items/[id].patch.ts` and `items/batch.patch.ts` -- applied `body.pitch`/`body.fieldsDraft`/`body.promptDraft` unconditionally, with no server-side check against the item's actual stage status. `modelBuilderStore.ts`'s `isStageEditable` gate (ready/stale/rejected only) governs every content-editing call site client-side, but that gate is client-side only, so a direct PATCH (bad client state, a replayed request, or curl) for an item whose PITCH or FIELDS_AND_PROMPTS stage was already `approved` (or still `locked`) could silently overwrite reviewed content server-side while the badge kept showing `approved`. Same gap class as PR #1139's server-side commit gate, just reached through the item-edit route instead of commit, which had never gotten the equivalent check. Fixed by adding `assertContentStageEditable()`, called from `prepareItemUpdate()` before each content field is applied; both PATCH routes share the helper so one fix covers both. Added `utils/scripts/verifyModelBuilderItemPatchStageGuard.ts` + `.test.ts` following the established narrow-textual-checker convention, wired into `package.json`. Verified independently: new checker + selftest pass; all 6 pre-existing model-builder verify scripts + selftests still pass (no regression); `npx eslint` on changed files clean; `npx prettier --check` clean; full-project `npm run test` (`vue-tsc --noEmit`) exit 0. kind_robots PR #1227 (branch `claude/relaxed-lovelace-by95dz`): all 13 CI checks green (TypeScript, Contract verifiers, facet-catalog, facet-alias-smoke, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian) -- merged squash `36e40c2d`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay in this sandbox. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-08-01T06:41Z (conductor Agent run, session claude-quirky-curie-20260801T0627Z-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, both catalog helpers, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and all 9 existing verifyModelBuilder*.ts regression guards, against the complete exclusion list of every bug class already fixed by prior cycles (all PROGRESS/RAN entries above, most recently the prepareItemUpdate()/assertContentStageEditable() PITCH/FIELDS_AND_PROMPTS gate in PR #1227). Found a real, previously-unfixed sibling gap in that same function: `prepareItemUpdate()` applied `body.artImageId` to the Prisma update unconditionally, with no server-side check against the item's `GENERATE_ASSETS` stage status -- PR #1227's fix covered `pitch`/`fieldsDraft`/`promptDraft` but missed `artImageId`, the fourth content field the same route can PATCH. Concrete failure: a direct `PATCH /api/model-builder/items/:id` (replayed/retried request, stale client state, or a raw curl call) with `{ artImageId: <someId> }` for an item whose `GENERATE_ASSETS` stage was already `approved` could silently repoint the item at a different `ArtImage` with no re-review, while the badge kept showing `approved` for content the user never actually reviewed. Fixed by extending `assertContentStageEditable`'s `stageKey` union to accept `GENERATE_ASSETS` and calling it before `data.artImageId = normalizeNullableId(body.artImageId)`, mirroring the existing gates exactly. Extended `utils/scripts/verifyModelBuilderItemPatchStageGuard.ts` + `.test.ts` to cover the new gate. Verified independently: `npx eslint`/`npx prettier --check` clean on all 3 changed files; `npx vue-tsc --noEmit -p tsconfig.json` exit 0, full project; `npx tsx utils/scripts/verifyModelBuilderItemPatchStageGuard.test.ts` passes; the guard fails against the pre-fix route (confirmed via `git stash`) and passes post-fix; all 9 pre-existing model-builder verify/selftest scripts still pass (no regression). kind_robots PR #1230: all 11 CI checks green (TypeScript, Contract verifiers, facet-catalog, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian) -- merged squash `443789a5`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay in this sandbox. This closes the last remaining field-level gap in `prepareItemUpdate()`'s stage-approval mirror (pitch/fieldsDraft/promptDraft/artImageId are now all covered) -- no new kaizen filed this cycle. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      RAN 2026-08-01T08:29Z (conductor Agent run, session claude-scheduled-20260801T082937Z-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, both catalog helpers, all six components/model-builder/*.vue files, every server/api/model-builder/** route, and prisma/schema.prisma, against the complete exclusion list of every bug class already fixed by prior cycles (all PROGRESS/RAN entries above, most recently the artImageId stage-gate in PR #1230). Found a real, previously-unfixed gap distinct from every prior client-side cancelledRunIds fix: none of the write-capable item routes (item PATCH, batch PATCH, artifact POST, commit POST) ever checked `ModelBuildRun.status` at all, only ownership via `assertRunAccess`. `cancelRun()` marks a run `CANCELLED` server-side specifically to stop further work landing on it, but that intent was enforced only client-side and only within the one store instance that issued the cancellation (`cancelledRunIds` is an in-memory Set, wiped on reload; `cancelRun()` only clears the *current* tab's own `modelBuilder:runId` localStorage key). A second tab -- or a tab reloaded later with a stale `modelBuilder:runId` -- had no way to learn the run was cancelled elsewhere and could keep editing, generating, and durably `commitItem()`-ing (creates/links/promotes a canonical record, unlike everything else in the surface) into a run the user had already abandoned. This gap was actually already implicitly flagged by `utils/scripts/verifyModelBuilderCommitCancelledRunGuard.ts`'s own comment ("the commit POST durably creates/links/promotes the target server-side regardless -- that can't be undone from here") without ever being closed at the server layer. Fixed by adding `assertRunWritable(run)` next to `assertRunAccess` in `server/api/model-builder/runs/index.ts` (409 if `status === 'CANCELLED'`) and calling it in all four write routes, adding `status: true` to each route's `Run` select. Verified: `npx eslint` clean on all 5 changed files; `npx vue-tsc --noEmit -p tsconfig.json` exit 0, full project; `npx prettier --check` drift on 2 files confirmed pre-existing via `git stash` (same convention as every prior cycle). Did not add a new `verifyModelBuilder*.ts` textual guard this cycle (kept the diff to the 5 route/helper files; a follow-up guard script -- `npm run test:...` + `contract-tests.yml` wiring -- felt like more unverified scope than a single-session cycle should add) -- filed as this cycle's kaizen instead. kind_robots PR #1239: all 11 CI checks green (TypeScript, Contract verifiers, facet-catalog, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian) -- merged squash `a3de06a0`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay in this sandbox. Kaizen suggestion: add `utils/scripts/verifyModelBuilderRunWritableGuard.ts` (+ `.test.ts` + a `contract-tests.yml` step) asserting `assertRunWritable(...)` is called in all four write routes, mirroring `verifyModelBuilderItemPatchStageGuard.ts`'s pattern -- so a future refactor can't silently drop this check the way the client-only `cancelledRunIds` gap sat unfixed for this long. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      REVIEWED 2026-08-01T11:28Z (conductor Agent run, session reviewer-conductor-quirky-curie-78vtpp-20260801T112704Z): reviewed kind_robots PR #1243 (branch claude/relaxed-lovelace-0quf0x, opened by an earlier iteration of this session claiming t-029 as reviewer). Confirmed the diff matches the PR description exactly: added `setStatusForRun(runId, tone, message)` to `stores/modelBuilderStore.ts` (only writes the store-wide status banner if `state.run?.id === runId`, mirroring `autoBuildRun`'s existing same-run gate) and replaced all 9 bare post-await `setStatus(...)` calls across `generateItemAsset`, `pollAsyncArtJob`, `generateItemAssetAsync`, and `commitItem` with it -- closing a real gap where a slow render/commit finishing after the user navigated to a different run (via resetRun/resetAll/openRun, all of which deliberately leave the abandoned run resumable rather than cancelled) could pop a misleading success/error toast in the wrong run's banner. Verification claims checked against the PR body: eslint/prettier clean modulo pre-existing drift, `vue-tsc --noEmit` exit 0, all 9 pre-existing `verifyModelBuilder*.ts` regression guards + selftests still pass. All 12 CI checks green (TypeScript, Contract verifiers, facet-catalog, facet-alias-smoke, Storybook/Narrative/Taskmaster contracts, verify, GitGuardian), no review comments beyond this session's own review-claim marker. Merged squash `4f7a6ee7`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay. Kaizen: the PR's own suggestion (add `verifyModelBuilderRunWritableGuard.ts` asserting `assertRunWritable` is called in all four write routes, from PR #1239) is a re-list of t-029's own 2026-08-01T08:29Z kaizen already noted above -- not a new task; deferred as already-tracked rather than re-filed. Rearmed to `ready` per the recurring-task rule; claim fields cleared below.

      Fixed a concrete run-history retry regression in kind_robots: the History panel now closes only after the requested run is actually active, instead of dismissing itself after a failed openRun fetch. Branch worker/model-builder-t029-history-open-k9r4 is one commit ahead of current main with one Vue file changed (+2/-1).

      RAN 2026-08-08 (session 2026-08-08T202645Z-model-builder-t029-k9r4): fixed a concrete Run History retry regression in kind_robots PR #1620. `model-builder-run-history.vue` previously awaited `store.openRun(run.id)` and unconditionally closed the history panel, even though `openRun()` leaves the active run unchanged on a failed fetch. The panel now closes only when the requested run actually became active, preserving the immediate retry path after transient failures. Exact implementation head b6121efbe60dc151e30c9456c0bf025fbe1b3088 passed all 13 visible PR workflows, including TypeScript Type Check #31277304135 and Contract Tests #31277304175, before squash merge 64acb593d92a6440d2b243135fc7635f4b559369. The original dashboard/tutorial art deliverable remains outstanding, so rearm this recurring polish task to ready.

      RAN 2026-08-10T09:32Z (conductor scheduled agent run, session 20260810T-sched-conductor-mb-t029): step (4) follow-on -- dispatched a general-purpose subagent to read the full store, both catalog helpers, all `components/model-builder/*.vue` files, every `server/api/model-builder/**` route, and all 12 existing `verifyModelBuilder*.ts` regression guards, against the complete exclusion list of every bug class fixed by the ~33 prior cycles of this task (spot-confirmed still fixed, not exhaustively re-verified). Found a real, previously-unfixed gap: `autoBuildItem()`'s own doc comment promises "draft what's empty" and the PITCH branch honors it (`if (!item.pitch.trim())` before drafting), but the FIELDS_AND_PROMPTS branch's `artPrompt` draft fired unconditionally whenever art was wanted, ignoring any content already in `item.promptDraft`. Reachable through normal use: a user hand-types a custom prompt into the item panel's "Generation prompt" textarea (persisted via `updatePrompt`), doesn't yet approve that stage, then clicks "Auto" (or a batch/run-level auto-build) expecting the rest to finish automatically -- and their prompt is silently discarded for a fresh, unreviewed AI draft, which `generateItemAsset` then renders art from instead of what they wrote. Fixed by gating the `artPrompt` draft behind `!item.promptDraft.trim()`, mirroring the PITCH branch exactly; `fieldsDraft` deliberately left unguarded since it's always pre-filled with a non-empty `defaultFieldsTemplate` skeleton, so an emptiness check there couldn't distinguish "still the skeleton" from "user-edited." Extended `utils/scripts/verifyModelBuilderAutoBuildDraftGate.ts` + its `.test.ts` with a new textual-shape check for this gate. Verified: `npx tsx utils/scripts/verifyModelBuilderAutoBuildDraftGate.ts`/`.test.ts` pass; all other 11 `verifyModelBuilder*.ts` guards unchanged and pass; `npx eslint`/`npx prettier --check` on all 3 changed files show only the same pre-existing `no-empty` errors/drift in `modelBuilderStore.ts` (confirmed via `git stash`), the edited region itself untouched by drift; full-project `npx vue-tsc --noEmit -p tsconfig.json` exit 0. kind_robots PR #1711 (branch `worker/model-builder-t029-autobuild-artprompt-overwrite`): all 13 CI checks green, `mergeable_state: clean` -- merged squash `a4da530e`. Step (1) (dashboard-tab + tutorial .webp art) remains the sole outstanding original deliverable, still blocked on the external art-generation relay in this sandbox. Kaizen suggestion (deferred, not fixed this cycle): `prepareItemUpdate()` in `server/api/model-builder/runs/index.ts` applies `body.targetType`/`body.targetId` unconditionally with no stage/ownership gate, unlike `pitch`/`fieldsDraft`/`promptDraft`/`artImageId` (all gated via `assertContentStageEditable`) -- currently unreachable via the store and low-impact, but worth a guard for defense-in-depth if a future feature starts sending those fields. Rearming to `ready` per the recurring-task rule; claim fields cleared below.

      Scoped Model Builder hardening branch rejects client PATCH attempts to mutate commit-owned targetType/targetId metadata in both single-item and batch item routes; normal store callers do not send these fields.

      RAN 2026-08-10 (session 20260810T131806Z-auto-35ed50): found and fixed a server-side commit-provenance integrity gap. Model Builder's generic single-item and batch PATCH routes accepted targetType/targetId even though those fields are written by the commit executor and drive idempotent replay/history. Added a shared patch-policy guard that rejects client attempts to mutate those server-managed fields; normal store callers do not send them. kind_robots PR #1719 exact head 66df74054399ffd5bf2c3959bd0c4e644fe5b3f9 passed all 13 observed PR workflows, including TypeScript Type Check #31393328448 and Contract Tests #31393328437 (full verifier job green), then squash-merged as f406345809f5561e266b4339ec8384437664fce2. Original dashboard/tutorial generated-art deliverable remains outstanding, so rearm this recurring polish task.

      Required-CI hardening slice: the existing approved-asset and item-patch stage guards were callable from package.json but not enforced by the required Contract Tests job. The already-required run-writable guard entry point now executes both sibling guard contracts too; implementation branch worker/model-builder-t-029-20260810-a6c3.

      PROGRESS 2026-08-10 (20260810T134452Z-modelbuilder-t029-a6c3): merged kind_robots PR #1720 as fa2059d214e81d90b3905cbcd63037fa866af7d3. Two existing Model Builder regression guards (approved-asset overwrite protection and item-PATCH stage review protection) had package scripts but were not enforced by the required Contract Tests workflow. The already-required run-writable guard entry point now executes both sibling contracts against their canonical source files, making those invariants merge-blocking without a risky large workflow-file rewrite. Exact PR head 506d9642cb4261db99c42433a769b0fd419e24bc passed all 12 observed workflows, including Contract Tests #31395370404 and TypeScript #31395370790. No runtime, database, or production-data behavior changed; original generated-art substep remains outstanding for a later token/art-capable cycle.
    updated: '2026-08-10T14:00:40Z'

  - id: t-030
    milestone: m6
    title: "Batch item PATCH endpoint to cut N per-item saves to one request"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-027 (kind_robots PR #353, 2026-07-17). The new batch editor and autoBuildRun
      loop the existing single-item PATCH (/api/model-builder/items/:id) once per item, so a
      batch-set or batch-draft across N items fires N requests. Add a batch endpoint (e.g.
      PATCH /api/model-builder/items/batch accepting [{ id, ...fields, stageStatuses }]) and a
      store batchPushItems helper so a whole group edit persists in one round-trip, recording the
      same revision/stage metadata the per-item PATCH does. Front-end + one server route; no schema
      change. Keep the per-item PATCH for single edits.
    claimed_by: claude-conductor-scheduled-20260718T1730Z
    claimed_at: '2026-07-18T11:06:30Z'
    updated: '2026-07-18T11:06:30Z'

  - id: t-031
    milestone: m6
    title: "Live smoke test of the t-025 async ArtJob queue path on /model-builder"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Kaizen from t-025 (kind_robots PR #539, 2026-07-19). t-025 added
      artStore.enqueueCurrentArt/getArtJobStatus/finalizeQueuedArtImage and
      modelBuilderStore.generateItemAssetAsync + a per-item poll loop, verified only via
      vue-tsc/eslint (no reachable DATABASE_URL/render backend in the sandbox — the
      ComfyUI/Alexandria relay behind the ArtJob queue was failing every hourly run all day
      per coloring-book/t-022's notes). Once that backend is confirmed healthy again, do a
      live /model-builder run: queue GENERATE_ASSETS on 2-3 build items concurrently via the
      new async button, confirm each shows independent queued -> rendering -> done state
      (not sharing/clobbering artStore's global queueState), confirm the resulting
      artImageId/imagePath land correctly per item, and confirm the existing synchronous
      "Generate candidate" button still works unchanged. Record findings; file any fix as a
      new task.

  - id: t-032
    milestone: m6
    title: "Add a dev-time consistency check between CREATE_TARGETS and linkSourceToTarget"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    updated: '2026-07-28T06:12:07Z'
    note: >
      Kaizen from t-029 (kind_robots PR #1079, 2026-07-28, Character->Scenario link gap;
      same shape as PR #1005's earlier Dream->Bot gap). server/api/model-builder/items/[id]/commit.post.ts's
      linkSourceToTarget is a hand-maintained switch over (sourceType, targetType) pairs with
      no structural guarantee it covers every CREATE_TARGETS entry in stores/helpers/modelBuilderFields.ts
      that corresponds to a real bidirectional Prisma relation -- two such gaps have now been
      found by manual read-through across separate cycles. Add a small dev-time check (unit
      test or a boot-time assertion) that walks CREATE_TARGETS, and for each (sourceType,
      targetType) pair where prisma/schema.prisma defines a relation between the two models,
      asserts linkSourceToTarget has a matching case. Also worth a look while there: two
      lower-confidence sibling gaps noticed during t-029's 2026-07-28 cycle but not verified --
      Reward -> Character (Character -> Reward and Reward.Characters both exist) and other
      CREATE_TARGETS pairs (Project/Facet/Reward -> Scenario, etc.) whose matching relations
      weren't confirmed. Front-end/test-only change; no schema change expected.
    claimed_by: claude-conductor-burst-20260728T061206-mb-t032
    claimed_at: '2026-07-28T06:12:07Z'

  - id: t-033
    milestone: m6
    title: "Restrict relationship-expansion CREATE outputs to source types with a real relation"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    note: >
      Kaizen from t-032 (kind_robots PR #1081, 2026-07-28). The relationship-expansion
      recipe's OUTPUT_CATALOG (stores/helpers/modelBuilderRecipes.ts) does not currently
      restrict which expand-* outputs are shown per source type -- RECIPES['relationship-
      expansion'].sourceTypes just lists every eligible source model generically, so
      nothing in the UI stops e.g. a Reward source from picking "Manager bot" or
      "Narrator bot" (targetType Bot), which have no Prisma relation to Reward and would
      create an orphaned, unlinked Bot on commit. Add a per-output source-type
      restriction (e.g. a `sourceTypes?: SourceTypeKey[]` field on BuildOutputConfig,
      populated from the same relation graph t-032's verifyModelBuilderLinkCoverage.ts
      already derives from prisma/schema.prisma) so the picker only ever offers
      expand-* outputs a source type actually has a relation for. Front-end/store change;
      no schema change expected.

      IMPLEMENTED 2026-07-28 (burst-20260728-mb-t033-relexpand): kind_robots PR #1100.
      Added `sourceTypes?: SourceTypeKey[]` to `BuildOutputConfig`
      (stores/helpers/modelBuilderRecipes.ts), populated per expand-* output from the
      actual (sourceType, targetType) pairs `linkSourceToTarget` handles in
      server/api/model-builder/items/[id]/commit.post.ts: expand-characters ->
      [Dream, Reward, Scenario], expand-rewards -> [Dream, Character], expand-scenarios
      -> [Dream, Character], expand-narrator-bot -> [Dream] (links via
      Dream.narratorId), expand-manager-bot -> [Project] (links via
      Project.managerBotId), expand-signature-rewards -> [Character].
      `getOutputsForRecipe(recipeKey, sourceType?)` now filters by this list when
      present; `modelBuilderStore.ts`'s `recipeOutputs` computed and `selectRecipe()`
      both pass `state.sourceType` through, so the existing recipe-selector component
      picks this up with no template change. Verified
      `npx tsx utils/scripts/verifyModelBuilderLinkCoverage.ts` still passes (this
      change doesn't touch commit.post.ts or the schema). Could not run the full
      vue-tsc typecheck locally (no node_modules in this sandbox) -- PR CI's
      TypeScript check is the verification gate.

      KAIZEN FOUND: Facet is still listed in SOURCE_TYPES.recipes as eligible for
      relationship-expansion, but has zero linkSourceToTarget cases at all -- so after
      this fix, a Facet source now sees 0 available expand-* outputs for that recipe
      (previously it saw all 6, all of which would have orphaned on commit). Strict
      improvement, but the recipe is effectively non-functional for Facet until real
      link cases exist. Filed as t-034.

      DONE 2026-07-28: kind_robots PR #1100 merged (CI green). conductor PR #1317
      (roadmap/TALKBACK bookkeeping) merged (Worker PR CI, Roadmap Audit, Security
      Audit, Process task events all green).
    claimed_by: burst-20260728-mb-t033-relexpand
    claimed_at: '2026-07-28T11:12:17Z'
    updated: '2026-07-28T11:23:34Z'

  - id: t-034
    milestone: m6
    title: "Give Facet real relationship-expansion targets or drop it from relationship-expansion's sourceTypes"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    note: >
      Kaizen from t-033 (kind_robots PR #1100, 2026-07-28). SOURCE_TYPES['Facet'].recipes
      in stores/helpers/modelBuilderRecipes.ts still lists 'relationship-expansion' as
      eligible, but server/api/model-builder/items/[id]/commit.post.ts's
      linkSourceToTarget has zero `sourceType === 'Facet'` cases -- no real Prisma
      relation from Facet to Character, Reward, Scenario, or Bot exists to link a
      created record back to it. Before t-033, a Facet source silently showed all 6
      expand-* outputs, every one of which would orphan on commit (create a record
      with no link back to the source Facet). t-033's per-output sourceTypes filter now
      hides all of them for Facet instead (0 outputs shown), which is strictly safer
      but leaves the recipe chip visibly selectable and functionally empty for Facet.
      Either (a) add real relation(s) from Facet to one or more of Character/Reward/
      Scenario/Bot in prisma/schema.prisma plus matching linkSourceToTarget case(s) and
      matching expand-* sourceTypes entries, if such a relation makes product sense, or
      (b) remove 'relationship-expansion' from Facet's SOURCE_TYPES.recipes entirely if
      it doesn't. Decide by checking whether Facets are meant to spawn related
      Characters/Rewards/Scenarios/Bots at all in the product design -- this task does
      not assume the answer.

      DECIDED (b): removed 'relationship-expansion' from Facet's SOURCE_TYPES.recipes
      (and 'Facet' from relationship-expansion's own sourceTypes list). Facet's only
      real Prisma relations to Dream/Scenario are DreamFacet/ScenarioFacet -- explicit
      tag-attachment join tables that attach a Facet to an *existing* Dream/Scenario
      (server/api/{dreams,scenarios}/[id]/facets.put.ts, scenarioGenreFacetSync.ts),
      the same shape used for genre/mood/theme tagging. That's structurally different
      from every linkSourceToTarget case, which all link a newly-created child record
      back to its parent -- there is no such parent->child relation from Facet to
      Character, Reward, Scenario, Dream, or Bot. Facet keeps art-upgrade only.
      Verified via eslint, verifyModelBuilderLinkCoverage.ts (still passes, unaffected),
      and `npm test` (vue-tsc --noEmit, clean). kind_robots PR #1108 merged (CI green,
      7/7 checks passed).
    claimed_by: conductor-scheduled-20260728T1614Z-mb-t034-burst
    claimed_at: '2026-07-28T16:15:00Z'
    updated: '2026-07-28T17:05:00Z'

  - id: t-035
    milestone: m6
    title: "verifyModelBuilderLinkCoverage.ts doesn't catch join-table-only relations, letting non-functional recipe entries ship silently"
    status: done
    passes: 0
    stakes: reversible
    owner: reviewer
    note: >
      Kaizen from t-034 (kind_robots PR #1108, 2026-07-28). It took three cycles
      (t-032 built the coverage guard, t-033 found Facet had zero real
      linkSourceToTarget cases despite listing relationship-expansion as eligible,
      t-034 finally removed it) to fully resolve one bad SOURCE_TYPES.recipes entry,
      because verifyModelBuilderLinkCoverage.ts only matches direct-typed Prisma
      relation fields (the shape most CREATE_TARGETS pairs use) and has no way to
      flag a source type whose only relations to a candidate target are join-table
      tag-attachments (Facet's DreamFacet/ScenarioFacet) rather than a parent->child
      creation link. Extend the guard to also detect this case -- e.g. when a source
      type lists a recipe/output eligible for a target type, but the only
      prisma/schema.prisma relation between them is via an explicit join model (no
      direct FK either direction), flag it the same way an entirely-missing relation
      is flagged today. This would have caught Facet's bad relationship-expansion
      entry at t-032 time instead of needing a dedicated t-033/t-034 investigation.
      Front-end/test-only change; no schema change expected.


      DONE 2026-07-28 (conductor scheduled Agent run, claude-conductor-scheduled-20260728T181326Z-mb-t035):
      kind_robots PR #1109 (squash b5cc4026) merged green. Added extractCreateTargetMap,
      extractOutputCatalogEntries, schemaHasJoinTableRelation (requires the @@id([...])
      composite-key shape every real join table in this schema uses, to avoid
      false-positiving on broad hub models like ArtImage that hold list-type
      back-references to dozens of unrelated models), and
      findJoinTableOnlyEligibilityPairs to verifyModelBuilderLinkCoverage.ts. Verified
      via tsx run (clean on current config), a simulated regression (re-added Facet to
      expand-scenarios's sourceTypes -- correctly failed, naming ScenarioFacet as the
      join model), full vue-tsc (clean), and eslint (clean). First CI attempt failed
      Contract verifiers: a repo-wide capture-group-guard heuristic linter
      (verifyCaptureGroupGuards.ts, distinct from vue-tsc's real type narrowing) didn't
      recognize a combined `if (!a || !b) continue` as guarding each variable, nor
      `match[1]!` as guarding `match` before indexing. Fixed by splitting into two
      single-variable guards and switching to `match?.[1] ?? null`; re-verified
      unchanged behavior, second CI run green on all 6 checks.
    claimed_by: claude-conductor-scheduled-20260728T181326Z-mb-t035
    claimed_at: '2026-07-28T18:13:26Z'
    updated: '2026-07-28T18:13:26Z'

  - id: t-036
    milestone: m6
    title: "Add a lint/regression guard for GENERATE_ASSETS-style completion-write races in modelBuilderStore.ts"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    note: >
      Kaizen from t-029 (kind_robots PR #1114, 2026-07-28): a GENERATE_ASSETS render
      completion handler (generateItemAsset/pollAsyncArtJob/generateItemAssetAsync)
      held only its original item reference and unconditionally overwrote the stage
      status on completion, silently clobbering a concurrent upstream reopen's
      'stale' marker. Fixed ad hoc with finishGenerateAssets(item, next), which only
      writes if the stage is still 'in-progress'. This is now the third distinct
      "review/re-review gate silently bypassed" bug class found by manual read-through
      in modelBuilderStore.ts (after canApproveAssets and the batch-editor
      stage-approval-gate fixes) -- each one only caught because a cycle happened to
      read that exact code path. Add a repo-side static check (mirroring
      verifyModelBuilderLinkCoverage.ts's approach from t-032/t-035) that walks every
      async completion/write site touching a stage-status field in
      modelBuilderStore.ts and flags any that don't gate on the stage's current status
      before writing -- so this bug class is caught mechanically instead of requiring
      a full manual store re-read every burst cycle. Front-end/test-only change; no
      schema change expected.

      Provenance note: this task idea originated in a concurrent session's PR
      (conductor PR #1360) that collided with this session's own t-029 close-out
      (conductor PR #1362, merged first) -- both sessions independently reviewed the
      same kind_robots PR #1114 within the same minute. PR #1360's roadmap/pitch edits
      were superseded and closed unmerged, but this specific kaizen idea was salvaged
      here since it's a genuinely good, independent suggestion distinct from anything
      this session's own review proposed.

      PROGRESS 2026-07-28 (conductor scheduled Agent run, claude-conductor-agentrun-20260728T220700-mb-t036):
      implemented on kind_robots branch claude/confident-bardeen-oevtq7, kind_robots PR
      #1122 (open, CI running). Added utils/scripts/verifyModelBuilderCompletionGate.ts,
      mirroring verifyModelBuilderLinkCoverage.ts's regex-extraction approach: walks
      every async function in modelBuilderStore.ts, finds any item.stages.<KEY> = write
      after that function's own first await, and fails if it isn't wrapped (directly or
      via a named helper like finishGenerateAssets) in an
      if (item.stages.<KEY>.status === '...') guard for the same key. Added a self-test
      (verifyModelBuilderCompletionGate.test.ts) against a synthetic fixture, wired both
      into package.json and contract-tests.yml. Ran the checker against the store BEFORE
      writing any fix and it immediately found a real, previously-unfixed instance of the
      same bug class in commitItem(): the COMMIT stage write after the commit POST
      resolves was unconditional, so a concurrent upstream edit landing mid-request would
      silently overwrite a markDownstreamStale-set 'stale' marker back to 'approved'.
      Fixed with finishCommit (mirrors finishGenerateAssets exactly), marking COMMIT
      'in-progress' for the request duration and resetting it on failure via the same
      gate. Verified: self-test passes; the checker also independently confirmed via
      git-stash re-run against the pre-fix file that it correctly flags exactly that one
      violation. eslint clean (2 pre-existing no-empty errors in modelBuilderStore.ts
      confirmed pre-existing via git-stash). prettier --check clean. Full-project
      vue-tsc --noEmit exit 0. DONE 2026-07-28 (conductor scheduled Agent run):
      kind_robots PR #1122 merged (all 13 checks green, squash 5b97ffd).
    claimed_by: claude-conductor-agentrun-20260728T220700-mb-t036
    claimed_at: '2026-07-28T22:06:58Z'
    updated: '2026-07-28T22:06:58Z'

  - id: t-037
    milestone: m6
    title: "Cross-check batchAutoBuild/autoBuildRun against a manual per-item action before processing that item"
    status: done
    passes: 0
    stakes: reversible
    owner: null
    note: 'RAN 2026-08-01T01:37Z (conductor Agent run, session claude-quirky-curie-20260801T0130Z-mb-t037): implemented the outcome distinction in kind_robots. autoBuildItem() now returns a three-way outcome (''committed'' | ''skipped'' | ''failed'') instead of a boolean -- the same-item reentrancy guard (PR #1223) and manual-action-in-flight guard (PR #1224) both now return ''skipped'', a failed draft/generate/commit returns ''failed'', and completing (or already complete) returns ''committed''. batchAutoBuild and autoBuildRun tally skipped separately and append a "(N skipped -- manual action in progress, retry after it finishes)" note to their status message when nonzero, so a stalled batch item now reads as busy rather than silently looking identical to a real failure. Did not add automatic re-queuing of skipped items: the task note flagged it as optional to investigate, and re-queuing from inside the same loop risks re-racing the same still-in-flight manual action if the loop reaches the item again before it resolves. Extended verifyModelBuilderAutoBuildDraftGate.ts''s textual checks and self-test fixtures to require the new ''skipped''/''failed'' literals at the same anchor points the prior ''return false'' checks used. Verified: npx eslint/npx prettier --check clean on touched files (2 pre-existing no-empty errors and pre-existing prettier drift in the store confirmed unrelated via git stash); npx vue-tsc --noEmit -p tsconfig.json exit 0 full project; both draft-gate npm test scripts pass. kind_robots PR #1225: all 12 CI checks green, merged (0fa5b70). Kaizen suggestion: batchAutoBuild/autoBuildRun''s own triggering buttons have no pre-run ''N items busy'' advisory before starting, unlike the single-item Auto button''s isManualActionInFlight disable -- filed as t-038.'
    claimed_by: claude-quirky-curie-20260801T0130Z-mb-t037
    claimed_at: '2026-08-01T01:28:25Z'
    updated: '2026-08-01T01:40:32Z'

  - id: t-038
    milestone: m6
    title: "Pre-run 'N items busy' advisory for batchAutoBuild/autoBuildRun before starting"
    status: done
    passes: 0
    stakes: reversible
    owner: worker
    note: 'Merged kind_robots PR #1226: promoted the per-item manual-action check (model-builder-item-panel.vue''s isManualActionInFlight) into modelBuilderStore.ts as isItemManualActionInFlight(itemId), and added a busyCount badge + tooltip to the batch editor''s ''Auto-build group'' and the progress matrix''s ''Auto-build all'' buttons. Advisory only, no hard block. Verified vue-tsc + eslint + prettier clean on all touched lines.'
    claimed_by: claude-scheduled-20260801T042657Z-mb-t038
    claimed_at: '2026-08-01T04:47:09Z'
    updated: '2026-08-01T05:04:33Z'

  - id: t-039
    milestone: m6
    title: "Add a verifyModelBuilderRunWritableGuard.ts regression guard for the new assertRunWritable() server-side cancelled-run check"
    status: done
    passes: 0
    stakes: reversible
    owner: null
    note: >-
      Kaizen from t-029 (kind_robots PR #1239, 2026-08-01): assertRunWritable(run) was
      added in server/api/model-builder/runs/index.ts and is called from all four
      write-capable item routes (items/[id].patch.ts, items/batch.patch.ts,
      items/[id]/artifacts.post.ts, items/[id]/commit.post.ts) to refuse writes once
      ModelBuildRun.status is CANCELLED. Unlike the sibling client-side
      cancelledRunIds checks, this one has no dedicated textual regression guard --
      a future refactor could silently drop the assertRunWritable() call from one of
      the four routes with nothing failing except a live end-to-end test this
      sandbox can't run. Add
      utils/scripts/verifyModelBuilderRunWritableGuard.ts (+ .test.ts) following
      verifyModelBuilderItemPatchStageGuard.ts's narrow-textual-checker convention --
      assert each of the four routes calls assertRunWritable(...) after
      assertRunAccess(...) and before its first write -- and wire it into
      package.json + .github/workflows/contract-tests.yml alongside the other Model
      Builder guard steps.
    claimed_by: null
    claimed_at: null
    updated: '2026-08-01T12:40:33Z'
