project: taskmaster
kind: software

goal: >
  Taskmaster uses stories to finish tasks: it wraps real objectives (conductor
  HONEYDO/needs-human items, Project selection) in a second-person, choice-driven
  narrative, keeping the actual objective visible alongside the fiction, with
  every real write-back an explicit, user-confirmed Apply action.

notes_from_silas: |
  Retroactive onboarding note (2026-07-29): this project is NOT new work. It is
  conductor tracking catching up to an already-shipped kind_robots product.

  Taskmaster is the permanent rename of what this repo used to track as the
  `serendipity` project (see projects/serendipity/roadmap.yaml, now retired --
  its m1-m5 milestones and t-001..t-011 are the real build history of this
  product, just under the old name). kind_robots
  docs/products/storybook-taskmaster-boundary.md documents the rename as
  intentional and permanent: "The former task-story product is renamed
  completely to Taskmaster, leaving the Serendipity name and route free for
  the voice-led experience." The file-level cutover was kind_robots commit
  5509ac4 ("Complete permanent Taskmaster cutover and narrative art
  profiles"): serendipity-page.vue -> taskmaster-page.vue,
  serendipityStore.ts -> taskmasterStore.ts, /serendipity route/content
  deleted. /serendipity was separately reclaimed the same morning (commit
  36a86bf) for the unrelated Alexa voice-led surface, already tracked under
  conductor's alexa-integration project.

  Product boundary (from the boundary doc, permanent): Taskmaster owns direct
  objective entry, project and HONEYDO selection, practical checkpoints,
  narrative framing, blocked/deferred outcomes, explicit progress review, and
  confirmed write-back. It is a sibling of Storybook (which creates stories
  for their own sake) and must not inherit Storybook's persistent-narrative-state
  concerns, and Storybook must not inherit Taskmaster's task/write-back
  concerns -- separate Pinia stores, separate state machines, on purpose.

  Verified via code inspection (2026-07-29): components/pages/taskmaster-page.vue
  (724 lines) and stores/taskmasterStore.ts (999 lines) in kind_robots are a
  mature, complete implementation, not a placeholder scaffold. Canonical
  placement already exists in kind_robots utils/projectPlacements.ts
  (`taskmaster: { channelKey: 'play', tabKey: 'taskmaster', route: '/taskmaster' }`)
  and in the dashboard/tutorial-card systems (stores/helpers/dashboardHelper.ts,
  stores/helpers/tutorialCards.ts under the 'scenario' section, alongside
  Storybook).

milestones:
  - id: m1
    title: "Story-weaving engine (inherited from serendipity/m1-m4, done under the old name)"
    weight: 60
    status: done

  - id: m2
    title: "Serendipity -> Taskmaster rename and route cutover"
    weight: 15
    status: done

  - id: m3
    title: "Audit, polish, and dedicated art for the Taskmaster surface"
    weight: 25
    status: in-progress

tasks:
  - id: t-001
    milestone: m3
    title: "Audit the Taskmaster surface and groom a real task list"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      AUDIT RESULT (2026-07-29): read components/pages/taskmaster-page.vue (724 lines),
      stores/taskmasterStore.ts (999 lines), and docs/products/storybook-taskmaster-boundary.md
      in kind_robots. (1) Capability check against old serendipity/t-001..t-011: theme
      picker (NarrativeIngredientPicker for location/genre), story weaving loop
      (beats/checkpoints), HONEYDO/needs-human task weaving, write-back
      (applyWriteBack/pendingWriteBacks), and session recap are all present and intact
      under the new name. The `taskmaster-session` localStorage key correctly follows
      the boundary doc's persistence-key invariant (no leftover `serendipity-session`
      key). One capability was found BROKEN, not just renamed: AGENT-todo
      badging/filtering (old t-011). kind_robots stores/todoStore.ts's
      `isSerendipityAgentTodo()` still matched only the pre-rename shapes (icon
      kind-icon:sparkles, title "story decision on ...", description "captured by
      serendipity for conductor task ..."), but taskmasterStore.ts's
      `answerCurrentBeat()` write-back has created todos with a different shape since
      the rename (icon kind-icon:gearhammer, title "Taskmaster decision on ...",
      description "Captured by Taskmaster for conductor task ..."). Every
      Taskmaster-created needs-human todo since the rename was silently falling into
      the generic AGENT bucket instead of the Todos surface's "Story" tab -- fixed
      directly this cycle rather than just filed, see t-002. (2) Confirmed Taskmaster
      still borrows Storybook's art: both stores/helpers/dashboardHelper.ts and
      stores/helpers/tutorialCards.ts have explicit "Shared temporarily until the
      dedicated Taskmaster art set lands" comments pointing at
      tabImage('scenario','storybook') / tutorialImage('scenario','storybook'); no
      projects/images/taskmaster-{icon,card,hero}.webp exist in this repo either, and
      taskmaster has no entry yet in root ART-PROMPTS.md. Filed as t-003, the standard
      "Polish and upgrade X front-end surface" pattern used by every other project
      here. (3) Milestone m3 moved to in-progress (audit done, t-002/t-003 open).
    claimed_by: claude-conductor-scheduled-20260729T170000Z-tm-t001
    claimed_at: '2026-07-29T17:04:44Z'
    updated: '2026-07-29T17:04:44Z'

  - id: t-002
    milestone: m3
    title: "Fix Taskmaster AGENT-todo badging broken by the Serendipity rename"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Found during t-001's audit: kind_robots stores/todoStore.ts's
      isSerendipityAgentTodo() only matched the pre-rename icon/title/description
      shapes, so every Taskmaster-created needs-human AGENT todo since the rename
      was silently miscategorized (see t-001's note for the full diagnosis). Fixed
      by widening the matcher to recognize both the pre- and post-rename shapes
      (backward compatible with any already-created rows). kind_robots PR #1157,
      merged (squash 0102dddb5822912468e178a98f5ed671dc44d694), all 11 checks green
      including the Taskmaster checkpoint contract.
    claimed_by: claude-conductor-scheduled-20260729T170000Z-tm-t001
    claimed_at: '2026-07-29T17:04:44Z'
    updated: '2026-07-29T17:04:44Z'

  - id: t-003
    milestone: m3
    title: "Polish and upgrade Taskmaster front-end surface"
    status: done
    passes: 0
    stakes: reversible
    note: |-
      RESOLVED (conductor scheduled sweep, 2026-08-11T01:55Z): the dashboard-tab/tutorial delivery gap
      this task has tracked since 2026-07-29 is closed. KR_API_TOKEN was present in this runtime (unlike
      the immediately prior session), and conductor PR #2047's destination-preservation fix is confirmed
      working end-to-end: submitted two fresh targeted requests via projects/art-prompts.yaml requests:
      (target_repo: silasfelinus/kind_robots, image_path public/images/dashboard-tabs/scenario/taskmaster.webp
      and public/images/tutorials/scenario/taskmaster.webp) through consume_art_requests_to_media.py's
      queue/enqueue path. Both ArtJobs (8232, 8233) rendered via the home relay (claimedBy: Silas-PC) and
      were written directly to self-hosted media -- verified live with HEAD + content fetch against
      https://media.acrocatranch.com/images/dashboard-tabs/scenario/taskmaster.webp and
      .../tutorials/scenario/taskmaster.webp (both HTTP 200, correct dimensions, content matches the
      submitted prompts). Both requests marked status: done in art-prompts.yaml.

      IMPORTANT SIDE FINDING: the dashboard-tab path already had a *different*, unrelated, garbled image
      sitting at it since 2026-07-28 (last-modified date on the old file) -- not a Taskmaster asset at all,
      a broken mockup-style render with illegible UI text ("Tabkcmaster", "Server Connections", ...). Every
      prior session's diagnosis checked ArtJob/DB completion status or the local (git-ignored, always-empty)
      kind_robots checkout, never a live HEAD check against the actual media origin -- so this pre-existing
      wrong file was never noticed. The already_satisfied() existence-only check in
      consume_art_requests_to_media.py would have treated it as "done" and skipped regeneration entirely;
      had to bypass it with a one-off forced-submission script (deleted after use, not committed) to get the
      correct image rendered and delivered over the wrong one. Filed t-004 to audit other dashboard-tab/
      tutorial paths for the same silent-wrong-asset class, since finding one this way strongly suggests
      there are more.

      All 5 originally-scoped assets (icon, card, hero, dashboard-tab, tutorial) are now delivered and live,
      and the Quest Desk UI redesign (kind_robots PR #1469) has been in production since 2026-07-29. Full
      milestone m3 scope is met. Not doing a fresh phone/tablet/desktop visual pass in this same sweep --
      the UI itself hasn't changed this session, only site-wide static assets referenced by existing,
      already-shipped-and-verified components.

      --- prior history ---
      Kaizen from t-003 (kind_robots PR #1160, conductor PR #1443, 2026-07-29). Queued taskmaster's dedicated icon/card/hero + dashboard-tab/tutorial art via consume_art_queue.py --live --no-wait (ArtJobs 2846-2850) and pointed dashboardHelper.ts/tutorialCards.ts at the new paths. Step (4) liveUrl/channelKey/tabKey confirmed already correct via sync_projects.py (UNCHANGED). Remaining: confirm jobs 2846-2850 rendered and run distribute_images.py (or confirm the distribute-images workflow already placed them) before closing to done.

      Implemented the objective-first Quest Desk redesign on a focused Kind Robots branch. Setup now removes the generic narrator slab, groups optional story ingredients into compact disclosures, adds a mobile-safe action rail, and moves active quests into a story-plus-mission-rail layout with an explicit minimum stage height.

      Merged Kind Robots PR #1469 and verified production /taskmaster at merge commit 3209253b598b4da104310cb7018e00240c9f51df. The objective-first Quest Desk UI is complete. Remaining task scope is to confirm ArtJobs 2846-2850 rendered and distribute the dedicated Taskmaster dashboard-tab and tutorial assets into Kind Robots. Lesson: responsive narrator stages need an explicit desktop minimum height and should mount only when contextual story content exists -- objective-first setup prevents decorative narrative UI from obscuring real work.

      RECHECK (conductor scheduled sweep, 2026-08-10T07:39Z): confirmed all 5 ArtJobs (2846-2850) are DONE via GET /api/art/queue/{id} -- artImageIds 13366 (icon), 13367 (card), 13368 (hero), 13369 (dashboard-tab), 13370 (tutorial). Icon/card/hero are verified LIVE: projects/images/manifest.json's taskmaster-{icon,card,hero}.webp entries carry prompts that match jobs 2846/2847/2848 exactly, landed 2026-07-29 via the art-generate.yaml batch path (conductor repo, served via raw.githubusercontent -- no kind_robots filesystem dependency).

      Dashboard-tab (13369) and tutorial (13370) rendered successfully but were queued as generic ArtImages with no imagePath/targetRepo direct-delivery metadata, so they never reached kind_robots' self-hosted image storage: distribute_images.py's own docstring is explicit that any kind_robots-targeted file is RETAINED in projects/process/, never copied into a checkout, because /public/images/** is git-ignored there and real delivery only happens via the relay's direct self-hosted media path (an IMAGES_PATH-backed write outside git and outside this sandbox's reach) or a similar entity-linked completion path -- neither of which this pair of jobs had wired. This is the same structural gap model-builder/t-029 already flagged for its own dashboard-tab/tutorial step, not a new or taskmaster-specific bug, and not something a re-run of distribute_images.py can fix regardless of how many times it's invoked. Not spending a fresh generation cycle guessing at a delivery path with no confirmed working precedent for this asset class.

      The front-end UI work (Quest Desk redesign, PR #1469) is complete and live in production. NOT closing to done, correcting course from an earlier draft of this note: model-builder/t-029 hits the identical dashboard-tab/tutorial delivery gap and deliberately stays `ready` rather than `done` for it, and this repo's own lifecycle validator (test_current_project_lifecycle.py) confirmed why -- closing taskmaster's last open task would leave an `active` project with zero open work while its stated goal (dedicated art for the Taskmaster surface) is still 3/5 delivered. Re-armed to ready, matching the model-builder precedent, until the dashboard-tab/tutorial delivery mechanism exists (see the kaizen suggestion on conductor PR #2010: a generic entity-linked-style delivery path for project dashboard-tab/tutorial art).

      Post-merge cleanup for conductor PR #2010: keep t-003 ready because dashboard-tab/tutorial delivery remains unresolved, and clear the stale owner left by the PR branch.

      Implemented a shared Conductor ArtJob destination-preservation fix so ordinary targeted dashboard/tutorial and missing-image requests retain targetRepo/imagePath delivery metadata through enqueue, with focused regression coverage.

      Merged Conductor PR #2047 as 0daf956edf3d74f3d5ec648fed73f6cb1a31cae4. Generic targeted art requests now preserve targetRepo/imagePath/sourceUrl/pageUrl plus request identity into durable ArtJobs, closing the structural delivery-metadata gap that affected Taskmaster dashboard/tutorial art. Exact-head Worker PR CI #31424354799, Security Audit #31424354684, and Process task events #31424354839 all completed successfully. Historical Taskmaster ArtImages 13369/13370 were already rendered without destination metadata; KR_API_TOKEN is absent in this runtime, so no production requeue/redelivery was attempted. Keep ready until those two assets are delivered and their final media URLs are verified.
    claimed_by: 20260811T013500Z-taskmaster-t003-sweep
    claimed_at: '2026-08-11T01:35:05Z'
    updated: '2026-08-11T01:55:00Z'
    owner: worker

  - id: t-004
    milestone: m3
    title: "Audit other dashboard-tab/tutorial image paths for silently-wrong assets"
    status: ready
    passes: 0
    stakes: reversible
    note: |-
      Kaizen from t-003 (2026-08-11). t-003's dashboard-tab image
      (public/images/dashboard-tabs/scenario/taskmaster.webp) had a completely unrelated,
      garbled image sitting at that exact path since 2026-07-28 -- not a render failure,
      not a missing file, just the wrong content silently serving as if correct. It was
      only caught because a live HEAD+fetch check against media.acrocatranch.com happened
      to run at all; every prior session's "is this delivered" check looked at ArtJob/DB
      status or the git-ignored local kind_robots checkout, neither of which would ever
      surface a wrong-but-present file.

      Scope: enumerate every dashboard-tab and tutorial image path referenced by
      dashboardHelper.ts / tutorialCards.ts (kind_robots), HEAD+fetch each one against the
      self-hosted media origin, and visually spot-check a sample for content that doesn't
      match its section (garbled text, wrong subject, obvious mismatch) rather than just
      checking HTTP 200. Any confirmed-wrong asset gets a targeted regeneration request the
      same way t-003 did (projects/art-prompts.yaml requests: + consume_art_requests_to_media.py).
      This is a site-wide asset-correctness gap, not taskmaster-specific -- if it's worth
      tracking under a home project, this repo's global-ui/interface-vision is probably a
      better fit than taskmaster; use judgment on where to file the actual sweep if this
      note's project placement doesn't fit by the time it's picked up.
    owner: null
    claimed_by: null
    claimed_at: null
