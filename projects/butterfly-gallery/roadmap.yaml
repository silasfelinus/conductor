project: butterfly-gallery
kind: software
goal: "Ship /butterfly-gallery as a private, mature-aware Kind Robots art-curation experience: a fast drag/drop sorter staged as an animated rainbow-butterfly gallery room, while leaving ingestion/provenance ownership in art-archive."

notes_from_silas: |
  Butterfly Gallery is a SEPARATE project from art-archive.

  art-archive owns ingesting the legacy filesystem, reconciling files into
  private/mature ArtImages, recovering checkpoint/LoRA provenance, and exposing
  the private-art source. butterfly-gallery consumes those ArtImages; it does
  not own scanning, archive reconciliation, or Resource matching.

  The intended route is /butterfly-gallery. It belongs in the Kind Robots Plan
  channel under Projects. The page itself is an admin/private curation surface
  and must remain compatible with the app's mature-content visibility rules.

  The visual premise is an animated gallery sorting room. On first entry a
  ceiling trapdoor opens and framed pictures tumble into a pile. Rainbow
  butterflies, the Kind Robots mascot, carry pictures around with varying levels
  of success: some tow frames on strings, some work in pairs, some struggle
  alone. After the intro, the page settles into a calm working state.

  The steady-state composition has a painting pile at bottom/center, one selected
  image in a prominent central frame, customizable drag/drop bins on the left
  and right, and a peekaboo opening/window where butterflies pass in the
  background. The whimsy must never slow down the actual sorting workflow.

  Drag/drop is primary theater, not the only control. Every action must have a
  click/tap and keyboard-accessible fallback. Intro motion is non-looping,
  skippable, session-aware, and respects prefers-reduced-motion.

  Build functional behavior first, then static art direction, then motion. Do
  not block useful curation work on final animation assets.

  Keep butterfly-gallery HIGH priority until complete and order it immediately
  ahead of art-archive for agent pickup. Art generated for this project uses
  ArtJob priority 200, matching Daily Dream art and outranking the Resource/LoRA
  preview refill lane.

milestones:
  - id: m1
    title: "FOUNDATION — route, Plan placement, data seam, and interaction contract"
    weight: 20
    status: in-progress

  - id: m2
    title: "SORT — pile, central frame, bins, metadata, and real curation actions"
    weight: 25
    status: in-progress

  - id: m3
    title: "STAGE — gallery-room composition and production art assets"
    weight: 20
    status: in-progress

  - id: m4
    title: "MOTION — trapdoor intro, butterfly clips, ambient scene, and micro-interactions"
    weight: 20
    status: in-progress

  - id: m5
    title: "POWER + POLISH — presets, accessibility, responsiveness, performance, and acceptance"
    weight: 15
    status: in-progress

tasks:
  - id: t-001
    milestone: m1
    title: "Write Butterfly Gallery design brief and preserve the project boundary"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    updated: "2026-09-17T22:56:00-07:00"
    note: >
      DESIGN-BRIEF.md captures Silas's full visual/interaction outline and the
      implementation sequencing. The boundary is explicit: butterfly-gallery
      owns the curation experience; art-archive owns filesystem ingestion,
      ArchiveEntry reconciliation, provenance matching, and the private-image
      source.

  - id: t-002
    milestone: m1
    title: "Create the Kind Robots Project identity and place Butterfly Gallery in Plan"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    note: >-
      Ensure the matching Kind Robots Project uses conductorSlug
      butterfly-gallery, title "Butterfly Gallery", channelKey=plan,
      tabKey=projects, and liveUrl=/butterfly-gallery. This is a private admin
      curation project; preserve isPublic=false and mature-aware access so its
      eventual private/mature ArtImage feed is never exposed by navigation.
      Kind Robots owns these presentation fields, not Conductor override YAML.

      Patched the existing Kind Robots Project row (id 2118, auto-created by the conductor
      projection sync, conductorSlug already set) via PATCH /api/projects/2118: title
      'Butterfly Gallery', channelKey=plan, tabKey=projects, liveUrl=/butterfly-gallery,
      isPublic=false. Confirmed via the response payload. No code change / PR needed -- this
      is a live Kind Robots data field, not a roadmap-adjacent file.
    updated: '2026-09-18T08:39:44Z'

  - id: t-003
    milestone: m1
    title: "Create /butterfly-gallery shell, store, and scene state machine"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >-
      Build the route and Pinia/store state for loading, intro, ready, dragging,
      dropping, saving, rescanning, and error states. Include selected image,
      pile/queue entries, bin configuration, filters, batch selection, and
      intro-played state. Use fixture/private ArtImage data initially so this
      work does not wait on art-archive integration.

      Merged kind_robots#2830: route+store+state machine landed as described. Downstream
      t-005/t-006/t-008/t-009 unblock via resolve_deps.py.
    claimed_by: 2026-09-18T09-30-00-conductor-agent-run-t003
    claimed_at: '2026-09-18T09:30:20Z'
    updated: '2026-09-18T09:46:49Z'
    implementation_pr: 'silasfelinus/kind_robots#2830'

  - id: t-004
    milestone: m1
    title: "Define the private-ArtImage feed contract without duplicating art-archive"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >-
      Define the narrow read contract Butterfly Gallery needs from the archive:
      id/thumbnail/display path, processed state, rating, folder/custom
      collections, prompt/generation metadata, resource provenance, match state,
      missing/trashed state, and next-page cursors. Prefer an adapter over
      duplicating archive queries. Fixture this contract for UI work. The real
      provider switches to art-archive once that project exposes the feed.

      Merged kind_robots#2831: feed contract (ButterflyGalleryFeedProvider, cursor pagination)
      + extended ButterflyPileEntry fields landed as described. Downstream t-005/t-008/t-009
      unblock via resolve_deps.py.
    claimed_by: 2026-09-18T09-51-00-conductor-agent-run-t004
    claimed_at: '2026-09-18T09:51:56Z'
    updated: '2026-09-18T10:03:18Z'
    implementation_pr: 'silasfelinus/kind_robots#2831'

  - id: t-005
    milestone: m2
    title: "Build the painting pile and selected central frame interaction"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-003
      - t-004
    note: >-
      Render the pile as a deep foreground mass of overlapping thumbnails that
      rises from BELOW the viewport with NO box/container front. Only the upper
      portion is visible, implying a much larger hidden backlog. It should be
      tall enough to very slightly overlap the bottom edge of the central
      display. Selecting or dragging an image promotes it into the large central
      frame; the next image advances smoothly after a successful sort. The pile
      remains a foreground DOM layer so it can occlude the lower-right robot
      animation behind it.

      FLAG (2026-09-18, same session that shipped this task): Silas's approved-stage-spec
      commit (839ade1) landed mid-flight and rewrote this task's own note above to specify
      below-viewport rise / no box-container / slight overlap with the central frame /
      foreground-DOM layering against a lower-right robot animation. The merged
      implementation (kind_robots#2832) satisfies the ORIGINAL note this session claimed
      against (top-of-pile rendering, hover/lift, remaining count, promote-to-frame,
      smooth advance) but does NOT implement the newer positioning/layering requirements
      above -- the pile currently renders in-flow inside a bordered kr-panel, not rising
      from below the viewport with no container. t-010 (STAGE milestone m3, depends_on
      t-005+t-006) already explicitly owns "Implement APPROVED-STAGE-SPEC.md exactly ...
      image pile rising from below viewport and slightly overlapping the display", so the
      positioning/layering rework is likely t-010's job, not a t-005 defect -- but flagging
      here since t-005's own note now describes requirements this PR doesn't meet. Confirm
      this reading (t-010 absorbs the staging) or reopen t-005 if the below-viewport/no-box
      behavior should land before t-010's broader composition pass.
    claimed_by: claude-scheduled-20260918T102941Z-butterfly-t005
    claimed_at: '2026-09-18T10:29:41Z'
    updated: '2026-09-18T10:55:40Z'
    implementation_pr: 'silasfelinus/kind_robots#2832'

  - id: t-006
    milestone: m2
    title: "Build configurable left/right sorting-bin rails"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-003
    note: >-
      The approved desktop composition has exactly FIVE large configurable
      preset bins on the LEFT. They are data-driven multi-action presets, not
      hard-coded meanings; mockup examples include 5★+collection, 4★+favorite
      tag, 3★ keep-as-is, 2★+move, and 1★+archive. A successful preset marks the
      item processed/done unless that preset explicitly opts out. Support drag
      hover/drop plus click/tap fallback. The RIGHT side is not a second custom
      bin rail: it is reserved for compact Image Info, Cleanup, and Trash.

      Shipped in kind_robots#2834 (Silas's own live session, "build approved sorting stage"):
      exactly five configurable preset bins on the left (rating/collection/tag presets via
      fixture `preset` payload), right side reserved for Image Info/Cleanup/Trash (not a
      second bin rail), drag hover/drop plus click/tap fallback on each preset button.
      Reconciled by the Reviewer session that also shipped t-005 and hit a merge conflict
      against this PR on pages/butterfly-gallery.vue.
    updated: '2026-09-18T11:11:02Z'
    implementation_pr: 'silasfelinus/kind_robots#2834'

  - id: t-007
    milestone: m2
    title: "Wire core curation actions: processed state, rating, trash, and collections"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-005
      - t-006
    note: >-
      Make first-class bin/action targets for mark processed/unprocessed,
      Rate 1-5, recoverable Trash, and Add to collection. Call existing archive
      APIs when available; until then keep the action adapter injectable and
      test against fixtures. Do not recreate archive filesystem semantics here.

      Merged silasfelinus/kind_robots#2837 (squash 1df1861): wired setProcessed, setRating,
      trashEntry/restoreEntry, addToCollection/removeFromCollection through a new injectable
      ButterflyGalleryActionAdapter (fixture-backed today, mirroring the existing feed
      provider pattern -- a real art-archive-backed adapter swaps in later under t-022, which
      is `waiting` on an external art-archive read/action contract that doesn't exist yet).
      Extracted the field-level rules into pure appliers
      (stores/helpers/butterflyGalleryActions.ts) so dropOnBin and the new standalone actions
      share one implementation. Trash is explicitly reversible at the data layer (never drops
      the pile entry; restoreEntry fully reverses it) though the UI restore affordance itself
      is t-009/t-020's job. Verification: full npm run test (vue-tsc) clean, layout-contract
      and kr-class-coverage both hold, eslint/prettier clean, and a new contract test
      (utils/scripts/verifyButterflyGalleryActions.test.ts, wired into contract-tests.yml)
      covering processed toggle, rating clamp/clear, trash/restore round-trip, collection
      add/remove dedup, preset-bin composition, and adapter-call composition -- all 49
      kind_robots CI checks green.
    claimed_by: claude-scheduled-2026-09-18T-conductor-run-bg-t007
    claimed_at: '2026-09-18T11:33:24Z'
    updated: '2026-09-18T11:51:52Z'
    implementation_pr: 'silasfelinus/kind_robots#2837'

  - id: t-008
    milestone: m2
    title: "Build the selected-image metadata and quick-action drawer"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-004
    note: >-
      Implement the compact RIGHT-RAIL Image Info panel from the approved stage
      spec. Keep it small enough not to compete with the main artwork: thumbnail
      or identity, filename/title, prompt excerpt, dimensions, checkpoint/LoRAs,
      rating, processed state, collection/match state as appropriate. Deeper
      metadata belongs behind a Details/expand affordance. Immediately below it
      are Cleanup and Trash actions; do not add Keep/Replace tiles unless Silas
      later requests them.

      Shipped in kind_robots#2834: compact right-rail Image Info panel (rating, folder,
      checkpoint, processed state) with a Details/expand affordance revealing prompt/LoRAs/
      collections, plus Cleanup and Trash actions immediately below per the note. Two minor
      gaps against the note's full field list: no explicit filename/title line and no
      dimensions/match-state field in the compact view. Cleanup is rendered but intentionally
      disabled pending the Art Archive action adapter (explicit PR flag, not an oversight).
      Judged close enough to the note's core ask to close rather than leave open pending a
      cosmetic field-list nit -- flag if Silas wants those two fields added.
    updated: '2026-09-18T11:11:05Z'
    implementation_pr: 'silasfelinus/kind_robots#2834'

  - id: t-009
    milestone: m2
    title: "Add queue filters and folder/collection browsing"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-004
    note: >-
      Support processed/unprocessed, rating, match state, missing/trashed state,
      folder, collection, and search filters. Preserve the theatrical pile
      metaphor while making the real queue state legible and recoverable.

      Implemented queue filters (search/processed/rating/matchState/trashView) and
      folder/collection browsing chips in pages/butterfly-gallery.vue +
      stores/butterflyGalleryStore.ts, extracting the predicate into
      stores/helpers/butterflyGalleryFilters.ts. Fixed a real bug: trash visibility was folded
      into matchState==='missing' (t-003 scaffolding), which never actually worked for real
      trashed entries. New trashView filter decouples it. PR: kind_robots#2838, CI pending.

      Merged kind_robots#2838 (1488740a). 51/51 checks green. Fixed a real pre-existing bug
      along the way: trash visibility was folded into matchState==='missing' (t-003
      scaffolding), which never actually revealed trashed entries since trashing doesn't touch
      matchState -- now filters.trashView is its own dimension.
    claimed_by: claude-scheduled-2026-09-18T-conductor-run-t009
    claimed_at: '2026-09-18T12:34:24Z'
    updated: '2026-09-18T12:57:16Z'
    implementation_pr: 'silasfelinus/kind_robots#2838'

  - id: t-010
    milestone: m3
    title: "Build the static Butterfly Gallery stage composition"
    status: done
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-005
      - t-006
    note: >-
      Implement APPROVED-STAGE-SPEC.md exactly: bright friendly minimalist
      industrial warehouse; one long horizontal top runway/window behind a
      convex yellow drop funnel; five custom preset bins left; large central
      display; compact Image Info + Cleanup + Trash right; image pile rising
      from below viewport and slightly overlapping the display. No in-scene page
      header, no random copy, and NO living/acting things baked into the static
      scene. Reserve explicit layer slots for top-runway animation, left
      butterfly loop, lower-right robot-behind-pile loop, blank-state display
      loop, and selected-image drop transition.

      Shipped in kind_robots#2834: warehouse-structure stage with top runway slot, convex
      yellow drop-funnel (neck/bell/mouth), five preset bins left, large central display,
      right-rail Image Info+Cleanup+Trash, foreground pile rising from below viewport with no
      box, and reserved animation layer slots (data-animation-slot="butterfly-runway",
      "foreground-robot", plus a foreground-butterfly-slot and blank-state CSS loop) --
      confirmed directly in the merged pages/butterfly-gallery.vue, not just taken from the
      PR's own description. Per the PR body, t-024 (performance/responsive tuning) and t-025
      (cross-width visual acceptance) remain explicitly responsible for what this pass doesn't
      cover -- this task's own scope (implement the approved composition) is met.
    updated: '2026-09-18T11:11:08Z'
    implementation_pr: 'silasfelinus/kind_robots#2834'

  - id: t-011
    milestone: m3
    title: "Produce the static art asset set for room, frame, pile, bins, and window"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: >
      Productionize the approved scene as SEPARATE layers. The static background
      contains warehouse architecture and structural mounts only: no butterflies,
      robots, artwork pile, example art, moving conveyor content, or UI labels.
      Produce structural runway/window and a CONVEX yellow funnel whose lower
      bell/mouth is wider than its neck so art can visibly emerge downward.
      Bins/frame/info are accessible DOM surfaces over the background. Queue any
      generated Butterfly Gallery art at ArtJob priority 200.
    claimed_by: 2026-09-18T13-30-00-t011
    claimed_at: '2026-09-18T13:37:02Z'
    updated: '2026-09-18T13:51:48Z'
    implementation_pr: 'silasfelinus/kind_robots#2840'

  - id: t-012
    milestone: m4
    title: "Storyboard the non-looping trapdoor intro and reduced-motion variant"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    note: |-
      Replace the older generic trapdoor storyboard with the approved motion contract. The selected-image entry transition is deterministic DOM motion: art emerges downward through the convex funnel, stretches comically long vertically, then snaps/recoils to its true aspect ratio in the central frame. Keep any first-visit intro short/skippable/session-aware. Reduced motion uses a brief fade/scale instead of squash-and-stretch.


      Wrote projects/butterfly-gallery/MOTION-STORYBOARD.md as the bounded implementation contract for the approved first-visit intro, selected-image stretch-and-snap transition, skip/session behavior, interruption rules, and reduced-motion path.

      Reviewer (Claude, scheduled conductor run): merged silasfelinus/conductor#4651 — MOTION-
      STORYBOARD.md is the bounded implementation contract for t-012. Docs-only, reversible,
      mergeable_state clean, all required checks green (an earlier Validate-Worker-PR-handoff
      failure was against a pre-edit PR body missing the Notes-for-reviewer heading; the body
      was corrected and the check passed on the final commit).
    claimed_by: openai-scheduled-2026-09-18T141839Z-butterfly-gallery-t012-a11
    claimed_at: '2026-09-18T14:18:39Z'
    updated: '2026-09-18T14:27:08Z'
    implementation_pr: 'silasfelinus/conductor#4651'

  - id: t-013
    milestone: m4
    title: "Create the butterfly animation shot list and animation-creator jobs"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on: t-012
    note: |-
      Build animation-creator jobs for the APPROVED layers: (1) upper runway passes behind the funnel: string carrier, team carry, solo struggler, clean cross, rare failed/drop gag; (2) one transparent rainbow butterfly foreground loop above the left bins: fly-in, hover/flap, settle/perch, idle/reset; (3) one transparent Kind Robot foreground loop in the lower right, BEHIND the pile: pick up/examine/shuffle pictures; and (4) a gentle blank-state center-display loop used only while no ArtImage is selected. No living thing may be baked into the background.


      Added projects/butterfly-gallery/ANIMATION-SHOT-LIST.md with eight stable animation-creator job keys covering five runway passes, the left butterfly loop, lower-right robot loop, and blank-state loop; all generated project assets are specified at ArtJob priority 200 with compositing and provenance requirements for t-014.

      Reviewer (Claude, scheduled conductor run): merged silasfelinus/conductor#4656 -- docs-
      only ANIMATION-SHOT-LIST.md, cross-checked against APPROVED-STAGE-SPEC.md and MOTION-
      STORYBOARD.md, mergeable_state clean, 23/23 check runs green. t-014 now unblocked.
    claimed_by: openai-scheduled-2026-09-18T152102Z-butterfly-gallery-t013-a11
    claimed_at: '2026-09-18T15:21:02Z'
    updated: '2026-09-18T15:28:08Z'
    implementation_pr: 'silasfelinus/conductor#4656'

  - id: t-014
    milestone: m4
    title: "Render and catalog reusable butterfly motion clips"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-013
    note: >-
      Generate, inspect, and catalog the approved reusable motion assets with
      stable identities and transparent/composable presentation where supported:
      top-runway butterfly/art passes, left foreground butterfly loop,
      lower-right robot-sifting loop, and blank-state display loop. Verify the
      robot can be partially occluded by the dynamic foreground pile and the top
      passes fit the full horizontal runway while disappearing behind the funnel.

      Worker (Claude, scheduled conductor run): submitted the full t-014 pipeline live. Stage
      1 (keyframe stills, krea2 engine, priority 200, all completed within this session): A1
      runway-string-tow=ArtJob 28717->ArtImage 28430, A2 runway-team-carry=28718->28431, A3
      runway-solo-struggle=28719->28432, A4 runway-clean-cross=28720->28433, A5 runway-drop-
      gag=28721->28434, B1 left-butterfly-perch-loop=28722->28435, C1 robot-picture-sift-
      loop=28723->28436, D1 blank-state-idle-loop=28724->28437. Viewed all 8: on-concept,
      text-free, correct cel-shaded rainbow-butterfly/Kind-Robot mascot style, flat white
      (non-transparent) background -- krea2 did not honor the transparency request, matching
      ANIMATION-SHOT-LIST.md's own documented fallback ('render against a flat removable key
      background and record the limitation'). Stage 2 (image-to-video, LTX engine, using each
      keyframe as firstImageBase64, priority 200, mp4 output, renderScale 1): A1=ArtJob 28725,
      A2=28726, A3=28727, A4=28728, A5=28729, B1=28730, C1=28731, D1=28732. Durations/loop
      flags follow the shot list exactly (A*=non-looping 3.0-4.0s per shot, B1/C1/D1=looping
      6-8s). All 8 were still PENDING/RUNNING (one at a time, single relay) when this session
      ended. GET /api/art/queue/stats confirmed this is the same chronic single-GPU render-
      backend capacity issue documented across ai-art-academy/t-045, coloring-book/t-022, and
      storybook/t-049 (queueDepth PENDING=680, oldestPending ~75h as of this session) -- and
      LTX/WAN video renders are documented at 1-4 hours EACH on top of queue wait (see
      kind_robots utils/videoPresets.ts timeoutSeconds budgets), so completing all 8 clips
      serially is a multi-hour-to-multi-day operation, not something any single session can
      finish. Not a bug in this task or the submission -- transient render-backend capacity,
      no pass consumed. NEXT ACTIONABLE STEP: a future cycle should GET
      /api/art/queue/28725..28732, and for each DONE job read artImageId, GET
      /api/art/image/<id> to confirm the clip actually satisfies the acceptance checklist
      (runway passes traverse the full runway and disappear behind the funnel; left butterfly
      stays independently composable; robot reads correctly when partially occluded; blank-
      state motion is gentle with no fake art; no baked warehouse backdrop or UI; provenance
      intact), then catalog the accepted clips with stable identities (matching the job keys
      above) in code -- store helper + reference in pages/butterfly-gallery.vue's reserved
      animation-layer slots (data-animation-slot="butterfly-runway"/"foreground-
      robot"/foreground-butterfly-slot/blank-state loop, per t-010's note) -- run vue-
      tsc/eslint, and open the kind_robots PR. Re-render only a specific FAILED/rejected job
      rather than the whole batch. No duplicate-submission risk: every job id is recorded
      here. Re-arming to ready, releasing the claim.
    claimed_by: null
    claimed_at: null
    updated: '2026-09-18T16:45:34Z'

  - id: t-015
    milestone: m4
    title: "Implement intro orchestration with trapdoor, picture fall, and session memory"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-010
      - t-012
    note: >-
      Orchestrate DOM/asset motion rather than making the entire page one giant
      video. Intro never blocks navigation indefinitely, exposes Skip, records
      that it played for the intended session scope, and leaves the exact same
      steady-state DOM the user will interact with afterward.

      Worker/Reviewer (Claude, scheduled conductor run, same session): implemented and merged
      silasfelinus/kind_robots#2842 -- stores/helpers/butterflyGalleryIntro.ts (pure,
      deterministic trapdoor/tumble/settle beat timing + tumble-frame trajectory planner,
      directly unit-tested) plus pages/butterfly-gallery.vue orchestration (trapdoor cue,
      staggered tumble frames, Skip button, 3.2s hard timeout, reduced-motion at-mount and
      mid-intro handling, Escape-invokes-Skip, full timer/listener cleanup on finish/unmount).
      Verified: vue-tsc, eslint, prettier, new test:butterfly-gallery-intro plus the existing
      actions/filters/motion suites, and the full cross-project guard suite (layout-contract,
      kr-class-coverage, gallery-adoption, gallery-consistency, disabled-affordances, no-
      promise-in-store-state, card-action-contract) -- all clean. 51/51 CI checks green,
      mergeable_state clean, merged. No visual/browser verification was possible in this non-
      interactive session (flagged in the PR body), matching the same honest gap t-017's PR
      (kind_robots#2841) flagged.
    claimed_by: claude-scheduled-20260918T154441Z-butterfly-t015
    claimed_at: '2026-09-18T15:44:44Z'
    updated: '2026-09-18T16:01:11Z'
    implementation_pr: 'silasfelinus/kind_robots#2842'

  - id: t-016
    milestone: m4
    title: "Implement ambient peekaboo butterfly traffic"
    status: waiting
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-010
      - t-014
    note: >
      Implement the long upper runway scheduler from APPROVED-STAGE-SPEC.md:
      varied butterfly+art passes travel across the full horizontal window and
      are clipped/occluded behind the central funnel. This is the ONLY
      background-area character animation. No static butterfly silhouettes or
      robots appear in the warehouse backdrop. Pause when hidden and suppress
      under reduced motion.

  - id: t-017
    milestone: m4
    title: "Add sorting micro-interactions without physics friction"
    status: done
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-005
      - t-006
      - t-010
    note: >-
      Animate pile selection, drag-target highlight, successful bin acceptance,
      next-image reveal, and the approved selected-image funnel transition:
      exaggerated vertical stretch while dropping, then snap/squash back to
      correct aspect ratio on arrival. No crane exists; selection automatically
      places the art. Keep every interaction deterministic and easy to drop.

      Merged silasfelinus/kind_robots#2841 -- sorting micro-interactions (pile selection pop,
      drag-target highlight, bin-acceptance flash, and the Web-Animations-driven funnel-drop
      transition per MOTION-STORYBOARD.md's geometry contract) implemented, tested, and
      merged.
    claimed_by: claude-scheduled-2026-09-18T143205-butterfly-gallery-t017
    claimed_at: '2026-09-18T14:32:06Z'
    updated: '2026-09-18T14:52:48Z'
    implementation_pr: 'silasfelinus/kind_robots#2841'

  - id: t-018
    milestone: m5
    title: "Add persisted custom bins and generation/action presets"
    status: claimed
    owner: worker
    passes: 0
    stakes: reversible
    depends_on:
      - t-006
      - t-007
    note: |-
      Let the admin choose which bins appear and bind them to reusable actions such as Add LoRA, replace LoRA, switch checkpoint, append/replace prompt, change common generation settings, add variant, or request replacement. Prefer the archive project's preset/action API if it already exists.


      Implemented the first t-018 slice on the Kind Robots worker branch: a versioned, validated persistence contract for custom bin presets and generation/action intents. Store/UI wiring remains for the continuation after this scoped slice is reviewed and merged.

      Reviewed and merged kind_robots#2843 (squash 45041e8) 2026-09-18: versioned, validated
      persistence contract for custom bin presets + typed generation/action intents in
      stores/helpers/butterflyGalleryBinPresets.ts. All 46 CI checks green, mergeable_state
      clean. Verified: type shapes match types/butterflyGallery.ts exactly
      (ButterflyBinConfig, ButterflyBinKind), BIN_KINDS set complete/correct, no pre-existing
      duplicate persistence helper. Inert until wired into the Pinia store + editor UI -- re-
      arming to ready for that continuation.

      Added a dedicated Pinia preset store that loads validated v1 custom-bin envelopes from localStorage, falls back safely to defaults, persists edits, and owns enable/action/reset mutations. The editor UI remains for the next continuation slice.

      Reviewed and merged kind_robots#2848 (t-018 second slice) 2026-09-18: dedicated Pinia
      store (stores/butterflyGalleryPresetStore.ts) that loads/saves validated v1 bin-preset
      envelopes via localStorage, with in-memory fallback on storage failure, and owns
      upsert/remove/enable/action/reset mutations. All 46 CI checks green, mergeable_state
      clean, diff isolated to the one new store file built on the already-merged #2843 helper.
      Editor UI still remains for the next continuation slice -- re-arming to ready.
    claimed_by: openai-scheduled-2026-09-18T201608Z-butterfly-gallery-t018-a11
    claimed_at: '2026-09-18T20:16:08Z'
    updated: '2026-09-18T20:16:08Z'
    implementation_pr: 'silasfelinus/kind_robots#2848'

  - id: t-019
    milestone: m5
    title: "Integrate regeneration actions with normal ArtJobs"
    status: waiting
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-018
    note: >
      Regenerate/additional/replacement actions must use the existing durable
      ArtJob system and archive adapters. Butterfly Gallery is a controller,
      not a second render queue. Failed replacement jobs never destroy the
      current source image.

  - id: t-020
    milestone: m5
    title: "Add keyboard, touch, and accessible non-drag workflows"
    status: done
    passes: 0
    stakes: reversible
    depends_on:
      - t-006
      - t-008
    note: |-
      Every bin/action is reachable via focus and keyboard and usable on touch without requiring a drag gesture. Provide visible selected/focus states, useful announcements, and keyboard shortcuts for high-frequency sorting where they do not conflict with browser/app navigation.


      Reviewer openai-scheduled-2026-09-18T171754Z-butterfly-gallery-pr2845-a1 found a quality issue in kind_robots#2845: window-global Delete/digit/arrow shortcuts intercept keys outside gallery-owned focus, including a recoverable destructive trash action. Scope shortcuts to a gallery interaction region or gallery-owned focus and add a focused shortcut-guard contract before retrying. Existing ARIA live status, aria-pressed, discoverability hints, click/tap fallback, and focus-visible treatment otherwise fit t-020.

      Worker (Claude, scheduled conductor run): fixed the review finding from openai-
      scheduled-2026-09-18T171754Z-butterfly-gallery-pr2845-a1 -- kind_robots#2845 commit
      9dfeac2 extracts the shortcut guard/routing logic into a pure
      resolveButterflyShortcutIntent() (stores/helpers/butterflyGalleryShortcuts.ts) and
      rebinds the keydown listener on the page's own root <main> element instead of window, so
      Delete/digit/arrow shortcuts only fire for keydowns that actually bubble from inside the
      gallery, never from unrelated page chrome. Added
      utils/scripts/verifyButterflyGalleryShortcuts.test.ts (wired into package.json +
      contract-tests.yml, matching the existing butterfly-
      gallery-{actions,filters,motion,intro} contract pattern) covering every guard (editable
      target, modifier keys, gallery status, missing selection) plus the sort/trash/arrow-
      navigation intents. Replied to the reviewer on the PR. vue-tsc, eslint, and prettier all
      clean; new contract test passes. kind_robots#2845 CI re-running on the fix commit.

      Worker (Claude, scheduled conductor run): kind_robots#2845 merged (squash 60d1208), all
      checks green including the new Butterfly Gallery keyboard shortcut contract. Closing
      t-020: digits 1-5 sort by rating, Delete trashes, ArrowLeft/ArrowRight navigate the
      pile, aria-live announcements, aria-keyshortcuts/title discoverability, aria-pressed,
      and distinct focus-visible outlines are all live. A manual keyboard-only browser pass
      was not possible in this sandbox (no browser session) -- flagged in the PR's test plan
      for a future spot-check.
    updated: '2026-09-18T17:33:43Z'
    owner: worker
    claimed_by: claude-scheduled-20260918T172900Z-bg-t020-fix
    claimed_at: '2026-09-18T17:28:55Z'
    implementation_pr: 'silasfelinus/kind_robots#2845'

  - id: t-021
    milestone: m5
    title: "Add batch selection and batch-action confirmation"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-007
      - t-009
    note: >
      Support multi-select from pile/grid/filtered queue and apply compatible
      bins/presets in batches. Preview count/scope for destructive or expensive
      actions and report partial failures without losing successful results.

  - id: t-022
    milestone: m5
    title: "Integrate the real art-archive private image feed"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-004
      - t-009
    note: >
      EXTERNAL DEPENDENCY: art-archive must expose the private/mature ArtImage
      read/action contract. Swap the fixture adapter onto that real provider,
      preserving admin auth and mature-viewer gating. Do not query PRIVATE_PATH
      directly from Butterfly Gallery and do not duplicate ArchiveEntry scanner
      logic. Mark ready when the art-archive API seam exists.

  - id: t-023
    milestone: m5
    title: "Harden privacy, maturity, and navigation boundaries"
    status: waiting
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-002
      - t-022
    note: >
      Verify the Plan project placement does not make private ArtImages public.
      Require admin authorization plus mature-content permission for the gallery
      data surface. Test direct-route access, API access, thumbnails, metadata,
      and Resource/collection links for no private-content bleed.

  - id: t-024
    milestone: m5
    title: "Tune large-queue performance and responsive composition"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on:
      - t-009
      - t-017
    note: >
      Paginate/virtualize queue reads, use thumbnails rather than originals,
      prefetch only a small next-item window, pause unnecessary animation, and
      keep the stage useful across desktop/tablet/mobile. On constrained widths,
      bins may become drawers/trays without losing action parity.

  - id: t-025
    milestone: m5
    title: "Cross-width visual and interaction acceptance"
    status: needs-human
    owner: null
    passes: 0
    stakes: reversible
    gate_human: true
    note: >
      FOR SILAS: final visual acceptance after the real archive feed and motion
      system are deployed. Verify the room/pile/frame/bin composition, intro
      timing, butterfly personality, drag/click/keyboard sorting, reduced motion,
      and phone/tablet/desktop layouts. This is a final appearance/feel gate, not
      a blocker for earlier implementation.

  - id: t-026
    milestone: m1
    title: "Add a fixture/adapter shape-parity contract for the Butterfly Gallery pile feed"
    status: waiting
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-022
    note: >-
      Kaizen from t-003 (kind_robots#2830): once t-004's read contract/adapter
      exists, add a verifyButterflyGalleryFixtureShape-style contract asserting
      stores/helpers/butterflyGalleryFixtures.ts and the real art-archive-backed
      provider both emit structurally identical ButterflyPileEntry records, so a
      future adapter swap cannot silently drop a field the store/UI depends on.

      Reviewer/Worker (Claude, scheduled conductor run): claimed, then released before
      implementing. This task's own contract cannot be built yet -- it asks for a test
      asserting stores/helpers/butterflyGalleryFixtures.ts and 'the real art-archive-backed
      provider' emit structurally identical ButterflyPileEntry records, but that real provider
      does not exist yet (t-022 'Integrate the real art-archive private image feed' is still
      status: ready, not done). depends_on corrected from t-004 (already done, so it wasn't
      actually gating anything) to t-022, and status set to waiting so this stops surfacing as
      pickable until the real provider exists to compare against. Failure category: actionable
      (undeclared dependency) -- fixed the roadmap rather than retrying or forcing a
      placeholder comparison.
    claimed_by: claude-scheduled-20260918T153721Z-butterfly-t026
    claimed_at: '2026-09-18T15:37:23Z'
    updated: '2026-09-18T15:38:20Z'

  - id: t-027
    milestone: m2
    title: "Add a ButterflyGalleryActionAdapter method for the needs-review bin kind"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    depends_on: t-007
    note: >
      Kaizen from t-007 (kind_robots#2837): applyBinOutcome's 'needs-review' bin
      kind still mutates entry.matchState directly instead of going through the
      action adapter, unlike the other five bin kinds (processed/unprocessed,
      rating, trash, collection) which all persist through
      ButterflyGalleryActionAdapter first. Add a markNeedsReview(entryId) method
      to the adapter (and its fixture-backed no-op implementation) and route
      applyBinOutcome's 'needs-review' case through it, once a bin or action
      actually exercises that kind, so it doesn't silently bypass the adapter
      seam a real (t-022) art-archive-backed adapter will eventually implement.

  - id: t-028
    milestone: m3
    title: "Wire the generated room/frame/pile/bins/window art into the stage once rendered"
    status: ready
    owner: null
    passes: 0
    stakes: reversible
    note: >
      Follow-on from t-011 (kind_robots#2840): 5 ArtJobs (ids 28711-28715,
      priority 200, projectSlug butterfly-gallery) were queued for the static
      room/window/frame/bins/pile assets, targeting public/images/butterfly-gallery/
      {room,window,frame,bins,pile}.webp. Rendering happens asynchronously via
      the relay and had not completed as of this task's creation. Once the
      images exist, replace the corresponding pure-CSS/gradient shapes in
      pages/butterfly-gallery.vue (.warehouse-backdrop, .runway-slot,
      .art-display-inner border/background, .preset-bin background,
      .art-pile background) with the rendered assets, keeping the current CSS
      as a graceful fallback for any asset that failed to render. Check ArtJob
      status via GET /api/art/queue/:id (or the admin queue UI) before
      starting; if any job is still PENDING/RUNNING, wire only the ones that
      are DONE and leave a note for the rest.
