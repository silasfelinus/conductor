# Butterfly Gallery — TALKBACK

## 2026-09-18 | Reviewer/Worker (Claude, scheduled conductor run) | butterfly-gallery/t-002 | resolution

type: resolution

**Decision:** closed t-002 to `done`.

**Subject:** First task closed on this newly created project. Set the live Kind Robots Project identity fields via the admin API rather than a code PR.

**Detail:**
- The matching Kind Robots Project row (id 2118) already existed — auto-created by the conductor projection sync (`server/api/conductor/sync.post.ts`) with `conductorSlug: butterfly-gallery` already set — but carried placeholder presentation fields: `title: "butterfly-gallery"` (the raw slug), `channelKey: null`, `tabKey: null`, `liveUrl: null`, `isPublic: true`.
- Read `server/api/projects/[id].patch.ts` in the local kind_robots checkout first to confirm the admin-token auth path (`x-api-key` → `getOptionalApiUser` resolves an admin user → `assertProjectAccess` allows admin regardless of row ownership) and the accepted field set, then `PATCH https://kindrobots.org/api/projects/2118` with the five fields the task specifies. Re-fetched `GET /api/projects` afterward and confirmed all five landed.
- No conductor override YAML or kind_robots code change was needed — Kind Robots owns these presentation fields per the task's own note, so this was purely a live data fix via the existing admin API, not a PR.

**What was good:**
- N/A — first task on this project, no prior Worker output to critique yet.

**What to improve:**
- N/A.

**Kaizen task:** deferred — this project has no code surface yet (m1 is identity/scaffolding); the next roadmap task is the natural next step rather than an invented improvement.

---
_Generated by [Claude Code](https://claude.ai/code)_

## 2026-09-18 | Reviewer/Worker (Claude, scheduled conductor run) | butterfly-gallery/t-003 | resolution

type: resolution

**Decision:** implemented, opened PR, merged `silasfelinus/kind_robots#2830`, closed t-003 to `done`.

**Subject:** First code surface for Butterfly Gallery — the route shell, Pinia store, and sort state machine, built entirely against fixture data per the task's own instruction not to wait on art-archive.

**Detail:**
- Researched conventions first rather than guessing: `pages/admin/curation-studio.vue` and `pages/admin/lora-triage.vue` for the admin-gate pattern (inline `userStore.initialize()` + `v-else-if="!userStore.isAdmin"`, no route middleware), `stores/loraTriageStore.ts` for the composition-API store shape (individual `ref()`s, not one reactive object; a `computed` layer over raw state), and `types/curationStudio.ts` for the shared-types-file convention. No prior butterfly-gallery code existed anywhere in kind_robots — t-001/t-002 were docs/data-only, as their own notes say.
- Built `types/butterflyGallery.ts` (shared types), `stores/helpers/butterflyGalleryFixtures.ts` (fixture pile entries + default bins, shape-compatible with the future art-archive adapter), `stores/butterflyGalleryStore.ts` (the state machine: loading/intro/ready/dragging/dropping/saving/rescanning/error, filters, batch selection, bin CRUD), and `pages/butterfly-gallery.vue` (the route itself, functional but not yet art-directed).
- Verified locally before opening the PR: `npm run test` (vue-tsc) clean, `eslint`/`prettier` clean on all 4 new files, `npm run test:layout-contract` holds. This is the first session-provisioned kind_robots typecheck run via `scripts/provision_kind_robots_deps.sh` for this project — worth noting it caught nothing wrong here, but it's what let the PR land with zero review round-trips.
- Deliberately did NOT touch `utils/projectPlacements.ts` — t-002 already wired the live Kind Robots Project row's placement fields via the admin API, and that map's `tabKey: 'projects'` entries point at a generic `/plan/projects/<slug>` catalog route rather than a feature's own page, so it wasn't a drop-in fit for this project's actual `/butterfly-gallery` route anyway. Flagged this reasoning in the PR for the next session to confirm or correct.
- CI on kind_robots#2830 took noticeably longer than usual on its "Contract verifiers" (Contract Tests workflow) job — around 5 minutes after every other one of 48 checks had already gone green — plausibly from the heavy concurrent PR/merge volume on kind_robots today (per root TALKBACK, multiple sessions merging art-archive/storybook PRs within the same hour). Waited it out rather than merging early; it finished green with no failures.
- Closed the roadmap loop with `close_task.py butterfly-gallery t-003 done --implementation-pr silasfelinus/kind_robots#2830`, ran `resolve_deps.py` (unblocked t-006 to `ready`; t-005/t-008/t-009 still wait on t-004, which does not depend on t-003), and filed a kaizen follow-up (t-026, `waiting` on t-004) rather than inventing unrelated polish work.

**What was good:**
- Matched two independent existing conventions (page-gating pattern, store shape) closely enough that the PR needed zero review feedback — first-pass clean CI across typecheck/lint/format/layout-contract.
- Kept the fixture data behind one seam (`loadPile()`/`rescan()` in the store, backed by `butterflyGalleryFixtures.ts`) so the t-004 adapter swap is a data-source change, not a component rewrite — exactly what the task asked for ("does not wait on art-archive integration").

**What to improve:**
- Bin icons and fixture image paths are unverified placeholders (noted honestly in the PR's Flags section) — a follow-up session doing t-005/t-006 should confirm the `kind-icon` names actually exist before relying on them for real UI polish.

**Kaizen task:** t-026 — add a fixture/adapter shape-parity contract once t-004 lands (`waiting` on t-004, not started this cycle).

---
_Generated by [Claude Code](https://claude.ai/code)_

## 2026-09-18 | Reviewer/Worker (Claude, scheduled conductor run) | butterfly-gallery/t-004 | resolution

type: resolution

**Decision:** implemented, opened PR, merged `silasfelinus/kind_robots#2831`, closed t-004 to `done`.

**Subject:** Defined the private-ArtImage feed contract as an adapter interface (`ButterflyGalleryFeedProvider`) with a fixture implementation, continuing directly from t-003 in the same session.

**Detail:**
- Added `ButterflyFeedQuery`/`ButterflyFeedPage`/`ButterflyGalleryFeedProvider` to `types/butterflyGallery.ts` (a `fetchPage({ cursor, limit })` contract returning entries plus a `nextCursor`), and extended `ButterflyPileEntry` with `resource: ButterflyResourceProvenance` (checkpoint + loras), `negativePrompt`, and `generationMetadata` to cover every field the task's note listed (resource provenance, prompt/generation metadata, next-page cursors) that t-003's narrower first pass hadn't needed yet.
- Built `stores/helpers/butterflyGalleryFeedProvider.ts`: a fixture-backed provider (offset encoded as an opaque string cursor) plus a swappable module-level "active provider" (`setButterflyGalleryFeedProvider`) so a real art-archive-backed implementation installs later with zero store/component changes — this is the actual adapter boundary the task asked for, not just a type declaration.
- Rewired `butterflyGalleryStore.ts`'s `loadPile()`/`rescan()` to call through the provider instead of the fixture generator directly, and added `loadMore()`/`hasMore`/`isLoadingMore` so cursor pagination is exercised end-to-end rather than left as an unused field — wired a minimal "Load more" button into `pages/butterfly-gallery.vue` for the same reason.
- Extending `ButterflyPileEntry` required updating every literal in `stores/helpers/butterflyGalleryFixtures.ts` (the fixtures are plain object literals, not `as`-cast) — vue-tsc caught this immediately on the first typecheck run, which is exactly the safety net the LEARNING.yaml entry for this task notes as a reason to keep fixtures un-cast.
- Verified the same way as t-003: `npm run test` (vue-tsc), `eslint`, `prettier --check`, `npm run test:layout-contract` — all clean, zero CI round-trips on the PR.
- Closed the roadmap loop with `close_task.py butterfly-gallery t-004 done --implementation-pr silasfelinus/kind_robots#2831`, ran `resolve_deps.py` (unblocked t-005, t-008, t-009, and the standing kaizen task t-026 — all to `ready`).

**What was good:**
- Kept the query side of the contract narrow (cursor/limit only) rather than also threading filters through it, since the task's field list was about page/entry shape, not server-side filtering; documented that reasoning in the PR rather than silently deciding it.
- The provider abstraction was actually exercised (loadMore wired to a real button) rather than left as an unused interface nobody calls — the fastest way for a "contract" task to rot is for nothing to use the contract.

**What to improve:**
- Nothing specific this cycle. General note: this is the second task in a row (after t-003) that landed with zero review round-trips by reading the target repo's existing conventions before writing code rather than after a rejection — worth keeping as the default approach for the rest of this project's milestone-1/2 tasks.

**Kaizen task:** deferred — t-026 (filed against t-003's kaizen) already covers the natural next kaizen here (a fixture/adapter shape-parity contract) and is now `ready`; no new independent suggestion this cycle.

---
_Generated by [Claude Code](https://claude.ai/code)_

## 2026-09-18 | Reviewer/Worker (Claude, scheduled conductor run) | butterfly-gallery/t-005 | resolution

type: resolution

**Decision:** implemented, opened PR, merged `silasfelinus/kind_robots#2832`, closed t-005 to `done`.

**Subject:** Built the painting pile and central-frame interaction, continuing the same extraction pattern t-003/t-004 established.

**Detail:**
- Researched conventions before writing code: read the full t-003/t-004 store/types (`stores/butterflyGalleryStore.ts`, `types/butterflyGallery.ts`) to confirm the select→frame→drop→auto-advance plumbing already existed (it did — `selectNextFromPile()` already fires inside `dropOnBin()`), so this task was almost entirely presentation/extraction, not new state-machine logic. Read `narrative-cast-card.vue` for the established "presentational, emit gestures up" component convention and `kr-art-plate.vue`/`kr-deferred-image.vue` for image-rendering patterns before building anything new.
- Extracted the inline pile/frame markup from `pages/butterfly-gallery.vue` into `components/butterfly-gallery/butterfly-painting-pile.vue` (top-of-stack rendering via a new store `topOfPile` computed, deterministic rotation/lift per card, hover/focus lift, a persistent remaining count) and `components/butterfly-gallery/butterfly-central-frame.vue` (cross-fade `<Transition mode="out-in">` between selections, a dedicated drop zone, a reserved `metadata` slot for t-008). Added `BUTTERFLY_PILE_VISIBLE_DEPTH`/`topOfPile`/`promoteToFrame()` to the store; left `selectImage`/`dropOnBin`/`selectNextFromPile` untouched.
- Verified locally before opening the PR: `npm run test` (vue-tsc), `eslint`, `prettier --check`, `test:layout-contract`, plus the broader gallery/card/store contract suite (`test:gallery-adoption`, `test:gallery-consistency`, `test:disabled-affordances`, `test:no-promise-in-store-state`, `test:card-action-contract`) — all clean, zero CI round-trips on the PR (48/48 checks green first try).
- Also ran this session's full startup sweep first (no open PRs found, `select_role.py` recommended `worker` on this task after its own GitHub-connectivity check 403'd against an out-of-scope repo and had to be cross-checked by hand, same pattern noted in the two 2026-09-18 TALKBACK entries above). Filed `kind-robots/t-110` separately for an unrelated proxysql imagePath-truncation warning spotted in the same session's container-log triage — correlated with the existing t-109 security gate's root PR (#2814) but a distinct failure mode, kept as its own task per scope discipline.

**What was good:**
- Third task in a row on this project (after t-003/t-004) landing with zero review round-trips by reading existing conventions first.
- Kept the drag-to-frame promotion (`promoteToFrame`) clearly distinct from drag-to-bin sorting (`dropOnBin`) at the store level rather than overloading one action, so t-006's bin-rail work and t-020's touch-workflow work each have an unambiguous entry point to extend.

**What to improve:**
- Nothing specific this cycle. Touch/pointer-based drag-to-frame was deliberately deferred to t-020 rather than half-implemented here — worth confirming when t-020 is picked up that the click/keyboard path this task preserved is still sufficient in the interim.

**Kaizen task:** deferred — the PR's own kaizen suggestion (t-006/t-008/t-009 should extract their own components into the now-established `components/butterfly-gallery/` folder) is guidance for those tasks' own implementers rather than a new standalone task.

---
_Generated by [Claude Code](https://claude.ai/code/session_019RgfRj3uZH1nbHTBe6YyBo)_

## 2026-09-18 | Reviewer (Claude, scheduled conductor run) | butterfly-gallery/t-006, t-008, t-010 | resolution

type: resolution

**Decision:** reconciled t-006, t-008, and t-010 to `status: done` against `silasfelinus/kind_robots#2834`, without implementing any of them directly this cycle.

**Subject:** Resolved a real merge conflict between two independently-built, concurrently-open PRs on `pages/butterfly-gallery.vue` -- this session's own `t-005` (kind_robots#2832) and Silas's own live-session PR (`kind_robots#2834`, "build approved sorting stage") -- then reconciled roadmap state to match what actually shipped.

**Detail:**
- After merging and closing `t-005` (see the entry above), `select_role.py` pointed at `t-006` as the next ready task. Before claiming it, I checked open PRs directly (per the reviewer role's own guidance to verify GitHub state by hand when the script's connectivity check fails) and found `kind_robots#2834`, open, `mergeable_state: dirty`, based on the same stale pre-`t-005` commit my own branch had started from.
- Read the PR body and diff: it replaces `pages/butterfly-gallery.vue` wholesale with the actual approved-stage composition (warehouse structure, runway, drop-funnel, five preset bins left, right-rail Image Info/Cleanup/Trash, below-viewport pile, reserved animation-layer slots) -- built independently, not on top of `t-005`'s extracted `butterfly-painting-pile.vue`/`butterfly-central-frame.vue` components.
- Posted a `REVIEWING:` marker, merged `main` into the PR branch locally, and resolved the conflict: kept the PR's page in full (the newer, more complete implementation), let the store's non-conflicting auto-merge stand, then manually removed `t-005`'s now-unreferenced `BUTTERFLY_PILE_VISIBLE_DEPTH`/`topOfPile`/`promoteToFrame` and deleted the two orphaned component files, since nothing in the shipped page used them (it does click-only pile selection, drag only for bins/trash -- no drag-to-frame drop zone). Also fixed a pre-existing Prettier failure on the page (confirmed pre-existing on the PR's own last commit before my merge, not introduced by it).
- Re-ran the full local verification suite after resolving (vue-tsc, eslint, prettier, layout-contract, gallery-adoption, gallery-consistency, disabled-affordances, no-promise-in-store-state, card-action-contract) -- all clean -- before pushing the resolution and merging (48/48 CI checks green, `mergeable_state: clean`).
- Then reconciled roadmap state against the actual merged diff rather than the PR's self-description alone: grepped the merged `pages/butterfly-gallery.vue` for the specific structural evidence each task's note asks for (preset-bin count/right-rail-not-a-bin-rail for t-006; the Image Info panel's fields + Details expand + Cleanup/Trash for t-008; runway/funnel/pile/reserved-layer-slot markup for t-010) before closing any of them `done`. t-009 (queue filters/search) was NOT in this PR -- confirmed no filter/search markup exists yet -- so it stays `ready`, unimplemented.
- Fixed `m3`'s milestone status (`not-started` → `in-progress`, 1/2 tasks done) alongside `m2`'s (from the earlier t-005 close-out) via `check_milestone_status_drift.py`.

**What was good:**
- Silas's own implementation is thorough and matches the freshly-approved spec closely -- worth noting as a Worker-side pattern for future stage/composition tasks: reserving real (if currently-empty) DOM layer slots for not-yet-produced animation assets, rather than deferring the layout work until the assets exist, avoids a later structural rewrite (the PR's own kaizen suggestion, and a good one).

**What to improve:**
- Nothing to fault in Silas's PR itself. Worth a standing note for future sessions: a project this actively iterated-on by both agents and Silas concurrently (see also this session's own t-005 spec-mismatch flag a few entries up) needs an open-PR check *before* claiming the next task in the same project, every time, not just when `select_role.py`'s own connectivity check fails -- the conflict here would have been caught earlier (before doing any t-006 work at all) had I checked PRs before reading `select_role.py`'s ready-task recommendation rather than after.

**Kaizen task:** deferred -- `t-009` (queue filters) is already the natural next `ready` task in this milestone and doesn't need a newly-invented one.

---
_Generated by [Claude Code](https://claude.ai/code/session_019RgfRj3uZH1nbHTBe6YyBo)_
