name: superkate_services_calculator
description: "Superkate Services Calculator — built with AppMaker on conductor."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3: ^2.7.2
  sqlite3_flutter_libs: ^0.5.31

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
