version: 1
project: kind-robots
task: t-046
operation: review
session: claude-conductor-burst-20260726T-kr-t046
note: >
  Wired /video-generator into site navigation. pages/video-generator.vue was
  already a fully working LTX/WAN image-to-video generator, but the only
  reference to it was a dead dashboardConfigs.art 'video' tab that routes
  through components/art/art-manager.vue, which has zero callers anywhere.
  Added a real top-level channel/tab entry, content/channels/lab/video-generator.md
  (channelKey lab, tabKey video-generator, route /video-generator, sort 65
  next to coat-dance), and registered the surface in utils/dataSurfaceManifest.ts
  (navEntry lab/video-generator) per that file's own contract. No content
  page doc needed: the route is a real file-based Nuxt page, which already
  renders ahead of the [...slug].vue catch-all. Verified locally: npm run
  test:channel-content (66 tabs, up from 65, 0 errors), npm run
  test:data-surface-manifest (2 surfaces, 2 wired, 0 errors), npm run
  test:channel-resolver (passes), npx eslint/npx prettier --check clean on
  both changed files, full-project npm run test (vue-tsc --noEmit) exit 0
  with no errors. kind_robots PR #988 opened
  (https://github.com/silasfelinus/kind_robots/pull/988), subscribed for
  CI/review follow-up; will flip to done once merged.
