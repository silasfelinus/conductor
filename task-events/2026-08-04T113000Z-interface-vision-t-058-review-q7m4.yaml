version: 1
project: interface-vision
task: t-058
operation: review
owner: worker
session: 2026-08-04T112754Z-t058-verifier
updated: '2026-08-04T11:30:00Z'
note: >-
  Verifier slice: taught verifyLayoutContract.ts's one-scroll rule
  v-if/v-else-if/v-else exclusivity, fixing a flat-sum false-positive over
  tab-switcher-shaped components and a latent false-negative (a literal '>'
  inside a v-if expression truncating the tag-attribute regex before it
  reached a later class= attribute). memory-dungeon.vue and chat-gallery.vue
  resolved outright; animation-manager.vue newly (and correctly) flagged.
  Baseline ratchets one-scroll from 10 to 9. kind_robots PR #1429.
