version: 1
project: brainstorm
task: t-006
operation: done
owner: worker
session: 2026-08-10T101700Z-brainstorm-t-006-f6a9
updated: '2026-08-10T10:41:00Z'
note: >-
  Merged Kind Robots PR #1713. Brainstorm now has POST /api/brainstorm/generate with canonical
  Server-row resolution, provider-aware structured generation, exact-count/duplicate validation,
  modern creative-quality prompting, mana charging only after usable output, and backend/server
  trust guards. Official OpenAI uses native Responses JSON schema; Anthropic, Ollama, and
  compatible servers normalize through the shared provider caller. The dedicated generation
  contract, TypeScript, Layout, API client, and full repository contract suites passed.
learning: >-
  The current text stack has multiple useful abstractions but no single provider layer covers
  every Kind Robots text server: generic textServer assumes OpenAI-style requests while Suggest
  explicitly handles Anthropic/Ollama/compatible servers. Brainstorm therefore resolves the
  canonical Server row first, then uses the provider-specific caller while keeping one validated
  candidate envelope. Never forward first-party provider credentials to arbitrary compatible URLs.
