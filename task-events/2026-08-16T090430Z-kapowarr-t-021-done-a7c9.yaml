version: 1
project: kapowarr
task: t-021
operation: done
session: 20260816T090200Z-kapowarr-t021-import-a7c9
updated: '2026-08-16T09:04:30Z'
note: >-
  Implemented and merged silasfelinus/Kapowarr#13 (squash 5c4d5f5). The existing
  review importer now surfaces ComicVine throttling instead of converting it to a
  missing match; Continuous Auto-Import snapshots the unimported folder queue once,
  processes it one folder at a time with conservative ComicVine pacing and cooldown,
  auto-imports the current best match without renaming, leaves unmatched folders for
  review, and exposes checked/imported/review/remaining progress through the existing
  background-task API. The importer page reconnects to a running task after navigation.
  PR and post-merge Python 3.8-3.12 matrices, docs, and container builds all passed;
  main published ghcr.io/silasfelinus/kapowarr:latest and sha-5c4d5f5 with digest
  sha256:bc810d0645f82016304533d4013cd742643f611f41141eb04398387c70452b9a.
  Follow-up confidence-margin policy and durable task persistence across container
  restarts remain explicit future refinements after live-library testing.
learning: >-
  The old library-import path masked ComicVine throttling as a no-match, making a
  reliability failure look like harmless empty search results. Large unattended jobs
  also need a stable work snapshot: self-review caught and removed an initial design
  that would have rescanned the entire library for every processed folder. Finally,
  the fork's Python Tests workflow was development-only, so PR/main test triggers were
  enabled and verified across Python 3.8 through 3.12 before merge.
