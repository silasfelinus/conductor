version: 1
project: ai-art-academy
task: t-044
operation: needs-human
session: 2026-07-27T07:35:00Z-worker-ai-art-academy-t044-a31d
owner: worker
soft_gate: true
updated: '2026-07-27T07:49:00Z'
note: |-
  FOR SILAS: PR #1212 merged the offline ComfyUI LoRA path comparator and tests. The tool is `scripts/compare_comfy_lora_paths.py`; the exact private capture/run instructions are in `projects/ai-art-academy/docs/t-044-comfy-lora-path-diagnostics.md`.
  It compares Kind Robots Resource `localPath` values against ComfyUI's authoritative `LoraLoaderModelOnly` dropdown, accepts normalized exact or unique-basename matches, and refuses ambiguous guesses.
  TO RESUME: from a Tailscale-connected machine, save ComfyUI `/object_info` and Kind Robots `/api/resources` as private local JSON files, run the documented comparator command, and use the report to correct exact Resource paths or implement one shared resolver. Do not commit the captures because they may expose private model inventory and local paths.
  Before marking this task done, run one Kontext-native and one FLUX-dev LoRA through the real queue and confirm ComfyUI accepts the workflow and saves an image. Then return t-044 to ready for the implementation/live-verification pass; this unblocks t-045.
