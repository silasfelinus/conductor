# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/

# Platform folders are generated by `flutter create .` — committed once generated locally.
# Secrets must never be committed.
*.env
*.keystore
key.properties
**/GoogleService-Info.plist
**/google-services.json
